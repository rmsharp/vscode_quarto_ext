# Roadmap

## Current Milestone
**v1 — the core authoring loop.** An installable, MIT-licensed `.vsix` that: highlights `.qmd`, renders, live-previews, runs code cells, and completes cross-references + citations — degrading gracefully when the Quarto CLI or a kernel is absent. Defined in `docs/planning/2026-06-27-extension-architecture-plan.md` §7. Architecture: TextMate grammar (Tier A) + in-process providers (Tier B), shelling out to the Quarto CLI. Visual editor / Zotero / Assist / project-wide indexing are out of v1. **Status (Session 12): all v1 features complete; the GitHub remote is wired (`origin` → `rmsharp/vscode_quarto_ext`, branch `master`); marketplace metadata + the listing README are done; and the F5 visual pass is done — 5 faithful screenshots (highlighting, outline, `@` completion, preview, render), captured in a dev host isolated with `--disable-extensions`, are wired into the README `## Screenshots` gallery (closing the "F5-only visual residue" every prior phase carried). The `npm audit` posture decision is done (Session 13): all 7 vulnerabilities are dev-only — none ship (`dependencies: {}`, no `node_modules` in the `.vsix`) — and are accepted + documented in `docs/SECURITY-AUDIT.md` (no `--force` fix, which would only break the toolchain). **v1 release prep is now agent-complete; the only remaining step before listing is the operator-only `vsce publish`** (Marketplace publisher `rmsharp` + PAT; `preview: true` is set).**

## Planned
<!-- The active task list is in BACKLOG.md. This is the higher-level shape. -->
- **v1 phases (vertical slices, one session each):** 1 scaffold → 2 grammar → 3 render → 4 preview → 5 run-cell → 6a outline → 6b cross-ref completion → 6c citation completion. **All complete as of Session 9.** See the plan for per-phase DONE gates, verification commands, and "here be dragons" flags.
- **v2 (in progress):** YAML/cell-option completion (**Phase 6d: planned Session 17, slice 6d-1 `#|` cell-option KEY shipped Session 18, 6d-2 `#|` cell-option VALUE shipped Session 19, 6d-3 schema reader shipped Session 20, 6d-4 front-matter KEY shipped Session 21, 6d-5 front-matter VALUE shipped Session 22, 6d-6 v1 nested `execute:` KEY shipped Session 23, 6d-6 nested `execute:` VALUE shipped Session 24, 6d-6+ `format:` names shipped Session 25, 6d-6+ top-level `format:` scalar value shipped Session 26; 6d-6+ continuation (per-format options, deeper nesting) optional/v2.x — or descope 6d here**), embedded-cell language completion (virtual-document forwarding — **Phase 6e: planned Session 27**, 5 slices, start with 6e-1), authoring aids (snippets, image paste, **formatting toggles — shipped Session 14**, **math preview — shipped Session 15**, **diagram preview (Mermaid) — shipped Session 16; graphviz rendering follow-up pending**), project/convert/walkthrough commands.
- **Tier C (deferred until a driver appears):** out-of-process LSP for cross-file project indexing or multi-editor reuse — cheap to reach because the intelligence core is kept `vscode`-free.

## What's Built
- **Phase 6d-6+ (v2, b1) — top-level `format:` scalar value completion (Session 26). Ninth 6d slice — the format-key value side.** Inside the YAML front matter, after the colon on the top-level `format:` key (`format: <here>`), typing now offers Quarto **output-format names** (`html`/`pdf`/`docx`/`revealjs`/`beamer`/`typst`/…) as the value — the small adjacent win flagged in Session 25's handoff. Previously it offered nothing useful: the flat `schema/document-*.yml` models `format` only as a same-named **epub-scoped string** field with no value enum (a name collision; the only `format` doc-field is `document-epub.yml` `schema:"string"`). The fix is **pure-core, one change** — the **name-collision pattern**: `core/yaml-schema.ts` `indexOf` enriches the top-level `format` field's `.values` with the format names (the same reader-derived-or-curated set offered as KEYS one level under `format:`), **derived into a new `topLevelFields` list so the raw arrays stay untouched** (existing shape-locks stay green), serving BOTH the parsed reader and the curated fallback. The **context AND provider were both UNCHANGED** — the 6d-5 `frontmatter-value` context already returns `parentPath:["format"]` for a top-level `format:` value, and `providers/yaml.ts`'s generic value branch already resolves `frontMatterKeys([]).find(name).values`. Reader-derived (tracks the user's Quarto), degrading to the curated `CURATED_FORMAT_NAMES` subset offline. Built strict-TDD (2 vertical layers, checkpoint each) + a 5-lens refute-by-default review (**0 raised**; the 7th consecutive clean review). **Gate-d:** the reader tests assert the reader-only `docbook`/`texinfo` (absent from the curated fallback); a **runtime-conditional** `quartoSharePath`-throw break-revert reds ONLY the `texinfo` enrichment test ("got exactly the 14 curated formats") while the `html` positive and the no-leak control stay green from curated — proving reader-derivation AND graceful degradation (the break must be runtime-conditional: an unconditional early `throw` broke *compilation* via unreachable-code type-narrowing, so the test never ran — a build break is not a behavioral RED). Verified by +2 vitest + 5 `@vscode/test-electron` cases (**405 unit / 121 integration**). Clean 33-file `.vsix` (1.3 MB). The remaining 6d-6+ (per-format options, deeper nesting — the 🐉) is optional/v2.x. Completion-popup visual is F5 residue. (CLAUDE.md Learning #33.)
- **Phase 6d-6+ (v2) — `format:` → format-name nested completion (Session 25). Eighth 6d slice — the format container.** Inside the YAML front matter, on an indented key line one level under `format:`, typing now offers Quarto **output-format names** (`html`/`pdf`/`docx`/`revealjs`/`beamer`/`typst`/… — 65 visible). Unlike the curated-only `execute:` children, the FULL format list is **reader-derived** from the user's installed schema: `core/yaml-schema.ts` `collectFormatNames` reads the live `pandoc/formats.yml` list, concatenates Quarto's synthesized formats (`md`/`hugo`/`dashboard`/`email`), and drops the hidden legacy variants (`html4`/`html5`, `epub2`/`epub3`, `docbook4`/`docbook5`) — replicating Quarto's own `makeFrontMatterFormatSchema` (found in the installed `editor/tools/yaml/web-worker.js`) — so format completion tracks the user's Quarto version, degrading to a curated `CURATED_FORMAT_NAMES` subset offline. **Design distinction:** format names HAVE a clean readable list, so reader-derived + curated fallback is the faithful choice (the 6d-3 pattern), whereas the `execute:` children are curated-only (no single readable list). Per §3.3, pure `vscode`-free core: `core/yaml-context.ts` added `"format"` to `NESTED_CONTAINERS` (ONE allow-list entry — the Session 23 nested KEY+VALUE detector already generalizes over the container, bailing on per-format-option deeper nesting); `core/yaml-schema.ts` added `collectFormatNames`/`isHiddenFormat`/`FORMAT_SYNTHESIZED`/`CURATED_FORMAT_NAMES` + an `indexOf` 3rd `formatFields` param serving `frontMatterKeys(["format"])`. **`providers/yaml.ts` was UNCHANGED** — its `frontmatter-key` branch already queries `frontMatterKeys(ctx.parentPath)` generically (like Session 23). Per-format options, deeper nesting, and the top-level `format: html` scalar are deferred (FM #18). Built strict-TDD (3 vertical layers, checkpoint each) + a 5-lens/3-refuter refute-by-default review (**5 raised → 0 confirmed**; the 6th consecutive clean review; one applied test-hardening made the hide-filter assertions fully discriminating). **Gate-d:** the detector allow-list was break-revert-proven; the integration enrichment uses `texinfo` (reader-only, absent from the curated fallback) and a `quartoSharePath`-throw break-revert reds ONLY `texinfo` (curated 14 serve) while the curated-served positives stay green; a `revealjs`-not-at-top-level paired control proves no cross-pollution (no format name collides with a top-level document key — verified firsthand). Verified by +12 vitest + 7 `@vscode/test-electron` cases (**403 unit / 116 integration**). Clean 33-file `.vsix` (1.3 MB). The remaining 6d-6+ (per-format options, deeper nesting) is optional/v2.x. Completion-popup visual is F5 residue. (CLAUDE.md Learning #32.)
- **Phase 6d-6 cont. (v2) — nested `execute:` VALUE completion (Session 24). Seventh 6d slice — the value-side complement of the nested-KEY slice.** Inside the YAML front matter, on an indented line one level under `execute:`, after a known execute child-key `key:`, typing a value now offers that child's grounded enum/boolean values (`echo: ` → true/false/fenced, `output: ` → true/false/asis, `cache: ` → true/false/refresh, `freeze: ` → true/false/auto, the rest true/false; `daemon`'s number-timeout form is non-enumerable). Values are grounded firsthand against the installed Quarto 1.7.33 schema and are **curated-only** — the same source as the nested keys (`frontMatterKeys(["execute"])` = `CURATED_EXECUTE_KEYS`). Per §3.3, pure `vscode`-free core: `core/yaml-context.ts` `nestedKeyContextAt` gained a value branch (past the colon → `{frontmatter-value, parentPath:[container, key]}`), with the value-slot grammar extracted into a shared `valueSlotAfterColon` helper used by both the top-level and nested paths; `core/yaml-schema.ts` `CURATED_EXECUTE_KEYS` gained grounded `values` (exact full-set equality test). `providers/yaml.ts` generalized its `frontmatter-value` branch from `frontMatterKeys([])` to `frontMatterKeys(parentPath.slice(0,-1))` — ONE line resolving both top-level and nested values, reusing the entire 6d-2/6d-5 value path with no new value machinery. Built strict-TDD (3 vertical layers, checkpoint each) + a 5-lens/3-refuter refute-by-default review (**1 raised → 0 confirmed**; the 5th consecutive clean review; one applied test-hardening keying a negative on enum-bearing data). **Gate-d:** `cache`/`freeze`/etc. are in the flat `document-*` list (so naive tests passed even with the old provider — non-faithful), but the cell-shared flags (`echo`/`eval`/`output`/`warning`/`error`/`include`) are not (verified firsthand), so the `echo:` nested-value test is the faithful RED→GREEN discriminator; the detector value branch was break-revert-proven. Verified by +6 vitest + 7 `@vscode/test-electron` cases (**391 unit / 109 integration**). Clean 33-file `.vsix` (1.3 MB). The remaining 6d-6+ (`format:` names, per-format options) is optional/v2.x. Completion-popup visual is F5 residue. (CLAUDE.md Learning #31.)
- **Phase 6d-6 v1 (v2) — nested KEY completion under the `execute:` container (Session 23). Sixth 6d slice — the first nested slice.** Inside the YAML front matter, on an indented line one level under `execute:`, typing a key now offers the execute child-keys (`echo`/`eval`/`output`/`warning`/`error`/`include`/`cache`/`freeze`/`enabled`/`daemon`/`daemon-restart`/`keep-md`/`keep-ipynb`) from a curated, grounded set. This is the plan's "cheap one-level approximation": ONE allow-listed container, ONE level deep, KEYS only; the parent-scan detector **bails (`null`) on any ambiguity** (non-allow-listed container, scalar/block-scalar/flow parent, deeper nesting, sequence item, value position). Per §3.3, pure `vscode`-free core: `core/yaml-context.ts` `frontMatterContextAt` now takes the full text and falls through to `nestedKeyContextAt`/`nearestShallowerLine` gated by `NESTED_CONTAINERS={execute}`; `core/yaml-schema.ts` `CURATED_EXECUTE_KEYS` served by `frontMatterKeys(["execute"])` for both the parsed and curated index (nested is curated-only — Quarto assembles the execute object across 5 files via a `document-execute` context glob with no single readable list, and render-validation is lenient, so a grounded curated set is the faithful v1, mirroring 6d-1→6d-3). `providers/yaml.ts` was UNCHANGED (generic over `parentPath`). Built strict-TDD (3 vertical layers, checkpoint each) + a 5-lens/3-refuter refute-by-default review (**2 raised → 0 confirmed**; the 4th consecutive clean review; one applied docstring-provenance fix verified firsthand). Verified by +13 vitest + 6 `@vscode/test-electron` cases (execute children complete under `execute:`; the `echo`-nested-only paired control proves the path runs without cross-pollution; both-direction gating; **385 unit / 102 integration**); the two load-bearing detector guards break-revert-proven. Clean 33-file `.vsix` (1.3 MB). The remaining 6d-6+ (nested values, `format:` names, per-format options) is optional/v2.x. Completion-popup visual is F5 residue. (CLAUDE.md Learning #30.)
- **Phase 6d-5 (v2) — front-matter top-level VALUE completion (Session 22). Fifth 6d slice — the value-side complement of 6d-4.** Inside the YAML front matter, after a known top-level `key:`, typing a value now offers that key's enum/boolean values — `toc: true/false`, `number-sections: true/false`, and schema-resolved enums like `editor: source/visual` and `engine: jupyter/knitr` — read from the user's **installed** Quarto schema, degrading to curated `toc`/`number-sections` booleans offline. Gated as the complement of the cell-option value provider (no document-value items in prose/code/cells, none for a free-text key; `@` completion stays suppressed in front matter). Per §3.3, all new logic is pure `vscode`-free core: `core/yaml-context.ts` `topLevelKeySlot`→`topLevelSlots` (key AND value spans on a top-level FM line, mirroring `slotsOf`'s value grammar — skip leading ws, strip an unquoted trailing inline comment, keep quoted scalars) and `frontMatterContextAt` returns a `frontmatter-value` context (`parentPath`=the key) past the colon; `core/yaml-schema.ts` gave curated `toc`/`number-sections` `values:[true,false]`. **The runtime schema reader was unchanged** — it already resolves document-key value enums (`parseSchemaIndex`→`toField`→`valuesOfSchema`, reused since 6d-3/6d-4; 173 of 378 visible doc fields carry resolvable values, confirmed by probing the real schema first). Thin `providers/yaml.ts` serves them from `frontMatterKeys([])`, reusing the 6d-2 value path (`valueItems`/`valueItem` parameterized by `detail` → `"Quarto document option value"`, the leading-space-on-`:` normalization, whole-token replace range). Built strict-TDD (3 vertical layers, checkpoint commit each) + a 5-lens/3-refuter refute-by-default review (**2 raised → 0 confirmed**; the 3rd consecutive clean review; one applied test-hardening of the prose gating test). Verified by 11 vitest + 8 `@vscode/test-electron` cases (`toc`/`editor` values complete in FM; the schema-only `editor`→source/visual case proves the reader; both-direction gating; **372 unit / 96 integration**) with gate-d proven by a `quartoSharePath`-throw break-revert (enrichment reds, curated `toc` stays green — after a first ineffective break that used an ignored CLI flag, Learning #29d). Clean 33-file `.vsix` (1.3 MB). Completion-popup visual is F5 residue. (CLAUDE.md Learning #29.)
- **Phase 6d-4 (v2) — front-matter top-level KEY completion (Session 21). Fourth 6d slice — the first front-matter slice.** Inside the YAML front matter (the leading `---`…`---`), typing a top-level key now offers document keys (`title`, `author`, `format`, `bibliography`, `toc`, `csl`, …) read from the user's **installed** Quarto schema (`schema/document-*.yml` — the 378 visible flat names), degrading to a curated `CURATED_FRONTMATTER_KEYS` set when the schema can't be read. It is the COMPLEMENT of the cell-option provider on the shared `{language:"quarto"}` selector: gated so neither cross-pollutes (no document-option items in prose/code/cells/nested/value positions; `@` cross-ref/citation completion stays suppressed in front matter). Per §3.3, all new logic is pure `vscode`-free core: `core/qmd/model.ts` gained the missing front-matter region surface — `findFrontMatter`/`inFrontMatter`/`frontMatterContentLines`, all **views over the single `scanRegions` pass** (no second scanner — Learning #14), with a break-revert-proven agreement guard, and `core/frontmatter.ts`'s duplicate `---` parser was folded onto it; `core/yaml-context.ts` gained a `frontmatter-key` branch (`topLevelKeySlot` — column-0 keys only; indented/value/sequence/comment/fence → null); `core/yaml-schema.ts` gained `SchemaIndex.frontMatterKeys` + a shared `collectFields` over `schema/document-*` + `CURATED_FRONTMATTER_KEYS`. Thin `providers/yaml.ts` serves them tagged `detail:"Quarto document option"`. **No new dependency** — reuses the entire 6d-3 reader/cache/fallback adapter. Known limitation: pure CONTAINER keys like `execute` are not in the flat `document-*` name list (object graph → 6d-6), so `execute` is offered only from the curated fallback (offline). Built strict-TDD (6 vertical layers, checkpoint commit each) + a 5-lens/3-refuter refute-by-default review (**3 raised → 0 confirmed**; the second 6d slice with a clean review; one docstring-precision fix). Verified by 35 vitest + 7 `@vscode/test-electron` cases (top-level keys complete in front matter; a schema-only key `csl` proves the reader enriched the FM set; both-direction gating; **361 unit / 88 integration**) with gate-d proven by TWO break-reverts (the agreement skip; the `quartoSharePath` reader→curated degradation). Clean 33-file `.vsix` (1.3 MB). Completion-popup visual is F5 residue. (CLAUDE.md Learning #28.)
- **Phase 6d-3 (v2) — `#|` cell-option SCHEMA READER (Session 20). Third 6d slice — the 🐉 dragon.** Cell-option completion now reads the user's **installed** Quarto schema (`<share>/editor/tools/yaml/yaml-intelligence-resources.json`, resolved via the undocumented `quarto --paths`): the **full** option set (94 of 97; `hidden` excluded) with descriptions, value enums **resolved from the live schema** (`column` → all 18 `page-column` values from `definitions.yml` — no hand-transcription), **engine-filtered** (knitr options not offered in `{python}`/`{ojs}`). Degrades to the curated `CURATED_CELL_OPTIONS` fallback on ANY failure (Quarto absent / `--paths` shape changed / file unreadable / JSON malformed) — never an error. Runtime-read → zero version drift, +0 `.vsix` bytes, **no NOTICE**. Swaps only the data source: detection/discriminator/gating and the 6d-2 value surface are unchanged. Two new pure modules per §3.3: `core/quarto-paths.ts` `parseSharePath` (defensive) + `core/yaml-schema.ts` `parseSchemaIndex` (never-throw; resolves the simple value forms `boolean`/`enum`/`anyOf`/`maybeArrayOf`/`ref`/`string.completions`, defers the non-enumerable rest); thin `quarto/cli.ts` `quartoSharePath()` + `features/yaml-schema-source.ts` (load cached once/session, fallback) + async `providers/yaml.ts`. Built strict-TDD (5 vertical layers, checkpoint commit each) + a 5-lens/3-refuter refute-by-default review (**7 raised → 0 confirmed**; the fidelity lens found zero defects — the reader eliminates the transcription trap). Verified by 29 vitest + 2 `@vscode/test-electron` cases (a schema-only option appears end-to-end; **326 unit / 81 integration**) with gate-d degradation proven by a documented `quartoSharePath` break-revert. Clean 33-file `.vsix` (1.29 MB). Completion-popup visual is F5 residue. (CLAUDE.md Learning #27.)
- **Phase 6d-2 (v2) — `#|` cell-option VALUE completion (Session 19). Second 6d slice.** After a known cell-option key's colon, typing on a `#|`/`//|` line offers that option's **value** enum from a curated set grounded in the live Quarto 1.7.33 schema — booleans (`eval`/`warning`/`error`/`include`/`cache`), `echo` (+`fenced`), `output` (+`asis`), `code-fold` (+`show`), `fig-align`, and `column` (the full 18-value `page-column` enum). Fires ONLY in the value slot; suppressed at the key, in prose/code/front-matter, for an unknown key, and inside a trailing inline comment; `@` completion still works in prose. Free-text/numeric options offer no values. Accepting yields valid `key: value` (a space is inserted when the value abuts the colon). The runtime schema reader (full set + descriptions) is the later 6d-3. Grew the same pure modules per §3.3: `core/qmd/model.ts` `findCellOptionLines` (+`valueSlot`, computed with `keySlot`, comment-terminated), `core/yaml-context.ts` `completionContextAt` (+`cell-option-value` branch), `core/yaml-schema.ts` `CURATED_CELL_OPTIONS` (+grounded `values`); thin `providers/yaml.ts` adapter (value items + leading-space normalization). Built strict-TDD (checkpoint commit per layer) + hardened by a 5-lens / 3-refuter refute-by-default adversarial review (4 raised → 4 confirmed / 0 refuted, both fixes test-first: a dropped `page-column` value (`page-inset`) restored + the spot-check test tightened to full-enum exact equality; a trailing inline comment excluded from the value span). Verified by 24 vitest + 9 `@vscode/test-electron` cases (positive RED→GREEN; inverted-gating negatives faithful via the RED baseline; edge cases on in-memory docs). 297 unit / 79 integration green; clean 33-file `.vsix` (1.29 MB). Completion-popup visual is F5 residue. (CLAUDE.md Learning #26.)
- **Phase 6d-1 (v2) — `#|` cell-option KEY completion (Session 18). First 6d slice.** Typing on a `#|`/`//|` option line inside an executable cell offers cell-option **key** suggestions from a curated set (descriptions included); fires ONLY in a key slot, suppressed in prose/code/value/front matter (the inverse of the `@` providers' prose gate — plan §4.3), and `@` completion still works in prose. The runtime schema reader (full 97-option set) is the later 6d-3 — this ships the curated fallback. Pure `core/qmd/model.ts` `findCellOptionLines` (a view over the shared `findAllCells`, no 2nd scanner; `CELL_OPTION_PREFIX` matches Quarto's real `^#\s*\| ?`) + pure `core/yaml-context.ts` `completionContextAt` (position discriminator → `cell-option-key` or `null`) + pure `core/yaml-schema.ts` `CURATED_CELL_OPTIONS` (~18 grounded names, own descriptions) per §3.3; thin `providers/yaml.ts` adapter. Built strict-TDD (31 vitest cases) + hardened by a 5-lens / 3-refuter refute-by-default adversarial review (8 raised → 3 confirmed / 5 refuted, all fixes test-first: indented-`#|` over-detection + `# |` under-detection both fixed by matching Quarto's actual `optionCommentPattern`; two trivially-green skip-region tests replaced with break-revert-proven faithful ones). Verified by 31 vitest + 7 `@vscode/test-electron` cases (no-cross-pollination via positive+negative `detail`-discriminator controls + a RED baseline; no CLI/kernel). 273 unit / 70 integration green; clean 33-file `.vsix` (1.29 MB). Completion-popup visual is F5 residue. (CLAUDE.md Learning #25.)
- **Phase 7 (v2) — Diagram preview webview (Session 16).** `Quarto: Preview Diagram` (`quarto.previewDiagram`) detects ```` ```{mermaid} ```` / ```` ```{dot} ```` cells in the active `.qmd` and renders each **Mermaid** cell in a webview beside the editor with **vendored Mermaid** (MIT, `media/mermaid/`), re-rendering live (debounced); one reused panel. `{dot}` is detected but shown as source + a "not yet rendered" note (graphviz rendering = a future WASM slice). Pure `core/diagram-regions.ts` (`findDiagramRegions` — a thin filter over the shared `findAllCells`, no second scanner) + pure `core/diagram-preview-html.ts` (`buildDiagramPreviewHtml` — Mermaid webview HTML + nonce CSP, `img-src … data:` for C4/architecture icons) per §3.3; thin `features/diagram-preview.ts` adapter. The 3.56 MB Mermaid UMD bundle was verified self-contained (no `'unsafe-eval'`) and vendored as a single file with sha256 provenance in `NOTICE`, **not** a devDependency (`npm audit` unchanged at 7). Built strict-TDD (22 red→green) + hardened by a 6-lens refute-by-default adversarial review (4 raised → 4 confirmed / 0 spurious: CSP `img-src` gap for C4 icons fixed; two gate-d test over-claims renamed to what they prove; one shared-`CELL_INFO` over-detection of glued malformed info strings documented + backlogged). Verified by 16 vitest + 6 `@vscode/test-electron` cases (webview opens/reuses/updates via `tabGroups`/`TabInputWebview`; no CLI/kernel). 242 unit / 63 integration green; clean 33-file `.vsix` (1.29 MB; Mermaid is a webview asset, not a runtime dep). Mermaid visual render is F5 residue. (CLAUDE.md Learning #23.)
- **Phase 7 (v2) — Math preview webview (Session 15).** `Quarto: Preview Math` (`quarto.previewMath`) detects inline `$…$` / display `$$…$$` LaTeX in the active `.qmd` and renders every region in a webview beside the editor with **vendored KaTeX** (MIT, `media/katex/`), re-rendering live (debounced) as the document changes; one reused panel. Pure `core/math-regions.ts` (`findMathRegions` — Pandoc `tex_math_dollars` rules, consumes the shared `scanRegions`, masks inline code spans) + pure `core/math-preview-html.ts` (`buildMathPreviewHtml` — KaTeX webview HTML + nonce CSP) per §3.3; thin `features/math-preview.ts` adapter. Built strict-TDD (26 red→green) + hardened by a 5-lens refute-by-default adversarial review (5/14 confirmed: inline-code-span phantom fixed via the shared `maskInlineCode` relocated to `core/qmd/model`; a `toContain` CSP test strengthened to exact-equality + break-revert; live re-render debounced; two mid-line-HTML-comment shared-model gaps documented + backlogged; one over-fix correctly refuted). Verified by 24 vitest + 6 `@vscode/test-electron` cases (webview opens/reuses/updates via `tabGroups`/`TabInputWebview`; no CLI/kernel). 226 unit / 57 integration green; clean 32-file `.vsix` (368.5 KB; KaTeX is a webview asset, not a runtime dep). KaTeX visual render is F5 residue. (CLAUDE.md Learning #22.)
- **Phase 7 (v2) — Formatting toggles (Session 14). First v2 feature.** `Quarto: Toggle Bold` / `Toggle Italic` / `Toggle Code` wrap or unwrap the current selection — or the word at a bare cursor — in `**` / `*` / `` ` `` markers (`ctrl/cmd+b`, `ctrl/cmd+i`; all three on the palette). Pure `core/format-toggle.ts` (`toggleFormat` — wrap/unwrap with `*`-vs-`**` disambiguation, Unicode-aware word expansion) per §3.3; thin `features/formatting.ts` adapter gated to `.qmd`, primary selection. Built strict-TDD (10 red→green core behaviors) + hardened by a 5-lens / 3-refuter adversarial review (3/11 confirmed, all fixed TDD: an empty-`**` silent-deletion disambiguation hole, ASCII-only word expansion, a substring keybinding-scope test). Verified by 12 vitest + 9 `@vscode/test-electron` cases (faithful real-editor offset mapping, no CLI/kernel). 202 unit / 51 integration green; clean 10-file `.vsix`. Multi-cursor deferred. (CLAUDE.md Learning #21.)
- **Phase 6c — Citation completion (Session 9). v1 FEATURE-COMPLETE.** Typing `@` in `.qmd` prose offers the citekeys from the document's `bibliography:` (BibTeX or CSL-JSON), with the entry title as detail, coexisting with cross-refs on `@` (VS Code merges the two providers and filters by typed text). Pure `core/frontmatter.ts` (`bibliographyPaths` — scalar/flow/block forms of the one key the region model skips) + `core/citations.ts` (`parseCitations`, plus `citationCompletionContext` — a citekey-aware `@`-token scanner that handles `:`/`.` keys); adapter `src/providers/citation.ts` reads the bib relative to the document and is gated to prose via `isReferenceableLine`. Verified by 8 real-host `executeCompletionItemProvider` tests (coexistence, in-cell gating, a colon-key whole-token replace, and three degradation paths) and hardened by a 5-lens / 3-verifier refute-by-default adversarial review (12 confirmed fixes — the key catch: reusing the cross-ref `ID_CHAR` scanner had silently broken colon/dot citekeys). **v1 — highlight / render / preview / run-cell / outline / cross-ref + citation completion — is feature-complete; next is release/packaging prep.**
- **Phase 6b — Cross-reference completion + go-to-definition (Session 8).** Typing `@` in `.qmd` prose offers the document's cross-ref labels (`@fig-/@tbl-/@sec-/@eq-/@lst-`) and Go to Definition jumps to a label's definition. Pure `core/refs.ts` indexes labels from three sources over the shared scanner (heading `{#sec-…}` ids — now kept as `Heading.id`; `#|`/`//|` `label:` cell options; inline `{#fig-…}` on prose), plus `isReferenceableLine`/`crossrefCompletionContext` helpers; adapter in `src/providers/crossref.ts` (completion trigger `@` + definition, both gated to prose). Verified by 9 real-host tests via `vscode.executeCompletionItemProvider`/`executeDefinitionProvider` (faithful, no CLI/Jupyter; incl. in-cell negative + prose control) and hardened by a 5-lens adversarial review (7 fixes; 1 indented-code-block phantom deferred). **v1 remaining: Phase 6c (citations) — the last v1 phase.**
- **Phase 6a — Document outline / symbols (Session 7).** A `DocumentSymbolProvider` populates the Outline view + breadcrumbs for `.qmd` with headings (nested by level) and code cells (leaves under their section). Establishes the shared **`core/qmd/model.ts`** region model — one `scanRegions` pass (front matter, HTML comments, fences, ATX headings) feeding `findHeadings`/`findAllCells`/`findCellAtPosition`/`buildOutline`; **Phase 5's `core/cells.ts` folded in** as a shim so cell-finding is shared with heading detection. Adapter in `src/providers/outline.ts`. Verified by a real-host test via `vscode.executeDocumentSymbolProvider` (faithful, no CLI/Jupyter) and hardened by a 5-lens adversarial review (6 fixes: front-matter/comment skip now shared with cells, 0–3-space fence-indent cap, `{#sec-id}` attribute stripped, empty closing-hash heading dropped). Setext headings deferred (backlog). **v1 remaining: Phase 6c.**
- **Phase 5 — `Quarto: Run Cell` family (Session 6).** `quarto.runCell` / `runCellAndAdvance` / `runCellsAbove` / `runAllCells` / `insertCell` find the code cell at the cursor and **delegate** execution to the user's Jupyter/R/Julia extension (feature-detected command IDs), degrading gracefully to a warning when none is installed. No CLI shell-out, no long-lived process; runs the in-editor buffer. `ctrl/shift+enter` bind only inside a cell (`quarto.inCodeCell` context key; `onLanguage:quarto` activation). Pure `core/cells.ts` (CommonMark backtick/tilde fence scanner, excludes plain/`{{}}`/`{.}`/nested fences) + `core/execution-delegate.ts`; adapter in `src/features/execution.ts`. Verified by 27 vitest + 8 real-host tests (faithful dispatch via a stand-in delegate) and hardened by an adversarial review (10 confirmed fixes). **v1 remaining: Phases 6a–6c.**
- **Phase 4 — `Quarto: Preview` (Session 5).** `quarto.preview` spawns `quarto preview --no-browser`, parses the `Browse at <url>` line (stderr, ANSI-wrapped), and embeds the live preview in a webview beside the editor (auto-reload on save via Quarto livereload). Owns the process lifecycle — the server **and its detached deno worker** are reaped (process-group kill) on pane close / doc close / deactivate, with no orphan. Pure `core/preview-url.ts` + `core/preview-html.ts` (CSP-locked); adapter in `src/features/preview.ts`. Verified by a real-host integration test that asserts the worker is reaped (faithful no-orphan probe), hardened by an adversarial review (TOCTOU race + 4 fixes).
- **Phase 3 — `Quarto: Render` (Session 4).** The `quarto.render` command shells `quarto render <active.qmd>`, streams output to a dedicated Output channel, shows the produced path on success (with an Open action) or surfaces the error verbatim on non-zero exit, and fails soft when the CLI is absent. Pure `core/render-args.ts` (arg-builder + ANSI-tolerant output-path parser) keeps logic headlessly testable. Verified: success renders to HTML and a deterministic-failure fixture confirms no crash, both in a real `@vscode/test-electron` host. Phases 4–6c remain.
- **Phase 2 — `.qmd` highlighting (Session 3).** `.qmd`/`.rmd` register as the `quarto` language; an original `text.html.quarto` grammar highlights prose (via the built-in markdown grammar by reference), YAML front matter, and `{python}/{r}/{julia}/{ojs}` cells, with bracket/comment/folding config. Verified headlessly via `vscode-textmate` tokenization + a real-host registration test.
- **Phase 1 — walking skeleton (Session 2).** Installable `.vsix` builds from a TypeScript/esbuild scaffold; the `core/`-vs-adapter boundary is established (`vscode`-free `core/`); the `Quarto: Verify Installation` command resolves the CLI and reports its version; unit (vitest) + integration (`@vscode/test-electron`) test harnesses both run green headlessly.

## Completed Milestones
- **Methodology bootstrap** (Session 0) — installed the Iterative Session Methodology and instantiated project docs.
- **Architecture planning** (Session 1) — resolved the load-bearing language-support decision and produced the phased implementation plan.
- **Phase 1 walking skeleton** (Session 2) — scaffold + `Quarto: Verify Installation` + test harness; confirmed headless integration testing works.
- **Phase 2 `.qmd` highlighting** (Session 3) — language registration + TextMate grammar + config; resolved the base-grammar decision (include-by-reference, no fork); added headless grammar tokenization tests.
- **Phase 3 `Quarto: Render`** (Session 4) — render command + Output channel + pure arg/output-path core; verified success and failure paths in a real host.
- **Phase 4 `Quarto: Preview`** (Session 5) — live webview preview + owned process-group lifecycle (deno-worker reaping, no orphan); pure URL/HTML-CSP core; faithful no-orphan + TOCTOU regression tests.
- **Phase 5 `Quarto: Run Cell` family** (Session 6) — delegated cell execution (Jupyter/R/Julia) with graceful degradation; pure cell-boundary + delegate-selection core; faithful stand-in-delegate dispatch tests; adversarial review (10 fixes).
- **Phase 6a Document outline / symbols** (Session 7) — `DocumentSymbolProvider` over the new shared `core/qmd/model.ts` region model (headings + cells), folding in Phase 5's cell finder; faithful real-host symbol test; adversarial review (6 fixes, setext deferred).
- **Phase 6b Cross-reference completion + go-to-definition** (Session 8) — pure `core/refs.ts` label index over the shared scanner (heading `{#sec-}` ids, cell `label:` options, inline `{#fig-}`); `@`-completion + definition adapter gated to prose; faithful real-host `execute*Provider` tests; adversarial review (7 fixes, indented-code-block phantom deferred).
- **Phase 6c Citation completion** (Session 9) — pure `core/frontmatter.ts` `bibliographyPaths` + `core/citations.ts` `parseCitations`/`citationCompletionContext`; `@`-citekey completion adapter reading the bib relative to the doc, gated to prose, coexisting with cross-refs; faithful real-host `executeCompletionItemProvider` tests; adversarial review (12 fixes; the cross-ref-scanner-reuse colon-key regression caught). **v1 feature-complete.**
