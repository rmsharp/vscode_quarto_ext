# Backlog

*Open, actionable work items only. Completed work → `CHANGELOG.md`. Feature inventory & plans → `ROADMAP.md`.*

## Active

*(empty — pick the next item from "Up Next" below.)*

- [x] **Item 18 — SHIPPED DEFECT: embedded-language forwarding did not reach real language servers. FIXED Session 87** (Slice 0 of `docs/planning/2026-07-12-embedded-lsp-scheme-and-semantic-tokens-plan.md`; Learnings #94/#95). Discovered Session 86 while grounding item 16.
  - **Root cause:** all three vdoc features forwarded through a **custom URI scheme**. Real language servers filter by scheme in their LSP `documentSelector` (typically `file:`/`untitled:`/`vscode-notebook-cell:`), so **no provider was ever registered for our vdoc** — `executeXxxProvider` correctly returned nothing, with no error and no warning.
  - **Fix:** one shared `file:`-scheme vdoc (`src/core/embedded/vdoc-path.ts` pure naming/ownership + `src/features/embedded-vdoc.ts` lifecycle), written to `<workspaceRoot>/.quarto/vdoc-mit/` (gitignored; `mkdtemp` 0700 fallback for untitled docs). Fresh path per content change (defeats the ≈1017 ms async model-cache race, M3) and path REUSE when content is unchanged (so it is not a per-keystroke disk write). All three `registerTextDocumentContentProvider` calls are gone. Sweep is bounded by two independent guards — our own directory, and our own filename prefix — so it can never reach Posit's `.quarto/vdoc/`; **both guards are break-revert-proven independent** (loosening either alone still keeps Posit's vdocs safe; only breaking BOTH turns the test red).
  - **Verified against REAL Pylance** (`npm run test:lsp`, new): completion / hover / in-cell symbols / correct per-cell attribution under concurrency / no-stale-window on edit — all green, each with a control proving Pylance was alive (307 completions on a plain `.py`). `{ojs}` no-regression gate met with a control: **52 semantic completions forwarded, control 52** (matching S86's pre-migration measurement exactly).
  - **A LATENT DOCUMENT-CORRUPTION BUG the fix exposed, now also fixed:** once a real formatter could finally answer, Format Cell **deleted the cell's fence lines** (`` ```{ojs}\nx   =   1\n``` `` → `` \nx = 1\n ``). The vdoc blanks non-body lines to whitespace-only runs; a real formatter trims them; and `rangeWithinCell` filtered on `findCellAtPosition`, which is INCLUSIVE of fences. Unreachable before (no formatter ever answered) and invisible to the stand-in (which only emitted body-confined edits). Now gated on `embeddedCellAt` (body-only), the primitive `embedded.ts`'s own filter already used correctly.
  - **`format-cell` against a real PYTHON formatter remains UNPROVEN** — the operator declined installing `ms-python.black-formatter` in the harness, so it is still not claimed either way. What IS proven is that the Format Cell forward reaches a **real, non-stand-in provider**: `test:lsp` drives it through VS Code's **built-in JavaScript formatter** via an `{ojs}` cell, which needs no extension.
  - **Why the tests never caught it (Learning #94):** every stand-in was registered on `{ scheme: <our-custom-scheme> }` — the exact axis real servers discriminate by — so the suite was *structurally incapable* of detecting that no real server registers there. All six stand-ins are re-keyed onto `{scheme:"file", pattern:"**/vdoc-mit.*"}`, and the shared `assertRoutedThroughVdoc` now fails loudly on any regression to a custom scheme.
  - `docs/POSIT-COMPARISON.md:286`/`:301` (which claimed parity for these exact features throughout) **corrected**.

- [x] **v1 release prep** — **v1 is feature-complete** (Phases 1–5 + 6a–6c). A packaging / README / marketplace-metadata pass to ship the `.vsix`. **All agent-actionable items done (Sessions 10–13); only the operator-only `vsce publish` remains.**
  - [x] **Item 1 — git remote + packaging metadata (Session 10):** `origin` wired (`rmsharp/vscode_quarto_ext`, default branch `master`), `repository` added to `package.json`, `--allow-missing-repository` dropped from the `package` script, README relative-link restriction lifted. (Learnings #5/#17.)
  - [x] **Item 2 — marketplace metadata + README (Session 11):** `publisher` → `rmsharp`; original `icon` (`media/icon.png`); `keywords`/`bugs`/`homepage`/`galleryBanner`/`preview`; polished `description`; `categories` Programming Languages + Data Science. `README.md` rewritten for the Marketplace (stale status line fixed; drafted via a judge-panel + accuracy-critic workflow). Caught + fixed the publisher→extension-ID coupling that broke 8 integration suites (Learning #18). Clean 10-file `.vsix`; 190 unit + 42 integration green.
  - [x] **Item 3 — F5 visual pass + screenshots (Session 12):** captured 5 faithful screenshots (highlighting, outline, `@` completion, live preview, render) in an Extension Development Host isolated with `--disable-extensions` (the user has Posit's `quarto.quarto` installed, which otherwise merges with ours), and wired them into the README `## Screenshots` gallery. Screenshots excluded from the `.vsix` (`.vscodeignore media/screenshots/**`); vsce rewrites the relative paths to repo raw URLs. This also closes the "F5-only visual residue" every prior phase carried. (Learning #19.)
  - [x] **`npm audit` posture decision (Session 13):** 7 vulns (4 moderate / 2 high / 1 critical), **all dev-only** — `dependencies: {}` is empty; `src/` imports none of them; the shipped bundle `dist/extension.js` contains none; the `.vsix` ships no `node_modules` (`vsce ls`). **Accepted as dev-only** and documented in `docs/SECURITY-AUDIT.md`. No fix applied: plain `npm audit fix` fixes 0 of 7, and `--force` is breaking + net-negative (esbuild major, vitest major, mocha **downgrade**) for zero shipped-artifact benefit. (Learning #20.)
  - **Operator step (not an agent task):** actual `vsce publish` needs a registered Marketplace publisher `rmsharp` + a PAT; `preview: true` is set — flip when the listing is deemed stable.
  - Mostly non-code (FM #18: one deliverable per session; don't also do the deferred polish). See plan §7 (v1 DoD).

## Up Next

- [ ] **Post-Posit-comparison feature roadmap — prioritized via a grill-me session (Session 43); extended Session 61 (grill-me) to decide the gaps Session 43 had parked/deferred rather than ranked.** `docs/POSIT-COMPARISON.md`'s 12 "Real gaps" + its own priority list, re-ranked against the operator's actual usage (multi-file Quarto projects/books are the dominant workflow; new-user adoption matters) rather than raw gap size. Full decision trail (including the resequencing rationale) in `SESSION_NOTES.md` "What Session 43 Did." **Visual (WYSIWYG) editor is explicitly EXCLUDED (operator directive, Session 43) — do not resurface as a candidate without a new operator decision.** Ranked, each its own future session (strict TDD gate applies except where noted):
  1. [x] **Project-level render — SHIPPED Session 45** (plan: `docs/planning/2026-07-09-project-level-render-plan.md`, Session 44, Learning #51; implementation Learning #52). New **`quarto.renderProject`** ("Render Project") command: `src/core/project.ts` `findProjectRoot` (pure, DI'd `exists`, ancestor-walk with an optional bounding directory) discovers the project root (`_quarto.yml`/`_quarto.yaml`, `.yml` preferred on a tie), then `src/features/render-project.ts` spawns `quarto render <root>` with `cwd` PINNED to `root` — root passed EXPLICITLY as an argument, never a bare no-args invocation (the plan's grounded headline finding: a bare `quarto render` from a project subdirectory silently renders only that subdirectory, not the whole project). Shipped as ONE vertical-slice session (operator-ratified over splitting into two, plan §9 Q1) across 4 checkpoint-committed layers: L1 core+unit tests (`cc66fbe`), L2 adapter/command/`package.json`/`extension.ts` wiring (`e61386b`), L3 the first `_quarto.yml`-having test fixture (`70dc234`), L4 integration tests (`0da5dab`) — both the D1 (cwd-pin) and D2 (whole-project-vs-partial-render) discriminators are break-revert-proven. **v1 = render only** — "Preview Project" remains a deliberate follow-up, not this item (`src/features/preview.ts`'s process-group-owning model doesn't trivially generalize to multi-output-file projects, confirmed Session 44). 490 unit / 176 integration; clean 35-file `.vsix`. Full detail in `SESSION_NOTES.md` "What Session 45 Did."
  2. [x] **YAML schema diagnostics — SHIPPED Session 47** (plan: `docs/planning/2026-07-09-yaml-schema-diagnostics-plan.md`, Session 46, Learning #53; implementation Learning #54). **Scope corrected and NARROWED from this item's original framing, empirically, not by choice** (see the plan/Learning #53) — only the **interior of `project:`/`website:`/`book:` blocks in `_quarto.yml`** is genuinely closed and safe to flag. New always-on feature: `src/core/yaml-schema.ts` `SchemaIndex.projectKeys(container)` (`resolveClosedKeys`/`resolveClosedKeysRef`/`resolveClosedKeysObject`, resolving `book:`'s two-level `super`/`$ref` merge chain via `book-schema`+`base-website`+`csl-item-shared`) + `src/core/project-yaml.ts` `findProjectConfigKeyLines` (pure bare-YAML scanner) + `isProjectConfigFileName` (exact-basename filename gate) + `src/features/yaml-diagnostics.ts` `registerYamlDiagnosticsFeature` (`DiagnosticCollection`, 350ms debounce, a generation-counter race guard) surface each unknown key as an Error diagnostic. Shipped as ONE vertical-slice session across 4 checkpoint-committed layers (`32d21b0`/`edf4491`/`fd126f1`/`4d1b4f1`), THEN a 26-agent adversarial `Workflow` review found and this session fixed 2 HIGH-severity false positives (quoted-key comparison, a filename-gate suffix-match) plus 6 more confirmed MEDIUM/LOW findings (`0680e98`/`2f503df`) — see `SESSION_NOTES.md` "What Session 47 Did" and Learning #54 for the full review/fix detail. Two LOW-severity, safe-direction (false-negative) gaps — an anchored/quoted container header disables scanning of that block — documented, not fixed (Polish/deferred, below). 523 unit / 183 integration; clean 35-file `.vsix`.
  3. [x] **Onboarding: getting-started walkthrough + project/document scaffolding commands — SHIPPED (all 3 tracks)** (plan: `docs/planning/2026-07-09-onboarding-walkthrough-plan.md`, Session 48). **Scope corrected from this item's original framing, empirically** (plan §0) — only the walkthrough (`contributes.walkthroughs`) is genuinely declarative/TDD-exempt; `quarto.newDocument` and `quarto.createProject` both require real new logic (a YAML-safe title-escaping template builder; an arg-builder + three prompt APIs — `showInputBox`/`showQuickPick`/`showOpenDialog`, none used anywhere in this codebase before — + a child-process spawn + a brand-new OS-temp-directory integration-test pattern this repo has never needed). `quarto create document` does not exist as a Quarto CLI feature at all (confirmed via source read + live invocation against 1.7.33) — the `.qmd` skeleton must be synthesized in TypeScript, not shelled out. Shipped as **THREE separate implementation sessions** (full rationale, interface contracts, and per-track vertical-slice contracts in the plan) — Posit-equivalent research (public sources only, AGPL look-but-don't-copy gate): `quarto.newDocument`/`quarto.createProject`, plus a 6-step "Getting started" walkthrough (v1.17.0) this project's own walkthrough is independently scoped from, not copied from. Ranked here (ahead of run-cell-family/snippets) because the operator confirmed new-user adoption matters (Session 43) — this is a new Marketplace user's first-five-minutes experience, not a retention feature for existing users.
     - [x] **Track A — `quarto.newDocument` — SHIPPED Session 49** (plan §2, Learning #56). Kickoff Q5 resolved (operator confirmed via `AskUserQuestion`): kept the plan's recommended optional title prompt rather than matching Posit's zero-prompt fixed template. `src/core/new-document.ts` `buildNewDocumentContent(title)` (pure — trims, falls back to `"Untitled"` on empty/whitespace, double-quote-escapes embedded `"`/`\` so untrusted input can't break the emitted YAML) + `src/features/new-document.ts` `registerNewDocumentFeature` (thin adapter: `showInputBox` → build content → open as an untitled `language: "quarto"` buffer, no disk write, no CLI shell-out) + `extension.ts` wire + one `package.json` command entry. Two checkpoint commits (L1 core+unit `a32c54d`; L2 adapter/wiring/integration `fde25d0`), full verify matrix at both. Confirmed (Learning #56) that the `showInformationMessage`/`openExternal` monkey-patch stub technique extends cleanly to `showInputBox`, written test-first (RED: `command 'quarto.newDocument' not found`). 528 unit (+5) / 186 integration (+3); clean 35-file `.vsix`.
     - [x] **Track B — `quarto.createProject` — SHIPPED Session 50** (plan §3, Learning #57). Two kickoff questions resolved before code: Q2 confirmed empirically against the real installed Quarto 1.7.33 CLI (scratch-dir spawn, 0.126s, exit 0, no new process) — `create-project` does NOT auto-launch an editor; Q3 resolved by the operator via `AskUserQuestion` — open the new project as the workspace (the plan's recommendation). `src/core/create-project-args.ts` `buildCreateProjectArgs(targetDir, type, title)` (pure, sibling of `render-args.ts`) + `src/features/create-project.ts` `registerCreateProjectFeature` (three sequential prompts — `showQuickPick` type → `showOpenDialog` parent folder → `showInputBox` name/title — → `resolveBinary` → spawn the legacy `quarto create-project` alias → on success, `vscode.commands.executeCommand("vscode.openFolder", ...)`) + `extension.ts` wire + one `package.json` command entry. Two checkpoint commits (L1 core+unit `343598f`; L2 adapter/wiring/integration `cdebaa4`), full verify matrix at both. Confirmed `showQuickPick`/`showOpenDialog` extend the monkey-patch stub technique cleanly (Learning #57); introduced a new OS-temp-dir integration-test pattern (`mkdtempSync`, removed unconditionally in `afterEach`) and a new `vscode.commands.executeCommand` interception technique to test the `vscode.openFolder` success path without triggering a real Extension Development Host reload. 534 unit (+6) / 189 integration (+3); clean 35-file `.vsix`.
     - [x] **Track C — the walkthrough — SHIPPED Session 51** (plan §4). Kickoff §7 Q4 (per-step `media` content) resolved via `AskUserQuestion`: minimal per-step markdown panel (the plan's own recommendation), not new screenshots or the icon. Pure `package.json` `contributes.walkthroughs` addition — one walkthrough (`quartoGettingStarted`, `featuredFor: ["**/*.qmd"]`), 5 steps (Install & verify Quarto → Create your first document → Create a project → Render and preview → Run a cell, then explore more), each with a `command:`-link action button, a `completionEvents` entry tied to its command, and a new `media/walkthrough/*.md` panel (5 new files). No new TypeScript — genuinely declarative, TDD-exempt per CLAUDE.md. **Went beyond the plan's own "no test required" call**: added `test/unit/walkthrough.test.ts` (23 tests, a manifest-shape regression guard in the same pattern as `package-activation.test.ts` — asserts exactly 5 steps, every step's `media.markdown` path resolves on disk, and every `command:`/`onCommand:` reference names a real registered command), break-revert-proven against a deliberately broken media path. 557 unit (+23) / 189 integration (unchanged, no adapter code); clean 40-file `.vsix` (+5 walkthrough media files vs. Session 50's 35-file baseline). **Verification gap, disclosed per FM #24:** the plan's own DONE criteria requires a manual F5 visual pass (steps render, buttons fire, checkmarks appear) — this agent has no GUI-driving tool in this session (no `code` CLI on PATH, no screenshot/click capability), so that pass was NOT performed, though the physical machine does have a display. The operator should confirm visually via `workbench.action.openWalkthrough` before relying on this feature. This BACKLOG item #3 (all three tracks) is now fully SHIPPED.
  4. [x] **Run-cell command family completion — SHIPPED Session 52.** Added the 4 missing commands (`quarto.runSelectedLines`, `quarto.runNextCell`, `quarto.runPreviousCell`, `quarto.runCellsBelow`) to `src/features/execution.ts`, each strict-TDD'd via a genuine RED (integration test written and confirmed failing with `command '...' not found`) before its implementation — 4 checkpoint commits, one per command. All reuse the existing `core/cells.ts` (`findAllCells`/`findCellAtPosition`) and `core/execution-delegate.ts` (`pickDelegate`) primitives; no new `core/` functions needed (mirrors the existing `runCellsAbove`/`runAllCells` precedent of adapter-only logic, integration-tested, no dedicated unit tests). `runNextCell`/`runPreviousCell` use a single unified `c.startLine > line` / `c.endLine < line` filter that works whether the cursor is inside a cell or in prose (no separate case-split needed) and move the cursor into the cell that ran, so repeated invocation steps through the document. `runSelectedLines` delegates the live editor selection, or the current line when the selection is empty. `package.json` gained 4 new `contributes.commands` entries plus keybindings across the FULL resulting 9-command family — including the 3 pre-existing commands (`runCellsAbove`/`runAllCells`/`insertCell`) that had never had a keybinding before this session. `ctrl+enter`/`shift+enter` (existing) are unchanged; the rest use `ctrl+alt+<mnemonic>` (a/b/n/p/i) plus `ctrl+alt+enter` (Run All) and `ctrl+shift+enter` (Run Selected Line(s)) — deliberately NOT `ctrl+alt+up`/`ctrl+alt+down` for Next/Previous, since VS Code's own defaults already claim those for multi-cursor add-above/below. 557 unit (unchanged — no new `core/` code) / 200 integration (+11); clean 40-file `.vsix` (unchanged file count — code + manifest only, no new media). `docs/POSIT-COMPARISON.md`'s run-cell-family entry corrected to reflect parity (11 real gaps, down from 12); a pre-existing, unrelated staleness in that same doc (3 OTHER already-shipped items still listed as gaps) filed as its own Polish/deferred item, not fixed here.
  5. [x] **Snippets — SHIPPED Session 53.** New `snippets/quarto.json` (13 snippets) registered via `package.json` `contributes.snippets` (`{language: "quarto", path: "./snippets/quarto.json"}`). Covers front matter, all 4 executable-cell languages (python/r/julia/ojs, deliberately fence-only — `#|` option entry is already handled by the existing Phase 6d completion feature), callouts (a single snippet with a `${1|note,tip,warning,caution,important|}` choice placeholder), fenced divs, tabset panels, and one snippet per cross-reference kind this extension's own `core/refs.ts` `KIND_PREFIX`/`INLINE_LABEL` patterns recognize (fig/tbl/eq/sec/lst) — grounded against `test/fixtures/crossrefs.qmd`, not Posit's AGPL extension (Learning #1 look-but-don't-copy gate). Genuinely declarative JSON, TDD-exempt per `CLAUDE.md`'s carve-out (consistent with `docs/POSIT-COMPARISON.md`'s own framing of this gap). Added a manifest-shape regression-guard unit test (`test/unit/snippets.test.ts`, 30 tests — precedent: Session 51's `walkthrough.test.ts`) plus a genuine runtime-verification integration test (`test/integration/suite/snippets.test.ts`, 2 tests — `vscode.executeCompletionItemProvider` surfaces VS Code's built-in configuration-based snippet provider, so this proves the contributed snippet fires for real and is correctly language-scoped, closing the kind of manual-only gap Session 51 had to disclose for the walkthrough). All four (2 unit break-revert pairs, 2 integration break-revert pairs) break-revert-proven. 587 unit (+30) / 202 integration (+2); clean 41-file `.vsix` (+1, the new `snippets/quarto.json`). `docs/POSIT-COMPARISON.md`'s snippets entry corrected to reflect parity (10 real gaps, down from 11).
  6. [x] **Workspace symbol provider — SHIPPED Session 54.** New `src/core/workspace-symbols.ts` (`flattenOutline` — depth-first view over the existing `core/qmd/model` `buildOutline` tree, tagging each symbol with its parent's name as `containerName`; `matchesWorkspaceQuery` — empty-query-matches-all, else case-insensitive substring) + `src/providers/workspace-symbols.ts` `registerWorkspaceSymbolsProvider` (a `vscode.WorkspaceSymbolProvider` searching every `**/*.qmd` file via `vscode.workspace.findFiles`, reusing the SAME outline tree the Outline view/breadcrumbs use — no new parsing). No `package.json` contribution needed (like `providers/outline.ts`); wired in `extension.ts`. Strict TDD: 6 unit tests for the pure core layer, then the adapter test-first (RED: 0 matches with neither the provider nor a workspace folder yet) before implementation. **New testing infrastructure discovered and shipped this session (Learning #62):** no prior integration suite in this project ever opened a workspace folder in the Extension Development Host — `vscode.workspace.findFiles` needs one to search, so `test/integration/runTest.ts` now passes `launchArgs` opening `test/fixtures/project` (the existing render-project fixture, 2 real `.qmd` files) as the shared workspace folder for the WHOLE integration run. Verified this is safe for the only other consumer of `workspace.workspaceFolders` in `src/` (`render-project.ts`'s `resolveStartAndBoundary`) by re-running the FULL integration suite (206 passing, 0 regressions), not just the new suite. 4 new integration tests via `vscode.executeWorkspaceSymbolProvider` (cross-file find, nested-subdirectory find, empty-query aggregation, non-matching-query filtering), all break-revert-proven. 593 unit (+6) / 206 integration (+4); clean 41-file `.vsix` (unchanged — new `.ts` bundles into `dist/extension.js`, ships no new file).
  7. [x] **Graphviz (`{dot}`) diagram rendering — SHIPPED Session 56** (plan: `docs/planning/2026-07-09-graphviz-dot-rendering-plan.md`, Session 55, Learning #63; implementation Learning #64). Operator resolved the plan's one open question (§9 Q1, EPL-2.0 Graphviz-core disclosure) via Phase 0/1 conversation: **accept-and-disclose** — proceed with vendoring as designed, correct the project's "unqualified MIT" self-description. Shipped as ONE vertical-slice session across the plan's pre-declared 4-layer contract: **L1** vendor `@viz-js/viz`'s `dist/viz-global.js` verbatim into `media/graphviz/` (re-verified this session — same version 3.28.0/sha256 as the plan's research, no drift) + `NOTICE` gains a Graphviz section (EPL-2.0 core + MIT Expat + MIT wrapper provenance, the first non-MIT-only vendored artifact) + `CLAUDE.md`/`CONTEXT.md`/`README.md`'s unqualified "MIT-licensed" framing corrected to disclose the vendored component (`c1a1864`). **L2** `core/diagram-preview-html.ts`: CSP `script-src` gains `'wasm-unsafe-eval'` only (exact-equality locked, break-revert-proven), the `dot` branch calls `Viz.instance().renderString(code, {format:"svg"})` instead of the old placeholder, `Viz.instance()` awaited lazily only when a dot region is present (`7e08247`). **L3** `features/diagram-preview.ts`: `graphvizRoot()` mirrors `mermaidRoot()`; `localResourceRoots` widened; `vizJsUri` wired into `render()` (same commit as L2). **L4** integration smoke test — a `{dot}` document opens the diagram-preview webview without crashing, parity with the existing Mermaid open-test (`08bb026`). Strict TDD throughout L2 (CSP directive, script tag, render-branch, lazy-instantiation guard each shown RED for the right reason before GREEN); L4's test is disclosed as coverage/parity rather than a genuine RED→GREEN discriminator (the panel opens regardless of dot-render correctness, per the plan's own §7 D1 note). **Phase 3E strengthened beyond the disclosed "F5-only residue" gap (Learning #64):** executed the REAL generated `<script>` template plus the REAL vendored `viz-global.js` in a hand-stubbed Node `vm` DOM, proving a real `{dot}` region produces real Graphviz-rendered SVG through the actual shipped code path (not a hand-reproduction) — real Chromium CSP/`localResourceRoots` enforcement remains genuinely F5-only, disclosed not silently assumed. 595 unit (+2) / 207 integration (+1); clean 42-file `.vsix` (+1, `media/graphviz/viz-global.js`, confirmed via `vsce ls --tree` with no extraneous `node_modules` leak); `npm run check-types` clean. `docs/POSIT-COMPARISON.md`'s diagram-preview entry, "Real gaps" count (10→9), and "Future Work" list item corrected to reflect parity — the 3 OTHER already-shipped items in that same "Real gaps" list (YAML diagnostics, project-level render, walkthrough), already filed as their own Polish/deferred item (`BACKLOG.md` line 68 at time of writing), deliberately left untouched.
  8. [x] **Notebook (`.ipynb`) conversion — SHIPPED Session 63** (plan: `docs/planning/2026-07-10-notebook-conversion-plan.md`, Session 62, Learning #69; implementation Learning #70). Operator resolved the plan's one open question (§8 Q1) directly: **Option B** — two Posit-mirroring commands, `quarto.convertToIpynb`/`quarto.convertToQmd` (not a single unified command), this project's first `contributes.menus.commandPalette` section (`when: editorLangId == quarto` / `when: resourceExtname == .ipynb`). Shipped as a pre-declared vertical slice across 3 checkpoint-committed layers: **L1** `src/core/convert-args.ts` (`inferConvertDirection`/`deriveConvertOutputPath`/`buildConvertArgs`, pure, strict TDD one test at a time, `01bd87f`); **L2+L3** `src/features/convert-notebook.ts` adapter + `extension.ts`/`package.json` wiring landed together with `test/integration/suite/convert-notebook.test.ts` (`ab18ee0`) — matching this project's own precedent for vscode-coupled adapters (`create-project.ts`, Session 50) rather than the plan's abstract L2/L3 split, both comfortably under the 5-file checkpoint cap. The adapter resolves the active `.qmd` `TextEditor` or `.ipynb` `NotebookEditor` (mutually exclusive VS Code concepts, plan §0.3), saves if dirty, derives the output path explicitly (never a bare CLI invocation), modal-confirms an overwrite (this project's first modal prompt — the bare CLI silently overwrites, plan §0.1/§0.4), spawns `quarto convert --output <path>`, and opens the result via the API matching its kind. **Real-CLI integration testing caught two genuine bugs** (Learning #70): a fire-and-forget race where the command's promise resolved before the converted file was actually opened (fixed: `runConvert` now awaits `openResult` before resolving), and cross-suite `workspace-symbol` pollution from a loaded-but-never-disposed `NotebookDocument` surfacing markdown-cell headings via VS Code's own built-in notebook features (fixed: heading-free fixture content). 220 integration (+5, all real-CLI round-trip against an OS-temp-dir fixture, both directions, overwrite confirm/cancel both exercised) / 617 unit (+7); clean 38-file `.vsix` (unchanged file count — new `.ts` bundles into `dist/extension.js`, ships no new asset). `docs/POSIT-COMPARISON.md`'s notebook row corrected to reflect parity (both *Ours*/*Posit's* cells and the previously-stale *Notes* line, plan §8 Q4). **Disclosed, not performed:** a live GUI (F5) visual pass of Command-Palette `when`-clause filtering and the overwrite modal's real appearance — the operator explicitly declined it (session-live-desktop disruption), given the automated coverage already exceeds this project's own documented "integration tests are stronger than F5 for wiring/activation/dispatch" standard (Learning #3) and the `when`-clause context keys used are the same ones already proven by 10+ existing keybindings in this exact `package.json`.
  - **Parked, but flagged as a likely future build-out (Session 61 grill-me decision, not ranked/scheduled yet):** a Contextual Assist Panel equivalent. Not pure UI consolidation on closer look — a Session 61 grep confirmed the two capabilities Posit's version has that we don't are genuinely uneven: **code-doc lookup** is a *partial* gap (`EmbeddedHoverProvider`, `src/providers/embedded.ts:206`, Session 30, already forwards hover into `{python}`/`{r}`/`{julia}`/`{ojs}` cell bodies via the user's own language extension — but nothing hovers Quarto's own YAML front-matter/`#|` cell-option keys, whose descriptions already exist as completion-item `detail:`/`documentation:` text in `src/providers/yaml.ts`/`src/core/yaml-schema.ts` `SchemaIndex` and could plausibly back a new hover provider with no new data source), while **image thumbnails** is a genuine zero (no thumbnail-on-hover code exists for any file type). Either capability would need a brand-new **sidebar `WebviewViewProvider`** (grep-confirmed absent everywhere in this codebase) to host it in Posit's "one panel" shape, rather than another Command-Palette webview. Its own future planning session if/when picked up.
  - **New, unranked (filed at Session 47 close-out per the YAML schema diagnostics plan §8/§9 Q5 — deliberately NOT promoted into this session's scope):** proactively validate a project's `_quarto.yml` project:/website:/book: keys even when the user is only editing a nested `.qmd`, never `_quarto.yml` itself — genuinely more valuable (catches project misconfig the user might never notice by never opening the config file) but materially larger than the shipped v1 (needs off-editor `fs.readFile`, re-triggering on `.qmd` switches, and reusing `src/core/project.ts` `findProjectRoot`, Session 45 — which the shipped v1 does NOT need). Its own future planning session.
  - **Front-matter/cell-option "unknown-key" diagnostics — CLOSED, not a real gap (Session 61 grill-me, confirming Session 46's own §0 finding).** `docs/POSIT-COMPARISON.md`'s framing ("front-matter and cell-option diagnostics remain unimplemented") reads as unbuilt future work, but the unknown-key-flagging version was already empirically investigated (`docs/planning/2026-07-09-yaml-schema-diagnostics-plan.md` §0, live-tested against the installed Quarto 1.7.33 CLI) and found **structurally unsafe**: front-matter top-level keys, cell options (`#|`), and per-format nested options are all OPEN schemas by Quarto's own design — a fabricated key and a genuine typo of a real option are byte-for-byte indistinguishable to `quarto render` (both exit 0, silent). Flagging unknown keys there would false-positive on every real document using legitimate custom metadata, with no way to tell a typo from an intentional field. **Permanent scope boundary, operator-confirmed Session 61** — do not resurface "flag unknown front-matter/cell-option keys" as a candidate without new evidence Quarto's own schema semantics changed.
  - **Front-matter/cell-option VALUE validation — PLANNED (Session 123); ready to implement.** Full plan: `docs/planning/2026-07-19-value-validation-plan.md` (v1.1, grounded firsthand + adversarially reviewed). Flags a wrong VALUE of an already-*recognized* key with an Error squiggle matching `quarto render` 1.7.33. **Design:** a NEW sibling feature `src/features/yaml-value-diagnostics.ts` (copies the `yaml-diagnostics.ts` skeleton); **enum-only-CLOSEDNESS-AWARE** (add `SchemaField.valuesClosed`/`acceptsBoolean` — validate ONLY provably-closed fields; a `string`/`string.completions` field is OPEN → never flag, or `engine: banana`/`documentclass: myclass` false-positive); severity **Error** (grounded — render-fatal in both front matter AND cell metadata). **Two implementation slices, each its own strict-TDD session:**
    - [x] **Phase 1 — cell-option (`#|`) value validation — SHIPPED Session 124** (strict TDD; Learning #137; commits `27b35a6` core + `0e1bd70` feature + `89897b4` review-fixes). `SchemaField.valuesClosed`/`acceptsBoolean` derived by `closednessOfSchema` (inverted-risk sibling of `valuesOfSchema`; curated cell constants annotated, `output` OPEN); pure `isWrongValue` (`src/core/yaml-value-check.ts` — six boolean spellings quote-rejecting + unquote/case-sensitive enum membership; skips open/non-scalar/empty; **quote-aware `unquote`**); `engineFor` exported; the sibling feature (`src/features/yaml-value-diagnostics.ts`, `languageId="quarto"` gate, own `quarto-value` collection, snapshot-based extraction). **Mandatory §9 adversarial review (10-agent `Workflow`, 6/6 CONFIRMED / 0 refuted, each `quarto render`-verified) caught 2 cardinal-sin FALSE POSITIVES** — enum-with-boolean-members (`message: True` renders exit 0 → `acceptsBoolean` keys on an enum containing a boolean) + quoted-value-plus-comment (`fig-align: "center" # note` renders exit 0 → quote-aware `unquote`) — plus a post-await `document.lineAt(staleLine)` throw; all fixed TDD. 1008 unit / 356 integration / clean 44-file `.vsix`. See plan §4.1.
    - [x] **Phase 2 — top-level front-matter value validation — SHIPPED Session 125** (strict TDD; Learning #138; commits `d6e6698` enumerator + `c55449a` feature-wiring + `ec69f10` review-FP-fixes + `ccdbeed` test-gap). New pure `findFrontMatterValueLines` (`src/core/yaml-frontmatter-values.ts` — mirrors `findProjectConfigKeyLines`, bounded by the single `findFrontMatter`/`frontMatterContentLines` scanner, reuses the exported `topLevelSlots` grammar); wired into the Phase-1 feature after the cell path (resolve each top-level scalar against `frontMatterKeys([])`, reuse the surface-agnostic `isWrongValue`). `toc: yes`, `number-sections: "false"`, `df-print: banana`, `cache: banana`, `pdf-engine: PDFLATEX` → Error; `documentclass: myclass` (OPEN), valid values, and **top-level `format` (intentionally UNVALIDATED — enum injected after closedness)** → nothing. **Mandatory §9 adversarial review (4-lens/13-agent `Workflow`, 9/9 CONFIRMED / 0 refuted, each `quarto render`-verified) caught 2 cardinal-sin FALSE POSITIVES** — a YAML node property on a value (`toc: &a true`/`!!bool true`/`*a` render exit 0 → shared-matcher skip guard extended to `&*!`) + multi-line flow-collection continuation lines at column 0 (`mymeta: {`/`toc: yes,`/`x: 1}` render exit 0 → enumerator flow-depth tracking) — both fixed TDD. 1026 unit / 361 integration / clean 44-file `.vsix`. See plan §4.2.
    - [x] **v2 — nested front-matter values — SHIPPED Session 128** (strict TDD, plan `docs/planning/2026-07-20-nested-frontmatter-value-validation-plan.md`; Learnings #140 plan / #141 impl; commits `67d5daa` L1 closedness-annotation + `0de922c` L2 enumerator+`nestedParentPath` export + `2f73839` L3 feature-wiring+fixtures + `1756a23` L4 review-FP-fix). Flags a wrong VALUE of an already-recognized nested key under `execute:`/`format:\n <fmt>:` (`execute.echo: maybe`, `format.html.toc: yes`, `format.html.df-print: banana` → Error; `theme`/`daemon`/`output`/unknown-key → nothing). EXTENDS `computeValueDiagnostics` with a third loop (NOT a new feature); reuses+exports `nestedParentPath` in a forward loop; inverts the completion lookup `frontMatterKeys(parentPath).find(name===key)`; hand-annotates ONLY `CURATED_EXECUTE_KEYS` closedness (11 closed; `daemon`/`output` stay OPEN — per-format reader path already correct). **The mandatory §9 adversarial review (a 4-lens `quarto render`-verified `Workflow`) caught ONE more CRITICAL cardinal-sin FP the plan's flow/block-scalar guards missed — a MULTI-LINE QUOTED scalar (`title: "…\n echo: x"` renders exit 0 but `echo` was flagged) → fixed by unifying the continuation tracker (`scanFlow`) to follow an open quote as well as flow brackets** (Learning #141); resolution/lifecycle/doc-drift lenses CLEAN. Unit 1055 / integration 372 / clean 44-file `.vsix`.
    - [ ] **v2 numeric type-aware — PLANNED Session 129** (`docs/planning/2026-07-20-numeric-frontmatter-value-validation-plan.md`; Learning #142). Ready to implement as ONE strict-TDD vertical slice (L1 `SchemaField.scalarType:"number"` + `numericTypeOfSchema` detector at the two `annotateClosedness` choke points + curated `daemon` → L2 `isWrongValue` numeric branch + `R` superset predicate + `valueMessage` numeric arm [GO-LIVE layer] → L3 fixtures/integration → L4 MANDATORY §9 review). Flags `fig-width: wide` / `columns: fat` / `execute.daemon: banana` / a cell `#| layout-ncol: two` — 39 marked numeric fields, all surfaces via the shared matcher, NO new loop. `.ipynb` (NotebookDocument plumbing), the offline `CURATED_FORMAT_OPTIONS` fallback, integer-typed pandoc-layer validation, and **other closed front-matter containers** (`crossref:`/`website:`/`brand:`/`jupyter:` — their known children ARE quarto-validated, e.g. `crossref.chapters: banana` → exit 1, but `NESTED_CONTAINERS={execute,format}` can't reach them; safe FN — a future slice widens the container set) stay deferred (plan §4.3).
  - [x] **Refactor — extract `createDebouncedDiagnosticsFeature({collectionName, gate, compute})` — DONE Session 126** (strict TDD, plan-mode gated; Learning #139; commits `91bb113` factory+suite + `caefbab` rewire yaml-diagnostics + `79dfdc3` rewire yaml-value-diagnostics). `yaml-diagnostics.ts` (unknown-KEY) and `yaml-value-diagnostics.ts` (VALUE) both copied the same ~75-line DiagnosticCollection/debounce/**generation-guard** skeleton (rule-of-two). New `src/features/debounced-diagnostics.ts` owns the machinery (350ms debounce, per-URI generation guard bumped before the await, D4 cancel-before-delete, prime-open-docs) and is a PURE WRITER: `compute(document, {source, isCurrent})` returns `Diagnostic[] | null` (null=superseded/closed, []=clear, non-empty=set). Both features became thin callers supplying gate + constants + a `compute` (old `refreshDiagnostics` body, guards + S124 snapshot relocated verbatim); `extension.ts` unchanged. Built via a synthetic-feature integration suite (6 tests RED→GREEN, incl. a deterministic Session-47 generation-guard race — new automated coverage). Plan-agent design audit (PASS) + post-impl adversarial diff review (no regression across 8 faithfulness checks). Net −240/+81 LOC. 1026 unit / 367 integration (+6) / clean 44-file `.vsix`. **The factory has two callers** — the v2 nested-front-matter validation (item 46 above, S128) did NOT become a third caller; it EXTENDED this second caller with a third value-source loop (one feature, three sources). The S126 "3rd caller" framing was corrected by the S127 plan §3.1.
  9. [x] **Spell checking — SHIPPED Session 65** (plan: `docs/planning/2026-07-10-spell-checking-plan.md`, Session 64), ranked Session 61 (grill-me decision: option (b), a scoped cspell config recipe), reversing the prior "deferred, coupled to WYSIWYG" framing. Posit's own spell check is Visual-Editor-only, which made this LOOK gated on building the Visual Editor first — Session 61 found that's the wrong frame: plain-source-editor spell check is a genuinely separate, independently buildable feature, and Posit's own team took exactly that separate path elsewhere (a PR titled "enable support for quarto documents" to the third-party `vscode-spellright` extension, authored by Quarto/Posit team member jjallaire — not built into their own AGPL extension). **Decision: do NOT build a spell-check engine.** `streetsidesoftware.code-spell-checker` ("cspell" — **the underlying `cspell`/`cspell-lib` engine is MIT, but the PUBLISHED VS Code extension itself is GPL-3.0-or-later, corrected Session 64; Session 61's original "MIT" framing was wrong** — not a blocker since this project only RECOMMENDS the extension for the user to install themselves, never vendors/bundles its code) already spell-checks any VS Code language ID by default (`cSpell.enabledFileTypes`'s `"*": true` wildcard default — corrected Session 64; the legacy `cSpell.enabledLanguageIds` Session 61 named is deprecated and has no such default), so spelling itself isn't the gap — SCOPE is: naively enabling it on `.qmd` checks code-cell variable names, YAML keys, and math too, not just prose (the exact unsolved problem named in the open upstream issue `quarto-dev/quarto#29`, "Integration with code spell checker" — CLOSED 2023, resolved via Quarto's own Visual Editor gaining native spell-check, not via any published cspell recipe, confirmed Session 64). This project already has the region classification a correct scope needs — `core/qmd/model.ts` `scanRegions` already distinguishes prose/code/YAML/comment regions, the same classification `math-preview`/`diagram-preview`/`refs` already build on. **Scope: ship a ready-made, correctly-scoped cspell config recipe** — a `cSpell.languageSettings` override for the `quarto` language ID using `ignoreRegExpList`/`includeRegExpList` to restrict checking to prose-only regions — declarative, no spell-check engine, no bundled dictionary, no `DiagnosticCollection` of our own. **Session 64's plan validated the recipe empirically** (firsthand `cspell` CLI testing against a multi-region fixture: 18 false positives down to the 6 genuine typos, zero false positives, including a CRLF-handling fix and an empirically-discovered additional false-positive source — cross-ref/citation tokens — beyond the three named above) and leaves 3 open questions for the operator/executor: where the recipe is documented (README pointer + a new `docs/SPELL-CHECK.md`, recommended), whether to ship the cross-ref/citation suppression in v1 (recommended: yes), and workspace- vs. user-scoped settings framing (recommended: workspace). This is a documentation-only deliverable — no `src/` code, no `package.json` change, TDD-exempt per `CLAUDE.md`'s declarative-content carve-out. **Session 65 implemented it**: operator resolved all 3 open questions via `AskUserQuestion`, all recommended options. New `docs/SPELL-CHECK.md` (setup steps, the 8-pattern `ignoreRegExpList` recipe, a why-the-naive-setup-fails table, the license note, 6 disclosed limitations, workspace- vs. user-scope guidance) + a short `README.md` "Spell checking" pointer section (first `docs/*.md` file ever linked from `README.md`, a small disclosed precedent change, `.vscodeignore`d like every other `docs/*.md`). The recipe was re-extracted programmatically from the FINAL doc text (not transcribed from memory) and re-verified against a real `cspell` CLI run (`npx cspell@10.0.1`) against a fresh multi-region fixture: 23 baseline issues (15 false positives) down to exactly 8 with the recipe applied, zero false positives, all 8 genuine typos caught including one placed immediately adjacent to a citation token (confirms no over-exclusion). `docs/POSIT-COMPARISON.md`'s spell-checking row corrected to reflect the new "we're ahead of Posit's own source editor" status (their spell check is Visual-Editor-only; matches this project's own "delegated companion-extension" pattern), and its "At a Glance" summary table corrected — both for spell checking AND for a pre-existing Session 63 miss (notebook conversion's own row already said "Parity reached" but the summary table/count had never been updated to match).
  - **Zotero integration — DECIDED Session 61 (grill-me): mostly already covered, no longer "deferred, coupled to WYSIWYG."** Grounded first: this project already reads/parses `.bib` files declared in a document's front matter and offers citekey completion for `@…` contexts (`src/providers/citation.ts`, `src/core/citations.ts`, shipped Phase 6c) — existing, working infrastructure. Zotero's popular **Better BibTeX** plugin continuously auto-exports a library to a `.bib` file, which that existing completion already consumes with zero Zotero-specific code. **Operator confirmed the `.bib`-export workflow already covers their actual need.** Posit's version additionally offers a live in-editor picker that queries Zotero's local HTTP API directly (browse/search without a `.bib` export step) — genuinely new work (a Zotero API client, a citekey-insertion command, graceful handling of Zotero-not-running) for a UX convenience layered on a citation path that already works. **The live-picker variant is parked** (operator decision) — no ranked future session; revisit only on a new operator decision.
  - **Excluded (operator directive, Session 43):** the Visual (WYSIWYG) editor itself — Posit's single largest feature, this project's single largest gap, out of scope for this round.
  - **Session 67 refresh (new items 10–17 below):** `docs/POSIT-COMPARISON.md` had not had a full re-sweep of Posit's *current* extension state since Session 42/61 — Posit had shipped 3 releases (v1.133.0–v1.135.0) past the doc's v1.132.0 citation ceiling. An exhaustive manifest/changelog/docs diff (not just re-verifying existing rows) found 11 new real gaps with no prior row at all, each adversarially refute-checked by 3 independent agents. Full detail, citations, and priority rationale in `docs/POSIT-COMPARISON.md`'s "Session 67 refresh" note and its "What This Suggests for Future Work" section. None of these have a plan yet — each needs its own planning/implementation session, strict TDD gate applies except where noted as declarative.
  10. [x] **Code-cell diagnostics forwarding (embedded-LSP diagnostics inside `.qmd` code cells). CLOSED Session 69 — investigated, not pursued (Option A).** (plan: `docs/planning/2026-07-10-code-cell-diagnostics-plan.md`, Learning #75). Session 68 corrected this item's original framing, empirically, not by choice: this is NOT "a new forwarding kind on the existing `src/providers/embedded.ts` architecture" — that architecture is pull-based (`vscode.executeXxxProvider`) and VS Code's own docs state directly that request-forwarding does not work for diagnostics (push-based only). A firsthand empirical Extension Development Host test (5 scratch tests) confirmed this project's existing vdoc pattern gets ZERO diagnostics even from a real on-disk, in-workspace file, unless the document is a genuinely VISIBLE, ACTIVE editor tab — which cannot coexist with the user editing their real `.qmd` tab. Posit's own PR prose (`quarto-dev/quarto` #980/#1013, public description text only) independently confirms the same wall from the outside: they solved it by abandoning "forward to the user's installed extension" and instead spawning + owning their own dedicated `vscode-languageclient` connections to specific servers (Pyrefly, Ark) — a materially heavier architecture, a first-ever departure from this project's "delegate to what the user already has installed" posture. The plan left the core decision to the operator (§9 Q1: Option A — accept as a permanent, documented gap, parity with the excluded Visual Editor, redirect effort to items 11+; Option B — commit to Posit's heavier architecture for Python first, MIT-licensed server candidates verified: `microsoft/pyright`, `astral-sh/ruff`, `facebookresearch/pyrefly`, starting with an unresolved empirical spike, plan §7 R1). **Session 69: operator chose Option A.** `docs/POSIT-COMPARISON.md`'s "Code-cell language embedding" row and "Session 67 refresh" priority list corrected to reflect the permanent-gap status. No implementation follows — redirect effort to item 11 (outline granularity) next.
  11. [x] **Outline granularity (in-cell code symbols + show/hide toggle). Slice 1 (toggle infrastructure) SHIPPED + fully verified (Sessions 71–72); Slice 2 (in-cell symbol forwarding) SHIPPED Session 73, pixel-level F5 visual confirmation completed Session 74 — both slices now fully verified at both the command/API and pixel layers** (`docs/planning/2026-07-10-outline-granularity-plan.md`, Learnings #77/#78/#79/#81). Since v1.133.0, Posit's outline extracts symbols from *within* code-cell bodies (not one node per cell) and offers a setting/command to show/hide cells in the outline (`quarto.symbols.showCodeCellsInOutline`/`quarto.toggleCodeCellsInOutline`); since v1.127.0, R-package-project markdown headers are excluded from workspace symbols by default (`quarto.symbols.exportToWorkspace`, PR #751). Unlike item 10, no push/pull architectural wall exists — reuses this project's existing pull-forwarding recipe from `src/providers/embedded.ts`'s pure `core/embedded/` helpers (unchanged; `embedded.ts` itself needs no code changes). Two real interface gotchas found empirically Session 70: (1) `vscode.DocumentSymbolProvider` has no refresh event — the toggle needs a dispose-and-re-register pattern (confirmed via VS Code core's own notebook outline and Posit's own shipped fix, both directly researched); (2) a firsthand empirical test found `vscode.executeDocumentSymbolProvider` caches per-URI internally — reusing `embedded.ts`'s existing stable-key vdoc convention would serve stale in-cell symbols after the first outline computation, so a new, version-varying-URI forwarding store is needed (plan §2.3/§5), not a shared/reused `VirtualDocStore`. **Operator resolved plan §9 Q1–Q4 via `AskUserQuestion` (Session 71):** **Q1 — split**, slice 1 (setting + command + dispose/re-register toggle, gating the already-existing flat cell nodes only, no in-cell symbols yet) this session; slice 2 (in-cell symbol forwarding, the new version-varying-URI store) a separate future session. **Q2 (R2 real-external-language-extension empirical spike) — deferred**, ship against the built-in JS/TS service first, revisit only if a real-world report surfaces a problem with a specific external extension (same treatment as item 10's own R1). **Q3 (sub-feature (c), R-package workspace-symbol exclusion) — deferred as its own unranked Polish/deferred item** (below). **Q4 (R3 cosmetic re-expand-on-toggle fix) — ship slice 1 without it**, filed as its own unranked Polish/deferred item (below) rather than fixed in-session. **Slice 1 SHIPPED Session 71:** `quarto.symbols.showCodeCellsInOutline` (default `true`) + `quarto.toggleCodeCellsInOutline`, gating the outline's already-existing flat cell nodes (no in-cell symbols yet). TDD throughout: `hideCellsInOutline` (pure recursive filter, `core/qmd/model.ts`) unit-tested first (vitest), then the `src/providers/outline.ts` adapter (live config read + dispose/re-register) integration-tested against a real Extension Development Host. **A stronger empirical finding than the plan anticipated (Learning #78):** the `executeDocumentSymbolProvider` staleness Session 70 found for virtual documents (Learning #77) ALSO affects REAL on-disk documents across a config-only change — meaning dispose-and-re-register isn't just Outline-UI polish, it's required for the toggle to work correctly at all through the standard command API. Confirmed via a real, previously-queried `test/fixtures/sample.qmd` going stale, then correct, once re-registration was added — proven RED→GREEN, not assumed. Pixel-level Outline-panel visual confirmation was attempted via a scripted F5-equivalent launch but blocked by a macOS Screen Recording permission this sandboxed session lacks (Learning #79); the underlying command-level mechanism is proven via the real EDH integration suite (228/228 passing) per this project's own Learning #3 precedent (test-electron > manual F5 for wiring/dispatch). **Pixel-level visual confirmation completed Session 72** once the operator granted the Screen Recording permission and restarted iTerm2: relaunched the isolated Extension Development Host, confirmed via 3 real screenshots that the Outline panel's `` ```{python} ``/`` ```{r} ``/`` ```{julia} ``/`` ```{ojs} `` cell nodes are present by default, disappear after running **Quarto: Toggle Code Cells in Outline** once, and reappear (chevron restored) after running it again — the live round-trip, confirmed visually (scripting the actual clicks/keystrokes needed two further environment gotchas beyond the Screen Recording wall, both recorded in `PROJECT_LEARNINGS.md` Learning #80). Slice 1 is now fully verified at both the command/API and pixel layers. **Slice 2 SHIPPED Session 73:** in-cell code symbol forwarding — each mapped-language cell's body forwards to that language's own `DocumentSymbolProvider` via a NEW per-cell virtual document (`buildCellVirtualContent`, `core/embedded/virtual-doc.ts`) distinct from `embedded.ts`'s `buildVirtualContent` (which keeps *every* cell of a language — correct for cursor-position forwarding, but would merge two same-language cells' symbols into one indistinguishable list here). A new adapter-layer `InCellSymbolStore` (`src/providers/outline.ts`, its own `quarto-outline-symbols` scheme, distinct from `embedded.ts`'s) mints a version-stamped, never-before-seen vdoc URI on every outline computation and evicts the previous version per cell — the plan §2.3 fix for `executeDocumentSymbolProvider`'s per-URI cache (Learning #78), now proven for the new store too via a real-EDH test that edits a cell's body between two outline computations and confirms both the content and the URI change. TDD via a scheme-keyed stand-in `DocumentSymbolProvider` (mirroring `embedded.test.ts`'s own pattern): RED (4 of 6 new tests failed for the right reason — forwarding not yet implemented) → GREEN. One test's first draft was itself wrong and had to be corrected mid-session: calling `executeDocumentSymbolProvider` twice with NO intervening edit does not re-invoke the provider a second time at all (VS Code only recomputes on an actual text change) — a genuinely new, directly-confirmed finding recorded as `PROJECT_LEARNINGS.md` Learning #81. No `package.json` changes needed (slice 2 activates automatically wherever slice 1's toggle is already on and a cell's language is mapped) — clean 40-file `.vsix`, unchanged file count. 643 unit (+5) / 234 integration (+6) passing; `npm run check-types` clean. **Pixel-level visual confirmation completed Session 74** (verification-only, no code changes): a scratch `outline-symbol-verification.qmd` fixture (a single `{ojs}` cell with a real `computeSum` function and a `total` variable) was opened in an isolated, `--disable-extensions` Extension Development Host. A real screenshot shows the Outline panel's `{ojs}` cell node recursively expanded into `computeSum` (itself containing a nested `data.reduce() callback` child — proof the built-in JS/TS language service's own recursive symbol tree forwarded through intact, not just a flat placeholder) and `total`, closing the gap Session 73 disclosed. Session 74's own scripted GUI-automation attempt hit a genuine environmental blocker first (this VS Code build's "Sign in to use GitHub Copilot" welcome dialog default-focuses "Continue with GitHub"; a scripted `Return` keypress meant to dismiss it instead triggered a real GitHub OAuth authorization page — caught before any authorization was granted, tab closed immediately, no code/state changed) — recorded as a process-level lesson (feedback memory, not a `PROJECT_LEARNINGS.md` row, since it is about operator-confirmation discipline, not this codebase); the operator then ran the isolated-EDH launch command by hand and captured the screenshot. Both slices are now fully verified at both the command/API and pixel layers. Sub-feature (c) (R-package workspace-symbol exclusion) remains its own separate, unranked item (below), untouched.
  12. [x] **Format Cell (delegate a code cell's body to the embedded language's own formatter). SHIPPED Session 75.** `quarto.formatCell` reuses item 11 slice 2's per-cell virtual-document recipe (`buildCellVirtualContent`, which already blanks `#|`/`//|` option lines and every other cell) plus `embedded.ts`'s simple reused-URI vdoc store, forwarding to `vscode.executeFormatDocumentProvider` and applying the returned edits via `WorkspaceEdit`, filtered to stay within the target cell (defense-in-depth, mirroring `filterOutOfCellEdits`). No new `core/` logic — TDD ran entirely at the integration layer via a scheme-keyed stand-in `DocumentFormattingEditProvider` (RED: 10/10 new tests failed on "command not found" → GREEN). An empirical staleness check (edit the cell, format again) confirmed `executeFormatDocumentProvider` does not share `executeDocumentSymbolProvider`'s per-URI result-cache quirk (Learning #83), so the simpler store sufficed — no `InCellSymbolStore`-style URI versioning needed. Ships with a `Ctrl+K Ctrl+F`/`Cmd+K Cmd+F` keybinding matching Posit's, gated on the existing `quarto.inCodeCell` context key (no collision risk with core's "Format Selection" default, which requires a whole-document formatter that `.qmd` never registers). No whole-document `DocumentFormattingEditProvider`/"Format Document" wiring — a deliberate scope decision, since there is no sensible formatter for a whole mixed-content `.qmd` file. `npm run check-types` clean; `npm test` 643/643 unit, unchanged; `npm run test:integration` 244/244 (+10); clean 40-file `.vsix`, unchanged file count.
  13. [x] **Quick, mostly-declarative wins (bundle — each still its own TDD-gated session unless purely declarative) — ALL 5 SUB-ITEMS SHIPPED (Sessions 76-78):** (a) **embedded-grammar breadth — SHIPPED Session 76.** Expanded the 5-scope `embeddedLanguages` map (`package.json`) + `syntaxes/quarto.tmLanguage.json`'s cell-language repository to 20 scopes total: added `bash`/`c`/`cpp`/`csharp`/`fsharp`/`rust`/`go`/`sql`/`lua`/`ruby`/`php`/`perl`/`java`/`dockerfile`/`powershell` (engine token, languageId, and TextMate scope — e.g. `source.shell`, `source.cs` — all confirmed directly against this repo's own bundled `.vscode-test` VS Code install's built-in extensions, not guessed or copied from Posit's manifest). Each new `cell-<lang>` repository rule mirrors the existing `cell-python`/`cell-r`/`cell-julia`/`cell-ojs` shape exactly (fence begin/end + `contentName: meta.embedded.block.<lang>` + `patterns: [{include: source.<scope>}]`), so `#`/`//`/`--`-style option-line comments are highlighted for free via the delegated grammar, with no new logic. TDD: proved the mechanism fully vertical for `bash` first (genuine RED — `repository.cell-bash`/scope missing — then GREEN) using the project's existing headless-tokenization harness (`test/unit/grammar.test.ts`/`tokenize.test.ts`, `vscode-textmate`/`vscode-oniguruma` — the real engines VS Code uses, documented there as "the automated equivalent of Developer: Inspect Editor Tokens and Scopes"); the remaining 14 (mechanically identical, and — per `CLAUDE.md`'s own carve-out — pure grammar JSON is TDD-exempt) were added as one disclosed batch, RED (all 14 failed for the same missing-rule reason) → GREEN, in a new dedicated `test/unit/grammar-embedded-breadth.test.ts` using small inline fixture strings (NOT `test/fixtures/sample.qmd`, which 11 other unit/integration suites assert exact cell counts/positions against). **Explicitly excluded from this pass:** `dot`/`mermaid`/`typst` (assigned to item 13(b) below — a distinct, separate registration mechanism, standalone languages outside `.qmd`) and Stan/PRQL/Scala/others from Posit's "and more" (no confirmed VS-Code-bundled scope for these — Scala in particular is NOT bundled, unlike everything else on this list — so a real one would need firsthand research, not assumption). `npm run check-types` clean; `npm test` 658/658 (+15, zero regressions); `npm run test:integration` 244/244 unchanged (no adapter/runtime code touched — purely a grammar/manifest change); clean 40-file `.vsix`, unchanged file count. (b) **standalone `.dot`/`.mmd`/`.typ` language registration — SHIPPED Session 77.** New `contributes.languages` entries for `dot` (`.dot`/`.gv`), `mermaid` (`.mmd`), and `typst` (`.typ`) + a dedicated `languages/<id>-language-configuration.json` each (comments, brackets, auto-closing/surrounding pairs) — independent of the embedded-grammar scopes used inside `.qmd` code cells (item 13(a)), and deliberately scoped to registration + config only, no TextMate grammar (so these standalone files get correct comment-toggling/bracket-matching but not our own syntax coloring). **Corrected a factual error carried in this project's own `docs/POSIT-COMPARISON.md`/this BACKLOG entry: Typst source files use the `.typ` extension, not `.typst`** — confirmed against the installed Quarto CLI's own bundled `.typ` template files (`/Applications/quarto/share/create/extensions/format-typst/**/*.typ`) and typst.app's own docs (`PROJECT_LEARNINGS.md` Learning #85). Every comment/bracket rule was grounded via `WebSearch`/`WebFetch` against each language's own official public docs (Graphviz DOT language reference, Mermaid syntax reference, typst.app syntax docs) — never Posit's AGPL extension. TDD-exempt (declarative `package.json` + JSON config), but proved genuine RED (missing `package.json` entries / config files) → GREEN via a new `test/unit/standalone-languages.test.ts` manifest-shape suite, THEN strengthened with real-EDH runtime proof: 3 new `test/integration/suite/language.test.ts` cases open real on-disk fixtures (`test/fixtures/sample.{dot,mmd,typ}`) and assert VS Code's own extension-based language detection actually resolves each to its registered language id. `npm run check-types` clean; `npm test` 680/680 (+22); `npm run test:integration` 249/249 (+3 for this item, +2 for item 13(c) below); clean 43-file `.vsix` (+3, the new `languages/*.json` files). (c) **the 2 residual snippets — SHIPPED Session 77.** `snippets/quarto.json` gained `qlisttable` (Quarto 1.9's bullet-based `::: {.list-table}` syntax for complex table-cell content — top-level bullets are rows, nested bullets are cells) and `qfragment` (reveal.js `::: {.fragment}` incremental slide reveal), grounded against Quarto's own public docs (the 1.9 release notes; the revealjs Advanced Reveal page), not Posit's AGPL extension — same shape/precedent as Session 53's snippet work. Declarative, TDD-exempt; the existing generic `test/unit/snippets.test.ts` regression suite covers both new entries automatically (no new unit test needed), and 2 new `test/integration/suite/snippets.test.ts` cases prove both fire for real via `vscode.executeCompletionItemProvider` (matching Session 53's own "genuine runtime-verification" bar for snippets specifically). (d) **cell navigation + cache-clearing commands — SHIPPED Session 78.** `quarto.goToNextCell`/`quarto.goToPreviousCell` (`src/features/execution.ts`) are pure cursor-navigation siblings of the existing `runNextCell`/`runPreviousCell` — same `findAllCells`-based cell lookup, but move the cursor with NO delegate dispatch, keybound `Ctrl+PageDown`/`Ctrl+PageUp` (`Cmd+` on macOS), matching Posit's own manifest exactly (confirmed no collision with this project's own `Ctrl+Alt+N`/`Ctrl+Alt+P` run-and-navigate bindings). `quarto.clearCache` (`src/features/clear-cache.ts`, new) spawns `quarto render <file> --cache-refresh` — the documented way to force-refresh a document's Jupyter/Knitr execution cache (confirmed against the installed Quarto CLI's own `render --help` and quarto.org's code-execution docs; there is no CLI flag to purge the cache without also rendering), mirroring `render.ts`'s Output-channel + graceful-degradation pattern; editor-title-menu placed (`when: editorLangId == quarto`), matching Posit's own placement, no keybinding (Posit's manifest has none either). Discovered `--cache-refresh` creates a `<doc>_cache/` directory as a real side effect — gitignored (`test/fixtures/*_cache/`) and cleaned up in the new integration test's `afterEach`. RED confirmed for the right reason at each layer (`buildCacheRefreshArgs` missing; `command not found` for all 3 new commands) → GREEN. (e) **the single residual run-cell command, `quarto.runCurrent` — SHIPPED Session 78.** Registers the SAME selection-or-current-line handler as the existing `runSelectedLines`, keybound `Ctrl+Alt+C`/`Cmd+Alt+C` — **NOT Posit's own `Ctrl+Enter`/`Cmd+Enter`, because this project's pre-existing `quarto.runCell` already claims `Ctrl+Enter`** (a genuine, disclosed keybinding-scheme cross-wire vs. Posit's own scheme, discovered this session: this project's `runCell`↔`Ctrl+Enter` and `runSelectedLines`↔`Ctrl+Shift+Enter` are the reverse of Posit's own `runCurrentCell`↔`Ctrl+Shift+Enter`/`runCurrent`↔`Ctrl+Enter`). Posit's own manifest (fetched via `gh api repos/quarto-dev/quarto/contents/apps/vscode/package.json`) confirms `quarto.runCurrent` ("Run Current Code") is a command/keybinding genuinely distinct from `quarto.runSelection` ("Run Selected Line(s)", → this project's `runSelectedLines`), but the exact internal behavioral distinction is unverifiable without reading Posit's AGPL source — out of bounds for this project's clean-room reimplementation policy. Implemented as a disclosed, defensible judgment call: the same operation as `runSelectedLines`, grounded in cross-validated public facts (VS Code's own built-in Python extension's identical `Ctrl+Enter`-run-current-line-or-selection convention; a `quarto-cli` GitHub discussion where a user's own expectation of `Ctrl+Enter` was "run my current line," uncorrected by a project collaborator) rather than inventing unverified new logic. All 5 sub-items of item 13 are now SHIPPED. `npm run check-types` clean throughout; `npm test` 682/682 (+2, the `buildCacheRefreshArgs` unit tests); `npm run test:integration` 262/262 (+13: 6 goTo + 4 clearCache + 3 runCurrent); clean 43-file `.vsix`, unchanged file count (pure code/manifest, no new static assets).
  14. [x] **`_quarto.yml` document links + filepath autocompletion — BOTH slices SHIPPED (Sessions 80–81).** (plan: `docs/planning/2026-07-11-quarto-yml-document-links-plan.md`, Session 79). Since v1.132.0 (PR #906): clickable `DocumentLink` navigation for file-path values in `_quarto.yml`, plus filepath autocompletion suggesting real project files. **Slice 1 (`DocumentLinkProvider`) SHIPPED Session 80** (Learning #88). **Slice 2 (filepath `CompletionItemProvider`) SHIPPED Session 81** (Learning #89): `src/core/project-links.ts` gained `valueContextAt` (a pure cursor-position value-slot detector — `{token, replaceRange}` or `null`, reusing `mappingColonIndex`/`valueSlotAfterColon` so it never disagrees with `findPathValueCandidates` on where a value begins) + `src/providers/filepath-completion.ts` `registerFilepathCompletionFeature` (a `CompletionItemProvider` on the SAME `{pattern:"**/_quarto.{yml,yaml}"}` selector, triggers `:`/`-`/`/`; splits the value-so-far at the last `/`, `readDirectory`s the resolved prefix relative to the config file's own dir, maps entries to `CompletionItemKind.File`/`Folder` with a trailing `/` on folders; `valueContextAt → null → undefined` inverse-gate). Whole-document, existence-grounded (no schema query), matching Posit's own PR #906 behavior and the Slice 1 discipline. A 7-agent adversarial-review `Workflow` on the SHIPPED slice (4 of 5 lenses clean) confirmed one real defect — `valueContextAt` lacked the upper bound its sibling `frontMatterContextAt` enforces, so a cursor PAST the value token (in a trailing inline comment) returned a context that would overrun the comment on accept — fixed at the root (`col > slot.endCol → null`). Strict TDD throughout (RED confirmed at core + integration for each behavior). 708 unit (+13) / 276 integration (+5); `check-types` clean; clean 43-file `.vsix`.
     - [x] **Slice 1 — `DocumentLinkProvider` — SHIPPED Session 80** (Learning #88). New pure `src/core/project-links.ts` `findPathValueCandidates` (whole-document scanner: every `key: value` scalar, `- value` sequence scalar, and `- key: value` inline-mapping item at any depth → value span + unquoted token; skips blank/comment/container lines + booleans; reuses `yaml-context.ts`'s now-exported `valueSlotAfterColon` grammar anchored at the mapping colon) + `src/providers/document-links.ts` `registerQuartoYamlDocumentLinksFeature` (existence-checked `DocumentLinkProvider` on a pattern-based `DocumentSelector` `{pattern:"**/_quarto.{yml,yaml}"}` — no events/debounce/generation-counter, plan §2.3; `fs.stat` each candidate via `Promise.allSettled`, link only what resolves; glob `*` excluded; directories link too, Q4) + `extension.ts` wire. Whole-document scope, NOT `project:`/`website:`/`book:`-only (plan §0, gate-d discriminator: a root-level `bibliography: refs.bib` links). Kickoff Qs resolved to the plan's recommendations (Q2 no manifest change — `onLanguage:yaml` already present; Q3 OS-absolute leading-`/`; Q4 directories link; Q5 pre-exclude booleans not numbers). Strict TDD (RED confirmed at core + integration). **A 24-agent adversarial-review `Workflow` caught the dominant real-world shape being unlinked** — inline-mapping sequence items `- href: page.qmd`/`- part: intro.qmd` (navbar/sidebar/book), which the plan's §2.5 example (bare scalars only) had led the first impl to miss; fixed via `mappingColonIndex` (Learning #88). 695 unit (+13) / 271 integration (+9); clean 43-file `.vsix`; `check-types` clean.
     - [x] **Slice 2 — filepath `CompletionItemProvider` — SHIPPED Session 81** (Learning #89; authoritative detail in the parent item 14 above + `CHANGELOG.md` 2026-07-12). *(This sub-bullet was left stale at `[ ] PENDING` when S81 flipped the PARENT to `[x]`; that lone stale `[ ]` is what the S91/S92/S93 handoffs kept grepping up and re-ranking as candidate #1 — corrected Session 94.)* Original planning context, superseded, kept for history: plan §6 Slice 2, §5.3 — added `valueContextAt` to `src/core/project-links.ts` (cursor-position value-slot detector) + `src/providers/filepath-completion.ts` (`vscode.workspace.fs.readDirectory` prefix-scoped listing → `CompletionItemKind.File`/`Folder`). Its own future session, after Slice 1. Reuses Slice 1's `project-links.ts` module. **Scope corrected from this item's original framing, empirically, not by choice** (plan §0) — "reuses existing `core/project-yaml.ts`/`core/yaml-context.ts` infrastructure" turned out to be the wrong foundation: that infrastructure covers only 15 of 50 empirically-confirmed path-typed schema fields (`project:`/`website:`/`book:` only). Operator-confirmed scope (mid-planning `AskUserQuestion`): a **whole-document, existence-checked heuristic** — link/complete any candidate value that resolves to a real file/directory on disk, anywhere in the document (`bibliography`, `csl`, `css`, `template`, etc., not just the three closed-schema blocks) — matching Posit's own shipped (non-schema-driven) behavior, grounded in public PR #906 facts only (clean-room). (Superseded: both `DocumentLinkProvider` and `CompletionItemKind.File` usage now ship — `src/providers/document-links.ts` + `src/providers/filepath-completion.ts`, wired at `extension.ts:65-66`.) Recommended as **two vertical-slice sessions** (plan §6/§9 Q1): Slice 1 (`DocumentLinkProvider`) and Slice 2 (filepath `CompletionItemProvider`), sharing one small new pure-core module (`src/core/project-links.ts`).
  15. [x] **Preview command family breadth (`previewScript`, `previewFormat`) — FULLY SHIPPED, item CLOSED.** `previewFormat` SHIPPED Session 82; `previewScript` PLANNED Session 83, Slice 1 (the command) SHIPPED Session 84, Slice 2 (Posit-parity gating) SHIPPED Session 85. (Learnings #90, #91, #92.)
     - [x] **`quarto.previewFormat` — SHIPPED Session 82.** A per-format preview QuickPick: `src/core/preview-format.ts` `parseDeclaredFormats` (enumerate a document's declared output formats from its front-matter `format:` block — scalar/block-mapping/block-sequence/flow-collection, reusing the shared `frontMatterContentLines` scanner, `[]` when none) + `buildPreviewArgs(file, {to})` (sibling of `buildRenderArgs`; `quarto preview <file> --no-browser --to <fmt>` — `--to` verified forwarded to `quarto render`, grounded firsthand via `quarto preview … --timeout N` self-exit). The command gates on `.qmd`, offers the declared formats (or Quarto's implicit `["html"]` default), and `PreviewManager.openPreview` gained a `{to}` option (same-format reveal / different-format restart, threaded through `PreviewSession.to`). Command + palette + editor/title manifest entries. Strict TDD (RED confirmed at core + integration). **A 21-agent adversarial-review `Workflow` (5 finder lenses × 2-skeptic) on the all-green slice caught 6 real defects** — a HIGH session-identity race (a restarted preview's dying old child `close`-disposing the new replacement session), the untested restart branch, a flow-collection garbage-`--to`-token bug, an untested `["html"]` fallback, a weak gate-d discriminator, and a LOW startup-window drop — all fixed at the root (session-identity guard on close/error/timeout; flow-collection destructuring; a no-format fixture; `starting`-guard reorder). 726 unit (+18) / 281 integration (+5); clean 43-file `.vsix`. See `SESSION_NOTES.md` "What Session 82 Did" + Learning #90.
     - [x] **`quarto.previewScript` Slice 1 (the command) — SHIPPED Session 84** (plan §6 Slice 1; Learning #92). Operator resolved all four plan §10 kickoff questions to the plan's own recommendations (`AskUserQuestion`): Shape B as two slices (Slice 1 only this session), the **intent-faithful grouped** percent regex, palette gated on `quartoRenderScriptActive` (a Slice-2 decision, recorded not implemented), and the title `"Preview Script..."` (matching the sibling `"Preview Format..."`). New pure `src/core/render-script.ts` `isRenderScript(fileName, text)` detects **both** kinds (Path A jupyter-percent `.py`/`.jl`/`.r`; Path B knitr-spin `.r` only), keyed on the file **extension** as Quarto's own `extname().toLowerCase()` is — so `.qmd`/`.rmd` are excluded and `preview`/`previewScript` can never both claim a file. `previewActiveScript` in `src/features/preview.ts` is the sibling gate of `previewActiveDocument` over the **unchanged** `PreviewManager.openPreview`; `package.json` gains the command entry only. Strict TDD (3 genuine core REDs; adapter-first integration RED). Both CLI regexes and the divergence were re-grounded firsthand against Quarto 1.7.33, incl. confirming the CLI genuinely accepts ordinary Python containing `raw]` (`Starting python3 kernel...`) — our detector refuses it. The `spin.R` fixture gives the accept path a **real kernel-free** `quarto preview` round-trip (`Browse at http://localhost:…`), verified firsthand. **A 21-agent adversarial-review `Workflow` on the all-green slice produced 8 candidates, 6 surviving two independent skeptics; 4 in-slice fixed at the root** — a **quadratic-backtracking ReDoS** in the spin regex (Quarto's own redundant `\s*`: 80k blank lines after an unclosed header = **3.7 s** blocking the single-threaded extension host, falsifying the plan's §4.2 "cheap on every keystroke" claim that **Slice 2 depends on**); a **surviving mutant** (every `.r`/`.R` unit case was an accept, so `return ext === SPIN_EXTENSION` — "every .r file is a render script" — passed all 738 tests); a **scheme-blind gate** (a `git:` diff of a spin script would have previewed the working-tree file); and an over-broad accept-path assertion the plan had explicitly forbidden. 741 unit (+15) / 283 integration (+5); clean 43-file `.vsix`. See `SESSION_NOTES.md` "What Session 84 Did" + Learning #92.
     - [x] **`quarto.previewScript` Slice 2 (Posit-parity gating) — SHIPPED Session 85** (plan §6 Slice 2; Learning #93). The `quartoRenderScriptActive` content-driven context key (`updateRenderScriptContext` in `src/features/preview.ts`, recomputed on active-editor change + document edit + at registration, mirroring `execution.ts:404` `updateCellContext` INCLUDING its background-editor clobber guard) + `isPreviewableRenderScript`, the ONE predicate shared by the command gate and the key so they can never disagree (break-revert-proven: deleting the scheme guard kills BOTH the key test and Slice 1's gate test) + the manifest: the mutually-exclusive `ctrl+shift+k`/`cmd+shift+k` pair on both preview commands, an `editor/title` entry, the palette gate, and activation events `onLanguage:r` + `workspaceContains:**/*.{qmd,rmd,Rmd}` + `workspaceContains:**/_quarto.{yml,yaml}`. **Two disclosed divergences from Posit, both grounded firsthand against the VS Code 1.128 build.** (a) *Keybinding scope (OPERATOR DECISION):* `ctrl+shift+k` is VS Code's built-in **Delete Line** (`editor.action.deleteLines`, `primary: 3113` = CtrlCmd|Shift|KeyK, weight 100; an external extension's keybindings register at weight 400+ and win every collision). Posit gates `quarto.preview` on a bare `!quartoRenderScriptActive` — true in essentially every editor — so mirroring it would hijack Delete Line in EVERY file type. Ours is scoped `editorLangId == quarto && !quartoRenderScriptActive`: mutual exclusion preserved, Delete Line survives outside Quarto docs. (b) *Palette gate on `resourceExtname`, NOT the context key* — see the review finding below. **A 34-agent adversarial review (6 lenses x 2 skeptics) found 3 real defects, all fixed at the root:** (1) **HIGH, found by 5 of 6 lenses, neither skeptic could refute** — gating the palette entry on `quartoRenderScriptActive` made the command **completely unreachable** for a lone `.py`/`.jl` script (the key is only settable AFTER activation; no activationEvent matches a lone `.py`; and VS Code does not activate an extension to evaluate a `when` clause) — a strict REGRESSION vs Slice 1, and one the plan simultaneously prescribed (§5.3 palette sub-decision) and denied (§5.3 "the palette command still works — auto-activates on invoke"); (2) `doc.getText()` materialized on every keystroke for every document before the cheap extension check could reject it (measured 9.5 ms/keypress on a 10 MB file) -> new pure `isRenderScriptExtension` prefilter; (3) `workspaceContains:**/*.{qmd,rmd}` is case-SENSITIVE and never matched `.Rmd`, one of this extension's own registered Quarto extensions. 754 unit / 287 integration; clean 43-file `.vsix`. **Disclosed, not fixed:** the lone-script-outside-a-Quarto-workspace activation gap (Posit has it too; the palette command still reaches it, which is exactly what finding (1) restored).
     - [ ] *(superseded planning context, kept for history)* **PLANNED Session 83** (plan: `docs/planning/2026-07-12-preview-script-plan.md`; Learning #91). Preview a standalone Quarto *render script*. **The plan corrects Session 82's premise on two firsthand-grounded points** (Session 82's "scan for `# %%`" framing, Learning #90, was wrong): (1) there are **TWO** render-script kinds, not one — **Jupyter percent** scripts (`.py`/`.jl`/`.r` whose FIRST non-blank line is a `# %% [markdown]`/`[raw]` cell → jupyter engine; a bare `# %%`-only script is rejected "Can't determine execution engine") AND **knitr *spin*** scripts (`.r`/`.R` with a `#' ---`…`#' ---` roxygen header → the **knitr** engine, NO jupyter kernel); (2) the CLI's percent-detection regex is **buggy** (unanchored `raw]` alternation matches ordinary code). The detector (`src/core/render-script.ts` `isRenderScript`, pure, keys on file EXTENSION not languageId) mirrors Quarto's `isJupyterPercentScript`/`isKnitrSpinScript` (recommended: the intent-faithful grouped form, a disclosed divergence from the buggy CLI regex — plan §5.2). The command reuses `PreviewManager.openPreview` unchanged. **Scope shape is an operator decision** (plan §5.3/§10): Shape A (palette-only command, 1 session) vs Shape B (Posit-parity — the `quartoRenderScriptActive` content-driven context key mirroring `execution.ts` `updateCellContext`, the mutually-exclusive `Ctrl+Shift+K` on both `preview`/`previewScript`, editor/title menu, and `onLanguage:r` + `workspaceContains` activation — 2 slices; recommended). A knitr `spin.R` fixture gives the accept-path integration test a real kernel-free `quarto preview` round-trip (jupyter is not required to test the success path). Four open questions (scope, detector semantics, palette visibility, command title) resolved at the implementation kickoff via `AskUserQuestion`. Strict TDD applies to the implementation session.
  16. [x] **Semantic highlighting via the embedded language's LSP — FULLY SHIPPED, item CLOSED.** PLANNED Session 86; Slice 1 (single-language `{python}`) SHIPPED Session 88; Slice 2 (multi-language merge + plan 🐉8) SHIPPED Session 89; **Slice 3 (theming / the D4 legend decision) SHIPPED Session 90** — see `docs/planning/2026-07-12-embedded-lsp-scheme-and-semantic-tokens-plan.md` (v2) and Learnings #97/#98/#99/#100. A `.qmd`'s code cells are now coloured by the user's own language servers, every language present merged into one ascending stream, proven end to end against **two REAL servers at once** (real Pylance + the real built-in TS/JS service).
     - [x] **Slice 1 — `{python}`, single language — SHIPPED Session 88** (Learning #97). New pure `src/core/embedded/semantic-tokens.ts` (`decodeTokens` / `encodeTokens` / `OUR_LEGEND`) + `src/providers/semantic-tokens.ts` (`registerDocumentSemanticTokensProvider` on `{language:"quarto", scheme:"file"|"untitled"}`) + `extension.ts` wiring + a new pure `hasCellOfLanguage` gate in `virtual-doc.ts`. **Proven against REAL Pylance** (`npm run test:lsp`, with a control): `CONSTANT` → `variable.declaration.readonly@5:0`, `main` → `function.declaration@7:4`, `w` → `parameter.declaration@7:9`, `getcwd` → `function@8:14`, every token on a real cell-BODY line. The **modifier-bitset remap is the load-bearing part and it is not theoretical**: Pylance puts `readonly` at bit 7, the standard legend has `modification` there, so a naive index copy would have told the theme a read-only constant was being *mutated*. **Measured drop rate: 13/36 (36%)** of Pylance's tokens are foreign to the standard legend (`module`, `selfParameter`, `builtinConstant`, `magicFunction`) and are dropped — they keep their TextMate colour, so the result is degraded, never *wrong*. That is plan §5.4's explicit fallback; **carrying them is D4 = Slice 3.**
     - [x] **Slice 2 — multi-language merge — SHIPPED Session 89** (Learning #98). `mergeSemanticTokens` + `embeddedLanguagesIn`; the provider forwards EVERY language present and merges the streams, each degrading on its own. Plan 🐉8 (the vdoc churn) resolved in the same slice. A 49-agent adversarial review caught that the 🐉8 fix had broken the identity mapping the whole embedded architecture rests on — fixed before shipping.
     - [x] **Slice 3 — theming / the D4 legend decision — SHIPPED Session 90** (Learnings #99/#100). **The obvious answer was a REGRESSION, and that is the finding.** Carrying all of Pylance's foreign token names to "recover" the measured 36% we drop would have made colours WORSE: a `.qmd`'s `{python}` cell is already coloured by VS Code's bundled MagicPython grammar, so the semantic layer paints OVER a grammar that is mostly right, and a carried-but-unstyleable name **overrides** TextMate rather than degrading to it. **The triage rule** (Learning #99): a serving extension's own `contributes.semanticTokenScopes` entry for a type IS that author saying "the superType default is the WRONG colour for this type" — and those entries are `language: "python"`-gated, so they are inert on a `.qmd`. Pylance ships one for `selfParameter`/`clsParameter`/`magicFunction`/`builtinConstant` + six punctuation types, and NONE for `module`. **The second rule** (Learning #100, learned from a defect the review caught in this slice's own work): a legend is not a promise of EMISSION — carry a name only when a REAL server is observed emitting it. `intrinsic` passed the triage rule and was carried, but Pylance never emits it (it sends `variable`+`builtin`; token type 18 has zero emission sites), so it was a dead legend entry AND a dead manifest rule. **Shipped: carry exactly `module`** (+ `contributes.semanticTokenScopes` for `language: "quarto"` → `entity.name.namespace`), the one name MagicPython gives no scope at all. `magicFunction` would have turned `__init__` from #DCDCAA to #d2a8ff purple. Proven against real Pylance: `module@3:7`, `module@8:11`. 811 unit / 314 integration / 12 real-LSP; clean 43-file `.vsix`.
  17. [ ] **SPLIT into individually-grabbable items 17a–17d (Session 109) — this line is now a POINTER, not a unit of work; the four items live in "Polish / deferred" at the END of this file.** The "lower priority / narrower audience" grouping was never one unit of work — its four sub-features touch unrelated files, config namespaces, and audiences, so it could not be picked or estimated as a single item. Decomposed into: **17a** cell-execution background highlighting (`quarto.cells.background.*`); **17b** Reticulate execution pathway (`quarto.cells.useReticulate`); **17c** create-project-family discoverability (`quarto.newPresentation`/`.newNotebook`/`.fileNewDocument`); **17d** notebook markdown-cell Quarto-aware renderer (`contributes.notebookRenderer`). Full per-item scope/severity/grounding is in each item at the end of the file (appended there, not inserted here, so no `BACKLOG:NNN` line-number citation shifts; `BACKLOG:64` still resolves to this pointer).
  - **Not proposed as ranked items:** the Session 67 refresh also found 3 "Soft / ambiguous comparison" findings (nested/deep YAML completion depth, project-wide/multi-file cross-ref intelligence, and developer-facing extensibility surfaces — a public CLI-query API and Quarto-Extension/Lua authoring support) that don't cleanly fit this project's document-authoring mission the way every ranked item above does. See `docs/POSIT-COMPARISON.md`'s "Soft / ambiguous comparison" bucket — not ranked here without a new operator decision, same treatment as the excluded Visual Editor above.

- [x] **Phase 6d — YAML/`#|` cell-option completion. COMPLETE (6d-1…6d-6 + format-name/scalar + per-format key/value + one-object-level deep-nested key/value all shipped Sessions 18–40; the residual b2-iii-deep DESCOPED Session 120 by operator decision — see below).** The 🐉 schema-source dragon is resolved: **runtime-read** the user's installed Quarto schema (`<share>/editor/tools/yaml/yaml-intelligence-resources.json`, MIT — the CLI, not the AGPL extension; resolve via the undocumented `quarto --paths`, parsed defensively) with a **tiny curated static fallback**; no vendoring, no NOTICE. 6d is **~5 vertical slices** (not one session) — see `docs/planning/2026-06-28-phase-6d-yaml-completion-plan.md` §6. Each = one session, strict TDD, vertical. The plan carries the grep-verified reuse/gaps inventory (§4), the pure-core interface contracts (§5), and the inverted provider-gating trap (§4.3). (v2)
  - [x] **6d-1** `#|` cell-option KEY completion (curated) — **SHIPPED Session 18** (Learning #25). `core/qmd/model.ts` `findCellOptionLines` (view over `findAllCells`; `CELL_OPTION_PREFIX` aligned to Quarto's `^#\s*\| ?`) + `core/yaml-context.ts` `completionContextAt` + `core/yaml-schema.ts` `CURATED_CELL_OPTIONS` + `providers/yaml.ts` gated to the key slot. 273 unit / 70 integration; adversarial review (8 raised → 3 confirmed/5 refuted, all fixes test-first).
  - [x] **6d-2** cell-option VALUE completion (curated enums/booleans) — **SHIPPED Session 19** (Learning #26). `CellOptionLine` gained a `valueSlot` (computed in `slotsOf` alongside `keySlot`, terminated at an unquoted inline comment); `completionContextAt` gained a `cell-option-value` branch (`parentPath:[key]`, whole-token `replaceRange`); `CURATED_CELL_OPTIONS` gained grounded `values` (booleans; echo+fenced; output+asis; code-fold+show; fig-align; the 18-value page-column enum); `providers/yaml.ts` offers them after the colon, prepending a space when the value abuts the colon so accepting yields valid `key: value`. 297 unit / 79 integration; adversarial review (4 raised → 4 confirmed/0 refuted, all fixes test-first: restored a dropped `page-inset`; excluded a trailing inline comment from the value span).
  - [x] **6d-3** schema reader (the 🐉 dragon-resolution slice) — **SHIPPED Session 20** (Learning #27). Runtime-reads `<share>/editor/tools/yaml/yaml-intelligence-resources.json` via the undocumented `quarto --paths`, enriches cell-option completion to the full 94-of-97 set (`hidden` excluded) + descriptions + value enums **resolved from the live schema** (`column`→`page-column`'s 18 values from `definitions.yml`), **engine-filtered**, degrading to `CURATED_CELL_OPTIONS` on any failure (proven by break-revert). New pure `core/quarto-paths.ts` `parseSharePath` + `core/yaml-schema.ts` `parseSchemaIndex`/`SchemaIndex`/`CURATED_SCHEMA_INDEX` (never-throw; simple value forms only — `boolean`/`enum`/`anyOf`/`maybeArrayOf`/`ref`/`string.completions`); thin `quarto/cli.ts` `quartoSharePath()` + `features/yaml-schema-source.ts` (cached load, fallback) + async `providers/yaml.ts`. 326 unit / 81 integration; adversarial review (7 raised → **0 confirmed**; fidelity lens found zero defects — the reader eliminates the transcription trap). Caches by load-once-per-session (a Quarto upgrade needs a window reload).
  - [x] **6d-4** front-matter top-level KEY completion — **SHIPPED Session 21** (Learning #28). The first front-matter slice. Inside the `---` block, typing a top-level key offers document keys read from `schema/document-*.yml` (the 378 visible flat names), degrading to `CURATED_FRONTMATTER_KEYS`. New shared-model surface `findFrontMatter`/`inFrontMatter`/`frontMatterContentLines` (views over the ONE `scanRegions` pass; break-revert-proven agreement test; `core/frontmatter.ts`'s duplicate `---` parser folded away); a `frontmatter-key` context (`topLevelKeySlot`, column-0 only); `SchemaIndex.frontMatterKeys([])` via the shared `collectFields`; provider items tagged `detail:"Quarto document option"`. Bidirectional inverted gating proven (no document-option items off-region; `@` suppressed in FM). The flat `document-*` names OMIT pure container keys like `execute` (object-graph, deferred 6d-6) — `execute`/`format` are curated-only. 361 unit / 88 integration; adversarial review (3 raised → **0 confirmed**; one docstring-precision fix). The `csl` enrichment test + a `quartoSharePath` break-revert prove the FM reader ran AND degrades (gate d).
  - [x] **6d-5** front-matter top-level VALUE completion — **SHIPPED Session 22** (Learning #29). After a known top-level `key:` in the `---` block, offers that key's enum/boolean values from the installed schema (`toc: true/false`, `editor: source/visual`, `engine: jupyter/knitr`, …), degrading to curated `toc`/`number-sections` booleans offline. `topLevelKeySlot`→`topLevelSlots` (key+value spans, mirroring `slotsOf`'s value grammar IN yaml-context.ts); a `frontmatter-value` context (`parentPath:[key]` past the colon); provider reuses the 6d-2 value path (`valueItems`/`valueItem` parameterized by `detail` → `"Quarto document option value"`, leading-space-on-`:`, whole-token replaceRange). **The schema READER was UNTOUCHED — `parseSchemaIndex`→`toField`→`valuesOfSchema` already resolves document-key values (173/378 visible doc fields carry values; grounded firsthand).** 372 unit / 96 integration; adversarial review (2 raised → **0 confirmed**, 3rd consecutive clean). The `editor`→source/visual schema-only enrichment test + a `quartoSharePath`-throw break-revert prove the FM-value reader ran AND degrades (gate d).
  - [x] **6d-6 (v1)** nested `execute:` KEY completion — **SHIPPED Session 23** (Learning #30). The first nested slice and the plan's "cheap one-level approximation": on an indented line one level under `execute:`, offers the curated execute child-keys (`echo`/`eval`/`output`/`warning`/`error`/`include`/`cache`/`freeze`/`enabled`/`daemon`/`daemon-restart`/`keep-md`/`keep-ipynb`). `core/yaml-context.ts` `frontMatterContextAt` now takes full `text` + new `nestedKeyContextAt`/`nearestShallowerLine`/`NESTED_CONTAINERS={execute}` (parent-scan detector that BAILS on any ambiguity — non-allow-listed container, scalar/block-scalar/flow parent, deeper nesting, sequence, value position); `core/yaml-schema.ts` `CURATED_EXECUTE_KEYS` served by `frontMatterKeys(["execute"])` for BOTH parsed and curated index (nested is curated-only in v1 — the live schema assembles the execute object across 5 files via a `document-execute` context glob, with no single readable list; `quarto render` validation is lenient). `providers/yaml.ts` UNCHANGED (its `frontmatter-key` branch is generic over `parentPath`). 385 unit / 102 integration; adversarial review (2 raised → **0 confirmed**, 4th consecutive clean; one applied docstring-provenance fix verified firsthand). Detector guards break-revert-proven; the `echo`-nested-only paired control proves the path runs + doesn't cross-pollute.
  - [x] **6d-6 (cont.)** nested `execute:` VALUE completion — **SHIPPED Session 24** (Learning #31). The value-side complement of the nested-KEY slice: past the colon on an indented line under `execute:`, offers the child's grounded value enum (`echo` true/false/fenced, `cache` true/false/refresh, `freeze` true/false/auto, the rest true/false). `core/yaml-context.ts` `nestedKeyContextAt` gained a value branch (`parentPath:[container,key]`) + the extracted shared `valueSlotAfterColon` helper; `CURATED_EXECUTE_KEYS` gained grounded `values`; the provider's `frontmatter-value` branch generalized to `frontMatterKeys(parentPath.slice(0,-1))` (unifies top-level + nested). Nested values are curated-only (same source as nested keys). 391 unit / 109 integration; adversarial review (1 raised → **0 confirmed**, 5th consecutive clean). The `echo` discriminator (cell-shared flags absent from the flat `document-*` list — verified firsthand) is the faithful RED→GREEN proof of the provider change (gate d).
  - [x] **6d-6+ continuation (a)** `format:` → format names — **SHIPPED Session 25** (Learning #32). On an indented key line one level under `format:`, offers Quarto output-format names (65 visible). `"format"` added to `NESTED_CONTAINERS`; `core/yaml-schema.ts` `collectFormatNames` reads the live `pandoc/formats.yml` + synthesized `md`/`hugo`/`dashboard`/`email` − hidden legacy variants (replicating Quarto's `makeFrontMatterFormatSchema` in the installed `web-worker.js`), with a `CURATED_FORMAT_NAMES` fallback — **reader-derived** (NOT curated-only, unlike `execute:`, because format names have a single readable list). `indexOf` 3rd `formatFields` param serves `frontMatterKeys(["format"])`; provider UNCHANGED (generic over `parentPath`). 403 unit / 116 integration; adversarial review (20 agents, 5 raised → **0 confirmed**, 6th consecutive clean). The `texinfo` reader-only enrichment + a `quartoSharePath`-throw break-revert (reds ONLY `texinfo`, curated 14 serve) prove the reader ran AND degrades (gate d).
  - [x] **6d-6+ continuation (b1)** top-level `format:` scalar value completion — **SHIPPED Session 26** (Learning #33). After the colon on the top-level `format:` key (`format: <here>`), offers the Quarto output-format names (the small adjacent win). PURE-CORE only: `core/yaml-schema.ts` `indexOf` enriches the top-level `format` field's `.values` with the format names (DERIVED into a new `topLevelFields` list, raw arrays untouched), serving BOTH the parsed reader and the curated fallback — the **name-collision pattern** (the flat `document-*` list models `format` only as an epub-scoped string with no enum, so model the real value-domain at the index view). **Context AND provider UNCHANGED** (the 6d-5 `frontmatter-value` context already returns `parentPath:["format"]`; the provider's generic value branch already resolves `frontMatterKeys([]).find(name).values`). 405 unit / 121 integration; adversarial review (5 agents, **0 raised**, 7th consecutive clean). The `texinfo` reader-only enrichment + a runtime-conditional `quartoSharePath`-throw break-revert (reds ONLY `texinfo`, curated `html` stays) prove the values are reader-derived AND degrade (gate d; the break must be runtime-conditional so it doesn't break the BUILD — Learning #33d).
  - [ ] **6d-6+ continuation (b2)** per-format options — **PLANNED Session 33** (`docs/planning/2026-06-30-phase-6d6b2-per-format-options-plan.md`, Learning #40). Completion for options under a concrete format name (`format:\n  html:\n    <opt>`). **🔑 The "recursive `schema.yml` walk 🐉" the earlier framing feared is DISSOLVED by grounding:** per-format scoping is a **flat `tags.formats` filter** over the `document-*`/`cell-*` field lists (alias-expanded via the closed 14-entry `format-aliases.yml`, negation-aware — Quarto's own `getFormatSchema`/`useSchema`), NOT a meta-schema walk. So b2 = **~2 slices** (each a separate session, strict TDD): (v2)
    - [x] **b2-i** per-format option **KEY** completion — **SHIPPED Session 34** (Learning #41). New pure `core/format-aliases.ts` (`expandFormatAliases` + `formatMatches`); `core/yaml-context.ts` `nestedKeyContextAt`→`nestedParentPath` bounded 2-level walk rooted at `format` (`nearestShallowerLine` now returns a line index); `core/yaml-schema.ts` `SchemaField.formats`+`.contexts`, `parseFormatAliases`, `perFormatSource`, the `frontMatterKeys(["format",fmt])` branch, `CURATED_FORMAT_OPTIONS`. **Provider UNCHANGED.** **🔑 The cell-* fold-in predicate is `tags.contexts` matches `document-*` (Quarto's `getFormatSchema`), NOT `tags.formats` present** — the adversarial review (5-lens/3-refuter, 3 raised → 1 confirmed/2 refuted) caught the over-offer of cell-only options (`fig-alt`/`external`/dashboard `color`…) and it was fixed test-first; the corrected predicate reproduces `getFormatSchema` EXACTLY over the real 1.7.33 schema (0 diff). 456 unit / 155 integration; clean 33-file `.vsix`. Both mechanisms + the fix break-revert-proven (gate d).
    - [x] **b2-ii** per-format option **VALUE** completion — **SHIPPED Session 35 (Learning #42), TEST-ONLY.** Traced firsthand that the whole value path already existed after b2-i (detector `yaml-context.ts:198-208` emits `{frontmatter-value, parentPath:["format",fmt,key]}`; provider `yaml.ts:102-115` generic over `parentPath`, UNCHANGED; reader `frontMatterKeys(["format",fmt])` returns per-format fields already carrying `.values`) → **NO production code changed; 6 tests only** (3 unit + 3 integration). Gate-d discriminator `code-overflow` (`$html-all`, `document-code` context, enum `scroll`/`wrap`, curated-absent): reader-only AND format-scoped (offers no values under gfm — the value discrimination falls out of the key filter). Strict-TDD RED = break-revert each load-bearing test (runtime-conditional). Review: 5-lens/3-refuter Workflow (11 agents, ~1.1M tokens), 2 raised → 0 confirmed (both the `toc`-label nuance, reworded). 459 unit / 158 integration; clean 33-file `.vsix`.
    - [ ] **b2-iii** deep nesting (`format:\n  html:\n    <opt>:\n      <sub>`) — object-valued format options' sub-keys/values — **PLANNED Session 36** (`docs/planning/2026-06-30-phase-6d6b2iii-deep-nesting-plan.md`, Learning #43). **🔑 The "recursive `schema.yml` walk 🐉" is right-sized (2nd over-estimate after b2, Learning #40a):** `schema/schema.yml` is the DSL meta-grammar; deep-nested completion is a **bounded, depth+cycle-guarded object-property resolver** (mirrors Quarto's `navigateSchemaByInstancePath` + `getObjectCompletions`) over `document-*` + `definitions.yml` — the same shape as the existing `valuesOfSchema`, NOT a meta-graph walk. **But it is genuine new code, NOT test-only** (adversary-refuted "free like b2-ii"): 40 object-valued options (all `document-*`; `allOf` unused; `super` only citation/brand), max depth 4, one reachable cycle (`about→links`). b2-iii = **~2 slices** (each a separate session, strict TDD): (v2)
      - [x] **b2-iii-key** deep-nested option **KEY** completion (one object level) — **SHIPPED Session 37** (Learning #44). Detector N-level format-rooted walk (`nestedParentPath`, generalized to climb any number of pure-mapping levels rooted at `format`); new pure `resolveObjectProperties`/`childDescription`/`objectChildren` in `core/yaml-schema.ts` (`SchemaField.children`; capped at exactly ONE object level); `frontMatterKeys(len≥3)` navigation branch. **Provider UNCHANGED** (generic over `parentPath`). Curated fallback added for `code-tools`/`theme`. **🔑 Grounding found the plan's own §5.2 pseudocode was incomplete: reaching the real 40 objects needs FIVE unwrap forms** (anyOf/ref/maybeArrayOf/**arrayOf**/**the `{schema:X}` wrapper**), not the three the plan named — omitting either undercounts to 36/38. Adversarial review (5-lens/3-refuter, 6 raised → 3 confirmed): (1, medium) a bare **unconditional** `arrayOf` (e.g. `other-links`, `filters`) must NOT be unwrapped the same as `maybeArrayOf` — it has no bare-mapping form, so offering its array element's properties as flat keys was schema-invalid; fixed by dropping the bare-`arrayOf` branch entirely (a `maybeArrayOf`, where a single mapping IS valid, is unaffected). (2, low) `properties` typed as a JSON array (`typeof x==="object"` is true for arrays) silently produced index-named garbage keys — fixed with an explicit `!Array.isArray` guard, consistent with this file's existing `Array.isArray` guards elsewhere. (3, low) `childDescription`'s actual resolved output was never asserted — added direct-sibling/nested/absent coverage. All three break-revert-proven; the arrayOf fix also break-revert-proven against the REAL installed schema (reproduced the actual 5-key leak). A prior ghost-session gap (a first attempt at this slice committed only the detector layer, then vanished without closing out) was discovered and closed at Phase 0 orientation — the checkpoint-commit discipline meant nothing was lost. 482 unit / 165 integration; clean 33-file `.vsix`. Flipped the `yaml-context.test.ts:325` + integration `yaml.test.ts:1167` deferral locks as planned.
      - [x] **b2-iii-value** deep-nested option **VALUE** completion — **SHIPPED Session 40** (Learning #47). Detector + provider UNCHANGED (both already generic, confirmed by trace + a break-revert-proven detector regression locking the 4-element value-context shape). All new code is THREE `valuesOfSchema` extensions, ground-truthed against the real installed 1.7.33 schema: the `{enum:{values:[…]}}` definition-enum form (`math-methods`), the `{tags, schema:…}` wrapper (`editor.render-on-save`), and the object-wrapped `{boolean:{description,default}}` DSL form (`crossref.chapters`/`ref-hyperlink`, `chalkboard.read-only` — the gap Session 37's review found and refuted as out-of-scope for b2-iii-key). Strict TDD: RED shown for each of the 3 gaps, then the minimal fix, 3 checkpoint commits. 6 new integration tests against the REAL schema, break-revert-proven (the 3 gate-d + leading-space tests go RED under a forced `quartoSharePath` throw while curated-served controls stay GREEN). 482 unit (+4) / 171 integration (+6); clean 35-file `.vsix`.
      - [~] **b2-iii-deep** 🐉 depth-4 (a 2nd object level) + full `super`/`resolveRef` merge — **DESCOPED Session 120 (operator decision).** 6d already ships a complete milestone and deep-nested completion is an enhancement, not a gap — the plan's own recommended stopping point was after b2-iii-value (shipped S40). Degrades gracefully today (the reader returns `[]` for `len≥4`; `citation` completes its 10 own keys, not the 123 CSL-inherited). Re-open only on a new operator decision — the plan (`docs/planning/2026-06-30-phase-6d6b2iii-deep-nesting-plan.md`) makes it buildable-if-chosen.
      - [x] **(unrelated, review-caught) `copyright` front-matter key is silently dropped to zero children under every format.** — **RESOLVED Session 41.** `collectFields`'s first-occurrence-wins dedup kept whichever of Quarto's TWO same-named `copyright` document options iterated first (`schema/document-attributes.yml`'s bare property-less JATS-scoped one, alphabetically before `schema/document-metadata.yml`'s real `{anyOf:[{object:{properties:{year,holder,statement}}}, "string"]}` one) — so the richer definition was discarded entirely, for every format, not just JATS. Fixed generically: `collectFields` now keeps the RICHEST same-named definition (by `children.length + values.length`), not whichever occurs first — also grep-verified the same pattern against 3 other duplicated document-key names (`logo`/`subject`/`footer`); none regress since their duplicates tie in richness. (Learning #48.)
    - **⚠ Rejected (do NOT ship):** a recursive `schema.yml` meta-graph walk (category error — `schema.yml` is the DSL grammar); b2-iii as a test-only slice (adversary-refuted — genuine new code in 3 layers). The operator may also **descope b2-iii entirely** — 6d already ships a complete milestone; deep-nested completion is an enhancement, not a gap.
    - **⚠ Rejected alternative (do NOT ship):** the naive flat-`document-*` **union** — adversary-verified wrong 49–76 %/format, makes every format identical, and isn't even cheaper (`tags.formats` already parsed & discarded at `yaml-schema.ts:485`). The correct filter is cheap AND faithful; ship it directly. 6d-1…6d-6 + format-name + format-scalar already ship a complete milestone, so the operator may descope 6d here.
- Phase 6e — embedded-cell language completion (virtual-document request forwarding) — **COMPLETE: PLANNED Session 27; 6e-1…6e-5 ALL SHIPPED Sessions 28–32** (`docs/planning/2026-06-29-phase-6e-embedded-cell-completion-plan.md`, Learnings #34–#39). The 🐉 offset-mapping risk is RESOLVED by whole-document blanking → identity offset mapping (no per-cell offset math, no LSP); forwards completion/hover/definition/signature-help into `{python}`/`{r}`/`{julia}`/`{ojs}` cells via a per-language virtual document + `vscode.execute*Provider`. 5 vertical slices, each a separate session, strict TDD — all done:
  - [x] **6e-1** Python completion forwarding + ALL shared infra (the tracer bullet) — **SHIPPED Session 28** (Learning #35). `core/embedded/lang-map.ts` (`cellLanguageId`, python only) + `core/embedded/virtual-doc.ts` (`embeddedCellAt` body-gate + length-preserving CRLF-safe `buildVirtualContent`, both views over `scanRegions`) + `providers/embedded.ts` (`VirtualDocStore` content provider keyed by canonical `uri.toString()` + owners-map eviction; `EmbeddedCompletionProvider` gated to cell BODY, before any await; `filterOutOfCellEdits` strips out-of-cell secondary edits) wired in `extension.ts`. **§9 Q8 RESOLVED** (the `.py` vdoc resolves to languageId python in the bare host). Gate-d: four guards break-revert-proven + an edit-sync regression proving rebuild-per-request defeats staleness. 418 unit / 128 integration; adversarial review (17 agents, 4 raised → **0 confirmed**, 8th consecutive clean). Keyed by canonical `uri.toString()` (not the plan's `uri.path.slice(1)` — `Uri.parse` decodes the path).
  - [x] **6e-2** extend to `{r}`/`{julia}`/`{ojs}` + graceful degradation — **SHIPPED Session 29** (Learning #36). A DATA change: `cellLanguageId` grew (`r→{r,r}`, `julia→{julia,jl}`, `ojs→{javascript,js}`, `js` alias); `embeddedCellAt`/`buildVirtualContent` + `providers/embedded.ts` UNCHANGED for forwarding. Pure `needsLanguageExtension(languageId, registered)` (keyed on `getLanguages()`, NEVER an empty result — §9 Q6) + a one-time non-blocking hint (`javascript`/`python` built-in → `{ojs}`/`{python}` never hint). **§9 Q8 split RESOLVED:** `.js` (ojs) resolves to languageId `javascript` (proven end-to-end — the token≠languageId dragon); `.r`/`.jl` likely don't → `{scheme:"quarto-embedded"}` stand-ins; the REAL r/julia feature needs the user's extension. Gate-d: the `{julia}` degradation test made discriminating (asserts the forward RAN) + break-revert-proven. 431 unit / 130 integration; review (8 agents, 1 raised → **0 survived**, 9th consecutive clean). 6e-1 + 6e-2 = a complete 6e completion milestone.
  - [x] **6e-3** *(optional, v2.x)* embedded hover — **SHIPPED Session 30** (Learning #37). A thin ADAPTER addition: `EmbeddedHoverProvider` + `mergeHovers` in `providers/embedded.ts` (forwards via `vscode.executeHoverProvider`, collapses the `Hover[]` into one Hover — flatMap contents + keep the first identity-mapped range; no remap, hovers carry no URI); `core/embedded/*` UNCHANGED (no new core code / no new unit test). No degradation hint in hover (completion owns the one-time `getLanguages()` hint). 431 unit / 137 integration (+7 hover); review 11 agents, 0 confirmed, 10th consecutive clean (two refuted `mergeHovers` coverage niceties applied test-first). Gate/identity-range/multi-hover-merge each break-revert-proven.
  - [x] **6e-4** *(optional)* go-to-definition — **SHIPPED Session 31** (Learning #38). The ONE residual-remap slice. A thin ADAPTER addition: `EmbeddedDefinitionProvider` + `remapDefinitions`/`remapDefinition`/`isLocationLink` in `providers/embedded.ts` (forwards via `vscode.executeDefinitionProvider`; **conditionally** swaps the returned `Location`/`LocationLink`'s vdoc URI back to the `.qmd` — only when it === the vdoc URI; a def into another file passes through; range identity-mapped, returned unchanged; handles BOTH shapes via a `targetUri !== undefined` type-guard); the heterogeneous `(Location|LocationLink)[]` return is cast once at the boundary (VS Code resolves each element at runtime). `core/embedded/*` UNCHANGED (no new core code / no new unit test). 431 unit / 145 integration (+8 go-to-def); review 5 agents, **0 raised**, 11th consecutive clean. Four guards (gate / conditional-swap / LocationLink-branch / identity-range) each break-revert-proven, each runtime-conditional so the build stayed clean (a TS6133 unused-param break is a build break, not a behavioral RED — Learning #38d). The skip-LocationLink break-revert also proved `executeDefinitionProvider` preserves the `LocationLink` shape at the outer boundary.
  - [x] **6e-5** *(optional, v2.x)* signature help — **SHIPPED Session 32** (Learning #39). The SIMPLEST request type and the FINAL 6e slice. A thin ADAPTER addition: `EmbeddedSignatureHelpProvider` in `providers/embedded.ts` (forwards via `vscode.executeSignatureHelpProvider`, returns the SINGLE `SignatureHelp` **UNCHANGED** — no array/URI/document-coordinate range/edits → no remap, no merge); registered with the sig trigger chars `(`/`,` (distinct from completion's `.`) sharing the one `VirtualDocStore`. `core/embedded/*` UNCHANGED (no new core code / no new unit test — the third request-type slice proving that). Off-region yields nothing; empty upstream → no signature help, no throw. 431 unit / 150 integration (+5: tracer + 3 gating + degradation); review 5 agents, **0 raised**, 12th consecutive clean. The gate is break-revert-proven (runtime-conditional, build stays clean); the faithful suite is correctly smaller than hover's/def's (no range/URI/merge to guard). **Grounded the return shape against the installed `@types/vscode` `.d.ts` FIRST** (Learning #39): single-result + no-remap was confirmed, not assumed. **← 6e-1…6e-5 ALL DONE; the Phase 6e plan is COMPLETE.**
  - Diagnostics are OUT OF SCOPE — the request-forwarding technique structurally cannot forward them (would need a real LSP / Tier C). (v2)
- [x] Phase 7 authoring aids — **remaining slice: image paste — SHIPPED Session 58** (plan: `docs/planning/2026-07-09-image-paste-plan.md`, Session 57, Learning #65; implementation Learning #66). **Phase 7 is now fully complete.** Operator resolved the plan's 3 open questions via `AskUserQuestion` before code: **Q1 destination = `images/` subfolder** (operator chose this over the plan's own recommended no-subfolder default); **Q2 drag-and-drop parity bundled into v1** (adds L4, the plan's own recommendation); **Q3 filename trusts `DataTransferFile.name` when present/non-empty, generates otherwise** (the plan's own recommendation). Shipped as ONE vertical-slice session across 4 checkpoint-committed layers: **L1** (`df3bd8f`) pure `core/image-paste.ts` (`extensionForMimeType`/`deriveImageName`/`resolveNonCollidingName`/`buildImageRelativePath`/`buildImagePasteInsertText`), strict TDD, the collision-avoidance loop break-revert-proven. **L2** (`5b0e26c`) `providers/image-paste.ts` `QmdImagePasteEditProvider` — `vscode.languages.registerDocumentPasteEditProvider({language:"quarto"}, ...)`, zero `package.json` contribution (same class as `providers/workspace-symbols.ts`). **L3** (`2763bad`) integration tests — **refined the plan's own D1 disclosure**: `vscode.DataTransfer`/`DataTransferItem` do not runtime-validate class identity, so a hand-built object satisfying the (interface, not class-with-hidden-state) `DataTransferFile` shape flows through the REAL registered provider exactly like a real OS paste would, closing most of the "byte-read path is F5-only" gap — tests exercise mime-routing, the no-file-backed-payload fallback, AND the real write + collision-avoidance path end-to-end via `vscode.workspace.applyEdit` + a real on-disk mkdtemp fixture (break-revert-proven). **L4** (`5942108`) `QmdImageDropEditProvider` — drag-and-drop parity bundled into v1 per Q2, sharing L1's core + a near-mirror adapter, its own 3 integration tests. Confirmed via VS Code's own MIT built-in source that `.qmd` gets ZERO built-in image-paste support today, and via Posit's public GitHub Discussions that their own source-editor `.qmd` paste-image is also unimplemented — **this ships ahead of Posit's source editor**, not parity catch-up (`docs/POSIT-COMPARISON.md` updated: "True parity in absence" 2→1, "We're ahead" 2→3). 610 unit (+15, all L1) / 215 integration (+8, L3+L4); clean 42-file `.vsix` (unchanged file count — zero size/manifest impact, per plan §5). What remains genuinely F5-only: the OS clipboard/drag event itself producing the `DataTransferFile` in the first place. **Formatting toggles** shipped Session 14 (`core/format-toggle.ts`; Learning #21). **Math preview webview (`Quarto: Preview Math`) shipped Session 15** (`core/math-regions.ts` + `core/math-preview-html.ts` + `features/math-preview.ts`; vendored KaTeX in `media/katex/`; CLAUDE.md Learning #22). **Diagram preview webview (`Quarto: Preview Diagram`) shipped Session 16** (`core/diagram-regions.ts` + `core/diagram-preview-html.ts` + `features/diagram-preview.ts`; vendored Mermaid in `media/mermaid/`; Mermaid rendered, `{dot}` detected + placeholder). **Snippets (`contributes.snippets`) shipped Session 53** (see BACKLOG "Up Next" item #5 above for full detail). **Graphviz (`{dot}`) rendering shipped Session 56** (vendored `@viz-js/viz`'s `dist/viz-global.js` in `media/graphviz/`, `script-src … 'wasm-unsafe-eval'`, `dot` cells render at parity with Mermaid; see BACKLOG "Up Next" item #7 above for full detail).

## Documentation

- [x] **Feature comparison vs Posit's official Quarto VS Code extension** (operator-requested, Session 29). — **RESOLVED Session 42.** `docs/POSIT-COMPARISON.md` — 31 feature-parity rows across 7 categories, grounded in OUR inventory (`ROADMAP.md` + `package.json` + `src/`) and a RESEARCHED Posit inventory (marketplace, `quarto.org` docs, the `quarto-dev/quarto` repo's public manifest/README/CHANGELOG — features only, per Learning #1's AGPL-3.0 look-but-don't-copy gate). Every parity/gap claim adversarially refute-checked (31 independent verify agents; 14/31 rows corrected — see Learning #49). The doc's own "What This Suggests for Future Work" section proposed 6 follow-ups — **promoted and re-ranked in the "Up Next" list above (Session 43, via a grill-me session)**, against the operator's actual usage rather than raw gap size; see that entry for the ranking and rationale.

## Polish / deferred

- [x] **`disposeAllVdocs` leaks the `mkdtemp` fallback directory when a deactivate races an unresolved `mkdtemp` — FIXED Session 101** (filed by the Session 91 adversarial review, LOW; fixed together with `:121` leg (b), which is the same ~6 lines of `fallbackDirPromise` state — see that item). **The premise was CONFIRMED by observation, but it is NOT a race and the filed fix was INSUFFICIENT — both findings are the substance of this session.** (1) **Deterministic, not racy.** `disposeAllVdocs` reaches its fallback check after `await Promise.all(all.map(deleteQuietly))`; when no vdocs are registered — the untitled-first-forward case — that is a microtask, and microtasks always fully drain before the event loop's poll phase can deliver `mkdtemp`'s libuv-threadpool completion. So the resolved value is *reliably* unset there and the leak is the common case, not an occasional one (measured: leaked on 2 of 2 unstubbed runs). (2) **The filed fix ("observe `fallbackDirPromise` … so a raced deactivate still `rmdir`s") does NOT work alone — measured, not argued.** With only that change the directory still survived every run, EMPTY (`survived=true contents=[]`): the in-flight forward writes its file into the temp dir before the non-recursive `rmdir` lands, `rmdir` fails ENOTEMPTY, and the forward's own epoch guard then deletes the file — leaving exactly the empty directory the fix was meant to reclaim. The fix therefore needs a THIRD change: a pre-write epoch re-check in `ensureVdoc` (bail before writing a file we are about to delete anyway). All three changes are break-revert-proven load-bearing, each against its own test. (3) **The "worse" re-enable-without-reload clause is UNVERIFIED and was not relied on**; the harm that IS established is an empty directory, since the epoch guard demonstrably deletes the source file. The resolved `fallbackDir` companion variable is DELETED — holding the memo only as its promise is what makes the state unmisreadable. Pins: `test/integration/suite/embedded-vdoc.test.ts` describe "the fallback temp directory's lifecycle (BACKLOG:102 + :121b)". **NOTE the OS-temp-dir sweep gap this item's own text mentions is now filed separately at the end of this section — it is a bigger defect than this one was.**
- [x] **The deactivate epoch guard covered the mid-await race but NOT a forward dispatched *after* `disposeAllVdocs` runs — FIXED Session 107 with a GLOBAL `deactivated` latch (the S105/S106 handoff's "needs a per-document latch" was REFUTED); both in-flight-test nits fixed too** (filed Session 91 adversarial review, LOW). (1) Because `disposeAllEpoch` moves exactly once per session, a forward DISPATCHED entirely after the bump snapshotted the post-bump value and passed its own guard, re-registering a vdoc holding the user's real cell source into the just-cleared maps (RED test observed a real stranded `vdoc-mit.*.py`; nothing in that session reclaims it). Reachable because `deactivate()` fire-and-forgets `void disposeAllVdocs()` (`src/extension.ts`) and the host disposes provider subscriptions AFTERWARD, so providers stay registered for an interval past the bump. **Fix:** a module-global `deactivated` boolean, set in `disposeAllVdocs` alongside the epoch bump and checked at the top of `ensureVdoc`, cleared by `resetDeactivation()` from `activate()` (a re-enable-without-reload retains the JS module, so a never-reset latch would leave embedded features dead — BACKLOG:102's lesson). It MUST be GLOBAL not per-document: an after-dispatch forward may be for a document in no map to key a per-owner latch on — the same reason `disposeAllEpoch` is global — **so the filed "would apply symmetrically to `disposeVdocs`" premise is FALSE.** The `disposeVdocs` (per-document close) sibling needs no latch: (a) unreachable — a closed document receives no forward; the one no-gesture forward, semantic tokens, has its retry timer cleared by `provider.forget()` in the same close handler (`providers/semantic-tokens.ts`) before `disposeVdocs`; and (b) self-healing — a hypothetical re-registration lands in `docFiles`, reclaimed at session-end `disposeAllVdocs`, whereas the deactivate case runs after the last `disposeAllVdocs` with no backstop. Documented in a comment at `disposeVdocs` (`embedded-vdoc.ts`). Break-revert-proven load-bearing; a 5-lens + completeness-critic adversarial review found no refutation. (2) Both nits fixed in `test/integration/suite/embedded-vdoc.test.ts`: the S88 doc-close in-flight test's dead `if (uri !== undefined)` branch is now `assert.strictEqual(uri, undefined)` (matching its S91 deactivate sibling); the cross-suite coupling is closed by `afterEach(resetDeactivation)` in every latch-setting describe (order-independent). Residual (noted, not a defect): the `activate()`→`resetDeactivation()` wiring is not integration-testable (idempotent `activate()` on the shared Extension Development Host) — pinned by a code comment at the call site.
- [ ] **R/Julia leak measurement under `diagnosticMode: "workspace"`** (filed Session 93 at the close of the python fix below; explicitly UNMEASURED). Candidate G's `# type: ignore` mute is python-gated (a `#` is a JS syntax error, and `# type: ignore` is **Pyright/PEP-484-specific** — an R (lintr) or Julia (LanguageServer.jl) server treats it as an inert comment, suppressing nothing). Slice 2 made `{r}`/`{julia}` vdoc minting PASSIVE (see the item below), so merely opening a `.qmd` with such a cell mints and opens its model — and if a real R/Julia server is installed, its diagnostics attach to the vdoc, the identical location-independent tracked-set leak. The confirmed leak is Pylance-only; whether R/Julia leak is **not measured** (no such servers in this repo's `test:lsp` harness). Small, evidence-gated: point at a real R (or Julia) server, drive the forwards under `QMD_LSP_DIAGMODE=workspace`, observe. **The FIX SHAPE (an in-content suppression directive on line 0) is what extends — NOT the string `# type: ignore`:** each server needs its OWN suppression directive if one exists (e.g. a lintr `# nolint` line, a Julia `#src`/pragma — to be researched; some may have none). If none exists, the fallback lever is the vdoc-minting-lifecycle item below — but note its S89-filed "gate vdoc minting on a *registered server*" fix was **REFUTED Session 94** (VS Code registers `r`/`julia`/`python`/`javascript` via built-in language basics, so `getLanguages()`/`needsLanguageExtension` cannot gate on server presence; Learning #105), leaving only the unavoidable-first-mint lifecycle change described there. Do NOT reach for the disposal-timing rewrite the S92 plan §5 rejected.
- [ ] **Format Cell adjacency edge from candidate G's line-0 mute — cell whose fence is at document line 0** (Session 93 review, completeness critic, LOW; in_scope but UNVERIFIED). `buildCellVirtualContent` now puts `# type: ignore` on vdoc line 0; when a `.qmd` opens directly with a `{python}` cell (fence at line 0, no front matter/prose), that comment sits immediately above the body (line 1) with no blank between. Format Cell forwards the cell vdoc to the user's real Python formatter; a formatter that inserts a blank line after a leading module comment could emit an edit at body line 1, which `format-cell.ts` `rangeWithinCell` (`:139`) ACCEPTS (it is a body line of the cell) — inserting a spurious blank into the user's cell. **Unverified and likely a non-issue:** the harness has no Python formatter (Format Cell against a real Python formatter is already documented UNPROVEN, `format-cell.ts:22`), and black specifically does NOT insert a blank after a leading comment (it attaches to the following statement). Pinned by the unit test "when the cell's fence is at line 0, the mute sits IMMEDIATELY above the body". Recheck when a real Python formatter (e.g. `ms-python.black-formatter`) is installed; if a formatter does emit the spurious edit, tighten `rangeWithinCell` to reject a leading-blank-insert at the body's first line, or skip the cell-builder injection when `cell.startLine === 0`.
- [ ] **Residual: transient PARSE/SYNTAX-error diagnostics on a mid-typing cell still leak under `diagnosticMode: "workspace"`** (filed Session 93 at the close of the python fix below). A file-level `# type: ignore` mutes Pyright's *type/name/import* diagnostics (the persistent phantom class the fix targets), but **not** *parse/syntax* errors — Pyright reports those before type-checking. So a vdoc built while the user is mid-typing an incomplete expression (`os.` with a dangling dot, `greet(` with an unclosed paren) still surfaces a transient syntax diagnostic on its `.quarto/vdoc-mit/` path under workspace mode. Directly observed Session 93 (the `test:lsp` completion/signature-help fixtures — `os.`, `greet(` — leak `Expected attribute name after "."` / `"(" was not closed` even WITH the mute). Lesser than the fixed class: transient (clears once the line is syntactically complete) and not the cross-cell/import pollution the item was really about; and it matches Posit's own acknowledged residual (their three-technique approach still leaks, `quarto` #855). No clean in-content fix (`# type: ignore` cannot suppress a parse error); would need the heavy own-a-client/close-the-model path the S92 plan rejected. Low priority — reassess only if users report it.
- [x] **Embedded vdocs publish diagnostics to the Problems panel under `python.analysis.diagnosticMode: "workspace"` — FIXED Session 93** (candidate G; plan `docs/planning/2026-07-14-embedded-vdoc-diagnostics-leak-plan.md`, Session 92; implementation Learning #103). **The obvious/heavy fixes this item speculated (own a `vscode-languageclient`; find an API to close the background model) were REFUTED by the S92 plan** — the leak follows Pyright's *tracked-set injection at `didOpen`*, which is location-independent, so relocating the vdoc out of the workspace does nothing either (measured: OS-temp vdocs leak identically). The shipped fix is a **third mechanism class the item's framing never named — in-content suppression**: inject a file-level `# type: ignore` on the vdoc's already-blanked **line 0**, in both pure builders (`src/core/embedded/virtual-doc.ts` `buildVirtualContent` + `buildCellVirtualContent`), gated python-only (a `#` is a JS syntax error) AND to non-whitespace python body content (so the `embeddedLanguagesIn ⟺ non-empty` invariant holds). Line 0 is never a code body line, so the write shifts no coordinate. Proven **RED→GREEN live** against real Pylance 2026.2.1 under `QMD_LSP_DIAGMODE=workspace` (`test/lsp/suite/real-lsp.test.ts` "MUTES phantom vdoc diagnostics …"): pre-fix the fixture's vdocs carried `"df" is not defined; "undefined_name_xyz" is not defined` and `Import "pandas" could not be resolved`; post-fix **zero** vdoc diagnostics, completion preserved (`os.getcwd` resolves), and **no** semantic token on `.qmd` line 0. Commits `48bdf2a` (L1) / `772e0dc` (L2). **Scope of the fix (honest):** it mutes the persistent **type/name/import** phantom diagnostics — the cross-cell "df is not defined" and unresolved-import pollution this item is really about. It does NOT suppress transient **parse/syntax** errors (`os.` mid-typing) — filed as its own residual item above — and R/Julia are UNMEASURED (filed as their own item above). Under the DEFAULT `openFilesOnly` mode there was never any leak (pinned by the sibling `test:lsp` case). Do not confuse with item 10 (which *wanted* embedded diagnostics and closed as not-pursuable); this was *unwanted* diagnostics leaking.

- [x] **🔑 The vdoc reuse cache CANNOT HIT for any whole-language forward — RESOLVED Session 89 (plan 🐉8).** Root cause was exactly as filed: the builders were **length-preserving** (non-code lines blanked to EQUAL-LENGTH space runs, which is what gave the identity mapping), so one prose keystroke changed the vdoc's bytes though every code line was identical, and `ensureVdoc`'s byte-compare could never hit — minting/writing/opening a fresh file per language on every debounced pass.
  - **Fix (and it is NOT the one filed).** The filed proposal — compare a canonical form while still serving the OLD file — was implemented, adversarially reviewed, and **found to be a real defect**: it left a vdoc on disk whose blanked-line lengths the document no longer had, and (worse) the canonicalization ran downstream of the builders, so it also emptied whitespace-only lines *inside a cell body*. Those are CODE lines the cursor sits on (press Enter in a Python function; auto-indent leaves you at column 4 of a blank line), and emptying one moved a column that completion/hover/definition/signature-help still forwarded verbatim. The shipped fix instead blanks non-code lines to **empty in the BUILDERS**, which are the only things that know which lines are body lines. The vdoc is then a function of the CODE alone (a prose edit is byte-identical → reuse; a code edit differs → fresh path, so the M3 stale-model race is untouched), no stale file ever exists, and body lines — blank ones included — are verbatim by construction.
  - **The identity-mapping contract is now stated as what it always actually was:** a **LINE** contract (line count preserved; every kept line at its own index with its columns intact), not a byte/offset one. `vscode.Position` is (line, character) and never an offset — byte-length equality with the `.qmd` was a strictly stronger property than anything needed, and it was the entire cause of this item.
  - Re-verified against **all four consumers on real servers** (`npm run test:lsp` 12/12): completion, hover, go-to-definition, signature help, in-cell outline symbols, the M3 no-stale-window race, Problems-panel cleanliness, `{ojs}` completion (52 = 52 vs control), both semantic-token cases, and Format Cell through the real built-in JS formatter.

- [x] **`disposeAllVdocs` never bumps the dispose EPOCH — an in-flight forward at deactivate re-registers its vdoc and strands the user's source. FIXED Session 91** (filed Session 89 adversarial review, HIGH; survived both skeptics). `disposeVdocs` bumps `disposeEpoch` FIRST and unconditionally (Session 88's fix), so a forward in flight when a *document* closes cleans up and forwards nothing. `disposeAllVdocs` (extension deactivate) clears `live`/`docFiles` but does **not** bump the epoch, so the same race at *shutdown* is unguarded: an `ensureVdoc` mid-await resumes, `filesOf()` re-creates the `docFiles` entry that was just cleared, and its file — a copy of the user's source — survives in `.quarto/vdoc-mit/` until the next session's activation sweep. Same class as the leak Session 88 fixed, on the sibling path; the sweep is the backstop, so it is bounded, not permanent. **The filed "one line, bump every owner's epoch" was WRONG, and the RED test proved it:** at deactivate there is no `docUri`, and the in-flight `ensureVdoc` has not yet reached `live.set`/`filesOf`, so it is in NO map to enumerate — a per-owner bump cannot reach it. The correct fix is a single monotonic module-global `disposeAllEpoch`, bumped synchronously-first in `disposeAllVdocs` and snapshotted before `ensureVdoc`'s awaits (so it holds even though `deactivate()` fire-and-forgets `disposeAllVdocs`). Commits `a634564` (fix + RED-proven test) / `43b0ac1` (test tightening). Adversarially reviewed (9 agents, 5/6 lenses empty); the two residual pre-existing LOWs it surfaced are filed at the top of this section (Session 91 review). See Learning #101.

- [ ] **`{r}`/`{julia}` semantic-token vdoc minting is PASSIVE (no user gesture) — but the S89-filed "cheap fix" is REFUTED; downgraded MEDIUM→LOW, residual harm bounded** (filed Session 89 adversarial review; investigated + corrected Session 94, Learning #105). Since item 16 Slice 2 the semantic-tokens provider forwards EVERY mapped language present, so merely *opening* a `.qmd` with an `{r}`/`{julia}` cell mints one vdoc per language on VS Code's debounced timer (writes a copy of the user's cell source to `.quarto/vdoc-mit/` + opens a model), whereas before Slice 2 (python-only forwarding) such a vdoc existed only on an active hover/completion/format gesture. **The S89-filed fix — "gate `embeddedLanguagesIn`'s targets on the languageId being registered via `needsLanguageExtension`/`getLanguages()`" — does NOT work, proven firsthand Session 94:** VS Code core ships built-in *language-basics* extensions (grammar + config, **NO server**) for `r`, `julia`, `python`, AND `javascript` (`resources/app/extensions/{r,julia,python,javascript}`, v1.128.0), so `vscode.languages.getLanguages()` returns all four in EVERY standard VS Code — extension or not — and `needsLanguageExtension` returns `false` for our whole mapped set. A RED integration test's premise assertion (`!getLanguages().includes("r")`) fired immediately, and the app-bundle inspection confirmed why. **Two facts constrain any correct fix:** (1) the FIRST speculative mint is architecturally *unavoidable* — there is no public API to detect whether a *semantic-tokens provider* exists for a language without handing VS Code a real `file:` document, and a `provideDocumentSemanticTokensLegend` returning `undefined` is NOT a reliable "no provider" signal (S89 measured the built-in JS service returning an undefined legend *transiently while it does have a provider*); (2) the residual harm is *bounded* — the vdoc is gitignored, disposed on document close (`providers/semantic-tokens.ts:111` → `disposeVdocs`), and swept on activation (`extension.ts:73` → `sweepStaleVdocs`). **The only CORRECT fix is a lifecycle change** ("mint speculatively; once a language exhausts its `INCOMPLETE_RETRIES` budget with zero usable streams, dispose its vdoc and stop re-minting it for that document until its cell code changes") — medium-complexity, race-prone (the class the S89/S91 reviews found bugs in), and it still cannot avoid that first write. Given the bounded residual harm, **downgraded MEDIUM→LOW** and left open for a future session that wants the lifecycle fix. The distinct concern "an installed R/Julia SERVER now analyses the vdoc and its diagnostics attach to it" is the **R/Julia-leak measurement item above** (unmeasured — needs a real R/Julia server; `# type: ignore` is Pyright-specific and does not extend). NOT verified firsthand for R/Julia (no such server installed here).

- [x] **A semantic-token pass rescans the whole document 2 + 2N times (N = embedded languages). FIXED Session 95** (filed Session 89 adversarial review, MEDIUM; both skeptics confirmed the mechanism and one reproduced the numbers, while both judged the severity inflated). `scanRegions` had no cache, and `findAllCells`/`findCellOptionLines` each call it: `embeddedLanguagesIn` scans twice, then `buildVirtualContent` scans twice more *per language*. On a debounced timer, for every visible `.qmd`. **Fix:** a single-entry (last-value) memo keyed on the exact document text — the scan body was renamed `computeRegions` and `scanRegions` now serves the cache (`src/core/qmd/model.ts`), collapsing all same-text calls in a pass to ONE scan while evicting naturally on text change (a `Map` keyed on full text would grow unboundedly; sound because `computeRegions` is a pure fn of `text`). The three accessors that handed out internal arrays (`findHeadings`/`findAllCells`/`findBodyLines`) now `.slice()` so the shared cache cannot be corrupted (spine copied; elements shared & immutable — the contract every consumer already honored). Strict TDD: contract tests (aliasing-independence + full-text keying) break-revert-proven; the 2+2N→1 collapse proven firsthand (10→1 scans on a 4-language doc, temporary instrumentation reverted). 825 unit / clean 43-file `.vsix`. Standing 10-agent review: 1 real LOW (docstring over-claim + a tautological test), both fixed. Learning #106.
- [x] **The integration suite had 4 failures in `semantic-tokens.test.ts` "multi-language merge (Slice 2)" — FIXED Session 96. The S95-filed diagnosis (below) was WRONG: it is NOT VS Code version drift, it is an unfaithful test stand-in meeting S93's `# type: ignore` injection.** (Filed S95; root cause re-derived, corrected + fixed S96, Learning #107.) Each failure was an EXTRA `variable.readonly@0:0` token the expectations do not list. **True root cause:** since S93, `buildVirtualContent` injects a file-level `# type: ignore` on a python vdoc's **line 0** (the `diagnosticMode:"workspace"` phantom-diagnostic mute). The Slice-2 stand-in `tokensForNonBlankLines` emits one token per **non-blank** line — so it emits a spurious token for that comment line, which (vdoc line N IS .qmd line N, no coordinate remap — `providers/semantic-tokens.ts:234`) lands at `.qmd` line 0. **All 4 failing fixtures contain a `{python}` cell** — including test 4, which S95 misread as "javascript-only": its fixture is an `{ojs}`+`{js}`+`{python}` mix (`pyCalls === 1`), so it too builds a muted python vdoc. **Version-independent — proven empirically:** the unfixed suite fails identically (311 pass / 4 fail, same 4× `@0:0`) on BOTH VS Code **1.128.1** (the version the expectations were written against) AND **1.129.0**, which kills the "1.129 built-in-tokenizer emits an extra token" theory outright. **Production was always correct** — real Pylance emits NO semantic token for the mute comment, pinned firsthand in `test/lsp/suite/real-lsp.test.ts:469` ("the line-0 `# type: ignore` mute must emit NO semantic token"); the stand-in was the only thing modelling a server that emits one. **Fix (two parts):** (1) the stand-in now skips the `# type: ignore` line — modelling the real server, which emits nothing for it — greening all 4 with their semantically-correct expectations UNCHANGED; (2) `test/integration/runTest.ts` now pins `version: "1.129.0"` — genuine hygiene (an unpinned harness makes "did the editor change or did we?" unanswerable) even though drift was NOT the cause here. 315 integration (was 311/4) / 825 unit / clean 43-file `.vsix`. **Note (not fixed, out of scope):** the extension passes vdoc-line-0 tokens straight through with no "is this inside a cell body?" filter; real Pylance never emits there (pinned) so it is inert today, but a defensive body-only token filter would harden it against a hypothetical server that does — a possible future LOW.

- [ ] **`disposeEpoch` grows unboundedly** — leg (a) only; **leg (b) (the latched `mkdtemp` failure) is FIXED Session 101** (Session 89 adversarial review, two LOWs, both confirmed; this item stays OPEN over leg (a) alone). **(a) STILL OPEN:** `disposeEpoch` is written for EVERY closed text document — including ones that never had a vdoc — and never pruned; it is a `Map<string, number>`, so the leak is small and bounded by the session, but it is unbounded in principle. **(b) FIXED Session 101** (with `:102` — the same `fallbackDirPromise` state; a rejected memo was latched for the whole session, so one transient ENOSPC/EMFILE left completion / hover / go-to-definition / signature help / in-cell outline / Format Cell silently dead on every untitled `.qmd` until a window reload). The filed text ("only ever reset in `disposeAllVdocs`") **understated it**: the reset sat behind `if (fallbackDir !== undefined)`, and the resolved value is assigned only in the `.then()` a rejection never runs — so the memo was cleared on the SUCCESS path only and a rejection was never cleared by *anything*, deactivate included. Fixed at the rejection site (a `.catch` that clears the memo — guarded on still being the same attempt, since `disposeAllVdocs` now also resets it unconditionally and a late rejection could otherwise clobber a newer attempt). Pinned by "retries mkdtemp after a transient failure, rather than latching untitled docs dead" (`test/integration/suite/embedded-vdoc.test.ts`), which induces a REAL ENOENT through the real syscall by pointing `$TMPDIR` at a per-run UUID path — no stub. Break-revert-proven.

- [x] **No test makes ONE language throw while another answers — PINNED Session 98.** (Session 89 adversarial review, MEDIUM; confirmed as a genuine test-coverage gap — the shipped code is correct.) `streamFor`'s try/catch is per-language, so a throwing server cannot take another language's tokens down with it — but the integration suite only ever made a stand-in throw when it was the *only* language, so "a failing server takes nothing with it" was asserted, not pinned. **Closed** by the new Slice-2 integration test `keeps the OTHER language's tokens when one server THROWS` (a throwing python stand-in + a healthy javascript one over the STRADDLED fixture; asserts javascript's one `variable.readonly@4:0` survives and python's throw is dropped, decoded against each language's own inverted legend). **Break-revert-proven to discriminate:** replacing `streamFor`'s `catch { return undefined }` with `catch (e) { throw e }` reddens exactly this test + the Slice-1 `…ERRORS` test, nothing else. 316 integration. **No production change** — the shipped code was already correct; the premise above is accurate (not reframed). The standing 4-lens adversarial review found zero test-quality defects. The reverse orientation (js-throws) is not a residual gap — a byte-for-byte identical, language/position-agnostic code path.

- [x] **Semantic tokens: the legend and the token stream can come from DIFFERENT providers — RESOLVED (characterization) Session 100: BOTH registry axes are PINNED (DOCUMENT S99; RANGE incl. its THROW leg S100), coverage is STRUCTURALLY exhaustive over every language, and no fix or guard is constructible on either — a VS Code platform boundary, not our bug. Kept as two tripwire pins; nothing actionable remains.** (Filed Session 88 Slice-1 review, MEDIUM; premise proven firsthand S99 in a real Extension Development Host, mechanism corrected S99 and again S100.) **The mechanism (verbatim, VS Code 1.129.0 — the version `runTest.ts` pins).** Both commands read the SAME registry and the SAME top tie-group (`orderedGroups(model)[0]`) but index it by DIFFERENT rules, so they agree **iff `group[0][0]` answers** — a top provider that DECLINES is the precondition, and the original filing omitted it. **(a) DOCUMENT axis — PINNED S99:** `rdn` -> `group[0][0].getLegend()` taken BLIND (never called); `mki` -> the FIRST member that ANSWERS (`for(let a of r){if(a.error)throw a.error;if(a.tokens)return a}`). **(b) RANGE axis — PINNED S100, both legs, in a real EDH.** The built-in TS extension registers ONLY a range provider (1.129.0: 0 doc / 1 range), so `hki(i,t)` is false for every `.js` vdoc and BOTH commands fall through: the legend one passing NO range, landing on VS Code's own self-warned blind branch (`if(!t||!q.isIRange(t))return console.warn("…might be out-of-sync…"),r[0].getLegend()`) even though the CORRECT `ZNt`-based pairing sits one line below it; the tokens one passing `t.getFullModelRange()` -> `ZNt` -> first-that-answers. **BROADER in kind than (a): a THROW diverges too** — `ZNt` swallows per-provider errors (`catch(c){Fs(c),a=null}`) and `uki` has no error field at all, where the doc path's `dki` carries one and `mki` rethrows (so the doc path degrades safely to `undefined` via our try/catch while the range path paints a WRONG COLOUR). Pinned by `test/integration/suite/semantic-tokens.test.ts` "RANGE-registry provider divergence (BACKLOG:125 axis b)" — genuine RED on each leg (`function.declaration@1:0` where the answerer meant `variable.readonly@1:0`), break-revert-proven to DISCRIMINATE (swap the registration order so the ANSWERER is `group[0][0]` and both pins return the correct `variable.readonly@1:0`). **Why it needed its own `describe`:** the Slice-2 js stand-in is a DOCUMENT provider, so `hki` was TRUE in the suite and FALSE in production — every `{ojs}` test ran the doc path while every real `{ojs}` cell runs the range path (Learning #110). The new describe registers NO document provider for the `.js` vdoc; **that absence is the fixture.** **Coverage is STRUCTURAL, not per-language — this is what makes (a)+(b) exhaustive and is the argument S99 lacked:** `has(o){return this.all(o).length>0}` and `_orderedForEach` filter the same `_score>0` over the same `_entries`, so `hki(i,t)` <=> `rdn(i,t)!==null` and BOTH commands always pick the SAME registry — doc non-empty -> doc axis; doc empty & range non-empty -> range axis; both empty -> we degrade ("no server serves…" test). {r}/{julia} therefore need no separate measurement: whichever registry their server uses, it is already pinned. **The two-read race — CONSIDERED AND REFUTED S100; do not re-file it.** Our two `executeCommand`s do each re-read the registry (`_updateScores` per invocation — no shared snapshot OBJECT), but there is no window to mutate in: the ext-host send chain is synchronous, so `Promise.all` queues both messages in ONE tick on a FIFO channel; `CommandService` reaches the sync `_tryExecuteCommand` (no extension declares `onCommand:_provideDocument*SemanticTokens*`); and each handler completes its `Ago`/`rdn` read before its first await. A registration lands before BOTH or after BOTH. **Measured with a positive control** (real EDH): sequential `await legendCmd(); registerB(); await tokensCmd()` DOES diverge (B called), while our actual `Promise.all` shape with B registered at every earliest moment (same tick / microtask / `setTimeout(0)` / `setImmediate`) gives `bCalls=0` every time. Do NOT cite `providers/semantic-tokens.ts`'s S89 retry note as evidence for it — that is prose, not a measurement (#107), and the range mechanics point the other way (one provider registered but not ready => legend DEFINED via `if(r.length===1)`, tokens UNDEFINED via `ZNt`'s `tokens:null`). Honest residual: a provider from a DIFFERENT ext host (remote/web-worker) does not share this channel's ordering — same harm, same non-fix, unmeasured, not worth a guard. **Why no fix and no guard exists (not merely "unwarranted" — impossible):** the answering provider's identity never crosses the RPC boundary (`SemanticTokens` is `{resultId,data}`; registration carries no extension id; `exclusive` is proposed-API-gated), so we can neither pair atomically NOR DETECT that it happened. Switching production to the range pair is a REGRESSION (separate registry; the fallback runs doc->range and never range->doc, so it would silently drop every full-only server such as Pylance). **Reachability and harm, stated honestly:** both axes need TWO providers TIED at `CU`'s cap of 10 (assignment, not addition), so a DEFAULT INSTALL IS SAFE (`if(r.length===1)` returns the right legend); ordering within a tie is fully determined — the ext-host stamps `isBuiltin` on every selector and `_compareByScoreAndTime` sorts score desc -> non-builtin BEFORE builtin -> `_time` DESC, so a third-party JS extension always outranks the BUILT-IN TS service and owns the blind legend. Harm is bounded: it mis-COLOURS, never mis-POSITIONS (coordinates are legend-independent), and cannot crash us (`decodeTokens` drops an out-of-range index; `encodeTokens` drops a name outside `OUR_LEGEND`). **Escalation if a real user ever reports it:** file upstream against VS Code (port the range path's own `ZNt` re-run into `_provideDocumentSemanticTokensLegend`); nothing is fixable from an extension.

- [x] **Semantic tokens: the modifier axis — `typeHintComment` FIXED (a genuine wrong colour), `builtin` REFUTED (already matches `.py`). Session 97** (filed Session 88, sharpened Session 90; the filed `builtin` headline was corrected by firsthand grounding, Learning #108). The item's premise — "`print` arrives as `function.builtin`, emitted bare, so the builtin distinction is lost" — split in **opposite directions** once resolved firsthand (real Pylance 2026.2.1 probe + effective Dark-2026 theme trie, last-wins):
  - **(a) `typeHintComment` — GENUINE, FIXED.** Inside a legacy `# type: List[int]` comment Pylance emits `class.typeHintComment` (VERIFIED on the wire — the incidence the item flagged "unverified"). We already carry `class`, so clearing the modifier repainted a **comment interior** as a live type — `entity.name.type` **#4EC9B0 teal**, where a real `.py` (and MagicPython alone) shows the **#8b949e** comment colour via Pylance's OWN `*.typeHintComment` → `comment.typehint.type.notation.python` rule (python-gated, inert on a `.qmd`). Fix: carry the `typeHintComment` modifier in `OUR_LEGEND` + mirror Pylance's rule `quarto`-scoped in `package.json` (`*.typeHintComment` → `comment.typehint.type.notation.python`) — the exact shape as the shipped `module` fix, on the modifier axis. Now matches the `.py`.
  - **(b) `builtin` — REFUTED, NOT carried.** The item's headline. Pylance ships **no** `function.builtin`/`variable.builtin` scope rule (it styles `magicFunction`/`builtinConstant`/`*.overridden`/`*.typeHintComment` — never bare `builtin`), so a REAL `.py` resolves `print` through the plain `function` default to **#d2a8ff** and `__name__`/`__doc__` through `variable` to **#ffa657** — **EXACTLY what our bare-modifier `.qmd` already shows.** We already match `.py`. Carrying `builtin` + a scope rule would repaint `print` **#DCDCAA** and `__name__` **#79c0ff** — *diverging* from the `.py` **even in the default theme**, not converging. That is the S90 `selfParameter` trap on the modifier axis: the "obvious" carry is a regression against the match-`.py` target. (This corrects the filed lever "keyed on `*.typeHintComment` / `variable.builtin` plus carrying that one modifier" — `variable.builtin` was wrong.)
  - **Verified:** real-lsp gate (real Pylance) proves `typeHintComment` is emitted+preserved AND `builtin` is emitted+deliberately-cleared; 828 unit / 315 integration / 14 real-LSP / check-types clean / clean 43-file `.vsix`; standing multi-agent adversarial review (6 candidates → 3 test/doc-quality survivors, all fixed; the correctness lenses confirmed the decision, and one caught a hex-citation error now corrected). **Note (not fixed, disclosed):** the final colour resolution through VS Code's theme trie is proven firsthand + deterministically (the scope byte-mirrors Pylance's own verified rule), not by an automated theme-reading test — the same boundary the `module` fix accepted (a version-pinned uncommitted theme artifact makes such a test fragile).

- [ ] **Semantic tokens: `self` renders a different colour on a `.qmd` than in a real `.py`** (Session 90 review, LOW — raised as "dropping `selfParameter` is strictly worse than carrying it", and that framing was **refuted**, but the underlying observation is real). Resolved firsthand through VS Code's theme trie in the real default theme (Dark 2026): `.qmd` today shows `self` as **#79c0ff** (MagicPython's `variable.language.special.self.python`); a real `.py` shows **#c9d1d9** (Pylance's own scope rule). **Carrying `selfParameter` bare does NOT reconcile them — it invents a THIRD colour, #ffa657** (`parameter` → `variable.parameter`), matching neither. That is why D4 drops it, and the drop is correct. Closing the cosmetic gap for real would mean contributing Pylance's *exact* scope strings under `language: "quarto"` — a Pylance-versioned table that would silently rot when they rename a scope, in exchange for making `self` match `.py` at the cost of making it *less* distinctive than it is today. Filed, not recommended, and explicitly not a defect in the shipped behaviour.

- [ ] **Semantic tokens: the 23 STANDARD token types in `OUR_LEGEND` are pinned by nothing** (Session 90 review, LOW — a pre-existing test hole this slice neither created nor widened, found by the test-quality lens). 17 of the 23 standard names can be deleted from `OUR_LEGEND` with the entire suite (811 unit / 314 integration) still green: no test asserts that our legend actually *declares* the standard set, only that specific names round-trip. A silent deletion would uncolour those token types across every embedded language, with no test failing. Cheap fix: assert `OUR_LEGEND.tokenTypes` contains the full standard set (the same shape as the existing carried-name pin). Not fixed here — it is outside this slice's blast radius (FM #17) and the fix belongs with a broader legend-coverage test.

- [ ] **Semantic tokens: deleting the last `{python}` cell strands the previous vdoc until the `.qmd` closes** (Session 88 Slice-1 review, LOW). Once the language drops out of `embeddedLanguagesIn` (which replaced `hasCellOfLanguage` in Session 89) the provider returns early, so it never calls `ensureVdoc` and the previously-minted vdoc file (and its open model) survive until `disposeVdocs` runs at close. Bounded, self-healing at close, and swept at the next activation. The fix belongs with the churn item above, since both are about the whole-language vdoc's lifecycle.

- [ ] **Within-session accumulation of superseded cell vdocs during heavy editing** (Session 87 item-18 adversarial review — race + correctness lenses; the past-close leak is FIXED, the within-session churn is the residual). Cell vdocs (outline in-cell symbols, Format Cell) are keyed by absolute `cellStartLine`, so inserting/deleting a line above a cell changes its key and mints a new vdoc; a concurrent same-key double-mint (a hover racing a completion) likewise mints a second file. **The past-close leak is fixed** — `docFiles` now tracks every minted URI per document, so `disposeVdocs`/`disposeAllVdocs` reclaim all of them at document close (proven by the "reclaims a vdoc whose KEY changed" test), and the activation sweep is the cross-session backstop. The residual is that, *while a document stays open*, an actively-edited many-cell document can accumulate stale `.py` files (and open Pylance models — feeding the diagnostics item above) until close. Bounded and self-healing (reclaimed at close + next-session sweep), same order as the old in-memory `InCellSymbolStore` behaviour it replaced. A tighter within-session bound would require the outline provider to reconcile its full current cell-vdoc set on each computation (delete cell vdocs no longer in the live set), which is more surface than this fix warranted. Low priority.

- [ ] **Preview lifecycle: closing a non-`file:` editor (e.g. a Git diff) for a previewed document silently kills the live preview** (surfaced by the Session 84 adversarial review; **PRE-EXISTING**, not introduced by item 15 — it has affected `quarto.preview` since preview shipped; operator-confirmed as deferred rather than fixed in-slice, since `onDocumentClosed` is listed "unchanged" in the item-15 plan's §8 contract and the fix touches lifecycle shared by all three preview commands). `features/preview.ts` `onDocumentClosed` (:281-283) disposes by `doc.uri.fsPath`, **discarding `doc.uri.scheme`**. The built-in Git extension's read-only `git:` diff of a file has the SAME `fsPath` as the working-tree file, so `git:` document-close events reap a live preview for that path. Reproduced end-to-end by two independent reviewers in a real Extension Development Host (VS Code 1.128) against the genuine built-in Git extension: preview `report.qmd`, open its Git diff, close the diff tab → the preview server is killed and the pane disappears, with no error. **Severity: annoying, not dangerous** (no orphan, no corrupt state — the reap path is the normal one, just fired by the wrong document). A faithful fix ignores non-`file:` schemes in `onDocumentClosed` (a preview only ever exists for an on-disk file, so this is safe) or keys sessions by `uri.toString()` rather than `fsPath`; either way it needs its own TDD pass with a regression check across `preview`/`previewFormat`/`previewScript`. Session 84 DID close the same hole in the new `previewScript` **gate** (`uri.scheme !== "file"` — a `git:` diff of a spin script no longer previews the working-tree file), so the remaining exposure is the shared close-handler only. (Adversarial review, Session 84; Learning #92.)

- [ ] **`previewFormat`: nested flow collections in `format:` + a differing-format request dropped during preview startup** (both surfaced/filed by the Session 82 adversarial review, item 15). (1) `core/preview-format.ts` `parseFlowCollection` splits flow collections on commas naively, so a flow collection nested inside another (`format: {html: {a: 1, b: 2}}`) mis-parses the inner comma — astronomically rare in a real `format:` block, degrades to a MISSING name (never a wrong `--to`, since the caller falls back to Quarto's default), same fail-safe class as Slice-1's flow-collection scanner limitation. (2) `features/preview.ts` `openPreview`: a `quarto.previewFormat` request for a DIFFERENT format that arrives WHILE a preview is still starting (the up-to-60s `starting` window) is silently dropped by the `to`-unaware `starting` guard — the reorder-before-dispose fix (Session 82) already converted the nastier teardown-then-nothing outcome into a benign drop-and-keep-the-in-flight-preview, so the residual is only "the user's format switch during startup is ignored; re-run once the preview is up." A faithful fix makes the in-flight reservation `to`-aware (track the pending `to`, restart/queue on a differing format, or surface feedback). Both LOW severity. (Adversarial review, Session 82; Learning #90.)

- [ ] **`_quarto.yml` document-link scanner: flow-collection and pathological-quoted-key value shapes** (surfaced by the Session 80 Slice-1 adversarial review; both confirmed but deferred as fail-safe, documented limitations). `core/project-links.ts` `findPathValueCandidates` reuses the scalar value-token grammar, so a **flow collection** (`chapters: [intro.qmd, part.qmd]`, `opts: {file: x.qmd}`) is emitted as one token containing the brackets — its inner path elements are never split, so a real file inside a flow collection gets no link. Block style (`- intro.qmd`) is idiomatic in `_quarto.yml` and the item-14 plan (§2.5/§5.1) explicitly scoped the scanner to block style, so this was never claimed. Separately, a **quoted key containing `": "`** (a colon *and* a space, e.g. `"a: b": v`) can mis-anchor `mappingColonIndex` (a colon followed by a non-space, like `"a:b":`, is handled correctly; only the space-inside case is not) — astronomically rare in real config, and matches the existing `core/project-yaml.ts` `keySpanAt` naive-colon precedent. **Both degrade to a MISSING link, never a wrong one** (the existence check makes every scanner gap fail-safe). Low priority; if pursued, a faithful fix splits flow-collection elements into per-element candidate spans and makes the quoted-key colon detection quote-aware. (Adversarial review, Session 80; Learning #88.)

- [ ] **Sub-feature (c) — R-package workspace-symbol exclusion** (deferred from item 11's plan §9 Q3, filed Session 71). Since v1.127.0, Posit excludes markdown headers from workspace-symbol search (`Ctrl+T`/`Cmd+T`) by default when the workspace root is an R-package project (a `DESCRIPTION` file present), controlled by a three-way enum setting (`quarto.symbols.exportToWorkspace`: `default`/`all`/`none`, PR #751). Unrelated file (`src/providers/workspace-symbols.ts`/`core/workspace-symbols.ts`), narrower audience (R-package projects specifically), zero shared risk or architecture with item 11's (a)/(b) slices. `DESCRIPTION`-file/R-package detection has no precedent anywhere in this codebase — if pursued, it is new infrastructure, not an extension of existing logic. Low priority, unranked.

- [ ] **Outline show/hide toggle: cosmetic re-expand-on-toggle fix** (deferred from item 11's plan §9 Q4, filed Session 71). The dispose-and-re-register mechanism the toggle uses to force a live Outline refresh (plan §2.4) causes VS Code to rebuild the Outline tree **collapsed** each time the setting changes. Posit's own shipped fix (PR #974) adds an explicit re-expand call afterward to compensate, but the specific VS Code command their fix references was not independently verified this session as a stable, public API — verifying it and wiring the same compensating call is small, self-contained future work. Low priority, cosmetic only (the toggle itself functions correctly without it).

- [ ] **YAML schema diagnostics: an anchored or quoted `project:`/`website:`/`book:` container header silently disables scanning of that whole block** (surfaced by the Session 47 adversarial review; confirmed, both are false NEGATIVES — the safe direction, never a wrong flag — so shipped as a documented gap, not fixed in-session). `mappingContainerKey` (`core/yaml-context.ts`, shared with the 6d-6 nested-completion feature) requires the value after the colon to be empty or comment-only to recognize a line as a pure-mapping container; `project: &p` (a YAML anchor) and `"project":`/`'project':` (a quoted container name) both fail that check, so `findProjectConfigKeyLines` never enters the block at all — no diagnostics from it, ever, even for a genuinely unknown key inside. A leading-BOM false negative in the SAME class was fixed this session (child-key quoting was fixed too, since THAT is a false positive — the feature's core-promise violation); these two remain because fixing them touches `mappingContainerKey`, which is SHARED with the unrelated 6d-6 nested front-matter completion feature — a cross-module change needing its own TDD pass across both consumers (SAFEGUARDS: no cross-module refactor without plan mode). Low priority (anchors/quoted containers are rare in practice; the failure mode is silence, not a false alarm). (Adversarial review, Session 47.)

- [ ] **Embedded-completion secondary-edit filter — defense-in-depth for `command` + divergent primary `textEdit`** (surfaced by the Session 28 6e-1 adversarial review; both refuted 0/3 as out-of-v1-scope). `providers/embedded.ts` `filterOutOfCellEdits` strips out-of-cell `additionalTextEdits` by region membership (the v1 obligation — plan §6 6e-1 DONE #3), but does NOT inspect (a) a post-accept `item.command` (an arbitrary command an embedded server could attach) or (b) a divergent PRIMARY `item.textEdit`/`item.range`. Both are non-issues in v1: `command` is mitigated by `itemResolveCount=0` (the forward omits the 4th arg, and there is no `resolveCompletionItem`), and a divergent primary range is **structurally impossible** to land out-of-cell (VS Code clamps a primary completion range to a single line containing the cursor — vscode.d.ts `CompletionItem.range`). Becomes relevant only if a future 6e slice enables item resolution to surface real auto-imports — then region-check/strip `command` and validate the primary `textEdit` range too. Low priority. (Adversarial review, Session 28.)

- [ ] **Audit `src/providers/embedded.ts`'s existing completion/hover/go-to-def/signature-help forwarding vdocs for spurious global-Problems-panel leakage** (surfaced as a side effect of Session 68's code-cell diagnostics forwarding research, filed Session 69 per that plan's §9 Q4). Posit's own PR history (`quarto-dev/quarto` PR #832, pre-v1.133.0) shows their equivalent pre-existing request-forwarding vdocs were, before an explicit fix, accidentally surfacing spurious entries in VS Code's global Problems panel — a risk inherent to any project using request-forwarding virtual documents, independent of whether diagnostics forwarding is deliberately built (this project chose not to — see item 10, CLOSED Session 69). This project's own equivalent vdocs have never been checked for the same accidental-leakage bug. Small, self-contained, unranked — no known severity yet, since no firsthand check has been done.

- [ ] **`CURATED_EXECUTE_KEYS` hidden-visibility fidelity tuning** (surfaced by the Session 23 6d-6 adversarial review; refuted 0/3, the refuters defended inclusion 3/3). The curated execute children include `enabled`/`daemon`/`daemon-restart`, which carry `hidden:true` in the Quarto 1.7.33 schema — i.e. Quarto's OWN completion suppresses them. Including them is a deliberate, defensible call (`execute:\n  enabled: false` is this project's canonical render-clean idiom, and all three are valid/documented/render-accepted options), but a future session may choose to (a) mirror Quarto's visibility (drop the hidden ones), or (b) strengthen the exact-equality shape-lock test to cross-check the live schema with a `hidden` filter so the curated set can't silently drift from a real schema entry. Pure judgment/test-hardening; no wrong/missing completion today (the names ARE all genuine execute children, verified firsthand). Low priority. (Adversarial review, Session 23.)

- [ ] **Empty value slot abutting a trailing inline comment can glue value to comment** (surfaced by the Session 22 6d-5 adversarial review; refuted 0/3 as out-of-slice-scope, but a real cross-module pattern). When the value slot is EMPTY and a pre-existing inline comment follows (`key:  # c`, cursor before `#`), accepting a value inserts it with no separating space before the comment (`key:  true# c`). Root cause is the shared value-slot grammar: BOTH `core/qmd/model.ts` `slotsOf` (6d-2 cell path) and `core/yaml-context.ts` `topLevelSlots` (6d-5 FM path) compute an empty value span when the slot abuts a comment, and the provider's leading-space-on-`:` heuristic only fires when the slot abuts the COLON, not a comment. Contrived trigger (empty slot + pre-existing comment + cursor exactly before `#`); not a path a normal accept produces. A faithful fix covers BOTH call sites in one pass (suppress value completion when the empty slot abuts a comment, or append a separating space when the next non-space char is `#`) — cross-module, its own TDD pass (SAFEGUARDS: no cross-module refactor without plan mode; FM #18). Low severity. (Adversarial review, Session 22.)

- [ ] **Multi-cursor / multi-selection formatting** (deferred from Phase 7 formatting toggles, Session 14). `features/formatting.ts` toggles only the PRIMARY selection (`editor.selection`); a multi-cursor edit would need to apply N replacements and recompute N resulting selections across shifting offsets. Low priority, its own TDD slice.

- [ ] **Project-wide CLI spawn timeout** (surfaced by the Session 20 6d-3 adversarial review; refuted as a 6d-3-scoped defect, but a real cross-cutting hardening). No `quarto` spawn has a timeout — `quarto/cli.ts` `resolveBinary()` (`--version`, used by render/preview/verify) and the new `quartoSharePath()` (`--paths`, on the completion hot path) both `await execFileAsync(...)` with no `timeout`. A wedged/very-slow binary therefore HANGS rather than degrading: for the schema reader the first `#|` completion stays pending (never reaching the catch→curated fallback) and the cached promise pins it for the session. Low severity (pathological — a healthy `quarto --paths` returns in ~50 ms; VS Code also cancels slow completion providers). A consistent fix adds a `{ timeout }` to ALL CLI spawns in one pass (so the degrade-to-curated/degrade-gracefully guarantee holds under hang, not just under reject) — a deliberate cross-file change, its own TDD/verification pass, NOT a 6d-3 spot-fix (FM #18). (Adversarial review, Session 20.)

- [ ] **Schema-reader cache: retry after a transient first-load failure** (deferred from the Session 20 6d-3 review; refuted as v1-acceptable). `features/yaml-schema-source.ts` caches the load PROMISE once per session (`cached ??= loadSchemaIndex()`); since `loadSchemaIndex` always resolves (curated on failure), a transient first-load failure (e.g. a momentarily-busy CLI) PINS the curated fallback until a window reload, even after Quarto becomes reachable. Low severity (window reload fixes it; first-load failures are rare; documented as an accepted v1 limitation). A faithful fix caches only a SUCCESSFUL parse and retries (debounced) after a fallback, distinguishing "real schema" from "fell back to curated." Its own small TDD pass. (Adversarial review, Session 20.)

- [ ] **`parseSchemaIndex` all-or-nothing degradation across categories (forward-compat)** (surfaced by the Session 21 6d-4 review; refuted 0/3 as v1-acceptable). 6d-4 changed the fallback guard from per-category (`cellFields.length>0 ? … : CURATED`) to joint (`cellFields.length===0 && fmFields.length===0 ? CURATED_SCHEMA_INDEX : indexOf(cellFields, fmFields)`), consistent with replace-semantics (Learning #27). So a future schema that populates one category but EMPTIES the other (e.g. document-* present, all cell-* gone) would yield `[]` for the empty category instead of its curated set. Impossible today (the installed 1.7.33 file has 94 cell + 383 document fields; both always populated) and within the documented-acceptable degradation envelope (`[]` = fewer suggestions, never a false "unknown key"; never throws). A faithful fix would degrade PER-category to the matching curated set — but that mixes real+curated for the empty category, a deliberate divergence from replace-semantics; defer until a real partial-schema Quarto version appears. Trivial. (Adversarial review, Session 21.)

- [x] **Object-form `enum: { values: [...] }` resolution (forward-compat)** — **RESOLVED Session 40** as part of b2-iii-value. Turned out not to be latent after all: the `math-methods` definition (`ref`'d by the real `html-math-method` DOCUMENT option's `method` sub-property, not a cell option) uses exactly this form. Fixed generically in `valuesOfSchema` (covers every caller, cell or document). (Learning #47.)

- [ ] **Mid-line HTML comments in `scanRegions`** (deferred from Phase 7 math-preview adversarial review, Session 15; confirmed 3/3 refuters). `core/qmd/model.ts` classifies HTML comments per WHOLE line (`COMMENT_OPEN`/`COMMENT_FULL_LINE`), so a comment that **opens mid-line** (`text <!--`) or **closes mid-line** (`--> $x$`) is mis-handled at the character level. Two symptoms in `core/math-regions.ts`: math inside a mid-line-opened comment is reported (false positive), and real math after a mid-line `-->` is missed (false negative). **This is a shared-model gap — it affects `core/refs.ts` (cross-ref index) identically**, not just math. A faithful fix needs character-level comment tracking in `scanRegions` (interacting with inline code spans + fences) and a full regression across outline/refs/citations — its own TDD pass (SAFEGUARDS: no cross-module refactor without plan mode). Low severity (mid-line comments are uncommon in `.qmd`). Documented in `core/math-regions.ts`'s docstring.

- [ ] **`findDiagramRegions` over-detects glued malformed cell info strings** (deferred from Phase 7 diagram-preview adversarial review, Session 16; confirmed 3/3 refuters, verified live against Quarto 1.7.33). `core/diagram-regions.ts` reports a diagram for `{mermaid=x}`, `{dot=1}`, `{mermaid#id}`, `{mermaid.foo}` — but Quarto renders **none** of these as a diagram (they become a plain code block with a `data-…` attribute, a literal `<pre>`, or inline `<code>`). Root cause is the **shared** `CELL_INFO` regex (`core/qmd/model.ts:138` — `/^\{([A-Za-z][A-Za-z0-9_-]*)[^}]*\}$/`) which captures the engine token greedily then accepts any trailing chars via `[^}]*`. **Shared-model gap — it affects every `findAllCells` consumer (outline / run-cell / math / refs)**, not just diagrams: those see `{mermaid=x}` as an executable cell too. A faithful fix tightens `CELL_INFO` to require the engine token to end at whitespace / comma / `}` while KEEPING the legitimate knitr comma forms (`{r,echo=FALSE}` renders) — cross-cutting, its own TDD pass with a full regression across all consumers (SAFEGUARDS: no cross-module refactor without plan mode). Low severity (only non-idiomatic / malformed info strings). Documented in `core/diagram-regions.ts`'s docstring.

- [ ] **Honest test name for the math-preview "re-render on edit" integration test** (surfaced by the Session 16 diagram-preview review; the identical pattern exists in `test/integration/suite/math-preview.test.ts`). Its only assertion is panel-count, which proves no-stacking, NOT that a re-render occurred (webview content is F5-only residue). Rename the test + comment to what it proves, as was done for the diagram-preview test this session. Trivial, test-only — left untouched in Session 16 to honor FM #8/#18 (another feature's code). (Adversarial review, Session 16.)

- [x] **Deliberate dev-toolchain upgrade to clear the `npm audit` advisories — SHIPPED Session 108** (`BACKLOG:176`; surfaced Session 13). All 7 advisories (4 moderate / 2 high / 1 critical, dev-only, `docs/SECURITY-AUDIT.md`) are CLEARED — `npm audit` now reports 0. Done the right way, NOT `--force` (which downgrades mocha to 8.1.3): `esbuild` `^0.24.2`→`^0.28.1`; `vitest` `^2.1.8`→`^3.2.7` (resolves `vite`→7.3.6 / `vite-node`→3.2.4 / `@vitest/mocker`→3.2.7, all above their vulnerable ranges, and clears the critical vitest-UI advisory `<3.2.6`); and `overrides: { "serialize-javascript": "^7.0.7" }` (clears the serialize-javascript high AND mocha's transitive-only advisory — `mocha` KEPT at `^10.8.2` since even mocha 11.7.6 still pins `^6.0.2`, so bumping mocha cannot clear it; the pin is inert to runtime since mocha runs single-process). Verified full matrix: `check-types` clean, 834 unit (vitest 3.2.7, count unchanged), 333 integration (real EDH loading the esbuild-0.28.1 bundle), clean 43-file `.vsix`, `npm audit --json` total 0; the four-way "none ship" invariant re-verified. (Learning #20 → resolved.)
- [ ] **Consolidate the `#|`/`//|` cell-option-prefix grammar between `refs.ts` and `model.ts`** (deferred 6d-2 follow-up, surfaced by the Session 18 6d-1 adversarial review). Session 18 aligned `core/qmd/model.ts` `CELL_OPTION_PREFIX` to Quarto's real `^#\s*\| ?` (column-0-anchored, ws allowed between `#` and `|`), but `core/refs.ts` `CELL_LABEL_OPTION` still has a leading `\s*` — so it would index an **indented** `  #| label: fig-x` that Quarto treats as code (a pre-existing latent looseness, now *diverging* from the faithful model regex). The faithful fix routes refs' `label:` extraction through `findCellOptionLines` (which 6d-2 extends with a value slot, making the value/id available) and drops the duplicate regex — a cross-module change, so its own TDD pass (SAFEGUARDS: no cross-module refactor without plan mode). Low severity (indented cell options are non-idiomatic). (Adversarial review, Session 18.)
- [ ] **Consolidate the YAML value-slot grammar between `yaml-context.ts` and `model.ts`** (surfaced Session 24, 6d-6 cont.). Session 24 extracted `valueSlotAfterColon` in `core/yaml-context.ts` so the top-level AND nested front-matter value-slot grammar (skip leading ws, strip an unquoted trailing inline comment, keep quoted scalars) is ONE implementation. But `core/qmd/model.ts` `slotsOf` (the cell-option `#|` value path, 6d-2) still computes the SAME grammar in its own mirror. The faithful fix routes both through one shared helper — a cross-module change (SAFEGUARDS: no cross-module refactor without plan mode; FM #18), so its own TDD pass. Low severity (the two are byte-identical today; drift risk only). (Session 24, 6d-6 cont.)
- [ ] **De-duplicate the integration `EXTENSION_ID` constant** (surfaced Session 11; now 13 copies). `const EXTENSION_ID = "rmsharp.vscode-quarto-ext"` is copy-pasted into all 13 `test/integration/suite/*.test.ts` files (the Session 28 `embedded.test.ts` added the 13th). When the `publisher` changed this session, every copy went stale at once and RED'd 8 "should be discoverable" failures. Extract to one shared module (ideally derived from `package.json`'s `publisher`+`name`) so an identity change can't silently break discoverability again. Low risk, pure test refactor — its own TDD-exempt pass. (Learning #18.)
- [ ] **Indented (4-space) code blocks as a skip-region** (deferred from Phase 6b adversarial review). `core/qmd/model.ts` `scanRegions` skips YAML front matter, HTML comments, and *fenced* code, but not CommonMark §4.4 *indented* code blocks (a line indented ≥4 spaces after a blank line). So `findBodyLines` emits them and `core/refs.ts` indexes a `{#fig-…}` shown inside one as a **phantom** cross-ref label (verified against `quarto render` → `?@fig-…` unresolved). The faithful fix must not false-skip 4-space **list-item continuation** content (the model tracks no list context), so it needs its own list-aware TDD pass. Low severity (uncommon construct). (Adversarial review B/D, Session 8.)
- [x] **Setext headings in the outline — SHIPPED Session 66** (deferred from Phase 6a, Adversarial review #7, Session 7). `core/qmd/model.ts` now recognizes a single-line paragraph underlined with `=`/`-` as a level-1/2 heading, alongside ATX. The `---` disambiguation was grounded empirically against the real Quarto CLI (both bundled Pandoc and a separate system `pandoc`, cross-checked) rather than assumed from the CommonMark spec — Pandoc's own `markdown` reader (what `.qmd` files actually use) turned out to have 3 rules undocumented by CommonMark: a 2+-line paragraph never promotes (dissolving the "multi-line heading" scoping question), a `---` right after an ATX heading with no blank line swallows the ATX line's literal text (a real Pandoc quirk, deliberately NOT replicated — would have broken a pre-existing, unrelated test), and a lone bullet-list item nests/strips-the-marker (guarded against — declines rather than emitting a heading with a wrong, marker-included name) while an ordered-list/blockquote marker does not (needs no guard). `PROJECT_LEARNINGS.md` Learning #73 has the full empirical trace. 13 new unit tests (`test/unit/qmd-model.test.ts`) + 1 new integration test against a new `test/fixtures/setext.qmd` fixture (real `vscode.executeDocumentSymbolProvider`, mixing ATX and setext in one outline). 634 unit (+13) / 221 integration (+1); `npm run check-types` clean; unchanged 38-file `.vsix` (new logic bundles into `dist/extension.js`, the new fixture is test-only and `.vscodeignore`d).
- [x] **A crash strands an untitled document's vdocs in the OS temp dir — RESOLVED Session 106; the whole plan is now shipped: PHASE 1 (the reclaim) SHIPPED Session 103, PHASE 2 (0700 re-assert) SHIPPED Session 104, PHASE 3 (the comment fix) SHIPPED Session 106.** Plan: `docs/planning/2026-07-16-os-temp-vdoc-sweep-plan.md` (the spec; found S101, planned S102, Phase 1 implemented S103 as ONE operator-ratified 4-layer vertical slice — L1 `575c9a3`, L2 `c4aff29`, L3 `41f8956`, L4 `d437968`, review fixes `3797e2e`/`6514064`). **SHIPPED in Phase 1:** `mkdtemp` is now stamped `quarto-mit-vdoc-<host>-<pid>-XXXXXX` (`hostDiscriminator`/`tempVdocDirPrefix`/`tempVdocDirParse` in `core/embedded/vdoc-path.ts`), and `sweepStaleTempVdocs(dir?)` + `isProcessDead` (`features/embedded-vdoc.ts`) reclaim at activation ONLY dirs bearing our host tag whose owning PID is `ESRCH`-dead (**`EPERM` = ALIVE**). Wired at `extension.ts` beside `sweepStaleVdocs`. **RUNTIME-PROVEN** against a real EDH activation with fixtures planted in the real `$TMPDIR`: dead-PID dir reclaimed; foreign-host (G0), live-foreign-PID (G2) and foreign-file (G4/G5) dirs all survived; each guard individually break-revert-proven. 832 unit / 331 integration. **⚠ THREE OF THIS ITEM'S OWN 'AIRTIGHT' FACTS ARE NOW FALSE — S103 falsified them by fixing them, so do NOT re-derive the defect from this paragraph:** (a) "`os.tmpdir()` has **exactly ONE** executable site" is now **TWO** (`embedded-vdoc.ts:454` the sweep root, `:570` the `mkdtemp`); (b) "swept by nothing, ever" is now **swept**; (c) "`README.md:170-173` already promises this behaviour and is FALSE today" — **paid off in L3**, the README now names the temp-dir fallback and is true (it no longer over-promises `0700` on Windows, where `fs.mkdtemp` takes no mode argument at all). The `:295` comment that conceded the gap is rewritten. **SHIPPED in Phase 2 (Session 104):** `vdocDirFor` now `stat`s the memoised fallback dir before handing it back and re-mints a fresh 0700 `mkdtemp` when the OS reaper / user deleted it, so the user's source no longer lands in the **0755/0777** directory `vscode.workspace.fs.writeFile` silently re-creates via `mkdirp` (no mode arg). Identity-CAS on the memo so a concurrent re-mint does not orphan a directory (the BL-102 leak class the plan flagged as the un-named dragon). **Filed cause corrected on the record:** Phase 1's self-skip + ESRCH-only G2 mean OUR sweep can no longer delete a live dir, so the real trigger is OS/user deletion, NOT "a reclaim racing a live forward" as this item read pre-Phase-1. RED-proven (the identical test observed 0755 with the fix absent), 332 integration / 832 unit / clean 43-file `.vsix`. **STILL OPEN — the remaining work:** **Phase 3** — correct the false "self-healing" comment (the item below). **The S102 corrections that still stand:** the filed "56 dirs" evidence measured **ZERO** and "~1 per integration run" is a developer-machine artifact (the test host is SIGKILLed every run), NOT a user rate; the "0644 in a 0700 dir" security clause is **REFUTED** (`writeFile` with no mode is `0666 & ~umask`), so the bound is the **directory alone** ⇒ **disk-hygiene, not cross-user disclosure**; real bound **macOS = reboot**, **Debian = 10 days**, **Windows = unbounded** (Windows is the win); `globalStorageUri` REJECTED. **⏰ The grammar is now CHOSEN but not yet locked** — it locks at first publish, and the extension still has never shipped (v0.0.1, `preview: true`, 0 tags), so it remains free to change until `vsce publish`. Operator confirmed at S103 that **no publish is near-term**. **KNOWN RESIDUAL, filed not fixed:** a 24-bit host tag collides (measured: `node7582.cluster.local` and `node8137.cluster.local` both → `477c36`; ~3% at 1000 hosts), and a collision makes G0 **fail open** — only reachable when two machines share one `$TMPDIR`. Sizing: still **LOW-priority hygiene** — no lock file, no flock, no heartbeat.
- [x] **`embedded-vdoc.ts`'s "self-healing" claim WAS FALSE — comment FIXED Session 106 (Phase 3) at BOTH `self-healing` sites, and vdoc models self-destruct at ~180s (characterized: mental-model gap, code correct)** (found Session 102 while planning the OS-temp sweep above; **comment/mental-model defect, NOT a code defect — the code is correct**). ⚠ **LINE NUMBERS RE-PINNED Session 103** — S103's Phase 1 moved every citation this item was written against, and the old `:445-448` now lands *inside code S103 added*; re-verified by grep, not by memory. Current locations: the false comment is **`:348-354`** (in `sweepStaleVdocs`'s docstring, was `:333-337`); `isModelOpen()` is **`:627-630`** (was `:445-448`); the reuse branch is **`:190-199`** (was `:178-185`); the `:295` comment this item also asked to fix **no longer exists** — S103 rewrote it in the same change that falsified it, so Phase 3's scope was the TWO `self-healing` sites (`grep -ran "self-healing" src/` returned two, not one): the `:348-354` `sweepStaleVdocs` docstring AND the `:190-199` `ensureVdoc` reuse branch — BOTH corrected Session 106. The comment says a cross-window sweep deleting an in-use vdoc is *"self-healing: `ensureVdoc` notices the model is gone and re-mints on the next request."* **REFUTED by measurement in a real EDH:** `isModelOpen()` tests `vscode.workspace.textDocuments` — an in-memory model, not the file — and returned **true for a file deleted 10s earlier**, flipping only at an unrelated ~180s model-eviction mark. So `ensureVdoc` does NOT notice the delete; for up to 180s the reuse branch returns the URI of a file that no longer exists. **The comment's BOTTOM LINE ("not dangerous") survives, for the opposite reason it gives:** the model *outlives* the file, so `executeHoverProvider` still answered from RAM — the right framing is "harmless because the model outlives the file", not "self-healing because the model dies with the file". **Genuinely new and unrelated to any sweep:** every vdoc model self-destructs ~180s after `openTextDocument` in normal single-window operation and the reuse branch never refreshes that clock, so `isModelOpen()===false` on an unchanged-content reuse is the **steady state** for any cell idle >3 min (the code handles it correctly — falls through, mints a fresh version, deletes the old file — so this is churn, not breakage). **UNMEASURED:** VS Code's `_cleanup()` valve (at 60 live refs the oldest 10 are force-disposed) could drop models EARLIER than 180s on a many-cell document — that interaction is the one part worth characterizing. **Also unmeasured:** the harmlessness verdict used an IN-PROCESS hover provider; a real out-of-process LSP (Pylance) has its own file watcher and was NOT tested. FIXED Session 106 (Phase 3): both `self-healing` sites now state "harmless because the model outlives the file" (the measured mechanism — the model outlives the file, so consumers answer from RAM); `grep -ran "self-healing" src/` returns nothing; the `:295` half was already done by S103. **Retained in plan §3.5, not lost:** the ~180s model self-destruct is a characterized mental-model gap (code correct — churn, not breakage), and two interactions remain UNMEASURED (VS Code's `_cleanup()` valve possibly dropping models <180s on a many-cell doc; a real out-of-process Pylance file-watcher, vs the in-process hover used for the harmlessness verdict).
- [x] **FIXED Session 105** (byte-identical: the separator is now the escape `"\0"`, same runtime NUL byte but printable source; the docstring is re-quoted; `test/unit/source-hygiene.test.ts` scans `src/**/*.ts` for NUL bytes so it cannot recur; CHANGELOG has the entry). Original report: **`core/embedded/vdoc-path.ts` holds two raw NUL bytes, so git and grep both read it as BINARY — and it is the file that owns the delete loop's ownership grammar** (found by the Session 103 adversarial review; **pre-existing, NOT introduced by S103** — verified present at `c18eb72`, the session-claim commit). `vdocKeyString` uses a literal NUL as its field separator (`[...].join("\0")`, ~`:164`), and the docstring above it quotes that NUL inline (~`:159`). Two consequences, both measured: (1) **`grep -rn` silently skips the whole file** — `grep -rn TEMP_DIR_RE src/` returns NOTHING while `grep -ran` finds it at `:126`. This is the ONLY tracked file in the repo with the problem. It matters because `vdoc-path.ts` owns `TEMP_DIR_RE`, `isOurVdocFileName` and `VDOC_DIR_SEGMENTS` — the ownership gates on BOTH delete loops — so any future grep-based inventory over `src/` is silently blind to them. S103's own §7 inventory re-verification was unsound for this reason (its conclusions happened to be right only because the symbols genuinely did not exist yet). (2) **`git diff` renders it as `Bin 5465 -> 9023 bytes`, 0 insertions / 0 deletions** — so every change to the grammar is invisible to review, and S103's L1 diff could not be read in git at all. **Fix:** change the separator to a printable character that still cannot occur in a URI/languageId/extension, and re-quote the docstring. `vdocKeyString` is an IN-MEMORY `Map` key only — never persisted, never cross-session — so the change is behaviourally inert (grep `-a` to find its 3 call sites, since plain grep cannot see them). Add `*.ts diff` in `.gitattributes` if belt-and-braces is wanted. **Not fixed in S103 deliberately** (SAFEGUARDS: commit first, discuss scope — it is pre-existing and orthogonal to the temp-sweep capability). **Sizing: LOW, but the ROI is unusually high** — it silently defeats the standard verification method this project's planning sessions rely on (a grep-based evidence inventory is MANDATORY for deletion-shaped plans).
- [ ] **The `🐉1` EPERM discriminator cannot run as root or on Windows, and the whole temp-sweep suite is POSIX-shaped** (found by the Session 103 adversarial review; **the CODE is correct on every platform — this is test reach, not a defect**). `isProcessDead`'s only discriminating test asserts `isProcessDead(1) === false`, which works because `kill(1,0)` throws `EPERM` for a non-root user. Measured S103: **as root `kill(1,0) does NOT throw`, so the shipped and the naive `catch { dead }` implementations become identical and the assertion passes while proving nothing**; on Windows there is no PID 1 at all (NT allocates PIDs in multiples of 4). S103 made the test probe its premise and `skip()` loudly rather than pass vacuously, so it is honest — but that means **🐉1 is UNVERIFIED whenever CI runs as root (containers routinely do) or on Windows**, and the suite would go quietly green. Also unmeasured on those platforms: `process.getuid` is undefined on Windows so G3's uid check is skipped by design; `fs.mkdtemp` applies no POSIX mode there (`%LOCALAPPDATA%\Temp` is per-user, so privacy holds by a different mechanism). **Fix options:** a root-safe discriminator (spawn a child as another uid — needs privileges), or a seam that injects the `kill` probe so the three outcomes (no-throw / EPERM / ESRCH) can be exercised without a real foreign process. **Sizing: LOW** — this project runs its suite on macOS as a normal user, where the discriminator does work; the risk is a future CI change silently disarming the one test that catches the inversion.
- [x] **Item 17a — cell-execution code-cell background highlighting — SHIPPED Session 111 (strict TDD).** A config-gated whole-line `vscode.TextEditorDecorationType` tints the background of every executable `{lang}` cell across the visible `.qmd` editors, kept in sync on active/visible-editor, document, and config changes. Three `contributes.configuration` keys: `quarto.cells.background.enabled` (default `true`, Posit-parity), `.light` (`#0000000F`), `.dark` (`#FFFFFF14`) — a deliberately faint translucent tint. STATIC decoration of cells that *can* run (Run Cell delegates out-of-process, so no running-cell state exists), as scoped. Pure `src/core/cell-background.ts` (`cellBackgroundRanges` maps `findAllCells` to fence-inclusive line spans; `resolveCellBackgroundSettings`/`DEFAULT_CELL_BACKGROUND` resolve config) unit-tested RED→GREEN; `src/features/cell-background.ts` adapter integration-tested (bridge + enabled/languageId gating + apply path, break-revert-proven). Commits `a36f273` (core) + `b5cbf28` (adapter/config/wiring); 849 unit / 341 integration / clean 43-file `.vsix`. Learning #124. **Runtime path exercised in the EDH (activate ran the feature; `setDecorations` ran with real ranges); the rendered tint has no VS Code read-back API (eyeball/runtime-only).** Severity LOW (purely visual). Audience: all users, cosmetic.
- [ ] **Item 17b — Reticulate execution pathway for mixed R/Python Knitr documents** (`quarto.cells.useReticulate`; split from item 17, Session 109). In a Knitr-engine document Posit can route `{python}` cells through R's `reticulate` instead of Jupyter. **Grounded S109:** `reticulate` appears nowhere in `src/`; Run Cell today delegates by language only (`src/core/execution-delegate.ts` `pickDelegate`/`delegateCommandsFor`, Python → Jupyter) with no notion of the document's compute engine. So this needs (1) a config `quarto.cells.useReticulate`, (2) **engine detection (Knitr vs Jupyter) — which does not exist yet and is the load-bearing unknown to scope first**, and (3) a delegate-selection branch that, when engine=Knitr ∧ flag set, sends `{python}` cells down the R pathway. Severity LOW, **narrow audience** (only mixed R/Python Knitr users). ⚠ Confirm whether engine detection is worth building for this feature alone before picking it up.
- [x] **Item 17c — create-document-family command discoverability: `quarto.newPresentation` + `quarto.fileNewDocument` SHIPPED Session 110; `quarto.newNotebook` (`.ipynb` creation) SPLIT OUT to item 17e below.** Posit ships preset "new X" commands for command-palette / File▸New discoverability over the `quarto.newDocument`/`quarto.createProject` capability this project already has. **SHIPPED (S110, strict TDD):** `buildNewDocumentContent` (`src/core/new-document.ts`) is now parameterized by a `NewDocumentFormat` union (`html`|`revealjs`, default `html` so existing callers are byte-identical); `quarto.newPresentation` opens an untitled `.qmd` with `format: revealjs`; `quarto.fileNewDocument` reuses the `newDocument` html path and is contributed to `contributes.menus["file/newFile"]` (hidden from the palette via `when:"false"` so it doesn't duplicate `newDocument`). Commits `62a172d` (core) + `c76661b` (wiring); 835 unit / 337 integration / clean 43-file `.vsix`; break-revert-proven the integration wiring. The third Posit command, `quarto.newNotebook`, creates an `.ipynb` — a DIFFERENT mechanism (notebook creation, not a text buffer), so NOT the "thin wiring over `newDocument`" the rest of 17c was — split to item 17e (operator decision, S110). Severity LOW (discoverability polish). Audience: all users, minor convenience.
- [x] **Item 17d — notebook markdown-cell Quarto-aware renderer — SHIPPED Session 113 (strict TDD); first slice: the `callout-note` type.** A `contributes.notebookRenderer` (`quarto-mit.markdown-it-callout`) extending VS Code's built-in `vscode.markdown-it-renderer` now renders a Quarto `::: {.callout-note}` fenced div as a note-admonition block inside `.ipynb` **markdown** cells (instead of raw `:::` text). Genuinely new contribution-point infra: pure `vscode`-free markdown-it plugin `calloutNotePlugin` (`src/core/notebook-callout.ts` — container block rule, RED→GREEN, 10 unit tests via `md.render()`); webview entrypoint (`src/webview/notebook-renderer.ts` — `activate` → `getRenderer("vscode.markdown-it-renderer")` → `extendMarkdownIt`); `esbuild.js` 2nd browser/esm context → `dist/notebook-renderer.js` (2.8 KB; markdown-it NOT bundled — host-provided, so it is a devDep); 3 integration tests gate-d break-revert-proven via the manifest id. Mechanism grounded FIRSTHAND from the shipped built-ins (`markdown-language-features`/`markdown-math`/`ipynb` `notebook-out/*.js`). Commits `a89b4c9` (core) + `b0d632b` (infra/wiring); 865 unit / 349 integration / clean 44-file `.vsix`. Learning #126. **Follow-on (A) SHIPPED Session 114 (strict TDD):** all five callout types (note/tip/warning/caution/important) via a data-driven `CALLOUT_TITLES` map (`calloutNotePlugin`→`calloutPlugin`; per-type RED→GREEN; first-known-callout-class-wins for multi-class divs — an adversarial-panel-caught regression, fixed); commits `d8f656e`/`ffff176`/`e19e603`; 872 unit / 349 integration / clean 44-file `.vsix`; Learning #127. **Follow-on (C — generic fenced divs) SHIPPED Session 115 (strict TDD):** a non-callout Pandoc fenced div `::: {.className}` (id + class(es)) now renders as `<div id=… class=…>body</div>` (was raw `:::`); a new generic-div branch in the same block rule handles only what `calloutType` rejects, so the callout admonition path is byte-identical; `key=value` attrs are stripped before id/class extraction (deferred), and last-id-wins + interior-dot names (`.a.b`/`#id.class`) match pandoc — all grounded FIRSTHAND vs `quarto render` and hardened by a 5-lens adversarial panel that caught a MEDIUM (a `.`/`#` inside a quoted `key="value"` was misread as a phantom class/id, Learning #128) + 2 LOW fidelity gaps, all fixed TDD. Commits `2e073bb`/`87d51b1`/`572cc5c`/`c91b82c`; 886 unit / 349 integration / clean 44-file `.vsix`; markdown-it still host-provided (bundle 4.5 KB). **Remaining deferred follow-ons (all LOW):** (B) CSS box styling (still structural-only — the `:::` markers vanish + body renders under the type's title, but no coloured box yet; eyeball-only); **(D) custom callout titles — SHIPPED Session 118 (strict TDD):** a non-empty `title=` attribute (`::: {.callout-note title="Foo"}`) and/or a leading heading (any level, ATX `##` or setext; removed from the body) become the displayed title, rendered as inline markdown via `md.renderInline`; `title=` wins over a heading (heading stays in body), empty `title=""`/empty heading fall back to the default type name, generic-div `title=` stays a plain HTML attribute — all grounded FIRSTHAND vs `quarto render` 1.7.33; a 6-lens adversarial `Workflow` panel caught one in-scope regression (empty leading heading → blank title, fixed) and confirmed B/C/D/E below as pre-existing-root divergences; commits `ea9ba17`/`8325f72`/`7415739`/`6cff2bd`/`3c56ba9`; 946 unit / 349 integration / clean 44-file `.vsix`; Learning #131. **Follow-on (E) — collapse → `<details>` — SHIPPED Session 119 (strict TDD):** a `collapse` attribute makes a callout collapsible, rendered as a JS-free `<details>` whose `<summary>` is the header (quarto uses a Bootstrap-collapse `<div>` toggled by JS — grounded FIRSTHAND vs `quarto render` 1.7.33, so the SEMANTICS match, not the DOM). `collapse="true"`/unquoted `true` starts collapsed; **any** other value (`false`/`TRUE`/`True`/`maybe`/empty) is collapsible-but-expanded (`<details open>`) — quarto makes any collapse value collapsible and only exact `true` starts collapsed; no `collapse` attribute stays a non-collapsible `<div>` (unchanged). The control keys on the **SOURCE** `collapse` name (`calloutCollapse`), so a generic-div `collapse=` byte-matches quarto's `data-collapse` passthrough and a literal `data-collapse=` on a callout stays inert. A 6-lens adversarial `Workflow` panel (14 agents) caught ONE in-scope defect — a literal `data-collapse=` was wrongly hijacked as the control (both `collapse=` and `data-collapse=` normalise to the same `data-collapse` name), fixed by keying on the source key — and two pre-existing divergences (#12–#13 below); commits `0ba05f2`/`e137350`/`344310b`/`27c952d`; 960 unit / 349 integration / clean 44-file `.vsix`; Learning #132. **Follow-on (B) — CSS box styling — SHIPPED Session 120 (strict TDD):** the callouts now render as coloured admonition boxes. Pure-core `calloutStyles()` emits, per known type, the grounded quarto 1.7.33 accent (`border-left-color` + a theme-adaptive translucent header tint via `hexToRgb`, a disclosed divergence from quarto's fixed light tint so it reads in both VS Code themes) and the grounded per-type Bootstrap-Icons glyph (info-circle/lightbulb/exclamation-triangle/cone-striped/exclamation-circle) as a data-URI `background-image` on `.callout-title::before`; `injectCalloutStyles()` (in `src/webview/notebook-renderer.ts`) injects it once (idempotent, `<head>`-or-`documentElement`) into the renderer webview. Targets OUR class structure (`.callout`/`.callout-header`/`.callout-title`/`.callout-body`, both the `<div>` and `<details>`/`<summary>` forms), NOT quarto's Bootstrap DOM — a visual/semantic equivalent (same posture as the `<details>` collapse slice). Icons are Bootstrap Icons (MIT, disclosed in `NOTICE`). Strict TDD (4 RED→GREEN increments — base box, per-type accent+tint, per-type icon, the injection helper — + 2 grounded regression-lock coverage batches). A 5-lens adversarial `Workflow` panel (11 agents) found 3 in-scope findings: 1 refuted (global-scope `.callout` selectors over the user's own Quarto content — accepted design) and 2 genuine doc/structure defects (a stranded JSDoc + a stale `CALLOUT_STYLE` reference from the increment edits) fixed at root. Rendered box is EYEBALL-ONLY (no webview DOM read-back); verification is the unit suite (the CSS string) + an integration ship-check (the CSS/icons/injection-id ship in the bundle). 977 unit / 350 integration / clean 44-file `.vsix`; markdown-it still host-provided. **Item 17d STRUCTURE is fully shipped (A S114, C S115, D S118, E S119) and RUNTIME-CONFIRMED in a live notebook Session 121** — an operator-launched Extension Development Host eyeball verified (first time visually) that all five callout types, custom titles (D), and the collapsible `<details>` (E) render correctly in a real `.ipynb` markdown cell. Learning #133. ✅ **Follow-on (B) box CSS shadow-DOM channel FIXED Session 122 (strict TDD, commit `1515fd8`):** VS Code renders every notebook markdown cell inside a SHADOW ROOT and clones in only `#_defaultStyles` + elements carrying `class="markdown-style"`; the S120 `injectCalloutStyles` injected a bare `<style>` into `document.head` WITHOUT that class, so it never crossed the shadow boundary (RUNTIME-CONFIRMED NON-FUNCTIONAL S121 — plain unstyled title+body). S122 tags the injected `<style>` `class="markdown-style"` so it is cloned per-cell (the clone loop's else-branch clones a bare classed `<style>`; no `<template>` wrapper needed). **Box CSS now SHIPS via the correct channel** — 978 unit / 351 integration / clean 44-file `.vsix`; `grep markdown-style dist/notebook-renderer.js` → 1; mechanism firsthand-grounded in VS Code's shipped source + a 4-lens adversarial panel CLEAN on mechanism/regression — but the on-screen RE-EYEBALL is operator-gated and not yet re-run, so it is confirmed *shipping*, not yet confirmed *rendering on screen*. So 17d's STRUCTURE is runtime-confirmed (S121) and its box CSS is now RUNTIME-CONFIRMED rendering too (S122 — operator-launched EDH eyeball verified coloured boxes for all types). **Item 17d is now fully done end-to-end.** Learning #134 (diagnosis) + #135 (fix). **Generic-div `key=value` attributes + bare-word `::: foo` shorthand SHIPPED Session 116 (strict TDD):** `key=value` attrs now emitted with pandoc's HTML5 `data-` prefix rule (a 214-name known-attribute passthrough set derived FIRSTHAND from the quarto-bundled pandoc 3.6.3; `class=`/`id=` merge; attr values decoded via `md.utils.unescapeAll` for backslash/entity parity); bare word `::: foo` ≡ `::: {.foo}`; all grounded firsthand + hardened by a 6-lens adversarial panel that caught 4 real bugs (21 missing event handlers, backslash/entity value double-encoding, a `{…}`-glued-trailing bareword regression, a leading-underscore key over-acceptance), all fixed TDD; 918 unit / 349 integration / clean 44-file `.vsix`; Learning #129. Severity LOW (niche audience — direct `.ipynb` markdown-cell editing). See the new **nested-fenced-div** item (below) for a HIGH pre-existing bug the panel surfaced.
- [x] **Item 17e — `quarto.newNotebook`: create a new Quarto `.ipynb` notebook — SHIPPED Session 112 (strict TDD).** `quarto.newNotebook` opens a new in-memory (untitled) `jupyter-notebook` seeded with a raw YAML front-matter cell (`---`/`title`/`format: html`, a `NotebookCellKind.Code` cell with `languageId:"raw"` → Jupyter `cell_type:"raw"`) + an empty python starter code cell, and shows it. Pure `vscode`-free `src/core/new-notebook.ts` `buildNewNotebookCells(title)` returns `{ languageId, value }` descriptors (reuses `buildNewDocumentContent` for title normalization + YAML escaping; both cells are code cells, so no `kind` field — a markdown cell would leak workspace symbols, so the starter is deliberately heading-free); `src/features/new-notebook.ts` maps them to `vscode.NotebookCellData` and mirrors VS Code's own `ipynb.newUntitledIpynb` (seeds `NotebookData.metadata = {cells:[],metadata:{},nbformat:4,nbformat_minor:5}` per call). `contributes.commands` + a File▸New File… menu entry. Design Q2 verified FIRSTHAND against the shipped serializer (`notebookSerializerWorker.js` `S(t)` + `ipynbMain.node.js` `F1`) AND machine-verified in the EDH in BOTH directions (save: Code+"raw" → `cell_type:"raw"`; load: `cell_type:"raw"` → Code+"raw"). Adversarially reviewed: 5/5 load-bearing claims survived refutation. `check-types` clean, 855 unit (+6), 346 integration (+5, break-revert-proven gate d), clean 43-file `.vsix`. Learning #125.
- [x] **Notebook-renderer: nested same-length fenced divs are mis-paired — FIXED Session 117 (strict TDD).** (HIGH, PRE-EXISTING since S113; surfaced by the S116 6-lens panel, verified firsthand braced *and* bare-word.) The `calloutRule` closing-fence scan accepted the FIRST fence of ≥ the opener's length as the closer with **no depth tracking**, so an inner and outer div of the same `:::` length made the outer grab the INNER div's closer and close early — ejecting trailing content and folding the real outer closer into a literal `<p>`. **Fix (commit `f11e7c8`):** the scan now tracks nesting depth — each fence line (≥ `MIN_MARKERS` colons at the line start, not indented code) is a CLOSER when only whitespace follows the colons (decrement if `depth>0`, else it is our closer), else a nested OPENER when its params open a div by our own rules (new `isDivOpener`: a known callout class or a generic div with an id/class/attr), else body content. The closer's colon count is now **independent** of the opener's (needing only ≥ 3), so a `::::` div closes on a bare `:::` — all grounded FIRSTHAND vs quarto pandoc 3.6.3 (same-length, mixed-length, unbalanced, bare-word, callout-in-generic, generic-in-callout, triple nesting). The callout admonition path is byte-identical (17 callout tests unchanged; `calloutType` runs first). **A 5-lens adversarial `Workflow` panel then caught a REGRESSION the depth fix introduced** (verified firsthand pre-fix-matched-pandoc, post-depth-fix-broke): the flat scan had no fenced-code awareness, so a `:::`-looking OPENER inside a ```` ``` ````/`~~~` code block (e.g. `::: {.x}` shown as EXAMPLE CODE) was counted as a nested opener and consumed the div's real closer. **Fix (commit `66c2913`):** the scan tracks fenced-code state (```` ``` ````/`~~~` open/close, mirroring markdown-it's fence rule) and treats every line inside a code block — `:::` included — as literal; this also fixes the PRE-EXISTING bare-`:::`-inside-code early-close (both now match pandoc across opener/closer/sibling/nested cases). 6 of 8 nesting tests + 2 code-fence tests are genuine RED→GREEN. `check-types` clean, 930 unit (+12) / 349 integration (unchanged) / clean 44-file `.vsix`. The panel's other findings (empty `::: {}` and paragraph-glued openers nested inside a div) were adjudicated FIRSTHAND as **pre-existing, self-consistent, out of scope** — see the note appended to the divergences item below.
- [ ] **Notebook-renderer: minor pandoc-fidelity divergences (all LOW, mostly PRE-EXISTING; accepted/documented Session 116).** Surfaced by the S116 adversarial panel, each verified firsthand vs quarto pandoc 3.6.3; none fixed (LOW + pathological or cosmetic). (1) **Apostrophe not entity-escaped** — a `'` in a value renders literally (`title="Don't"`) where pandoc writes `&#39;`; markdown-it's `escapeHtml` deliberately omits `'`. **Cosmetic only — the value sits inside a double-quoted attribute, so the parsed DOM is byte-identical**; no injection. (2) **Empty `::: {}`** falls through unrendered; pandoc emits a bare `<div>` (pinned by an existing test; pointless input). (3) **Fenced div interrupts an open paragraph** (no blank line before `:::`) where pandoc requires the blank line — PRE-EXISTING (`alt:["paragraph",…]` ruler registration since S113/S114, byte-identical old-vs-new), shared with the callout path. (4) **1–3 leading spaces before `:::`** still open a div; pandoc requires column 1 — PRE-EXISTING opening-fence indent handling. (5) **Non-ASCII class/id/key names** (`.класс`) fall through; pandoc accepts Unicode letters — PRE-EXISTING ASCII `\w` grammar. (6) **Escaped separator in an UNQUOTED value** (`key=a\ b`) falls through; pandoc unescapes to `a b` (`unescapeAll` does not handle `\`+space). (7) **A bare `X` and a literal `data-X` in the same div** — the plugin dedups on the post-`data-` name and keeps one; pandoc emits both (a duplicate, invalid-HTML attribute) — the plugin's single-attr output is arguably cleaner. Each is niche; revisit only if a real document hits one. **S118 addendum (custom-title panel, all LOW, all PRE-EXISTING in root cause, each verified firsthand vs quarto render 1.7.33):** (8) **Pandoc heading attributes leak into an extracted title** — `::: {.callout-note}` / `## Heading {#id .cls}` shows title `Heading {#id .cls}` where quarto shows `Heading` (the `{…}` becomes the heading's attributes). Root cause is the PRE-EXISTING markdown-it gap that it does not implement pandoc's header-attribute syntax — the same literal `{…}` also leaks in a body `<h2>` (when `title=` is present so the heading is not extracted); the S118 change only makes it newly visible in the title bar. Fixing = parse pandoc heading attributes (affects all headings, not just titles), out of scope. (9) **`}` inside a quoted callout `title=` flips the callout to a plain generic div** — `::: {.callout-note title="a}b"}` renders `<div class="callout-note" title="a}b">` (no admonition) where quarto renders a titled callout, because `calloutType`'s strict `ATTR_BLOCK` gate `/^\{[^}]*\}$/` rejects any interior `}` while `parseDivAttrs`'s relaxed gate accepts a `}` in a quoted value. PRE-EXISTING (the strict gate since S113/S114); fix = derive the callout type from `parseDivAttrs`'s parsed classes so the two gates agree (a refactor of the byte-identical callout path). (10) **Multi-line setext "heading" extracted as a multi-line title** — `Line one` / `Line two` / `======` inside a callout: markdown-it treats it as a 2-line setext H1 and the extractor lifts it into the title (embedded newline), where pandoc does not treat it as a heading at all (default title, body paragraph). PRE-EXISTING markdown-it-vs-pandoc setext divergence; pathological. (11) **Whitespace-only quoted attribute value renders a callout** — `::: {.callout-note title=" "}` renders a callout with a single-space title where pandoc rejects the whole `:::` line as paragraph text. PRE-EXISTING `tokenizeDivAttrs` leading-space quirk affecting all quoted values (generic divs too). **S119 addendum (collapse panel, both LOW, both PRE-EXISTING in root cause, each verified firsthand vs quarto render 1.7.33):** (12) **A bare `collapse` flag (no `=value`) on a callout renders a plain non-collapsible callout** where quarto rejects the whole fenced-div block as literal `:::` text (invalid pandoc attribute syntax → no callout at all). Root cause is the PRE-EXISTING `calloutType`-vs-`parseDivAttrs` leniency: `calloutType`'s `ATTR_BLOCK` regex matches `.callout-note` even when `tokenizeDivAttrs` rejects the block, so `attrs` is `null`, the collapse (and title) paths are never reached, and the callout renders plain/default. Any invalid bareword inside a callout block (`{.callout-note xyz}`) manifests it identically; byte-identical pre-collapse-change. Same fix as divergence (9): derive the callout type from `parseDivAttrs`'s parsed classes so the two gates agree. Out of scope. (13) **A quoted collapse value with LEADING whitespace** (`collapse=" true"`) renders an expanded-collapsible callout where quarto emits the whole `:::` line as literal text. PRE-EXISTING leading-space handling — the SAME root as divergence (11); a `title=" Hi"` behaves identically (plain callout for us, literal text in quarto), an upstream fence-recognition property unchanged by the collapse work (trailing-only `"true "` and interior `"a b"` both parse in quarto and match us). Out of scope.
  - **S117 addendum (nested manifestation of (2) and (3)):** the S117 adversarial panel flagged that when an empty `::: {}` (divergence 2) or a paragraph-glued `::: {.inner}` (divergence 3) appears NESTED inside another div, the depth counter mis-pairs (empty `{}` is not counted as an opener since the plugin does not render it as a div; a glued opener IS counted since the plugin's `alt:["paragraph"]` registration interrupts paragraphs). Adjudicated FIRSTHAND (pre-fix vs post-fix vs pandoc): **both are consequences of the pre-existing standalone divergences here (2)/(3), NOT of the S117 depth fix** — for same-length fences the empty-`{}` case is byte-identical pre-fix and post-fix, and the glued case is now MORE self-consistent post-fix (the interrupted inner div is contained by the outer, where pre-fix left it orphaned). Because `isDivOpener` is deliberately consistent with what the plugin actually renders as a div, making it match pandoc's broader opener set (empty `{}`, trailing-colon `::: {.x} :::`, degenerate literal-brace fallbacks) would DE-sync depth from the rendered structure — so these stay out of scope until the standalone divergences (2)/(3) are themselves addressed. Still LOW/pathological.
- [x] **Notebook callout BOX CSS shipped through the WRONG channel — FIXED Session 122 (strict TDD, commit `1515fd8`): the injected `<style>` now carries `class="markdown-style"` so VS Code clones it into each cell's shadow root.** (Was a CONFIRMED runtime defect Session 121; repaired the S120 follow-on (B) box styling.) RUNTIME-VERIFIED S121 (operator-launched EDH eyeball — structure renders, boxes were plain unstyled text — plus a firsthand read of VS Code's shipped `markdown-language-features/notebook-out/index.js`). VS Code renders **every** notebook markdown cell inside a **shadow root** (`renderOutputItem:(r,o)=>{ … o.attachShadow({mode:"open"}) … }` — there is no light-DOM render path). The ONLY CSS that reaches that shadow root is `#_defaultStyles` plus every element carrying **`class="markdown-style"`**, which the built-in renderer clones per-cell into the shadow root (`for (const d of document.getElementsByClassName("markdown-style")) a.appendChild(d instanceof HTMLTemplateElement ? d.content.cloneNode(true) : d.cloneNode(true))`); the built-in ships its OWN default markdown CSS exactly this way (`n=document.createElement("template"); n.classList.add("markdown-style"); n.content.appendChild(styleEl); document.head.appendChild(n)`). Before S122, `injectCalloutStyles` (`src/webview/notebook-renderer.ts`) appended `<style id="quarto-mit-callout-styles">` to `document.head` **without** the `markdown-style` class, so it was never cloned into the shadow roots and a bare light-DOM `<style>` cannot pierce the shadow boundary → callouts rendered as PLAIN title+body (box/accent/tint/icon all absent). Artifact tell: `grep markdown-style dist/notebook-renderer.js` → **0** pre-fix, **1** post-fix (S122). `calloutStyles()` (the CSS string) is CORRECT and unit-tested; only the shipping MECHANISM is wrong. **Fix SHIPPED S122 (strict TDD, commit `1515fd8`):** tagged the injected `<style>` with `class="markdown-style"` (kept the `id` for idempotency); a bare classed `<style>` hits the clone loop's else-branch (`d.cloneNode(true)`) and is cloned per-cell — a `<template>` wrapper is not required (firsthand-grounded + adversarial-panel-confirmed). RED unit test (injected element carries `markdown-style` against the fake `StyleHost`) → GREEN; integration ship-check the bundle contains `markdown-style` (978 unit / 351 integration / clean 44-file `.vsix`). **SHIP-VERIFIED** (mechanism firsthand-grounded in VS Code's shipped `notebook-out/index.js` + a 4-lens adversarial panel returned CLEAN on mechanism/regression); and the on-screen RE-EYEBALL was **RUN + CONFIRMED (S122 post-close-out)**: an operator-launched Extension Development Host verified all callout types render as coloured admonition boxes (left accent bar + tinted header + per-type icon) in a live `.ipynb` markdown cell — the box CSS is now **RUNTIME-CONFIRMED rendering**, the first visual confirmation of the box styling. Severity LOW (niche — direct `.ipynb` markdown-cell callout editing). Learning #134 (diagnosis) + #135 (fix).
- [ ] **`docs/POSIT-COMPARISON.md` understates notebook-renderer parity (PRE-EXISTING, LOW).** Lines ~651–652 still assert "This project has no equivalent notebook-renderer contribution." — stale since S113 (when `contributes.notebookRenderer` `quarto-mit.markdown-it-callout` first shipped), NOT caused by the S122 box-CSS fix; surfaced by the S122 completeness-lens sweep over 17d. The project now ships a `notebookRenderer` extending `vscode.markdown-it-renderer` that renders Quarto callouts + generic fenced divs in `.ipynb` markdown cells (S113–S122). Fix = update that parity row to note the contribution (narrower scope than Posit's full-markdown renderer, but no longer "no equivalent"). Doc-only, no logic → TDD-exempt, its own small session. Filed rather than fixed in S122 to hold 1-and-done / avoid "while I'm at it" scope creep.
