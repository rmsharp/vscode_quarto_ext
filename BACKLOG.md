# Backlog

*Open, actionable work items only. Completed work → `CHANGELOG.md`. Feature inventory & plans → `ROADMAP.md`.*

> **⚠ Historical `BACKLOG:NNN` / `BACKLOG item N` references do not resolve — and never reliably did.**
> Sessions used to cite this file by **line number** (`BACKLOG` + colon + a line number) or by the
> roadmap's **item number** (`BACKLOG` + the word *item* + a number). Both are positional: any insertion
> above a cited line silently retargets it at a *different* item, which is worse than a broken link
> because it still looks valid. By Session 150, 12 of the 34 distinct cited line numbers already landed
> on blank or unrelated lines — and one had been wrong from the day it was written.
>
> Session 150 rewrote all **194** such citations in **live** artifacts (`src/`, `test/`, `docs/`, and this
> file) to name their target instead — `CHANGELOG: <title>, Session N` for work that has shipped, or
> `BACKLOG: <item title>` for work still open here. (The replacement text carries no quote or backtick
> on purpose: several citations sit inside `describe(...)`/`it(...)` string literals, where an inserted
> quote terminates the string and breaks the build.) It deliberately left the ~523 occurrences in the
> **append-only history** (`SESSION_NOTES.md`, `HANDOFFS.md`, `CHANGELOG.md`, `PROJECT_LEARNINGS.md`)
> untouched: rewriting those would edit the record of what past sessions actually wrote. **When you meet a
> `BACKLOG:NNN` in a historical document, read it as "some item in `BACKLOG.md` as it stood that day" and
> resolve it with `git show <commit>:BACKLOG.md`, not against the current file.**
>
> **Do not add new positional citations.** Cite items by title.

## Active

*(empty — pick the next item from "Up Next" below.*

***The `contentColumns` stack's spaces-only measurement — SHIPPED Session 194.** A line's
indentation is now measured in COLUMNS (`indentColumn`, tabs advancing to the next 4-stop,
absolute), at three call sites: the container-closing pop in `computeRegions`,
`indentedCodeLine`, and `rawTexMacroLineIsBlock`. Measured as an EQUIVALENCE over 432 rendered
ground documents — every tab spelling paired against the SPACE spelling reaching the same
column — and quarto answered identically in **276 of 276** pairs. 1,915 documents scored with
both directions separate, **new-error SET empty in both directions on every corpus**, 54
phantoms drained and 134 real headings recovered. ⚠ The `rawTexMacroLineIsBlock` half was a
**disclosed scope amendment**: it was out of scope at claim, and re-scoring Session 193's own
corpora measured 6 NEW LOST headings there once the pop was corrected — the old wrong pop had
been masking that row's pre-existing tab blindness. See `CHANGELOG.md`.*

***⚠ THE SAME RULE IS STILL WRONG IN TWO MORE PLACES, and both are now pinned with rendered
controls in `test/unit/qmd-model.test.ts`.** `listItemContentColumn` and
`CONTENT_COLUMN_4_OPEN` both anchor on `^( *)`, so a TAB-INDENTED list marker or footnote
definition opens no tracked column at all. SIX places in that file measure indentation for the
container-column machinery; three now share `indentColumn` and three still count spaces —
`SETEXT_H1`/`SETEXT_H2`, `listItemContentColumn` and `CONTENT_COLUMN_4_OPEN`.*

***PREREQUISITE P3 — the escape-decoding FP — SHIPPED Session 151**, **format-name Combo 3
(the `_quarto.yml` top-level scalar `format:` NAME) — SHIPPED Session 152**, the **`.qmd`
sibling-enumerator OLD arming fix — SHIPPED Session 153**, and the **`findCellOptionLines`
phantom-quote FN (Defect A on the THIRD/last value enumerator, the `#|` cell-option surface) —
SHIPPED Session 154**, and the **abutting-anchor node-property strip fix on the 3 front-matter/project
value enumerators — SHIPPED Session 155**, the **`findCellOptionLines` strip over-exclusion
parity fix — SHIPPED Session 156**, the **continuation-path node-property-blind `scanFlow`
lost TP — SHIPPED Session 157**, and the **block-scalar (`|`/`>`) cell-option FP — SHIPPED
Session 158** (all four continuation-guard arming sites now narrow the arm to a
first-char opener; the three value enumerators (S155) and the cell-option enumerator (S156) now all
use the YAML-exact anchor-name strip charset `[^\s,[\]{}]` — and on the cell-option surface it was a
live lost TP, not the safe-FN/UNVERIFIED it had been filed as; S157 then fixed the SIBLING decision
path at the shared root — `scanFlow` now skips `&`/`*`/`!` node-property NAMES, so a quote in an
anchor/alias/tag name in a CONTINUATION line of an open flow (or mid-flow on one line) no longer
phantom-arms, closing the anchor-name-quote class on BOTH cell-option decision paths and correctly
benefiting the three value enumerators' continuation paths too; S158 then made `findCellOptionLines`
block-scalar-aware — a `|`/`>` cell-option value now folds its more-indented `#|` continuation lines
into the block instead of emitting them, closing the last known cell-option continuation-fold FP,
grounded live (exit 0) not the "astronomically rare" it had been filed as — see `CHANGELOG.md`);
and the **quoted-KEY divergence, LEAF half — SHIPPED Session 159** (both `.qmd` front-matter
enumerators now unquote the key before resolving it against the schema's bare names, recovering a
LIVE lost TP on every quoted key — `"toc": banana` renders quarto exit 1 and was silent; the
`unquoteKey` rule is now shared from `yaml-context.ts` instead of private to `project-yaml.ts`.
S159's own §9 review then found that unquoting would have WIDENED a pre-existing cardinal-sin FP,
so it also taught `mappingColonAt` to skip a quoted key when scanning for the separator — which
removed the pre-existing front-matter FP as well — and REVERTED the cell-option half, filed below);
and the **`#|` leading-block FP — SHIPPED Session 160** (`findCellOptionLines` now reports only a
cell's LEADING contiguous run of directive lines, as quarto does, so an option below code, below a
blank or whitespace-only line, or below a plain `#` comment is no longer emitted or squiggled —
all four terminators grounded firsthand at quarto exit 0, on both the `#` and `//` comment-char
families. S160's own §9 review then found that using the strict `CELL_OPTION_PREFIX` as the
TERMINATOR would have WIDENED a one-line false negative into a whole-cell LOST TRUE POSITIVE, so
termination now tests quarto's own permissive directive pattern while emission keeps the strict
one — see `CHANGELOG.md`); and the **cell-option COMMENT CHAR is now scoped to the cell
LANGUAGE — SHIPPED Session 161** (`findCellOptionLines` builds both patterns per cell from
quarto's own `kLangCommentChars`, closing one root cause that was defective in BOTH directions —
`{sql}` + `--| echo: banana` renders exit 1 and we were silent, `{sql}` + `#| echo: banana`
renders exit 0 and we flagged it — plus the BLOCK-comment suffix rule (`c`/`css`/`sas`/`ocaml`
require a closing delimiter, which also terminates the block when absent) and the CASE-SENSITIVE
lookup that grounding turned up, neither of which the filed item described. Two false positives
S161's own first layer would have shipped were caught by its own verification and fixed before
release: the engine-scope one (`--| cache: banana` in `{sql}` renders exit 0 in a
markdown-engine document) and the block-comment closer landing inside the value span
(`/*| echo: false */` is VALID and rendered exit 0). Its §9 review then found a FALSE
justification in its own engine-scope integration pin and an unpinned `escapeRegExp`, both
fixed in-session — see `CHANGELOG.md`).*

***The `.Rmd` whole-document knitr scope — SHIPPED Session 170.** `documentEngineForScoping`
now answers `"knitr"` on `.rmd`/`.Rmarkdown` (case-insensitive) instead of `undefined`, so
BOTH surfaces follow quarto there: the validator flags a knitr-only key in an `.Rmd`'s
`{python}`/`{sql}`/`{ojs}` cell, and completion offers it. It is the one branch that reaches
knitr without reading anything — `claimsFile` runs before any front matter is partitioned —
and measurement confirmed neither a front-matter override, nor a project
`engines: [jupyter, knitr]` reorder, nor an `{{< include >}}` can take the file back. The
widening was swept rather than argued: all **20** fields `cellOptions("knitr")` adds over
`cellOptions("jupyter")` render `quarto` **exit 1** in a `{python}` cell of a `doc.Rmd`, and
the oracle grew twelve `.Rmd` rows (78 documents) which replay **8 lost TP → 0** against the
pre-S170 build with the cardinal-FP count **identical** on both sides. See `CHANGELOG.md`.*

***Remaining candidates:** the format-name family's **Combos 2 & 4** (the container-key form
`format:\n  banana:`, sub-items (b)–(d) below) is the natural continuation; the **cell comment-char
blindness** filed by S160's §9 review is a LIVE two-directional defect (both an FP and a lost TP,
across 13+ cell languages) and is the recommended next pick in the cell-option family; the
**quoted-KEY CONTAINER half** and the **cell-option half** (blocked on the cell arm-token grammar)
are LOW safe-FN follow-ons to S159; and one safe coverage gap filed by a §9 review — the
**`_metadata.yml` coverage gap** (S152). Operator picks at Phase 0.*

***A note on this file's history:** it accumulated completed items for ~150 sessions despite its
own header rule and SESSION_RUNNER Phase 3F. Session 150 removed 115 KB of completed work
(46% of the file) after verifying each block against `CHANGELOG.md`. **Keep it clean: when a
session completes a backlog item, delete it here in the same commit that records it in the
ledger.**)*

***The `CELL_INFO` fence-token grammar — SHIPPED Session 172.** `CELL_INFO` now carries
quarto's own cell recognizer, transcribed from `breakQuartoMd` in the installed 1.7.33: the
language class is `[=A-Za-z]` and the option tail is `( *[ ,].*)?`, replacing a class that
admitted digits/underscores/hyphens and a `[^}]*` tail that swallowed everything else. A token
quarto does not recognise is not a cell to it AT ALL — no cell is built, no options are
partitioned, nothing is validated — so we were squiggling documents quarto renders clean.
**The old tail was wrong in BOTH directions**, which is what all three filed items missed: it
TRUNCATED where quarto REJECTS (`{r.foo}` and `{ré}` each arrived as an `{r}` cell), and it
could not span a `}` inside a quoted chunk option, so the WELL-FORMED knitr header
`{r, fig.cap="}"}` was not a cell to us while quarto validates it. Closes three filed items at
once — the digit-bearing token (`{python3}`/`{d3}`/`{fortran95}` plus the S166 dotted `{r.foo}`
update), the glued `{mermaid=x}` whose language is really `mermaid=x`, and the Session 16
`findDiagramRegions` over-detection — and closes three shapes NO filed item named: hyphen,
underscore, and a non-ASCII letter. Measured by replaying the pre-S172 build over the same
99-document oracle corpus: **80 agree / 6 lost TP / 13 cardinal FP → 91 / 4 / 4** — nine
cardinal false positives closed, two lost true positives recovered, zero regressed. The
`=`-led raw-block branch was deliberately NOT adopted; see the re-rated item below, whose
"NOT re-verified" caveat this session discharged. See `CHANGELOG.md`.*

***The `test/unit` type-check gap — SHIPPED Session 173.** `tsconfig.unit.json` type-checks
all 62 unit test files (+45 `src` files transitively), and `check-types` runs it, so
`compile`, `package`, `vscode:prepublish`, `test:integration` and `test:lsp` all reach the
gate without naming it. Mutation-proven in both directions: re-creating S162's exact shape
(a hand-copied union missing a member) leaves **`npm test` exit 0 on 1589 passing tests**
while **`npm run check-types` and `npm run compile` exit 2**; and cutting the wiring reds
exactly 2 of `test/unit/type-check-gate.test.ts`'s 6 pins. **The filed cost estimate was
wrong by an order of magnitude, in the direction that would have wasted the session:** of
the "~25 pre-existing errors across ~10 files to adjudicate", only **2 are genuine** — the
other 23 are artifacts of checking the tests under the base project's `module: commonjs`
with no `resolveJsonModule`, i.e. settings that do not match how vitest executes them, and
"fixing" them would have meant editing tests that were never wrong. The real work was the
compiler options, which the item never mentions: a 7-combination matrix showed
`moduleResolution: bundler` — **the very incantation every handoff has quoted since
S162** — injects 6 phantom `TS2702` errors into `src/core/notebook-callout.ts`, and
`node16`/`nodenext` break `import.meta`; `es2022` + `node` is the one faithful pair.
`npm run check-types:unit` retires that per-file incantation. See `CHANGELOG.md`.*


***The `scratchpad/` `.vsix` packaging leak — SHIPPED Session 174.** The
untracked `scratchpad/` directory no longer ships: the artifact went from
**3087 entries / 29.73 MB** to **44 / 1.8 MB** (42 files plus vsce's two
manifest entries), verified by reading `unzip -Z1` on the real `.vsix`, not by
an exit code. The deliverable was not the two `.vscodeignore` lines but the
**inversion of the default**: `.vscodeignore` is a DENYLIST, so the packaged set
is allow-by-default and anything new at the repo root ships unless someone
remembers — which went wrong twice (`tsconfig.unit.json` at S173, `scratchpad/`
for ~11 days). `check-package.js` now checks the packaged set **deny-by-default**
against an explicit top-level allowlist, plus a presence check (an over-broad
exclusion that drops `dist/` reds too — that `.vsix` would package, publish and
install perfectly cleanly) and count/byte ceilings. Proof it is real, in three
directions: a stray root file makes `npm run package` **ERROR with no artifact
produced**; cutting the wiring reds 3 of the 4 pins; `dist/**` excluded reds the
presence check. **The wiring point was chosen by measurement:** pointing
`vscode:prepublish` at a marker probe showed `vsce package` runs it (1 marker)
and `vsce ls` does not (0) — so the gate sits on `vscode:prepublish`, which
catches even a bare `npx vsce package`, and can safely shell out to `vsce ls`
without recursing. **⚠ `scratchpad/` was deliberately NOT added to `.gitignore`**
(decided, Session 174, reasoning recorded in `.vscodeignore`): the
`?? scratchpad/` line in every Phase 0 `git status` is the only human-visible
trace that prior-session scratch lives here, it costs exactly one line
because git collapses the directory, and it masks no other stray file — the
packaging risk is carried by the gate instead. Reversing it is one line.
**⚠ CORRECTED Session 186 — the figure this sentence used to carry ("~97 MB")
was measured at 2.5 GB, and TWO of the three premises survive that measurement
while the third does not.** It really does cost exactly one line, and it really
does mask no other stray file (all 51,873 untracked-and-unignored files were
under `scratchpad/`; zero elsewhere). But **the signal is SIZE-BLIND**: a
collapsed `?? scratchpad/` line is byte-identical at 97 MB and at 2.5 GB, so 26×
of growth passed through it without changing the display by one character.
"Visible" was never "measured". **Known
blind spot, documented at both sites:** the allowlist is top-level only, so a
`media/screenshots/**` leak sits inside an allowed root where only the ceilings
could catch it — `docs/F5-VISUAL-CHECKLIST.md`'s manual `vsce ls` step is still
required. See `CHANGELOG.md`.*

## Up Next

- [ ] **⚠ OPERATOR-DESIGNATED NEXT DELIVERABLE (Session 195) — this file has re-accumulated completed work, for the SECOND time, and the deletion half alone is already known not to hold** (raised by the operator after Session 194's close-out, pointing at the `- [ ] **REMOVED — SHIPPED Session 182.**` line; **every figure below was measured firsthand, not estimated**). **The shape:** 17 lines are unchecked checkboxes whose body says `**REMOVED — SHIPPED Session N.**` (lines 200, 201, 203, 204, 206, 208, 210, 212, 235, 237, 239, 245, 261, 263, 285, 319, 332 as of Session 194's close), plus 19 further completed-work records elsewhere — **36 blocks, ~24 KB, 15% of this 165 KB file**, contributed by **30 distinct sessions between S137 and S194**. ⚠ **Session 194's own close-out added one of them** (commit `aa930d0`), so this is a live practice, not historical drift. **The defect is the `- [ ] ` marker, not the prose:** a completed record wearing an open-task checkbox, so the 114 unchecked boxes in this file include 17 that are not work — anything counting open items is wrong by 15%. **Why it happened, and why "just delete them" is the wrong fix:** the tombstones carry real knowledge — post-hoc corrections of the shipped item's OWN claims (line 206 records that a row filed as wrong in 3 positions was wrong in 7, and that a sibling filed "probably correct, LOW" produced MORE phantoms than the headline row). Sessions gutted the body and kept the bullet rather than lose that. **Session 150 already did the deletion half**, removing 115 KB (46% of the file) and writing "keep it clean" in the note at lines 95–99; it regressed within a few sessions because **nothing detects the regression**. Contrast the ledger, which has both a Phase 3F write-gate AND Phase 0 reconcile-on-read, and `check-package.js`, which is deny-by-default. This file has the instruction and neither backstop. **So the deliverable is TWO halves and the second is the point:** (a) verify each of the 36 blocks against `CHANGELOG.md` and `PROJECT_LEARNINGS.md` and delete only what is genuinely recorded elsewhere — **spot-checked already: the learnings those tombstones cite (#217, #220, #223, #227, #231, #235) all exist, and every session they name (178–184, 187) has CHANGELOG entries, so most are redundant — but "most" is not "all" and each needs its own check**, salvaging anything that would otherwise be lost; and (b) add the missing backstop, either a `bin/check-backlog` asserting no `- [ ] ` line contains `REMOVED`/`SHIPPED` (the `check-package.js` deny-by-default pattern) or a Phase 0 reconcile step (the ledger pattern). ⚠ Shipping (a) without (b) is the exact experiment Session 150 already ran, and it failed.

- [ ] **An `{{< include >}}` makes the document's language set unknowable, and the DECLINE we chose keeps a measured cardinal false positive alive** (found while shipping Session 165; **verified firsthand**, both options measured; **PRE-EXISTING** in effect, but the choice is now S165's). `fileExecutionEngine` resolves the engine from `project.resolveFullMarkdownForFile`, which runs `expandIncludes` FIRST (the include handler is the one directive whose stage is `pre-engine`), so quarto counts languages in the include-EXPANDED text. Measured, it flips the answer in both directions and position matters: a child holding `{julia}` above an `{r}` cell renders **exit 0** where the include-free control renders exit 1; a child holding `{r}` above a `{python}` cell renders **exit 1** where the control renders exit 0; the same include placed AFTER the `{r}` cell leaves it exit 1; and it recurses (doc → child → grandchild). S165 declines (`undefined` → the per-cell approximation), which cannot create a false positive but does not remove the pre-existing one: replayed end-to-end, the two include rows stay cardinal FPs, byte-identical to the pre-S165 tree. **The alternative was measured too:** answering `"ambiguous"` on any include-bearing document removes both, and costs the knitr-only fields on every OTHER include-bearing document — the ordinary book chapter, where quarto really is knitr (one corpus row shows exactly that). A rare conditional FP against a common lost TP is the reverse of the trade this project's doctrine settles (S162's 1-against-46, S163's 26-against-144), which is why the decline was chosen and the decision filed rather than made silently. A real fix reads the included files — off-editor `fs.readFile`, re-trigger on the child's edits — which is the same architectural gap the `_quarto.yml`-from-a-nested-`.qmd` item describes. **Do them together.**
- [ ] **`languagesInMarkdown`'s transcribed regex can backtrack pathologically on a malformed fence line** (reported by the Session 165 §9 review's structural lens; **NOT independently re-measured by S165 — treat the complexity claim as review-reported and time it before acting**). The transcription `/^[\t >]*```+\s*\{([a-zA-Z0-9_]+)( *[ ,].*)?\}\s*$/gm` has adjacent quantifiers that can backtrack on a line that opens like a fence and never closes the brace — the lens reports quadratic behaviour on a long run of spaces. It is quarto's OWN regex, applied to the same text quarto applies it to, so quarto shares any exposure; and it runs once per diagnostics pass over the whole document, on a debounce. LOW, but it is on the editor's hot path in a way quarto's batch render is not, so a measured bound (or a cheap pre-filter that requires a `{` on the line before running the full match) is worth having. Time it first: a synthetic worst-case line, then a real document.
- [ ] **An `.Rmarkdown` file never activates this extension at all, so the `.Rmd` scope S170 shipped is unreachable there** (found by Session 170 while shipping that item; the DECISION half verified firsthand, the ACTIVATION half read from `package.json`, not runtime-verified). Quarto's `kRmdExtensions` is `[".rmd", ".rmarkdown"]` and `isRMarkdownFileName` matches both, so `documentEngineForScoping` answers `"knitr"` for a `doc.Rmarkdown` and the oracle row `.Rmarkdown {python} + cache` agrees with a real `quarto render` (exit 1). But `package.json`'s `contributes.languages` registers only `.qmd`/`.rmd`/`.Rmd` for the `quarto` languageId, and `activationEvents` carries `workspaceContains:**/*.{qmd,rmd,Rmd}` — so opening a `.Rmarkdown` gives no diagnostics, no completion and no highlighting unless the user selects the language by hand. **Not a defect S170 introduced** (the extension has never opened those files) and the fix is a one-line `package.json` addition, but it should be grounded first: confirm the grammar and every feature behave on a real `.Rmarkdown`, since nothing has ever exercised that path. Note the asymmetry it leaves today — `document-engine.ts` handles an extension the editor never delivers.
- [ ] **REMOVED — SHIPPED Session 178.** *(The indented-cell-fence item lived here. See `CHANGELOG.md`, 2026-08-01. Two things it asserted are worth carrying forward, because both were wrong: its ⚠ CONFOUND warning said adopting `\s*` would turn a fence inside an indented code block into a cell "for the outline, run-cell, virtual documents and highlighting, which is the widening direction" — **measurement refutes the premise**, since knitr EXECUTES a fully indented cell (`6 * 7` printed `[1] 42` into the rendered HTML, after a paragraph+blank, i.e. genuine CommonMark indented-code context), so there is no documentation idiom to protect and a list-nested indented cell is a legitimate idiom we were blind to. And it rated the shape "non-idiomatic input" at LOW: the fix recovered **five** lost true positives over the corpus, including a NO-BREAK-SPACE indent, which is what pasting indented code out of a rendered web page produces. Learning #217.)*
- [ ] **REMOVED — SHIPPED Session 179.** *(The fence-closer LENGTH item and its unterminated-cell sibling both lived here; they were one root cause and shipped together. See `CHANGELOG.md`, 2026-08-02. Two things are worth carrying forward. (1) The item described the defect entirely at the FLAG surface — it was also deleting every heading below such a fence from the outline, breadcrumbs and sticky scroll, on documents quarto renders with those headings intact; the pre-S179 outline for a 4-tick/3-tick document was `[{Top → children: ["```{r}"]}]`. (2) It rated the pair MEDIUM "only on a mismatched-backtick document" — with the unterminated half, the outline surface and a previously unfiled lost true positive in the same root cause, that was low. Learnings #220–#222.)*

- [ ] **REMOVED — SHIPPED Session 180.** *(The `blank_before_header` item lived here. See `CHANGELOG.md`, 2026-08-02. Three things are worth carrying forward. (1) Its prescribed fix — "a one-line adjacency test in `computeRegions`" — is MEASURABLY WRONG: built and scored against the real renderer it moves 10 documents toward quarto and 5 AWAY, deleting real headings below a thematic break, a table row, an indented code block, a link-reference definition and a raw HTML block, including one in this repo's own `SESSION_NOTES.md`. (2) It named SETEXT as the hazard; setext turned out inert (that branch already clears the state) while the real hazards were block-level constructs it never mentions. (3) It described the blast radius as the outline family and stopped there — `core/refs.ts` also reads `findHeadings`, and there we were indexing a cross-reference target for a link quarto renders as `?@sec-methods`. Learnings #223-#226.)*
- [ ] **REMOVED — SHIPPED Session 181.** *(The setext-false-negative-above-an-indented-line item was named in Session 180's M8 pin and its handoff receipt but was **never actually filed here** — see the record-gap note below. See `CHANGELOG.md`, 2026-08-02. Three things are worth carrying forward. (1) The obvious implementation — "a block line resets the counter" — DELETES headings the pre-S181 build got right: an underline directly below a block line claims THAT line, so `    indented code` / `===` renders `<h1>indented code</h1>`, and `***`, `___`, `##`, `| a | b |`, `[x]: url` and `\clearpage` behave identically. The correct shape is a one-line deferral, not a reset. (2) `CLOSES_PARAGRAPH` and `OPENS_FRESH_BLOCK` have OPPOSITE safety polarity and must never be unified; the substitution manufactures 24 measured phantom headings and is now killed by a mutant pin. (3) The 95-document corpus that scored the design was blind to the shape the first fix broke, because it was generated from the filed item's own template. Learnings #227–#230.)*

- [ ] **REMOVED — SHIPPED Session 182.** *(BOTH `CLOSES_PARAGRAPH` items above — the `=+` entry and its thematic-break sibling — were one deliverable; see `CHANGELOG.md`, 2026-08-02. Four things are worth carrying forward. (1) The `=+` row was wrong in **7 of the 10** reachable positions, not the 3 filed — 30 phantom headings. (2) The thematic-break sibling was filed as "almost certainly correct, just unpinned, LOW"; it was in fact wrong in a whole **class** — against an OPEN paragraph these runs are lazy continuation, which `---` proves by rendering as an em dash — for 34 phantoms, MORE than the headline row produced. A row filed as "unpinned but probably correct" should name the document that established "probably", and if none was rendered it is unmeasured, not LOW. (3) The filed prescription ("remove the entry") DELETES 5 real headings: pandoc swallows an ATX line into a setext heading when a run follows it directly, and that swallow closes the block. (4) The scoring metric was blind before the corpus was — set-equality-per-document reported 0 regressions for a candidate that was deleting five headings. Learnings #231–#234.)*

- [ ] **REMOVED — SHIPPED Session 183.** *(The indented-code row and the whole nine-row audit were one deliverable; see `CHANGELOG.md`, 2026-08-02. Five things are worth carrying forward. (1) The defect was never one row: all nine ignored `paragraphOpen`, and gating the list removed **65 of 133** measured phantoms. (2) Gating is NOT sufficient and NOT safe on its own — three construct classes genuinely interrupt an open paragraph and had to be hoisted ahead of the bail (HTML block openers, `\begin{…}`/`\end{…}` TeX **environments** but not bare macros, and CLOSER lines `:::`/`...`), plus the gate must be suspended inside a **block quote**. Every intermediate variant that skipped one of these deleted real headings. (3) The reason is structural and worth remembering: **a construct's CLOSING line always looks like it faces an open paragraph** to a per-line scanner, because a closer follows its own construct's content (Learning #237). (4) A degenerate one-line corpus said twelve spellings should be REMOVED; a well-formed corpus flipped them to "correct, but gate them" (Learning #236). (5) The `KNOWN RESIDUAL` pins' stated justification was **refuted**: they claimed tightening would delete `SESSION_NOTES.md`'s "Session 83 Handoff Evaluation" heading; rendered, quarto swallows that line into the paragraph above it. It was a phantom. Learnings #235–#238.)*

- [ ] **REMOVED — PARTLY SHIPPED Session 184, and the rest is REFUTED or RECLASSIFIED.** *(The "`CLOSES_PARAGRAPH`'s patterns are too WIDE" item lived here. See `CHANGELOG.md`, 2026-08-02. Five of the item's own claims are worth carrying forward because measurement contradicted three of them. (1) SHIPPED: the link-reference row now excludes only a VALID footnote label — `^` then one or more characters that are neither whitespace nor another `^` — derived from an exhaustive sweep of 17 label spellings. (2) REFUTED: "matches a LONE `+`, which is a bullet marker" — a bare `+` is an EMPTY LIST ITEM and really is block-level; excluding it deletes 6 real headings. (3) REFUTED: requiring a LEADING pipe — pandoc pipe tables need neither leading nor trailing pipes, so a table's last row can be `c | d`; requiring one deletes 4. (4) REFUTED: the item's own warning not to copy `OPENS_FRESH_BLOCK`'s link-ref form was RIGHT, and now has a number — that form rejects `[]:` and deletes a 5th heading. (5) RECLASSIFIED: the `<` and raw-TeX rows are not narrowable by SHAPE at all; they are now the two items directly below.)*

- [ ] **REMOVED — SHIPPED Session 187.** *(The raw-HTML width row lived here. See `CHANGELOG.md`, 2026-08-08. The row is now `HTML_BLOCK_OR_INLINE_OPEN` — pandoc's `blockTags ∪ eitherBlockOrInline` plus processing instructions and complete comments. **Three things this item asserted are worth carrying forward, because two of them were WRONG.** (1) Its central claim is **REFUTED BY MEASUREMENT**: "`<ins>x</ins>` opens a block and `<em>x</em>` does not" — rendered on the real path the two lines are byte-identical, both staying inside the paragraph. Of the sixteen names it listed as block openers, only `meta`, `canvas` and `output` are in pandoc's `blockTags`; the other thirteen are `eitherBlockOrInline`, which is block ONLY where no paragraph is open. The item had conflated pandoc's two sets. (2) Its prescription — "transcribe pandoc's own tag classification and measure each entry" — was exactly right, and is what shipped. (3) Its framing "do them together" with the raw-TeX item is right about the SHAPE and wrong about the session boundary: two artefacts from two readers is two capabilities, so Session 187 split it at claim (FM #26).)*

***The raw-TeX block-MACRO list — SHIPPED Session 188.** `CLOSES_PARAGRAPH` and
`OPENS_FRESH_BLOCK` now carry pandoc's own macro classification, transcribed from
`Text.Pandoc.Readers.LaTeX` at pandoc 3.6.3 and MEASURED entry by entry over 5,680 rendered
documents. **The rule has THREE classes, not two**, and neither gate is the one this item
assumed: `endline` has no raw-TeX guard, and a paragraph is interrupted iff `inlineCommand'`
fails. Class A (73 names) is block in every context and splits by ARITY — `\par` is a block
and `\par{x}` is not; class B (the five names pandoc puts in both lists, plus every unknown
macro) is block only where no paragraph is open, and is the default; class C (294 names) is
inline everywhere. **This item's own framing was refuted twice over:** the block/inline split
it describes is context-dependent, and the `\newcommand{\foo}{bar}` case it names would have
been DELETED by a naive class-C list, because a one-argument probe is malformed for a
two-argument macro (22 of 316 candidates are blocks at their true arity). 40 headings
recovered, 39 phantoms removed, ZERO deletions. Both of the project's disclosed raw-TeX
residuals are closed. See `CHANGELOG.md`.*

- [ ] **`RAW_TEX_ENV_OPEN` matches an UNCLOSED `\begin{x}` and an ORPHAN `\end{x}`, and neither is a block** (found by Session 188 while measuring the macro classification; **MEASURED**, 4 rendered documents; **PRE-EXISTING**; phantom direction; MEDIUM — the row is tested PRE-BAIL, so it fabricates in EVERY context). `line one` / `line two` / `\begin{center}` / `# ATX Below` renders one paragraph and NO heading, and so does the same document with body text but no `\end`; a bare `\end{center}` with no opener likewise. Only a properly CLOSED environment is a block (`P_env_closed` renders the heading; `P_env_unknown` shows the environment NAME does not matter). Our row `/^ {0,3}\\(?:begin|end)\{[^}]*\}/` matches all four. A real fix tracks `\begin`/`\end` PAIRING, which is scanner state a per-line predicate does not have — the same missing capability as the two items below.

- [ ] **The raw-TeX TAIL family behind the paragraph bail — ~50 residual phantoms, and the obvious fix is MEASURED DELETING three real headings** (found by Session 188; **MEASURED** over 192 tail probes plus a 223-document blind adversarial corpus; **PRE-EXISTING**; phantom direction; MEDIUM by volume). A macro line is a block only when whitespace, its own argument groups, or further NON-INLINE macros follow it: `\clearpage and more prose`, `\clearpage.`, `\clearpage % a comment` and `\maketitle \textbf{b}` are all prose to pandoc and blocks to us. The tail rule IS implemented and shipped — but only on the class-A WIDENING, where over-matching costs a phantom. Carrying it into the narrowing was measured deleting `\vspace*{1em}` (the star is part of the starred form), `\vspace2` (TeX's single-token argument, i.e. `\vspace{2}`) and `\newcommand{\foo}{bar}` — three of the eleven spellings the item above named. **A fix needs an arity-tolerant tail rule**, not the strict one; see `PROJECT_LEARNINGS.md` #258.

- [ ] **23 residual heading losses in four families, none of them Session 188's** (found by Session 188's eight-lens blind adversarial sweep, 223 documents; **MEASURED**; **PRE-EXISTING** — the new-loss set against the pre-S188 build is EMPTY at set level; heading-DELETING; MEDIUM). (a) **Multi-line arguments, 8** — a macro whose braced argument closes on a LATER line (`\section{Argument that starts here` / `and finishes on the next line}`), or has a space before its brace; the raw block spans both source lines and a per-line scanner reads line two as prose. (b) **Container content column, 7** — a class-A macro at indent 4+, inside a block quote, an over-indented bullet, or a definition list. ⚠ **CORRECTED BY SESSION 189, which shipped the ` {0,3}` INDENT item and then RE-SCORED THIS CORPUS: the item drained NONE of these losses** (223 documents, 21 lost before and 21 lost after; 2 phantoms removed). The sentence this line used to carry — "this is exactly the `{0,3}` INDENT item below" — was the reason S188's receipt ranked that item #1, and it was wrong in a specific and useful way: the INDENT item is **phantom-removing on the class-B row**, while every loss in this family is **heading-deleting on the class-A row**, whose cap is too NARROW rather than too wide. Re-measured, 8 of the 21 are the new `RAW_TEX_BLOCK_MACRO` cap item below, and the rest are the multi-line-argument and block-quote-marker families. Two directions, two rows, one word ("indent") that hid the difference. (c) **Environments, 4** — a space before the brace, indent 6, and the setext consumer for a closed environment. (d) **Lookalikes, 3** — a triple backslash, a non-ASCII macro name, a lookalike followed by a real macro. ⚠ **FAMILY (a) EXTENDED BY SESSION 191 with a new spelling and a named mechanism: the OPTIONAL BRACKET argument, consumed across the newline.** `This paragraph is already open.` / `\maketitle` / `[Some Title]: https://example.com` / `===` renders `<h1>: https://example.com</h1>` — read off the rendered HTML directly, not inferred from an extractor. Pandoc's raw-LaTeX block takes the following line's leading `[...]` as `\maketitle`'s optional argument, so quarto's setext TITLE loses the bracket group while ours keeps it. It is a TEXT divergence, not a fabricated heading — both renderers emit an `<h1>` here. **This is the ONE of Session 191's ten new phantoms that the trigger-removal control does NOT explain**, and it is pinned in the unit suite with the `<div>` control that shows quarto's own text changing to ours once the macro is gone. A fix needs the scanner to model a macro's argument spanning two source lines.

- [ ] **REMOVED — SHIPPED Session 189.** *(The raw-TeX ` {0,3}` INDENT item lived here. See `CHANGELOG.md`, 2026-08-09. The row now tests the containing block's **content column**, carried by `computeRegions` as `contentColumns`. Five things are worth carrying forward. (1) The item's own two documents both REPRODUCED — the first filed item in this family in three sessions that did. (2) The marker → column rule is `markerIndent + markerLength + spacesAfter` with three corrections no reading of CommonMark gives: 5+ spaces collapse to 1, a tab expands to the next multiple of 4 COLUMNS, and a marker alone on its line gives `marker + 0` so `-` is column **1**. (3) A BLOCK QUOTE suspends the rule entirely and that is deliberate — the heading renders INSIDE the quote at every indent 0–8, and this model has no block-quote container. (4) The push side must be WIDER than pandoc: `iv.` is a roman-numeral list marker, a single-letter pattern misses it, and the corpus score caught that DELETING a heading. (5) Class A is indent-INSENSITIVE and was never part of this — it interrupts via `inlineCommand'` failing, which has no column rule.)*

- [ ] **REMOVED — SHIPPED Session 190.** *(The class-A ` {0,3}` cap item lived here. See `CHANGELOG.md`, 2026-08-09. `RAW_TEX_BLOCK_MACRO`'s leading-whitespace class is now `[ \t]*` with no cap; **556 real headings recovered over 2,772 scored documents, zero new losses at set level**. Five things are worth carrying forward. (1) **The item REPRODUCED exactly** — 11 deleted headings on its own documents against the current build, with every class-B, class-C, tail and arity control agreeing, so the defect was isolated to the cap before a line of code. (2) **Its magnitude claim was wrong and the direction of the error matters**: it said "8 of the 21 losses on S188's corpus"; re-scored after shipping, it drains **5** — identical whether counted by heading, by document, or by document-carrying-a-drained-loss. The mechanism and polarity were right; only the size was overstated, and a successor sizing work from it would have over-invested. (3) **The three new phantoms it produced are a MASK being lifted, not a defect being introduced** — see Learning #263 and the KNOWN RESIDUAL pins. (4) The `fresh` (no paragraph open) and SETEXT contexts are **byte-identical** across the change, measured in their own right rather than reasoned about — and measuring the setext one is what found the 216-heading `opensFreshBlock` gap now filed below. (5) `HTML_BLOCK_OPEN` had already made this exact repair at Session 185 for the same reason; the two rows are now consistent, and **the class-B row must stay inconsistent with both on purpose**.)*

- [ ] **REMOVED — SHIPPED Session 191.** *(The `opensFreshBlock` class-A pre-bail item lived here. See `CHANGELOG.md`, 2026-08-09. The function now tests `RAW_TEX_BLOCK_MACRO` beside `HTML_BLOCK_OPEN` ahead of its `paragraphOpen` bail; **722 real headings recovered over 2,555 documents rendered this session plus 2,780 re-scored from Session 190's corpora, with the new-loss set EMPTY at set level**. Five things are worth carrying forward. (1) **The filed magnitude was EXACTLY right** — 216 filed, 216 drained on S190's own container corpus, the first exact one in three sessions (S188's drained 0, S189's was overstated by 60%). (2) **The item under-described its own reach in the favourable direction**: the same hoist also fixes class A at indent 1–3 with NO paragraph open, which fell in the gap between the content-column row (indent 0) and `INDENTED_CODE_LINE` (indent 4+). (3) **A setext heading is a STRICTLY STRONGER test than an ATX one** — ATX needs only the paragraph closed, setext additionally needs the line below to START a fresh paragraph — which is why an eight-session-old divergence survived a 499-test integration suite. (4) The two sibling predicates `closesParagraph` and `opensFreshBlock` share a bail and their PRE-BAIL sets had silently diverged; diff them explicitly (Learning #267). (5) `RAW_TEX_ENV_OPEN` is also absent from `opensFreshBlock`, and Session 191 MEASURED it **INERT** — 12 of 12 documents agree, quarto renders no setext heading after a closed, unclosed or orphan environment in either context. Do not re-investigate it as a gap.)*

- [ ] **Three residual phantoms: an indented class-A macro inside a raw region this scanner does not track** (created knowingly by Session 190 as the disclosed side of a measured trade; **MEASURED** — the only new phantoms over a 240-document blind adversarial sweep from eight lenses; **the defect itself is PRE-EXISTING and that is proven, not asserted**; LOW). A multi-line **inline code span**, an **RCDATA `<textarea>`**, and a **CDATA section** each hide their content from quarto entirely, and a class-A macro indented inside one now closes a paragraph that pandoc never opened. ⚠ **The ` {0,3}` cap was masking these, not guarding them: the pre-S190 build already emits all three phantoms with the macro at COLUMN 0** — re-rendered, and pinned as a column-0 control beside each residual in `test/unit/qmd-model.test.ts`, so a future region-tracking fix is detectable rather than silent. Closing this needs region state (multi-line code spans, RCDATA elements, CDATA), which is a different capability from any indent rule and was held out under FM #26. The trade as measured: 3 phantoms against 556 recovered headings. ⚠ **EXTENDED BY SESSION 191 — the same blindness has a SETEXT spelling too, and the region list is longer than three.** A class-A macro inside one of these regions now also opens a fresh block, so a setext underline below it fabricates a heading. Two region kinds are NEW to this item and were produced by lenses that had never seen the earlier corpora: a **DOUBLE-backtick code span** and an **RCDATA `<title>`**. All five are pinned in the unit suite each with a `<div>` control that fabricates the identical heading on the pre-S191 build, so the defect is proven to be the untracked region rather than the macro row (Learning #269).

- [ ] **`FRONTMATTER_CLOSE`'s `...` terminator makes a document quarto REFUSES TO RENDER score as a divergence** (found by Session 190 when a blind adversarial document appeared as a fourth "new phantom"; **MEASURED** — `quarto render` exits **1** with a stack trace; **PRE-EXISTING**; scoring-instrument issue, LOW, but it costs real analysis time every session in this family). A document whose front matter closes with `...` instead of `---` does not render, so its truth row is empty and the scorer reads our headings as phantoms against a document that has no heading truth at all. **This is the already-filed `FRONTMATTER_CLOSE` item wearing a different hat** (see "`FRONTMATTER_CLOSE` accepts `...` as a front-matter terminator and quarto REFUSES TO RENDER such a document" below) — the new part is the *harness* consequence. **The cheap fix belongs in the scorer, not the model:** `render190.sh` already records the exit code in column 2, so any future score should EXCLUDE non-zero-exit rows and report their count separately rather than treating them as agreement data.

- [ ] **REMOVED — SHIPPED Session 193.** *(The `INDENTED_CODE_LINE` column-blindness item lived here. See `CHANGELOG.md`, 2026-08-09. The row is now `indentedCodeLine(line, columns)`, measuring the indent in COLUMNS against the containing block's content column + 4. **Four things are worth carrying forward, because two of them were wrong.** (1) **The item named ONE of the row's THREE consumers.** `CLOSES_PARAGRAPH` is the phantom-removing one it describes; `OPENS_FRESH_BLOCK` and the code-RUN exception in `computeRegions` both move headings the other way, and the run exception carried the largest single effect of the session — **80 recovered headings**, where a title at its container's own content column had been read as the second line of a code run. Nothing in the item predicts that. (2) **Its magnitude was overstated by 80%.** Re-scored against today's build, Session 189's seven cached corpora (4,124 documents) carry **61** residual phantoms and this change drains **20**, not 36; the **16** survivors in its own container corpus all share a shape it never mentions — an INDENTED ATX heading line — which is `ATX_HEADING`'s cap and is the item directly below. (3) Its prescription was otherwise EXACTLY right: the same `contentColumns` state, applied to one more row, and the threshold it predicted held in all ten containers measured. (4) **A TAB is indentation measured in COLUMNS and ABSOLUTE** — the old row hard-coded a lone tab as "deep enough" (wrong inside every container) while `    \t` matched nothing at all, losing a real heading at top level. Learnings #275–#278.)*

- [ ] **The `contentColumns` STACK's arithmetic is now read by THREE rows, and two independent defects in it are measured** (found by Sessions 192 and 193 while shipping the setext and indented-code consumers; **MEASURED**; phantom direction; MEDIUM, and the blast radius is the reason it ranks first). (a) **The POP measures a line's indent with `/^ */` — SPACES ONLY** — so a tab-indented line looks shallower than it is and pops a container column that is still open: three-deep bullets (columns 2, 4, 6) followed by `    \tzzz` pops column 6, leaves a base of 4 and a threshold of 8, which the line's real column of 8 then meets; quarto renders no heading and we emit one. **PROVEN BY CONTROL** — the identical column written as EIGHT SPACES agrees with quarto (`scratchpad/s193/ctl/t01` vs `t02`). These are Session 193's only two new errors. (b) **The RAGGED-stack pop**: a shallower sibling marker does not pop the deeper column, because the pop is suppressed while a paragraph is open (Session 192, measured). ⚠ **Both are SHARED machinery** — `rawTexMacroLineIsBlock` (S189), `setextUnderlineLevel` (S192) and `indentedCodeLine` (S193) all read this stack — so a fix moves THREE rows and each needs its own two-direction score. Pinned in `test/unit/qmd-model.test.ts` as FAMILY 1 of Session 193's disclosed residuals.

- [ ] **The BLOCK QUOTE is a container this model does not carry at all, and it is the largest measured residual family** (found by Session 193's 240-document blind adversarial sweep; **MEASURED**; **PRE-EXISTING**; BOTH directions; MEDIUM-LARGE — it needs new scanner state, not a rule change). While a quote may be open, `quoteOpen` passes `null` for the columns and every column rule falls back to a base of 0 — Session 189's deliberate fail-safe, which costs phantoms rather than deletions but does not remove either. Both directions are live and both are pinned as FAMILY 3: `> Foxtrot quote opener.` / `>` / `>     boxed sample text` / `> # Foxtrot Quoted Heading` renders the heading (4 past the quote's OWN content column really is code) and we emit nothing — 11 of the sweep's 24 losses have this shape; and `> Beacon quoted opener line` / `    lazy indented continuation` / `# Beacon Lazy Heading` renders NO heading (a lazy continuation keeps the paragraph open) and we emit one. A real fix models the quote as a container with its own content column, which is the same missing capability the fenced-div item below describes.

- [ ] **A QUARTO FENCED DIV / CALLOUT is a container `contentColumns` never pushes** (NAMED by Session 192's completeness critic as a lead it could not measure; **MEASURED by Session 193** and thereby promoted to a finding; **PRE-EXISTING**; phantom direction; LOW-MEDIUM, and the fix is derived to be small). `::: {.callout-note}` / `   ## Larkspur Callout Title` / `Body text follows the title.` / `:::` renders NO heading element — quarto consumes a callout's `##` line as the callout's own title — and we emit `h2:Larkspur Callout Title`. ⚠ Two questions, not one: pushing a content column for a `:::` opener (Session 189 measured a plain fenced div's content column as **0**, not 4, so the push is not the same as the footnote/definition row), and the separate callout-title rule. Pinned as FAMILY 4.

- [ ] **A BLANK LINE INSIDE AN INDENTED CODE BLOCK re-arms the following code line as a setext title** (found by Session 193's own completeness pass over the gaps its eight blind lenses left; **MEASURED**; **PRE-EXISTING** — identical on the pre-S193 build; phantom direction; LOW by volume, and the mechanism is worth more than the count). `Intro.` / (blank) / `    zzz` / (blank) / `    India Run Title` / `===` renders NO heading — a blank line does not end an indented code block for pandoc, so all of it is one code block — and we emit `h1:India Run Title`. The cause is that the blank resets `consecutiveBody` to 0 and the next code line increments it straight back to the eligible 1, so the code-RUN exception never gets to speak. **PROVEN BY CONTROL** in both directions: delete the blank and the run exception fires and agrees with quarto; and a LONE indented line really IS a setext title, so the fix is *not* "an indented line can never be a title". Pinned as FAMILY 6.

- [ ] **`rawTexMacroLineIsBlock` counts SPACES ONLY, so a TAB-indented raw-TeX macro at a container's content column is invisible** (a deliberate Session 189 choice, now the LAST place where a tab is judged differently from the indented-code row; **MEASURED by Session 193** over a 392-document sweep; **PRE-EXISTING**; heading-DELETING direction; LOW by volume). `-   line one` / `    line two` / (blank) / `\t\clearpage` / `    Some Title` / `    ===` renders `h1:Some Title` — the tab reaches column 4, which is the item's content column, so the macro is a raw block there — and we emit nothing; 6 losses. **PROVEN BY CONTROL**: the same macro written as four spaces is found. Session 189's docstring says the tab is "left to `INDENTED_CODE_LINE`, exactly as ` {0,3}` did"; since Session 193 that row expands tabs to columns properly, so the two rows can now be brought into line rather than left to disagree. Pinned as FAMILY 2.

- [ ] **`ATX_HEADING`'s own ` {0,3}` deletes a real heading at column 4 or deeper** (found by Session 189 when three of its own first-draft assertions failed for this reason rather than the one under test; **MEASURED**; **PRE-EXISTING**; heading-DELETING; MEDIUM). Pandoc's `rawTeXBlock` ends with `spaces`, which swallows the NEXT line's leading whitespace — so after a raw-TeX block the heading's indent is irrelevant to pandoc and `100. line one` / (blank) / `     \clearpage` / `     # ATX Below` renders `<h1>` at source column 5. `ATX_HEADING` is `/^ {0,3}(#{1,6})[ \t]+(.+)$/` and never sees it. The same `contentColumns` state answers this one too (a heading at the container's column is at ITS column 0), plus the whitespace-swallow rule, which is separate and needs its own measurement. ⚠ It is why Session 189's unit pins hold the heading at column 0 while the MACRO sweeps: mixing the two axes measures neither.

- [ ] **`A. x` — a single capital and EXACTLY one space — is not a list, and we open a container for it anyway** (created knowingly by Session 189 as the FP-safe side of a measured trade; **MEASURED** — the sole residual phantom over the 2,394-document marker corpus; LOW). Pandoc's initial-in-a-name rule ("B. Russell") means `A. line one` opens no list, so quarto renders no heading at column 3 and we do. Refusing the column would cost a real heading if the rule is ever narrower than measured, and `listItemContentColumn` is required to fail in the phantom direction. `A.  line one` with TWO spaces really is a list at column 4 — the two documents differ by one space and quarto answers them oppositely. Closing this needs the arity of pandoc's own `fancyList` guard, not a wider regex.

- [ ] **REMOVED — Session 183's `paragraphOpen` gate deletion item is now FULLY DRAINED.** *(Families (b) and (c) shipped Session 185; **family (a) shipped Session 187** — see `CHANGELOG.md`, 2026-08-08. A block-level tag outside `HTML_BLOCK_OPEN` against an open paragraph (`prose one` / `prose two` / `<meta charset="utf-8">` / `# ATX Below`) now interrupts it, along with 46 other pandoc block names the CommonMark list lacked. ⚠ **One thing from the original item remains OPEN and is NOT this item:** the lone `+` inside a list item (2 headings), whose candidate gate suspension is still measured at 2 recovered against 32 new phantoms — an operator decision, not shipped.)*

- [ ] **REMOVED — SHIPPED Session 187.** *(The `HTML_BLOCK_OPEN`-is-CommonMark's item lived here. See `CHANGELOG.md`, 2026-08-08. `basefont` and the other nine CommonMark-only names are gone; 47 pandoc names it lacked are in. **Its `<div`-with-no-`>` half is REFUTED and the `$` branch was deliberately KEPT:** pandoc's `htmlTag` consumes `manyTill anyChar endAngle`, which SPANS NEWLINES, so `<div` / `class="x">` / `# ATX Below` really does open a block and render the heading — removing the branch was measured DELETING it. The phantom survives only when the document contains no `>` at all afterwards, which a per-line regex cannot see.)*

- [ ] **A tag line with TRAILING CONTENT is prose, and we treat it as a block opener** (found by Session 187's ten-lens adversarial sweep; **MEASURED** over 219 rendered documents; **PRE-EXISTING** — identical on the pre-S187 build; **phantom direction**; MEDIUM). Measured: a tag line opens a block only if, after the tag name, the line ENDS AT a `>` (trailing whitespace allowed) or contains no `>` at all. `<div>`, `<div>x</div>` and `<title>Doc</title id="y">` are block; `<div> trailing text` and `</note> and that is deliberate.` are ordinary paragraph text and we fabricate a heading below both. Session 187 fixed the instances its own change INTRODUCED (the tail rule is now in both regexes) — so what remains here is narrower than it looks: the residual cases are the ones where a per-line regex still cannot decide, chiefly `<div` with no `>` anywhere later (pandoc's tag parser spans newlines, so the `>` may be lines below — **removing the `$` branch was measured DELETING a real heading**) and `<div 1bad="x">` (pandoc guards `all (isName . fst) attr`, so an invalid attribute NAME makes it not a tag at all). Both need state a per-line predicate does not have. **16 residual phantoms across the 2,223-document corpus, in four classes, each pinned in a test.**

- [ ] **An `eitherBlockOrInline` tag at indent 1–3 is NOT block, and at indent 0 it is** (found by Session 187's sweep; **MEASURED**; **PRE-EXISTING** in the ATX context, FIXED by S187 in the setext context; LOW). `Intro.` / (blank) / (indent)`<button>` / `# ATX Below` renders the heading at indent 0, 4 and tab but NOT at 1 or 3, while `<div>` — `blockTags` — renders it at every indent. The 4-space and tab rows are a **confound**: they are block via the indented-code rule on another row, not via the tag, and scoring them as agreement is what hid the axis. Session 187 anchored the class-2 alternation at column zero, which fixes the `opensFreshBlock` consumer; the `closesParagraph` ATX path still carries the phantom at indents 1–3 and it is pre-existing.

- [ ] **111 heading losses and 81 phantoms the adversarial sweep found in areas OUTSIDE the tag rule** (found by Session 187; **MEASURED** — 219 documents authored by ten agents that had never seen this session's corpora, every one rendered and adjudicated firsthand against two builds; **PRE-EXISTING** — identical counts on the pre-S187 build; **mixed direction**; MEDIUM-HIGH by volume). This is a characterization, not one defect, and it is the honest measure of how far the model still is from quarto on adversarially-chosen input. Three dominant families, each reproduced firsthand: **(a)** a tag inside a LIST ITEM — `- item prose here` / `- <div>` / `# ATX Below` renders the heading and we lose it, because every predicate here anchors at the line's start and the `- ` marker hides the tag (this is the same missing capability — the containing block's CONTENT COLUMN — that the raw-TeX indent item names); **(b)** a tag after a PIPE TABLE followed by a line block; **(c)** a heading inside an UNTERMINATED cell fence, where quarto renders the heading and we treat the line as cell content. Per-lens counts (lost/phantom): L01 14/1, L02 16/6, L03 10/10, L04 15/6, L05 9/5, L06 6/14, L07 5/14, L08 13/1, L09 6/18, L10 17/6. ⚠ **Do not read this as a regression** — the shipped build is strictly better than its predecessor on this corpus (phantoms 105 → 81, losses unchanged at 111, zero deletions introduced).

- [ ] **A whole-line HTML comment does NOT close a pandoc paragraph, and `COMMENT_FULL_LINE` treats it as if it does** (found by Session 185's adversarial sweep; **MEASURED**; **PRE-EXISTING** — identical on the pre-S183 and pre-S185 builds; **phantom direction**; MEDIUM). `prose one` / `<!-- a -->` / `# ATX Below` renders no heading — the comment is inline raw HTML *inside* the paragraph — and we emit one, because the `COMMENT_FULL_LINE` branch sets `paragraphOpen = false` before it `continue`s. The defect is independent of everything in the `CLOSES_PARAGRAPH` family and needs no tag table; it is a question about which region boundaries really end a paragraph. ⚠ Session 185 deliberately did not touch `scanRegions`, and note that the sibling item below (`COMMENT_CLOSE` is position-blind) lives in the same branch — **consider them together**.

- [ ] **An UNCLOSED CommonMark condition-1 tag swallows the rest of the document, and we emit headings from inside it** (found by Session 185's adversarial sweep; **MEASURED**; **PRE-EXISTING** in all three builds; **phantom direction**; LOW-MEDIUM). `prose one` / `prose two` / `<textarea>` / `# ATX Below` renders NO heading at all — the unclosed `<textarea>` consumes the remainder as raw text — and we emit one, at column 0 and at every indent. ⚠ Measured NOT to be uniform across the four condition-1 tags: an unclosed `<style>` renders the heading perfectly well, so this is not "condition 1 opens a skip region" but a per-tag question. A real fix belongs to the SKIP-REGION model (`computeRegions`), not to `CLOSES_PARAGRAPH`. Pinned as a disclosed residual in `test/fixtures/closes-paragraph-indent.qmd` and its `outline.test.ts` assertion.

- [ ] **A SETEXT underline below a line-block continuation is invisible to this model, and the obvious fix is REFUTED** (found by Session 185; **MEASURED**; **PRE-EXISTING** — the pre-S183 build loses it too; lost-TP direction; LOW). `| line one` / `  continued` / `Setext Title` / `===` renders `<h1>Setext Title</h1>` and we render nothing, because the continuation does not set `pendingFreshBlock`, so `consecutiveBody` is 2 rather than 1 at the underline. ⚠ **Adding the continuation to `OPENS_FRESH_BLOCK` is measured WRONG**: `| line one` / `  continued` / `===` / `# ATX Below` then fabricates a setext heading named `continued`, which quarto does not render (Learning #227 — the two lists have opposite polarity and a pattern wrongly present in `OPENS_FRESH_BLOCK` INVENTS a heading). A real fix needs the counter to distinguish "a block whose own line an underline may claim" from "a block whose line it may not". ⚠ **SIZED BY SESSION 191: this is the ENTIRE residual-loss set of a 2,159-document container sweep — all 12 remaining losses are the line-block container, classes B and C, at indents 1–3, and none of them is new.** Every other container in that sweep reaches zero losses. It is now the only characterized loss family left on the setext surface.

- [ ] **17 PRE-EXISTING deletions surfaced by the same sweep, none of them Session 183's or 184's** (found by Session 184; **MEASURED**, identical on the pre-S183 build; **heading-DELETING**; MEDIUM). Three families. (a) **Link-reference spellings the row's `[^\]]*` cannot span** — `[a\]b]:`, `[a[b]c]:`, a label broken across two lines, and a definition whose URL or title sits on the NEXT line. (b) **A link reference inside a block quote or list item** — `> [a]: http://e.com`, `- [a]: http://e.com`. (c) **An HTML block inside a block quote** — `> quoted one` / `> <div>` (also `<pre>`, `<style>`, `<script>`, `<table>`, `<ul>`, and the tight `>` spelling); the marker is stripped by quarto and not by us.

- [ ] **The comment region's `COMMENT_CLOSE` test is POSITION-BLIND, losing a heading after a comment with an unterminated sibling** (found by Session 184's own control assertion while narrowing the comment row; **MEASURED**; **PRE-EXISTING** — identical on the pre-S183 and pre-S184 builds; lost-TP direction; LOW). `<!-- a --><!-- b` / `still -->` / `# ATX Below` renders `<h1>ATX Below</h1>`; we render nothing. `scanRegions` asks `COMMENT_CLOSE.test(line)`, i.e. "does the line contain `-->` ANYWHERE", so the FIRST comment's terminator is read as closing the SECOND one, no skip region opens, and `still -->` is scanned as prose that re-opens a paragraph across the heading. A fix tracks the delimiter's POSITION rather than its presence. Session 184 deliberately did not touch `scanRegions`; the divergence is pinned as a KNOWN RESIDUAL in `test/unit/qmd-model.test.ts`.

- [ ] **The CLOSER-LINE exemption retains phantoms on purpose, and could be closed by tracking div nesting** (created knowingly by Session 183 as the FP-safe side of a measured trade; **MEASURED**; LOW-MEDIUM). `:::` and `...` are exempt from the `paragraphOpen` gate because a real construct's closer follows its own body text and a per-line scanner cannot tell that from prose. Cost: `prose` / `:::` / `# ATX Below` still reports a phantom heading (quarto renders one paragraph). Gating them instead was measured to DELETE four real headings (a closed `::: {.note}` div, a closed callout, and a `---`/`...` YAML block each render the heading below their closer), so the exemption is correct today. A real fix tracks fenced-div depth in `computeRegions` — the scanner already tracks fence depth for code fences, so the machinery is adjacent. Pinned in `test/integration/suite/outline.test.ts` and `test/unit/qmd-model.test.ts` as a disclosed residual.

- [ ] **A SETEXT heading formed inside a BLOCK QUOTE is invisible to this model** (found by Session 183's adversarial pass; **MEASURED**; **PRE-EXISTING**; lost-TP direction; LOW). `> quoted one` / `    ---` / `# ATX Below` renders `<blockquote><h2 id="quoted-one">quoted one</h2><h1 id="atx-below">ATX Below</h1></blockquote>` — inside a quote's lazy continuation a 4-space-indented `---` IS a setext underline (it needs exactly one body line above, the same requirement as our own setext rule), where the identical bytes at top level render one absorbed paragraph. We emit `ATX Below` (Session 183 preserves that by suspending the gate in quotes) but never `quoted one`. Closing it means giving `computeRegions` block-quote context, which it has never had — that is the same architectural gap the container-blind corpora exposed, and it is why the gate is suspended rather than refined there.

- [ ] **REMOVED — SHIPPED Session 192.** *(The `SETEXT_H1`/`SETEXT_H2` ` {0,3}` item lived here. See `CHANGELOG.md`, 2026-08-09. The underline's indent is now an EQUALITY against `[0, ...contentColumns]` via `setextUnderlineLevel`. **Four things are worth carrying forward.** (1) **THE ITEM'S MEASUREMENT WAS RIGHT AND ITS PRESCRIBED FIX WAS WRONG, in the heading-DELETING direction** — it said "anchored at column 0" and named `SETEXT_UNDERLINE_RUN` as the model, but every document behind that claim had NO container; inside one, pandoc accepts the underline at the container's content column, and three-deep nested bullets accept it at 0, 2, 4 AND 6 (Learning #271). (2) The cap was simultaneously too WIDE and too NARROW, so both one-directional edits were wrong and only the two-direction SET difference could see it (Learning #272). (3) **The item's own TABLE note did not reproduce** — `Some Title` / `   ---` renders as plain paragraphs, `p>p>p`, in every shape measured; `---` and `===` behaved identically at every one of the 511 scored cells. (4) A TAB is NOT the content column, which is why the indent capture is ` *` while the trailing class stays `[ \t]*`.)*

- [ ] **`listItemContentColumn` accepts `[a-zA-Z]{1,9}` before a `.`/`)`, so ordinary PROSE opens a container column — and the filed `A. x` item's description is NARROWER than its own code** (found by Session 192's blind adversarial sweep, two of them only by the completeness critic; **MEASURED** over 270 documents; **PRE-EXISTING**, proven by the pre-build's own output on the same document; phantom direction; MEDIUM). `Mr. Halloran signed the charter in March.` opens content column 4 and `xyz. A stray label found in the margin notes.` opens column 5, and each then legitimises an indented setext underline in a LATER paragraph — quarto renders no heading in either. Four of Session 192's eleven new phantoms are this family, the largest of them. ⚠ The existing `A. x` item below describes this as "a single capital and EXACTLY one space"; the regex accepts up to NINE alphanumerics, so the filed description understates the reach. ⚠ Session 189 created part of this knowingly as the FP-safe side of a measured trade — read that trade before narrowing. **Pinned with its controls in `test/unit/qmd-model.test.ts`.**

- [ ] **The `contentColumns` RAGGED-STACK pop: a shallower sibling marker does not close the deeper column, because `paragraphOpen` suppresses the pop** (found by Session 192; **MEASURED**; **GENUINELY NEW in its setext spelling**, though the arithmetic is untouched by that session; phantom direction; MEDIUM). `  - deep first item` / `- shallow next item` leaves column 4 alive where pandoc keeps only column 2, so an underline at column 4 further down is honoured. The tab sibling is the same defect: `  [^1]:\tnote body` and `  ~\tdefinition text` compute a content column pandoc does not agree with. ⚠ **This is SHARED machinery — the raw-TeX row has consumed the same `[0, ...contentColumns]` since Session 189 — so a fix moves TWO rows and needs both scored.** Whether the raw-TeX row ALREADY diverges on these same shapes is **UNMEASURED**, and is the one genuinely open question Session 192 leaves. **Pinned with its controls in `test/unit/qmd-model.test.ts`.**

- [ ] **A container OPENER line used as a setext title keeps its MARKER in the heading text** (found by Session 192; **MEASURED**; **PRE-EXISTING**, proven at column 0 where quarto strips the marker and both builds do not; text-divergence direction; LOW). `[^2]: Footnote Setext Candidate` / `    ---` renders `<h2>Footnote Setext Candidate</h2>` in quarto and `[^2]: Footnote Setext Candidate` here; likewise `:   `, `~   ` and `(a) `. `BULLET_LIST_MARKER` already declines the `-*+` spelling outright; these four marker classes are not covered. Note the direction: the pre-build LOST these headings entirely, so emitting them with marker-bearing text is strictly an improvement over the previous state. **Pinned with its controls in `test/unit/qmd-model.test.ts`.**

- [ ] **Quarto FENCED DIVS and callouts are a container `contentColumns` never pushes** (named by Session 192's completeness critic; **NOT INDEPENDENTLY MEASURED — a lead, not a finding**; loss direction if real; UNKNOWN severity). `:::` / `::::` blocks, including one indented inside a list item, are a container pandoc has and this scanner does not track, so a title line inside one lands at `consecutiveBody` 2+ and its setext heading is dropped. The critic also named two further untested mechanisms: a line CONSUMED as a setext underline or a thematic break itself pushes a content column (the `contentColumns` block runs above the setext branch), and `$$` display math or a raw LaTeX environment sitting between title and underline inflates `consecutiveBody`. ⚠ None produced a scored divergence in the 270-document sweep; all three need their own corpus before being treated as defects.

- [ ] **DOCUMENTATION DEFECT: `SETEXT_H1`'s docstring claims we match quarto by declining the ATX-swallow, and the claim is refuted** (found by Session 182; **MEASURED**; **PRE-EXISTING**; LOW, but it is load-bearing prose that a future session will reason from). The docstring says our behaviour matches "Quarto's `.qmd` reader's actual single-line-only rule, not the surprising ATX-swallowing behavior a plain `---`-adjacency edge case can otherwise produce in Pandoc". Measured on the real render path, quarto **does** swallow: `# Heading Above` / `===` renders `<h1># Heading Above</h1>` — literal `#` and all — and `# Heading Above` / `-` renders `<h2># Heading Above</h2>`. Our declining it is a deliberate, defensible choice (an outline row reading `# Heading Above` would be noise), but it is a DIVERGENCE, not a match, and it costs 9 residual rows where our heading text differs from quarto's. Session 182 depends on the swallow being real — `prevWasAtxHeading` exists precisely to recover the block closure it causes — so the docstring now contradicts working code twenty lines away.

- [ ] **RECORD GAP — Session 180 reported three items as "filed, not fixed" and none of them reached this file** (found at Session 181's Phase 0 reconcile; the ledger and handoff frontiers were a true zero, so this is not a ledger defect — the commits are all recorded — but "filed" overstated what happened). The three were: the setext false negative (which Session 181 then shipped, above), the five KNOWN RESIDUAL phantoms of the `blank_before_header` rule, and the project-level `_quarto.yml` `from:` gap (REASONED, NOT MEASURED). The authoritative list is the **S180 receipt's `next_steps` in `HANDOFFS.md`** — read it there rather than assuming this file is complete for that session. **Standing instruction this makes concrete: an item is filed when it is a `- [ ]` line in `BACKLOG.md`, not when it is described in a handoff receipt or a test comment.**

- [ ] **One measured corpus row that `breakQuartoMd` alone does not explain** (found by Session 179 while building its own controls; **MEASURED firsthand on BOTH engines; MECHANISM UNKNOWN — recorded as unknown rather than explained away**). An unterminated 4-tick `{r}` cell with a well-formed 3-tick `{r}` cell inside it renders **exit 1**, reporting the INNER cell's bad option. Read literally, `breakQuartoMd` predicts exit 0: the outer opener sets `inCode = 4`, the inner ` ```{r} ` line fails `inPlainText()` and is buffered, the inner ` ``` ` fails `[1].length === inCode`, and the final `flushLineBuffer("markdown", …)` emits everything with no code cell at all. Confirmed not engine-specific (`engine: markdown` and `engine: knitr` both exit 1). Our own answer AGREES with the measurement (we flag the inner cell, because the outer fence declines and the inner one opens), so nothing is broken — but every docstring in this family explains cell partitioning in terms of `breakQuartoMd`, and this row shows that explanation is incomplete. Settling it means finding which pass actually validates here (`validateDocumentFromSource`'s caller, or a second partition of the engine-processed markdown) and would sharpen `qmd/model.ts`, `quarto-yaml-regions.ts` and the corpus comments at once. Pinned as `S179 CONTROL declined 4-tick fence above a well-formed 3-tick cell + echo`, which carries the warning inline. LOW as a defect, MEDIUM as an explanation gap.
- [ ] **`{{< include >}}` / indented-`---` neighbours: quarto's own two partitioners disagree, and only one of them was ever mirrored** (filed Session 171 as a standing caution, not a defect). `partitionYamlFrontMatter` (ENGINE selection) runs `lines(markdown.trimLeft())`; `validateDocumentFromSource` (front-matter VALUE validation) gates on `breakQuartoMd`'s first cell literally `startsWith("---")`. **Measured: 10 of 17 keys whose bad value quarto rejects at line 0 render exit 0 behind a single leading blank line** (`number-sections`, `code-fold`, `fig-width`, `fig-align`, `keep-md`, `freeze`, `cache`, `link-citations`, `execute`, `bibliography`). Any future work tempted to "teach `scanRegions` quarto's `trimLeft`" would therefore trade one cardinal FP for ten. The guard is `test/unit/yaml-value-flags.test.ts`'s **FP GUARD** pin, which reds first. Recorded here because the previous item's own prescription was wrong in exactly this way for three sessions. **Still live and still the trap — but note S177 CLOSED the indented-`---` half from the OTHER side** (`CHANGELOG: a column-0 `---` swallows the cells below it, Session 177`): it mirrored `breakQuartoMd`'s column-0 delimiter rule on the VALIDATION surface instead of teaching `scanRegions` the `trimLeft`, so the ten front-matter keys above were never put at risk. The FP GUARD pin stayed green throughout, which is the evidence that the two surfaces stayed separate.
- [ ] **`FRONTMATTER_CLOSE` accepts `...` as a front-matter terminator and quarto REFUSES TO RENDER such a document** (found by Session 177 while mirroring `breakQuartoMd`'s delimiter rule; **verified firsthand**; **PRE-EXISTING**; LOW). YAML's own document-end marker is legal YAML but not legal quarto: `---`/`title: t`/`...` renders `ERROR: Expected YAML front matter to end with '---'` from `validateDocumentFromSource` and produces no output at all. Our `FRONTMATTER_CLOSE` is `/^(?:---|\.\.\.)[ \t]*$/`, so `scanRegions` closes the block there and every front-matter consumer (`findFrontMatter`, `inFrontMatter`, `frontMatterContentLines`, the front-matter value enumerators) models a document quarto will not build. NOT a cardinal false positive in the usual sense — quarto does not ACCEPT the document either — which is why this is LOW rather than urgent, and it is why the oracle scores such a row `unrelated` (nonzero exit, no validation text) and learns nothing from it. S177's new region scanner already matches quarto here (it closes on `---` only), so the two now disagree with each other inside our own codebase — that disagreement is the reason to file it rather than leave it implicit.
- [ ] **A `---` on the document's LAST line: our line split and quarto's disagree about whether the HR exemption applies** (found by Session 177 while pinning its own scanner's boundaries; **REASONED, NOT MEASURED — treat as unverified**; expected INERT). Quarto's `isYamlDelimiter` declines a `---` only when `index > 0 && index < srcLines.length - 1` with both neighbours blank. Our `quartoYamlRegions` splits on `/\r?\n/`, so a document ending in a newline yields a trailing EMPTY line that quarto's `rangedLines` does not produce — which can make our `index < length - 1` guard true where quarto's is false, i.e. we read a trailing `para`/blank/`---`/EOF as a horizontal rule where quarto opens a region. Argued inert because a region opening on the last line swallows no cells, so no flag can differ; NOT confirmed by measurement, and the argument is exactly the kind that has been wrong twice in this project. Cheap to settle: one `quarto render` of that shape plus a `quartoYamlRegions` call on the same bytes.
- [ ] **The FLOW spellings of the `engine:` override are honoured by quarto and invisible to us** (Session 164, verified firsthand; **PRE-EXISTING**, cardinal-sin direction). `execute: {engine: markdown}` on one line, and `execute:` + an indented `{engine: markdown}`, both render **exit 0** on `{r}` + `#| cache: banana`; neither value enumerator emits a flow-mapping MEMBER, so `documentEngineForScoping` sees nothing and we keep flagging. S164 makes such a document `"ambiguous"` when another selector IS readable (so it cannot produce a WRONG answer), but on its own it is still a missed narrowing. Same class: an engine-named key whose body is a COLUMN-0 sequence (`markdown:` then `- x`) renders exit 0 and is not counted as children, because `opensBlockAt` requires indentation > 0. **S165 UPDATE:** measured again at S165 — `execute: {engine: markdown}` ALONE renders exit 0, and letting the new language fallback answer there would have squiggled every cell of an r-first document. `documentEngineForScoping` now treats ANY scalar token on `execute:` as unreadable and declines, so the `{r}`-cell false positive is unchanged and no longer widens. The COLUMN-0 sequence half of this item is likewise now a decline (`readTruthiness` returns `"unreadable"` for an empty token with no children) rather than a silent non-selection.
- [ ] **Node-property spellings of the override, and the truthy values L6/L7 deliberately gave up** (Session 164, all verified firsthand; **MIS-LABELLED until Session 165 — see below**). We compare a RAW token where quarto resolves the PARSED node, so each of these declines and falls back to the language approximation: `engine: &a markdown` / `!!str markdown` / `*alias`; a TRUTHY plain-scalar body (`knitr:` / `  true` renders **exit 1** — knitr really is selected); a truthy node-property value (`knitr: !!bool true`, exit 1); and a NON-EMPTY block scalar (`jupyter: |` + a body). Declining is what killed three introduced cardinal FPs, so any fix must resolve the node properly — and inherits S163's `!!str false` / `!!bool "false"` trap plus a document-level anchor table. **S165 UPDATE — the old `FP-SAFE direction` label was wrong, and the cost is larger than filed.** Declining is FP-safe only in the sense that it adds no NEW flag; on any document holding an `{r}` cell the caller then reaches knitr through the per-cell approximation anyway, so each of these spellings leaves a live cardinal-sin FALSE POSITIVE on that cell — the very one the override exists to remove (the S165 §9 completeness critic caught the mislabelling, and the Phase 0 ranking would have under-ranked these items because of it). Because a decline no longer means "use the cell language" but "do not answer at all", S165 had to classify every unreadable spelling explicitly (`readEngineScalar`, `readTruthiness`), and the §9 review found that shipping them as UNMATCHED produced twelve cardinal false positives (all fixed, all pinned). The set that now declines, each measured firsthand at S165: `engine: &a markdown` / `*alias` / `!!str markdown` / `>-`+folded body / an empty token with an indented continuation; the nested `execute:`/`  engine: &a …`; `execute: *a` and `execute: &a`; an engine-named key with `!!bool true`, `&a true`, a `|` body, or a COLUMN-0 sequence body; and an engine-named key that is bare (indistinguishable from that column-0 body). Two of these are measured LOST TRUE POSITIVES rather than merely missed narrowings — `engine: &a knitr` and `engine: |` + a literal `markdown` body both render **exit 1** (quarto really does end up on knitr) and we are now silent. Separating them from their exit-0 twins needs real YAML node resolution including chomping semantics (`>-` strips the newline and matches, `|` keeps it and does not), which is exactly the trap S163 filed.
- [ ] **A project `engines:` list reorders quarto's selector loop and we cannot see it** (Session 164, verified firsthand; handled conservatively, not fixed). `_quarto.yml` `engines: [jupyter, knitr]` flips a document carrying BOTH `engine: knitr` and `jupyter: python3` from exit 0 to exit 1. S164 declines (`"ambiguous"` → agnostic scope) whenever the front matter selects more than one engine, so it is FP-safe; the cost is the knitr-only fields on such documents. **The §9 review established the bail is over-broad for one pair**: for `engine:` vs `execute.engine:` the winner is decided entirely by document order (both write the same `format.execute.engine` slot) and the project list provably does NOT change it — measured with and without the reorder. Document order IS visible to us, so that pair could be resolved exactly; any order-aware fix must also let an unrecognized LATER `engine:` clear the selection, or it reintroduces the FP L7 closed.
- [ ] **LATENT: a multi-engine `tags.engine` list is modelled as engine-AGNOSTIC** (Session 164, swept firsthand; inert today, cardinal-sin direction if it wakes). `engineTag` (`yaml-schema.ts`) maps `["knitr","jupyter"]` to `undefined`, so such a field lands in the `"unknown"`/agnostic set, while quarto's `makeEngineSchema` tests `engineTag.indexOf(engine) !== -1` and EXCLUDES it from the markdown and julia schemas. Verified by running quarto's own filter against ours over all 43 flaggable fields for all four engines: they agree EXACTLY, because the single multi-engine field in 1.7.33 (`tbl-colwidths`) is open-valued and so unflaggable. It becomes a live false positive the day quarto gives a multi-engine tag to a closed-valued field.

- [ ] **`CONTEXT.md` records the language-support decision as "awaiting operator ratification" — 175 sessions later** (found by Session 176's cross-reference sweep while shipping the `CLAUDE.md` Tech Stack rewrite; **verified firsthand**; the same staleness class S175/S176 just cleared from `CLAUDE.md`, in a different file). `CONTEXT.md:43` reads *"**Status:** RESOLVED (Session 1, awaiting operator ratification) → ship **Tier A** (TextMate grammar) + build to **Tier B** (in-process `register*Provider`s); **defer Tier C** (out-of-process LSP)."* The *decision content* is accurate and is exactly what `CLAUDE.md`'s Tech Stack section now states independently — Tier C was deferred and never built, there is no `vscode-languageclient` in `package.json` or any source file, and in-process providers plus embedded virtual documents are what shipped. Only the **"awaiting operator ratification"** clause is stale: ~175 sessions of shipped work under this architecture *is* the ratification, and a governing document that still frames a settled decision as provisional invites a future session to reopen it. Fix: mark it RATIFIED (or simply RESOLVED) with a pointer to the shipped evidence, and leave the Tier A/B/C content alone. Check the surrounding `CONTEXT.md` decisions for the same provisional framing while there — this one was found by sweeping for a *different* string, so it is unlikely to be the only one. LOW.

- [ ] **The two root-level vitest config files are type-checked by no tsconfig project** (found by Session 175 while measuring the type-check surface for the `CLAUDE.md` rewrite; **verified firsthand by `tsc --listFiles`, not read off the `include` arrays**). S173's gate closed the `test/unit` hole, and between them the three projects now cover `src` (`tsconfig.json`), `test/unit` (`tsconfig.unit.json`) and `test/integration`/`test/lsp`/`test/oracle` (`tsconfig.test.json`) — but `vitest.config.ts` and `vitest.oracle.config.ts` sit at the repo root and are in **none** of the three `include` lists, so nothing type-checks them. They are small and declarative, and vitest loads them through its own esbuild transform (so a type error there would surface as a runtime failure of the whole run rather than silently, unlike the S162 case that motivated the unit gate) — which is why this is LOW rather than a repeat of that defect. Worth closing for completeness of the "everything is type-checked" claim, most cheaply by adding a fourth `include` entry to `tsconfig.unit.json` (it already sets `rootDir: "."`) and confirming its `module: es2022` options suit a Vite config. Note the same measurement confirms `esbuild.js` and `check-package.js` are plain JS and so out of scope by construction. LOW.

- [ ] **A KNITR document validates a handful of chunk options in a cell-HANDLER cell, and S162's `"none"` scope deliberately gives one of them up** (found by the Session 162 §9 review, **verified firsthand by S162** with an exhaustive per-key sweep; **CAUSED BY S162**, knowingly, as the FP-safe side of a trade it could not avoid). Quarto exempts a `{dot}`/`{mermaid}` cell from its cell SCHEMA under every engine, but a knitr document additionally runs knitr's own, non-schema chunk machinery over it. Swept all **47** keys `cellOptions("unknown")` can flag in a `{dot}` cell, one `quarto render --no-execute` each, in both engines: in a **markdown-engine** document **46 of 47 render exit 0** — the one exception, `layout: banana` (exit 1 from pandoc's `_json.lua`, in BOTH engines), is an OPEN-valued field `isWrongValue` never fired on, so every key we could actually FLAG renders exit 0 there and the false-positive fix is exactly right, while in a **knitr** document exactly one — **`include`** — is a real, value-dependent failure: `//| include: banana` renders **exit 1** from knitr's own `if (options$include)` and `//| include: false` renders exit 0. Measured pre/post by bundling both trees, `include` goes `["L5:dot:include=banana"]` → `[]`. `cellOptionScopeFor` receives only the cell LANGUAGE and cannot know the document engine (the same limit S161 L2 documented), so the choice was one CONDITIONAL false negative against 46 UNCONDITIONAL false positives, and an over-flag is the cardinal sin. **Do this together with the front-matter `engine:` override item above** — that is the capability that would let a handler cell in a knitr document be scoped differently from one in a markdown document. Also measured and worth carrying: `layout: banana` in a `{dot}` cell renders **exit 1 in BOTH engines**, but `layout` is an open-valued field we never flagged either way — a **PRE-EXISTING** lost true positive, unchanged by S162. LOW (safe FN).
- [ ] **The `validate-yaml` escape hatch reaches a document from THREE places we still do not read, and per-format resolution can re-arm one we now silence** (found by Session 163 while implementing the `.qmd` half, **all four shapes verified firsthand by S163**; the first three are **PRE-EXISTING**, the fourth is **CAUSED BY S163** in the FP-safe direction). S163 taught `features/yaml-value-diagnostics.ts` to honour a TOP-LEVEL front-matter `validate-yaml: false` and a per-cell `#| validate-yaml: false`, which is what the filed item asked for. But quarto's render gate reads **RESOLVED metadata** — `const validate = context.format.render?.["validate-yaml"]; if (validate !== false) { validateDocument() }` — not the raw front-matter key, so the flag also arrives from places a single-document pass never looks at. Measured firsthand vs 1.7.33, each against a control differing only in the flag: (a) **`format:` sub-key placement** — `format:` / `  html:` / `    validate-yaml: false` renders **exit 0** and we still flag (a live cardinal-sin **FP**); (b) **`_quarto.yml`** — a project-level `validate-yaml: false` renders **exit 0** for every `.qmd` in the project AND for `_quarto.yml`'s own validation, and we flag in both (**FP**, and it hits THREE features: the `.qmd` one, the `_quarto.yml` VALUE one `features/yaml-project-value-diagnostics.ts`, and the `_quarto.yml` UNKNOWN-KEY one `features/yaml-diagnostics.ts` — measured firsthand by S163 at close-out: `_quarto.yml` with `project:`/`  bogus-key: 1` renders **exit 1**, and prepending a top-level `validate-yaml: false` renders **exit 0**, while `computeProjectKeyDiagnostics` flags it either way); (c) **`_metadata.yml`** — the same key in a directory's `_metadata.yml` renders **exit 0** for the documents beneath it (**FP**; note this compounds the already-filed `_metadata.yml` coverage gap below); (d) **⚠ per-format precedence OVERRIDES the top level** — `validate-yaml: false` at the root PLUS `format:` / `  html:` / `    validate-yaml: true` renders **exit 1**, i.e. quarto validates, while S163's document gate reports the document disarmed and we stay **silent**: a **lost true positive S163 introduced**, in the safe direction (silence, never a wrong squiggle), recorded at `isValidationDisabledByFrontMatter`'s docstring. **Why (a) and (d) were not done in S163:** both need a per-format answer and a diagnostics pass has no single "current format" to resolve against — a document may declare several, and quarto renders each separately with its own resolved value. That is a genuine design decision (which format's flag wins for a diagnostic that is not per-format?), so it is its own deliverable rather than a widening of S163's. **Why (b)/(c) were not done:** the `.qmd` feature reads no project or directory config at all — the same architectural gap the already-filed "proactively validate `_quarto.yml` while editing a nested `.qmd`" item describes (off-editor `fs.readFile`, re-trigger on `.qmd` switches, `core/project.ts` `findProjectRoot`). **Do (b) together with that item.** The `_quarto.yml`-honours-its-own-flag half of (b) is the CHEAP one — `features/yaml-project-value-diagnostics.ts` already reads the very document the key lives in, so it is the same shape as S163's front-matter gate applied to a second feature, and `core/validate-yaml.ts`'s `isValidationDisabledValue` is already the shared predicate. MEDIUM for (a) and the cheap half of (b); LOW for (d) (safe FN, exotic shape).
- [ ] **`validate-yaml: false` does NOT suppress the 26 top-level keys PANDOC validates, and S163's document gate silences all of them** (found by the Session 163 §9 review as a false universal in S163's own docstrings; the exception set **measured firsthand by S163** with an exhaustive sweep; **CAUSED BY S163**, knowingly, as the FP-safe side of a measured trade). S163's comments claimed the front-matter **format NAME** was "THE ONE SURFACE" the flag does not suppress. It is not. Swept all **170** top-level front-matter keys this feature can actually flag — the domain that matters is `isWrongValue("banana", field) === true`, not "keys quarto rejects", because a key we cannot flag can never cost a true positive (the S162 `layout` lesson) — one `quarto render --no-execute` each with `validate-yaml: false` set: **26 still render exit 1**, and each of the 26 renders exit 1 without the flag too, so every one is a real value error we reported before S163 and are silent on now. The survivors: `ascii`, `cite-method`, `citeproc`, `columns`, `dpi`, `email-obfuscation`, `epub-chapter-level`, `eol`, `fail-if-warnings`, `html-q-tags`, `incremental`, `ipynb-output`, `listings`, `number-offset`, `preserve-tabs`, `reference-location`, `section-divs`, `shift-heading-level-by`, `slide-level`, `strip-comments`, `tab-stop`, `toc`, `toc-depth`, `top-level-division`, `trace`, `wrap`. **Mechanism:** they are consumed by **pandoc**, whose Aeson decoder rejects them (`Error in $: expected Bool, but encountered String`, `Unknown wrap method "banana"`, constructor-tag errors for `eol`/`ipynb-output`), rather than by the quarto YAML-schema layer `validate-yaml` gates; `number-offset` is quarto's own non-schema check (`Invalid value for number-offset`). The other **144** render exit 0, so suppression is correct for them and the design stands — 144 unconditional false positives removed against 26 lost true positives, and an over-flag is the cardinal sin. **Why it was not fixed in S163:** the survivor set is a property of *pandoc's* option decoder, so it varies with pandoc version AND with output format (the sweep used the default html format; several survivors are revealjs/beamer keys that happen to be decoded anyway), which makes hard-coding these 26 names exactly the brittle transcription Learning #174 warns about. A faithful fix needs a principled source for "keys pandoc validates independently of quarto's schema" — the sweep harness is the starting point, not the answer. MEDIUM (26 lost TPs on common keys including `toc`).
- [ ] **Four YAML spellings of `validate-yaml: false` disarm quarto and we keep squiggling** (found by the Session 163 §9 review, **all re-measured firsthand by S163** against controls identical but for `false`→`true`; **PRE-EXISTING** — the pre-S163 tree flagged these documents too, so S163 simply does not reach them). `isValidationDisabledValue` (`src/core/validate-yaml.ts`) compares the RAW token, while quarto compares the PARSED value (`annotation.result["validate-yaml"] !== false`), so these all render **exit 0** while we still flag — the cardinal sin: (a) **anchored** `validate-yaml: &a false`; (b) **tagged** `validate-yaml: !!bool false`; (c) **aliased** `xx: &a false` + `validate-yaml: *a`; (d) **value on the next line** `validate-yaml:` / `  false` (the front-matter enumerator emits no `rawToken` for a key whose scalar is on a following line). **⚠ Do NOT fix this by stripping the node property and re-comparing** — measured, `!!str false` renders **exit 1** (it is the string, so validation stays ON) while `!!bool "false"` renders **exit 0** (the tag coerces the quoted scalar), so an explicit tag INVERTS the quoted-scalar rule that the rest of the predicate depends on. A correct fix resolves the YAML node rather than pattern-matching it, and (c) additionally needs a document-level anchor table. Note the project already has a node-property precedent in the FN-safe direction (`src/core/yaml-value-check.ts`'s `/^[[\]{}|>&*!]/` skip, S125) — but that one SKIPS such tokens, which here would be the wrong direction, since skipping means "do not disarm" and so preserves the FP. LOW-MEDIUM (rare shapes, but each is a live cardinal-sin FP, and the same predicate is the one BACKLOG names for reuse when `_quarto.yml` is widened).
- [ ] **An `{r}` cell whose leading option block quarto guesses as OLD-KNITR format is never validated, but we flag it** (reported by the Session 162 §9 review's fp-cardinal lens; **NOT independently re-measured by S162 — treat as review-reported and ground it before acting**; **PRE-EXISTING**). `partitionCellOptionsMapped` calls `parseAndValidateCellOptions` only when `language !== "r" || guessChunkOptionsFormat(...) === "yaml"`. For an `{r}` cell whose partitioned option text is guessed `"knitr"` — the lens reports the heuristic as at least one line with no colon and no leading whitespace, with every non-blank line either ending in `,` or containing `=` — quarto returns `yaml: undefined` and validates nothing in that cell, so any value renders exit 0 while `findCellOptionLines` still emits and we still flag. If it holds it is a cardinal-sin FP on a legacy-style `{r}` cell. LOW-MEDIUM; ground the guess heuristic firsthand first, since its exact shape decides how many real documents are affected.
- [ ] **32 of the 46 rows in `LANG_COMMENT_CHARS` are pinned by nothing — a one-row typo survives the whole unit suite, in the cardinal-sin direction** (found by the Session 161 §9 review's test-quality lens, **reproduced by the lens firsthand; not independently re-measured by S161** — treat the exact counts as review-reported). L1's correctness rests on a hand-transcribed 46-row table, but the S161 describes pin only ~9 rows (sql, matlab, fortran, apl, stata, c, sas, ocaml, plus r/python/ojs implicitly). The lens mutated exactly one row (`lua: ["--"]` → `lua: ["#"]`) and all 330 unit tests still passed, while quarto renders `{lua}` + `#| echo: banana` at exit 0 — i.e. the typo lands as a false positive. The circular fix (re-transcribing the same table into a test) has little value; the non-circular one reads quarto's own installed `handlers/lang-comment-chars.yml` from `share/editor/tools/yaml/yaml-intelligence-resources.json` and diffs it against ours, which is ALSO what would have caught the missing `mermaid` row. That makes it a unit test with a machine-local dependency, so it needs a deliberate decision about where it lives (integration, or a skipped-when-absent unit test). **Session 162 removed the blocker on the missing `mermaid: ["%%"]` row**: with `cellOptionScopeFor` returning `"none"` for a handler language, nothing in a `{mermaid}` cell is validated no matter which lines the enumerator emits, so adding the row can no longer create a false positive. What remains is purely an EMISSION-side change, and for `{mermaid}` specifically its only live consumer is cell-option COMPLETION — the embedded virtual-doc builders bail on an unmapped `cellLanguageId` before consulting option lines, and neither `core/refs.ts` nor `core/cell-background.ts` calls the enumerator at all (measured, S162 §9 review).
- [ ] **`src/core/refs.ts` is a SECOND, independent `#| label:` scanner and did not get the leading-block rule** (found by the Session 160 §9 review's missed-sites lens; **PRE-EXISTING** — identical against the pre-S160 tree; the review's specifics on indentation/block-scalar blindness were **not independently re-verified by S160**). `CELL_LABEL_OPTION` in `refs.ts` matches a directive line with its own regex rather than routing through `findCellOptionLines`, so since S160 the two disagree about the same line: the model says a below-the-block `#| label: fig-a` is ordinary code, while the cross-reference index still registers it as a label. The review reports quarto emits an unresolved-reference WARNING for such a `@fig-a` (exit 0), i.e. the index is wrong — ground that before acting. This is the concrete, measured motivation for the already-filed **"Consolidate the `#|`/`//|` cell-option-prefix grammar between `refs.ts` and `model.ts`"** item under Polish / deferred; do them together. **Session 162 widened the divergence again** — `refs.ts` also never learned the cell-HANDLER rule, though there it happens to be harmless: quarto really does resolve `@fig-g` from a `//| label: fig-g` in a `{dot}` cell (measured firsthand, exit 0, `href="#fig-g"`, "Figure 1"), so the index is RIGHT to register it. That is worth recording because it shows the two scanners are not simply out of step — the crossref index and the value validator legitimately want different answers for a handler cell, so a consolidation must keep the handler scope on the DIAGNOSTICS side only. LOW (a stale completion/go-to-definition entry, never a wrong diagnostic).
- [ ] **REMOVED — SHIPPED Session 179.** *(The unterminated-cell-at-EOF item lived here. It was the same root cause as the fence-closer LENGTH item above and shipped with it — see `CHANGELOG.md`, 2026-08-02. It correctly named the fix ("require a closing fence before a cell is a cell") and correctly warned it was a `findAllCells` change reaching every consumer. What it rated LOW — "only on a document that is mid-typing or malformed" — was the part worth re-grading: the same rule was also deleting every heading below such a fence from the outline, and mid-typing is not rare.)*

- [ ] **Raw `` ```{=html} `` / `` ```{=latex} `` blocks ARE validated by quarto, and we still enumerate none of them** (filed by the Session 160 §9 review's fp-cardinal lens as unverified; **GROUNDED FIRSTHAND BY SESSION 172 — the claim holds, and the mechanism is now known**; **PRE-EXISTING**, lost-TP direction). Quarto's cell recognizer captures the language as `([=A-Za-z]+)` and `=` is INSIDE the class, so a leading `=` is a legal cell LANGUAGE: `breakQuartoMd` builds an ordinary code cell for `` ```{=html} `` whose language is `=html`. Measured vs 1.7.33: `{=html}` + `#| echo: banana` renders **exit 1**, as do `{=latex}`, `{=banana}` and even the degenerate `{=}`; the valid `#| echo: false` renders exit 0, so it is the VALUE. **And the scope is the DOCUMENT ENGINE's, not a reduced one** — `#| cache: banana` (knitr-only) in a raw block renders exit 0 under `engine: markdown` and **exit 1** in a knitr document, exactly like a real cell; `=html` misses quarto's handler list, so `parseAndValidateCellOptions` keeps the engine schema. The comment char is `#` (a `//|` line renders exit 0), and any column-0 `#|` line is read as cell YAML — `#| a comment in html?` renders exit 1 with `YAML value ... must be an object`. **S172 deliberately did NOT adopt it.** Its `CELL_INFO` keeps the letter-led rule, so this stays a lost TP — the safe direction. Adopting it WIDENS what we squiggle onto a whole new block class (the cardinal sin) and hands `cell.lang === "=html"` to the outline, the virtual-document language map, run-cell, the crossref index and `cellOptionScopeFor` at once, so it needs that consumer work as its own deliverable. ⚠ **The guard is the `FP GUARD` pin in `test/unit/cells.test.ts` and its twin in `test/unit/yaml-value-flags.test.ts`** — a mutant adopting quarto's grammar verbatim kills those two and NOTHING else in 1583 tests. Oracle row `S172 {=html} raw block + echo (KNOWN residual, lost TP)`. MEDIUM-LOW (real lost TPs, but only on raw blocks carrying cell options).
- [ ] **Quoted-KEY divergence — the CONTAINER half (the leaf half SHIPPED Session 159)** (verified firsthand S159, **lost true positives, safe direction, LOW**). S159 unquoted the LEAF key on both `.qmd` front-matter surfaces, but the intermediate CONTAINER key is still compared with its quotes attached, so a quoted container silently disables the block beneath it. Grounded firsthand vs quarto 1.7.33: (a) `"execute":` / `  echo: banana` renders **exit 1** with `Field "echo" has value banana` yet we are silent — `mappingContainerKey` (`src/core/yaml-context.ts`) returns the container key WITH quotes, and `nestedParentPath` feeds that raw string to both the `NESTED_CONTAINERS` gate and the `format`-root check; (b) `format:` / `  "html":` / `    code-fold: banana` renders **exit 1** on `code-fold` and we are silent — the quoted format NAME degrades the per-format option set (the review measured 170 fields → 70). The review also reports the same container hole on the `_quarto.yml` VALUE surface (the already-filed S47 container item covers only the KEY surface) — not re-verified firsthand by S159. Note `mappingContainerKey` is SHARED with the 6d-6 nested-completion feature, so this is a cross-consumer change: its own slice with its own TDD pass (SAFEGUARDS), and it is the same defect class as the already-filed "anchored or quoted `project:`/`website:`/`book:` container header" item below — worth doing together.
- [ ] **Quoted-KEY on the `#|` cell-option surface — a lost TP that is BLOCKED on the cell arm-token grammar** (verified firsthand S159; the fix was implemented, then deliberately REVERTED — see `CHANGELOG.md`, Session 159). `#| "echo": banana` renders quarto **exit 1** with `Field "echo" has value banana` exactly as the bare form does, and we are silent — the same lost TP S159 fixed on both front-matter surfaces. Unquoting the cell key ALONE is not shippable: `findCellOptionLines` derives its continuation-guard arm token from `m[4].indexOf(":")` (the FIRST colon, not the real separator), so an option key containing a colon (`#| a:b: "text`, and the `|` block-scalar variant) disarms the guard and the folded line below is emitted as a real option — on documents quarto renders **exit 0**. That FP is already live with a BARE folded key (`#| a:b: "text` / `#| echo: banana"` — verified firsthand, flagged identically pre- and post-S159); unquoting merely widens it to quoted folded keys, so S159 fixed the front-matter twin in the shared `mappingColonAt` and reverted the cell half rather than widen an FP. **Do the arm-grammar fix FIRST** (make the cell arm use the real separator, skipping a quoted key — the same rule `mappingColonAt` now applies), **then** the unquote. `src/core/qmd/model.ts` is deliberately import-free (`yaml-context.ts` imports IT), so sharing the rule needs the cross-module grammar consolidation tracked under "Polish / deferred" — this is the concrete, grounded motivation for that item.
- [ ] **Quoted keys diverge in three non-diagnostic readers and in YAML completion** (reported by the Session 159 §9 review's missed-sites lens; **NOT independently re-verified by S159 — treat the specifics as review-reported, ground before acting**). The lens reports that `src/core/refs.ts`'s `CELL_LABEL_OPTION`, `src/core/frontmatter.ts`'s `/^bibliography:/`, and `src/core/preview-format.ts`'s `/^format[ \t]*:/` all match a BARE key literal, so a quoted spelling (`//| "label": fig-a`, `"bibliography": refs.bib`, `"format":`) silently disables cross-reference indexing, bibliography discovery, and declared-format parsing respectively — on documents quarto honors normally. It also reports that completion offers no value items after a quoted key and nothing at all inside a quoted container. All are false negatives (silence), never wrong output. LOW; a natural companion to the container item above since they share the "compare a key literal without unquoting" root.
- [ ] **Validate the scalar `format:` NAME on the remaining surfaces + polish (S145 §4.3 deferrals)** — all reuse S145's `isKnownFormatName` (`src/core/format-name-check.ts`) + `SchemaIndex.formatNamesForValidation()`. **(a) Combo 3 — the `_quarto.yml` top-level scalar `format: banana` — SHIPPED Session 152** (a ~15-line branch in `yaml-project-value-diagnostics.ts`'s `"document"` arm mirroring the `.qmd` path, WITH the P3 backslash guard; fixtures `test/fixtures/yaml-project-format-name/`; see `CHANGELOG.md`). (b) **Combos 2 & 4** the container-key form `format:\n  banana:` on both surfaces — needs a NEW container-KEY emission (`.qmd` has no key enumerator; `_quarto.yml` never pushes the block-opener name); plus a diagnostic-placement question (quarto reports the unknown container on its first child line; we would squiggle the name token). (c) multi-format MAPPING-form names (`format:\n  html:\n  banana:`). (d) nearest-match hint (`Unknown output format 'reveal'. Did you mean 'revealjs'?` — Levenshtein). All LOW.
- [ ] **The project-value diagnostics feature does not cover directory-level `_metadata.yml`/`_metadata.yaml`** (filed Session 152, §9 review missed-sites lens, verified firsthand; **safe direction — under-flagging, no FP**). `isProjectConfigFileName` (`src/core/project-yaml.ts`, `PROJECT_CONFIG_FILENAMES = {_quarto.yml, _quarto.yaml}`) gates the WHOLE `_quarto.yml` project-value feature (`features/yaml-project-value-diagnostics.ts`) on those exact basenames, so a directory-level `_metadata.yml` (quarto applies it as metadata to every doc in that directory) receives NONE of these diagnostics: `format: banana` there renders `quarto render` exit 1 ("Directory metadata validation failed … has value banana, which must instead be 'ansi'") but is never flagged, and neither is any closed document-key value (`toc: banana`, etc.). **PRE-EXISTING** — the gate has been `_quarto.yml`-only since the feature shipped; Combo 3 (S152) made the format-NAME half of the gap newly visible but did not create it. A faithful fix is its own slice: `_metadata.yml` carries DOCUMENT-level metadata (document keys + `format:` + `execute:`), NOT the `project:`/`website:`/`book:` containers, so widening the gate must also scope which field sets apply — needs its own grounding of what quarto validates in `_metadata.yml`, plus fixtures. LOW (safe FN).

- [ ] **Migrate `CHANGELOG.md`'s header/preamble to the current v3.1+ "Authoritative Action Ledger" seed format** (grooming, filed Session 158; cosmetic/structural — ZERO behavioral impact, its own STANDALONE commit). `bin/status` (from the sibling `../methodology` checkout) flags `CHANGELOG.md` as `present (stale format)` because it was seeded PRE-v3.1 with the "Keep a Changelog" framing (`# Changelog` + "All notable changes… Format loosely follows Keep a Changelog"). The current seed (`../methodology/starter-kit/CHANGELOG.md`) uses `# Changelog — Authoritative Action Ledger` + the action-ledger preamble (tied to `SESSION_RUNNER.md` Phase 3F / Phase 0 reconcile / FM #27) + a "How to add an entry" section. **The ENTRIES already comply** (dated `### YYYY-MM-DD · [source-tag]`, newest-on-top, one per action) — only the top-of-file scaffold ABOVE `## [Unreleased]` lags. Fix: replace ONLY the header/preamble above `## [Unreleased]` with the current seed's header/preamble, **keeping every existing dated entry byte-intact**. Do NOT copy the seed's `METHODOLOGY-SEED-SENTINEL` line — it marks a FRESH, empty ledger, and this one carries ~150 sessions of history (`bin/status`'s freshness check keys on that sentinel + the absence of any real `### <date>` entry, so adding it to a populated ledger would be wrong). Verify: `../methodology/bin/status .` no longer marks `CHANGELOG.md` `(stale format)`; `git log`/entry content unchanged. LOW.

- [ ] **`methodology_dashboard.py` is two minor versions stale** (noticed at Session 174 Phase 0; the script says so itself). The local copy is **v2.8.0**, canonical is **v2.10.2**, and the tool prints its own re-sync command on every run: `python3 /Users/rmsharp/Development/methodology/starter-kit/methodology_dashboard.py --sync`. Purely a synced-file refresh with no project code involved, but it is worth doing deliberately rather than during another deliverable, because the dashboard is what Phase 0 reports health and risk from — a two-version-old scorer may be measuring against retired signals. Note the sibling `../methodology` checkout is also where `bin/status` lives, which the CHANGELOG-format grooming item below already depends on; **do them together.** LOW.

- [ ] **REMOVED — ACTED ON Session 186.** *(The "`scratchpad/` is ~97 MB and nothing prunes it" item lived here. See `CHANGELOG.md`, 2026-08-08. **Its headline figure was wrong by 26×**: measured at Session 186's Orient the directory was **2,600,816 KB (2.5 GB) across 52,796 files**, not ~97 MB, and the same stale figure was repeated at three other sites (`BACKLOG.md` line ~160 above, `CHANGELOG.md`'s S174 entry, and `.vscodeignore`'s decision comment) — all four now corrected. **Four of the item's own claims are worth carrying forward, because three were wrong.** (1) Its growth model — "~300 files per session-ish" — understated by ~15× on average and by **130× for the outlier**: `scratchpad/s183` alone held 39,069 files. (2) It described the contents as "probe scripts and fixture trees", which was wrong **in kind**, not just in degree: **90% of the bytes were regenerable `quarto render` byproduct**, because each ~200-byte probe document carries its own full copy of the bootstrap/quarto JS+CSS bundle in a sibling `*_files/` tree. (3) Its prescribed grep — "grep `docs/planning/` for `scratchpad/`" — reaches only **23 of the 78** paths cited across all tracked files, so anyone following the item's own instruction would have worked from under a third of the citation set. (4) What it got RIGHT, and what turned out to be the only thing that mattered: **"a blind `rm -rf` would break those citations."* Session 186 therefore gated the prune on a **proven disjointness check** — extract every `scratchpad/` token from every tracked file, keep those that still exist (38), enumerate the delete set (2,768 top-most `*_files/`, `.quarto/`, `_site/`, `_book/` dirs), and assert **zero** cited path lies inside it — before any `rm`, and re-assert all 38 afterwards. **Result: 2,332 MB reclaimed (91%), 52,796 → 19,788 files, ZERO cited paths lost**, build matrix byte-unchanged (`compile` 0, `npm test` 1754/65, `check-package` OK 42 files / 5.51 MB). ⚠ The residual 212 MB is deliberately NOT disposable — it is the authored corpora, probe harnesses and score tables that the citations point at.)*

- [ ] **51% of the `scratchpad/` paths cited by TRACKED documents no longer exist, and two are findings documents cited by committed planning docs** (found by Session 186 while gating its prune; **MEASURED firsthand**; **PRE-EXISTING** — every one of these was already gone before that session's prune, which lost zero; **durability defect**, MEDIUM). Of **78** distinct `scratchpad/` path tokens appearing in tracked files, **40 already dangle**. The pattern that produces them is structural, not accidental: a session measures something, writes the conclusion into a **committed** receipt, and cites the **uncommitted** evidence — which nothing preserves. Worst instances, because the citing document is durable and reviewable rather than a scratch log: `scratchpad/S131-findings.md` (cited by `docs/planning/2026-07-20-other-container-value-validation-plan.md`), `scratchpad/S134-findings.md` and `scratchpad/ground-project.cjs` (both cited by `docs/planning/2026-07-21-quarto-yml-value-validation-plan.md`), `scratchpad/matrix.json` (cited by the 07-20 plan), and `scratchpad/fmtgnd/probe.sh` (cited by `docs/planning/2026-07-22-quarto-yml-format-value-validation-plan.md`). ⚠ **The fix is a policy question, not a script:** either promote the small authored core out of `scratchpad/` into a tracked location at the session that creates it (the S185 harness family is ~60 KB), or stop citing scratchpad paths from durable documents and inline the evidence instead. Note that the currently-live `HANDOFFS.md` S185 receipt points the next session at `scratchpad/s185/`, so the exposure is **active**, not merely historical.

- [ ] **`.vscode-test/` is 18 GB — 7.2× `scratchpad/` — and being gitignored it has NO visible signal at all** (found by Session 186 while measuring the uncommitted surface; **MEASURED firsthand**; **not a defect**; LOW, but it inverts the attention this repo currently pays). `du -sh .vscode-test` → **18G** of a 21 GB repo, against `scratchpad/`'s 2.5 GB-before / 212 MB-after. Composition: `user-data/` **14 GB** (dominated by `WebStorage`) plus **four** VS Code downloads at 820–842 MB each — `1.126.0`, `1.128.0`, `1.128.1`, `1.129.0`. Only **`1.129.0`** is pinned, at `test/integration/runTest.ts:38`; ⚠ `test/lsp/runTest.ts` calls `runTests({…})` with **no `version` key**, so it takes `@vscode/test-electron`'s default and will re-download whatever is current — which is how three superseded copies accumulated. **The irony worth recording:** the directory carrying a documented decision (S174), a visible `git status` line, and three hard packaging gates is the SMALL one; the one 7.2× larger is invisible at every `-u` level because `.gitignore:9` hides it. Deleting the three superseded downloads reclaims ~2.5 GB and is reversible (they re-download); clearing `user-data/WebStorage` reclaims ~14 GB but was **NOT measured** for whether it perturbs `test:integration` / `test:lsp`. **Operator decision, not an agent task.**

- [ ] **`npm audit` went 2 → 6 advisories and no document in the repo records the change** (found by Session 186 at Orient; **MEASURED**; **devDependency-only**, so it reaches no shipped artifact; LOW). 5 high + 1 moderate: `brace-expansion`, `fast-uri`, `js-yaml`, `nanoid`, `postcss`, `undici`. `package-lock.json` has been untouched since `51e742e` (2026-07-21), so this is advisory publication, not a dependency change. **Every one reports `fixAvailable: true`**, and the production tree is empty (this project declares no runtime `dependencies` at all), so the shipped `.vsix` is unaffected in every case. Until Session 186 committed `dashboard_history.jsonl`, the uncommitted snapshot row was the ONLY record of the jump anywhere in the repo — every prose document still says 2, including the standing `fast-uri` item further down this file. Supersedes that item's framing.

- [ ] **DEPTH-3+ `_quarto.yml` project-config values** (`navbar.tools.*`, `sidebar.contents.*`) — reader one-level `.children` cap + enumerator `path.length===1` cap; much of depth-3 is item sequences, not closed scalars. **A safe false negative today** (we under-flag; we never wrongly flag). *Promoted to a top-level item by Session 150 — it was filed by Session 137 as a sub-bullet of the now-removed "DEPTH-2 `_quarto.yml` project-config VALUE validation — SHIPPED Session 137" block, where deleting the completed parent would have destroyed it.*

- [ ] **Sequence-form `navbar:`/`sidebar:` grandchildren** (`sidebar:\n  - id: …\n    style: x`) — needs sequence-item descent in the enumerator. **A safe false negative today.** *Promoted to a top-level item by Session 150 — same provenance as the depth-3+ item above: filed by Session 137 beneath the completed depth-2 block.*

- [ ] **Marketplace publish prerequisites — OPERATOR STEP, not an agent task.** An actual `vsce publish` needs a registered Marketplace publisher `rmsharp` **plus a PAT**; neither exists yet. `package.json:7` carries `"preview": true` — **flip it when the listing is deemed stable.** **No publish near-term** (operator decision, Session 103). *Promoted to a top-level item by Session 150 — it was a prose sub-bullet of the completed "v1 release prep" block, and is the ONLY record anywhere of what publishing actually requires: `CHANGELOG.md:669` mentions `vsce publish` only in a versioning-design context, and `package.json:7` carries the flag with no explanation. Deleting the parent would have destroyed it.*

- [ ] **`format-cell` against a REAL Python formatter remains UNPROVEN** — the operator declined installing `ms-python.black-formatter` in the harness, so it is **not claimed either way**. What IS proven: the Format Cell forward reaches a real, non-stand-in provider — `npm run test:lsp` drives it through VS Code's **built-in JavaScript formatter** via an `{ojs}` cell, which needs no extension. Closing this needs either a Python formatter extension in the harness or an explicit decision to leave it unproven. *Promoted to a top-level item by Session 150 — a prose sub-bullet of the completed "Item 18" block and the only record of this verification gap. Distinct from the open "Format Cell adjacency edge" item below, which is a different defect.*

- [ ] **Post-Posit-comparison feature roadmap — the residual open candidates.** Prioritized via grill-me sessions (Session 43; extended Session 61 to decide the gaps Session 43 had parked rather than ranked; refreshed Session 67 against Posit v1.133.0–v1.135.0). Source: `docs/POSIT-COMPARISON.md`'s "Real gaps" + its own priority list, re-ranked against the operator's actual usage (multi-file Quarto projects/books are the dominant workflow; new-user adoption matters) rather than raw gap size. **All 16 ranked items are SHIPPED or CLOSED** (project-level render, YAML schema diagnostics, onboarding walkthrough + scaffolding, run-cell family, snippets, workspace symbols, Graphviz, `.ipynb` conversion, spell checking, code-cell diagnostics forwarding, outline granularity, Format Cell, the declarative-wins bundle, `_quarto.yml` links + filepath completion, the preview command family, semantic highlighting) — see `CHANGELOG.md` for each. Item 17 was split into individually-grabbable items in Session 109; **only `Item 17b — Reticulate` remains open**, filed under "Polish / deferred" below. What is still genuinely open here:
  - **Parked, flagged as a likely future build-out (Session 61 grill-me decision; not ranked or scheduled):** a **Contextual Assist Panel** equivalent. Not pure UI consolidation on closer look — a Session 61 grep confirmed the two capabilities Posit's version has that we don't are genuinely uneven. **Code-doc lookup** is a *partial* gap: `EmbeddedHoverProvider` (`src/providers/embedded.ts:206`, Session 30) already forwards hover into `{python}`/`{r}`/`{julia}`/`{ojs}` cell bodies via the user's own language extension, but nothing hovers Quarto's own YAML front-matter / `#|` cell-option keys — whose descriptions already exist as completion-item `detail:`/`documentation:` text in `src/providers/yaml.ts` + `src/core/yaml-schema.ts` `SchemaIndex`, and could plausibly back a new hover provider with no new data source. **Image thumbnails** is a genuine zero (no thumbnail-on-hover code exists for any file type). Either capability would need a brand-new sidebar **`WebviewViewProvider`** (grep-confirmed absent everywhere in this codebase) to host it in Posit's "one panel" shape, rather than another Command-Palette webview. **Its own future planning session if picked up.**
  - **New, unranked (filed at Session 47 close-out per the YAML schema diagnostics plan §8/§9 Q5):** proactively validate a project's `_quarto.yml` `project:`/`website:`/`book:` keys **even when the user is only editing a nested `.qmd` and never opens `_quarto.yml` itself**. Genuinely more valuable than the shipped v1 (it catches project misconfiguration the user might never notice), but materially larger: needs off-editor `fs.readFile`, re-triggering on `.qmd` switches, and reuse of `src/core/project.ts` `findProjectRoot` (Session 45) — none of which the shipped v1 needs. **Its own future planning session.**
  - **⚠ Excluded — operator directive, Session 43:** the **Visual (WYSIWYG) editor** itself. Posit's single largest feature and this project's single largest gap, deliberately out of scope. **Do not resurface as a candidate without a new operator decision.**
  - **⚠ Not proposed as ranked items — same treatment as the excluded Visual Editor:** the Session 67 refresh also found 3 "Soft / ambiguous comparison" findings — nested/deep YAML completion depth, project-wide/multi-file cross-reference intelligence, and developer-facing extensibility surfaces (a public CLI-query API and Quarto-Extension/Lua authoring support). They do not cleanly fit this project's document-authoring mission the way every ranked item did. See `docs/POSIT-COMPARISON.md`'s "Soft / ambiguous comparison" bucket. **Not ranked here without a new operator decision.**

  *Condensed by Session 150 from a 44-line tracker that carried all 16 completed ranked items, their 17 completed sub-slices, and 3 closed grooming decisions (the "unknown-key diagnostics — CLOSED, not a real gap" finding, the Zotero "mostly already covered" decision, and the front-matter/cell-option VALUE-validation plan — the last of which was stale, having shipped across Sessions 124–149). All are in `CHANGELOG.md`; only the still-open candidates and the two standing operator directives are kept here.*

- [ ] **One container document makes `quarto render` CRASH — exit 139 (SIGSEGV), not a diagnostic** (found by Session 191's container sweep; **MEASURED**; **not a defect in this extension**; LOW, but it matters to any scorer). Exactly 1 of 2,160 documents in the container × class × indent × underline sweep exited **139**. Any corpus scorer must exclude non-zero-exit rows and report them separately, or a crashed render reads as "quarto produced no headings" and every heading the model emits there is counted as a phantom (Learning #270). `scratchpad/s191/score191.py` does this and reports the exclusions by name; the document is `scratchpad/s191/ctx/x01444.qmd`. ⚠ Worth reducing to a minimal repro and reporting upstream to quarto — a segfault on valid-looking markdown is a quarto bug, not ours. Not attempted this session (FM #26).

## Documentation

*(empty — the one item this section held, the Posit feature comparison, was completed in Session 42 and now lives in `CHANGELOG.md`; its deliverable is `docs/POSIT-COMPARISON.md`.)*

## Polish / deferred

- [ ] **The YAML key/value-separator grammar is implemented TWICE** (filed Session 148 while shipping ②). `src/core/yaml-context.ts` now exports `isMappingSeparator` + `mappingColonAt` (used by all four diagnostics value paths), while `src/core/project-links.ts` has carried a private `mappingColonIndex` since S80/81 that encodes the same YAML rule for the document-link scanner. They are not identical: `mappingColonIndex` accepts any `/\s/` after the colon where YAML — and `isMappingSeparator` — allow only space/tab, so the private one is slightly over-permissive. Deliberately NOT consolidated in S148: a different feature with its own tests, and a cross-module refactor is plan-mode work (SAFEGUARDS). Cross-referenced in both files' doc comments so neither side rediscovers the duplication. Belongs with the two standing "consolidate the grammar" items below (`refs.ts`/`model.ts` cell-option prefix; `yaml-context.ts`/`model.ts` value slot) — a single grammar-consolidation planning session could take all three.

- [ ] **Separator fix residual: two false-negative classes, both on quarto-REJECTED documents** (filed Session 148 with ②; safe direction, no FP risk). (a) **The no-separator form** — `toc:banana` / `#| echo:banana` makes the document a plain scalar, which `quarto render` rejects (exit 1), and we are now silent. This is the cost the plan explicitly accepted. (b) **`key:: value` on a CLOSED key set** — `project:\n  type:: banana` and `website:\n  page-navigation:: true` are quarto-REJECTED (exit 1, the key `type:`/`page-navigation:` is unknown to a closed set), but we are silent: the VALUE path skips, and the unknown-KEY enumerator (`findProjectConfigKeyLines` → `keySpanAt`, deliberately untouched — plan dragon 3) still splits at the first colon and so sees the KNOWN key before it. Before the fix we flagged these with a wrong-reason message ("Value : true is not valid…"); silence is a strict improvement in correctness but a small loss in coverage. Closing (b) means teaching the KEY enumerator the same separator rule, which ADDS flags and so needs its own FP grounding — a separate slice, not a tidy-up.

- [ ] **New `npm audit` advisory: `fast-uri` 3.1.2 — high, DEV-ONLY** (surfaced at Session 146 Phase 0; offered as a candidate and not picked). GHSA-v2hh-gcrm-f6hx (host confusion via a literal backslash authority delimiter) + GHSA-4c8g-83qw-93j6 (failed IDN canonicalization). Dependency path is `@vscode/vsce → @secretlint/node → @secretlint/config-loader → ajv@8.20.0 → fast-uri@3.1.2`, i.e. **packaging tooling only — it never ships in the `.vsix`**, so runtime exposure is nil. It landed after Session 108 cleared the previous seven advisories, and it is what the methodology dashboard's "high risk" flag currently reads (health 78/100). `npm audit fix` claims to resolve it; do it the S108 way (deliberate bump, never `--force`) and re-run the full matrix + `vsce package`. Small, self-contained, mostly declarative.

- [ ] **R/Julia leak measurement under `diagnosticMode: "workspace"`** (filed Session 93 at the close of the python fix below; explicitly UNMEASURED). Candidate G's `# type: ignore` mute is python-gated (a `#` is a JS syntax error, and `# type: ignore` is **Pyright/PEP-484-specific** — an R (lintr) or Julia (LanguageServer.jl) server treats it as an inert comment, suppressing nothing). Slice 2 made `{r}`/`{julia}` vdoc minting PASSIVE (see the item below), so merely opening a `.qmd` with such a cell mints and opens its model — and if a real R/Julia server is installed, its diagnostics attach to the vdoc, the identical location-independent tracked-set leak. The confirmed leak is Pylance-only; whether R/Julia leak is **not measured** (no such servers in this repo's `test:lsp` harness). Small, evidence-gated: point at a real R (or Julia) server, drive the forwards under `QMD_LSP_DIAGMODE=workspace`, observe. **The FIX SHAPE (an in-content suppression directive on line 0) is what extends — NOT the string `# type: ignore`:** each server needs its OWN suppression directive if one exists (e.g. a lintr `# nolint` line, a Julia `#src`/pragma — to be researched; some may have none). If none exists, the fallback lever is the vdoc-minting-lifecycle item below — but note its S89-filed "gate vdoc minting on a *registered server*" fix was **REFUTED Session 94** (VS Code registers `r`/`julia`/`python`/`javascript` via built-in language basics, so `getLanguages()`/`needsLanguageExtension` cannot gate on server presence; Learning #105), leaving only the unavoidable-first-mint lifecycle change described there. Do NOT reach for the disposal-timing rewrite the S92 plan §5 rejected.
- [ ] **Format Cell adjacency edge from candidate G's line-0 mute — cell whose fence is at document line 0** (Session 93 review, completeness critic, LOW; in_scope but UNVERIFIED). `buildCellVirtualContent` now puts `# type: ignore` on vdoc line 0; when a `.qmd` opens directly with a `{python}` cell (fence at line 0, no front matter/prose), that comment sits immediately above the body (line 1) with no blank between. Format Cell forwards the cell vdoc to the user's real Python formatter; a formatter that inserts a blank line after a leading module comment could emit an edit at body line 1, which `format-cell.ts` `rangeWithinCell` (`:139`) ACCEPTS (it is a body line of the cell) — inserting a spurious blank into the user's cell. **Unverified and likely a non-issue:** the harness has no Python formatter (Format Cell against a real Python formatter is already documented UNPROVEN, `format-cell.ts:22`), and black specifically does NOT insert a blank after a leading comment (it attaches to the following statement). Pinned by the unit test "when the cell's fence is at line 0, the mute sits IMMEDIATELY above the body". Recheck when a real Python formatter (e.g. `ms-python.black-formatter`) is installed; if a formatter does emit the spurious edit, tighten `rangeWithinCell` to reject a leading-blank-insert at the body's first line, or skip the cell-builder injection when `cell.startLine === 0`.
- [ ] **Residual: transient PARSE/SYNTAX-error diagnostics on a mid-typing cell still leak under `diagnosticMode: "workspace"`** (filed Session 93 at the close of the python fix below). A file-level `# type: ignore` mutes Pyright's *type/name/import* diagnostics (the persistent phantom class the fix targets), but **not** *parse/syntax* errors — Pyright reports those before type-checking. So a vdoc built while the user is mid-typing an incomplete expression (`os.` with a dangling dot, `greet(` with an unclosed paren) still surfaces a transient syntax diagnostic on its `.quarto/vdoc-mit/` path under workspace mode. Directly observed Session 93 (the `test:lsp` completion/signature-help fixtures — `os.`, `greet(` — leak `Expected attribute name after "."` / `"(" was not closed` even WITH the mute). Lesser than the fixed class: transient (clears once the line is syntactically complete) and not the cross-cell/import pollution the item was really about; and it matches Posit's own acknowledged residual (their three-technique approach still leaks, `quarto` #855). No clean in-content fix (`# type: ignore` cannot suppress a parse error); would need the heavy own-a-client/close-the-model path the S92 plan rejected. Low priority — reassess only if users report it.
- [ ] **`{r}`/`{julia}` semantic-token vdoc minting is PASSIVE (no user gesture) — but the S89-filed "cheap fix" is REFUTED; downgraded MEDIUM→LOW, residual harm bounded** (filed Session 89 adversarial review; investigated + corrected Session 94, Learning #105). Since item 16 Slice 2 the semantic-tokens provider forwards EVERY mapped language present, so merely *opening* a `.qmd` with an `{r}`/`{julia}` cell mints one vdoc per language on VS Code's debounced timer (writes a copy of the user's cell source to `.quarto/vdoc-mit/` + opens a model), whereas before Slice 2 (python-only forwarding) such a vdoc existed only on an active hover/completion/format gesture. **The S89-filed fix — "gate `embeddedLanguagesIn`'s targets on the languageId being registered via `needsLanguageExtension`/`getLanguages()`" — does NOT work, proven firsthand Session 94:** VS Code core ships built-in *language-basics* extensions (grammar + config, **NO server**) for `r`, `julia`, `python`, AND `javascript` (`resources/app/extensions/{r,julia,python,javascript}`, v1.128.0), so `vscode.languages.getLanguages()` returns all four in EVERY standard VS Code — extension or not — and `needsLanguageExtension` returns `false` for our whole mapped set. A RED integration test's premise assertion (`!getLanguages().includes("r")`) fired immediately, and the app-bundle inspection confirmed why. **Two facts constrain any correct fix:** (1) the FIRST speculative mint is architecturally *unavoidable* — there is no public API to detect whether a *semantic-tokens provider* exists for a language without handing VS Code a real `file:` document, and a `provideDocumentSemanticTokensLegend` returning `undefined` is NOT a reliable "no provider" signal (S89 measured the built-in JS service returning an undefined legend *transiently while it does have a provider*); (2) the residual harm is *bounded* — the vdoc is gitignored, disposed on document close (`providers/semantic-tokens.ts:111` → `disposeVdocs`), and swept on activation (`extension.ts:73` → `sweepStaleVdocs`). **The only CORRECT fix is a lifecycle change** ("mint speculatively; once a language exhausts its `INCOMPLETE_RETRIES` budget with zero usable streams, dispose its vdoc and stop re-minting it for that document until its cell code changes") — medium-complexity, race-prone (the class the S89/S91 reviews found bugs in), and it still cannot avoid that first write. Given the bounded residual harm, **downgraded MEDIUM→LOW** and left open for a future session that wants the lifecycle fix. The distinct concern "an installed R/Julia SERVER now analyses the vdoc and its diagnostics attach to it" is the **R/Julia-leak measurement item above** (unmeasured — needs a real R/Julia server; `# type: ignore` is Pyright-specific and does not extend). NOT verified firsthand for R/Julia (no such server installed here).

- [ ] **`disposeEpoch` grows unboundedly** — leg (a) only; **leg (b) (the latched `mkdtemp` failure) is FIXED Session 101** (Session 89 adversarial review, two LOWs, both confirmed; this item stays OPEN over leg (a) alone). **(a) STILL OPEN:** `disposeEpoch` is written for EVERY closed text document — including ones that never had a vdoc — and never pruned; it is a `Map<string, number>`, so the leak is small and bounded by the session, but it is unbounded in principle. **(b) FIXED Session 101** (with `:102` — the same `fallbackDirPromise` state; a rejected memo was latched for the whole session, so one transient ENOSPC/EMFILE left completion / hover / go-to-definition / signature help / in-cell outline / Format Cell silently dead on every untitled `.qmd` until a window reload). The filed text ("only ever reset in `disposeAllVdocs`") **understated it**: the reset sat behind `if (fallbackDir !== undefined)`, and the resolved value is assigned only in the `.then()` a rejection never runs — so the memo was cleared on the SUCCESS path only and a rejection was never cleared by *anything*, deactivate included. Fixed at the rejection site (a `.catch` that clears the memo — guarded on still being the same attempt, since `disposeAllVdocs` now also resets it unconditionally and a late rejection could otherwise clobber a newer attempt). Pinned by "retries mkdtemp after a transient failure, rather than latching untitled docs dead" (`test/integration/suite/embedded-vdoc.test.ts`), which induces a REAL ENOENT through the real syscall by pointing `$TMPDIR` at a per-run UUID path — no stub. Break-revert-proven.

- [ ] **Semantic tokens: `self` renders a different colour on a `.qmd` than in a real `.py`** (Session 90 review, LOW — raised as "dropping `selfParameter` is strictly worse than carrying it", and that framing was **refuted**, but the underlying observation is real). Resolved firsthand through VS Code's theme trie in the real default theme (Dark 2026): `.qmd` today shows `self` as **#79c0ff** (MagicPython's `variable.language.special.self.python`); a real `.py` shows **#c9d1d9** (Pylance's own scope rule). **Carrying `selfParameter` bare does NOT reconcile them — it invents a THIRD colour, #ffa657** (`parameter` → `variable.parameter`), matching neither. That is why D4 drops it, and the drop is correct. Closing the cosmetic gap for real would mean contributing Pylance's *exact* scope strings under `language: "quarto"` — a Pylance-versioned table that would silently rot when they rename a scope, in exchange for making `self` match `.py` at the cost of making it *less* distinctive than it is today. Filed, not recommended, and explicitly not a defect in the shipped behaviour.

- [ ] **Semantic tokens: the 23 STANDARD token types in `OUR_LEGEND` are pinned by nothing** (Session 90 review, LOW — a pre-existing test hole this slice neither created nor widened, found by the test-quality lens). 17 of the 23 standard names can be deleted from `OUR_LEGEND` with the entire suite (811 unit / 314 integration) still green: no test asserts that our legend actually *declares* the standard set, only that specific names round-trip. A silent deletion would uncolour those token types across every embedded language, with no test failing. Cheap fix: assert `OUR_LEGEND.tokenTypes` contains the full standard set (the same shape as the existing carried-name pin). Not fixed here — it is outside this slice's blast radius (FM #17) and the fix belongs with a broader legend-coverage test.

- [ ] **Semantic tokens: deleting the last `{python}` cell strands the previous vdoc until the `.qmd` closes** (Session 88 Slice-1 review, LOW). Once the language drops out of `embeddedLanguagesIn` (which replaced `hasCellOfLanguage` in Session 89) the provider returns early, so it never calls `ensureVdoc` and the previously-minted vdoc file (and its open model) survive until `disposeVdocs` runs at close. Bounded, self-healing at close, and swept at the next activation. The fix belongs with the churn item above, since both are about the whole-language vdoc's lifecycle.

- [ ] **Within-session accumulation of superseded cell vdocs during heavy editing** (Session 87 item-18 adversarial review — race + correctness lenses; the past-close leak is FIXED, the within-session churn is the residual). Cell vdocs (outline in-cell symbols, Format Cell) are keyed by absolute `cellStartLine`, so inserting/deleting a line above a cell changes its key and mints a new vdoc; a concurrent same-key double-mint (a hover racing a completion) likewise mints a second file. **The past-close leak is fixed** — `docFiles` now tracks every minted URI per document, so `disposeVdocs`/`disposeAllVdocs` reclaim all of them at document close (proven by the "reclaims a vdoc whose KEY changed" test), and the activation sweep is the cross-session backstop. The residual is that, *while a document stays open*, an actively-edited many-cell document can accumulate stale `.py` files (and open Pylance models — feeding the diagnostics item above) until close. Bounded and self-healing (reclaimed at close + next-session sweep), same order as the old in-memory `InCellSymbolStore` behaviour it replaced. A tighter within-session bound would require the outline provider to reconcile its full current cell-vdoc set on each computation (delete cell vdocs no longer in the live set), which is more surface than this fix warranted. Low priority.

- [ ] **Preview lifecycle: closing a non-`file:` editor (e.g. a Git diff) for a previewed document silently kills the live preview** (surfaced by the Session 84 adversarial review; **PRE-EXISTING**, not introduced by item 15 — it has affected `quarto.preview` since preview shipped; operator-confirmed as deferred rather than fixed in-slice, since `onDocumentClosed` is listed "unchanged" in the item-15 plan's §8 contract and the fix touches lifecycle shared by all three preview commands). `features/preview.ts` `onDocumentClosed` (:281-283) disposes by `doc.uri.fsPath`, **discarding `doc.uri.scheme`**. The built-in Git extension's read-only `git:` diff of a file has the SAME `fsPath` as the working-tree file, so `git:` document-close events reap a live preview for that path. Reproduced end-to-end by two independent reviewers in a real Extension Development Host (VS Code 1.128) against the genuine built-in Git extension: preview `report.qmd`, open its Git diff, close the diff tab → the preview server is killed and the pane disappears, with no error. **Severity: annoying, not dangerous** (no orphan, no corrupt state — the reap path is the normal one, just fired by the wrong document). A faithful fix ignores non-`file:` schemes in `onDocumentClosed` (a preview only ever exists for an on-disk file, so this is safe) or keys sessions by `uri.toString()` rather than `fsPath`; either way it needs its own TDD pass with a regression check across `preview`/`previewFormat`/`previewScript`. Session 84 DID close the same hole in the new `previewScript` **gate** (`uri.scheme !== "file"` — a `git:` diff of a spin script no longer previews the working-tree file), so the remaining exposure is the shared close-handler only. (Adversarial review, Session 84; Learning #92.)

- [ ] **`previewFormat`: nested flow collections in `format:` + a differing-format request dropped during preview startup** (both surfaced/filed by the Session 82 adversarial review, item 15). (1) `core/preview-format.ts` `parseFlowCollection` splits flow collections on commas naively, so a flow collection nested inside another (`format: {html: {a: 1, b: 2}}`) mis-parses the inner comma — astronomically rare in a real `format:` block, degrades to a MISSING name (never a wrong `--to`, since the caller falls back to Quarto's default), same fail-safe class as Slice-1's flow-collection scanner limitation. (2) `features/preview.ts` `openPreview`: a `quarto.previewFormat` request for a DIFFERENT format that arrives WHILE a preview is still starting (the up-to-60s `starting` window) is silently dropped by the `to`-unaware `starting` guard — the reorder-before-dispose fix (Session 82) already converted the nastier teardown-then-nothing outcome into a benign drop-and-keep-the-in-flight-preview, so the residual is only "the user's format switch during startup is ignored; re-run once the preview is up." A faithful fix makes the in-flight reservation `to`-aware (track the pending `to`, restart/queue on a differing format, or surface feedback). Both LOW severity. (Adversarial review, Session 82; Learning #90.)

- [ ] **`_quarto.yml` document-link scanner: flow-collection and pathological-quoted-key value shapes** (surfaced by the Session 80 Slice-1 adversarial review; both confirmed but deferred as fail-safe, documented limitations). `core/project-links.ts` `findPathValueCandidates` reuses the scalar value-token grammar, so a **flow collection** (`chapters: [intro.qmd, part.qmd]`, `opts: {file: x.qmd}`) is emitted as one token containing the brackets — its inner path elements are never split, so a real file inside a flow collection gets no link. Block style (`- intro.qmd`) is idiomatic in `_quarto.yml` and the item-14 plan (§2.5/§5.1) explicitly scoped the scanner to block style, so this was never claimed. Separately, a **quoted key containing `": "`** (a colon *and* a space, e.g. `"a: b": v`) can mis-anchor `mappingColonIndex` (a colon followed by a non-space, like `"a:b":`, is handled correctly; only the space-inside case is not) — astronomically rare in real config, and matches the existing `core/project-yaml.ts` `keySpanAt` naive-colon precedent. **Both degrade to a MISSING link, never a wrong one** (the existence check makes every scanner gap fail-safe). Low priority; if pursued, a faithful fix splits flow-collection elements into per-element candidate spans and makes the quoted-key colon detection quote-aware. (Adversarial review, Session 80; Learning #88.)

- [ ] **Sub-feature (c) — R-package workspace-symbol exclusion** (deferred from item 11's plan §9 Q3, filed Session 71). Since v1.127.0, Posit excludes markdown headers from workspace-symbol search (`Ctrl+T`/`Cmd+T`) by default when the workspace root is an R-package project (a `DESCRIPTION` file present), controlled by a three-way enum setting (`quarto.symbols.exportToWorkspace`: `default`/`all`/`none`, PR #751). Unrelated file (`src/providers/workspace-symbols.ts`/`core/workspace-symbols.ts`), narrower audience (R-package projects specifically), zero shared risk or architecture with item 11's (a)/(b) slices. `DESCRIPTION`-file/R-package detection has no precedent anywhere in this codebase — if pursued, it is new infrastructure, not an extension of existing logic. Low priority, unranked.

- [ ] **Outline show/hide toggle: cosmetic re-expand-on-toggle fix** (deferred from item 11's plan §9 Q4, filed Session 71). The dispose-and-re-register mechanism the toggle uses to force a live Outline refresh (plan §2.4) causes VS Code to rebuild the Outline tree **collapsed** each time the setting changes. Posit's own shipped fix (PR #974) adds an explicit re-expand call afterward to compensate, but the specific VS Code command their fix references was not independently verified this session as a stable, public API — verifying it and wiring the same compensating call is small, self-contained future work. Low priority, cosmetic only (the toggle itself functions correctly without it).

- [ ] **YAML schema diagnostics: an anchored or quoted `project:`/`website:`/`book:` container header silently disables scanning of that whole block** (surfaced by the Session 47 adversarial review; confirmed, both are false NEGATIVES — the safe direction, never a wrong flag — so shipped as a documented gap, not fixed in-session). `mappingContainerKey` (`core/yaml-context.ts`, shared with the 6d-6 nested-completion feature) requires the value after the colon to be empty or comment-only to recognize a line as a pure-mapping container; `project: &p` (a YAML anchor) and `"project":`/`'project':` (a quoted container name) both fail that check, so `findProjectConfigKeyLines` never enters the block at all — no diagnostics from it, ever, even for a genuinely unknown key inside. A leading-BOM false negative in the SAME class was fixed this session (child-key quoting was fixed too, since THAT is a false positive — the feature's core-promise violation); these two remain because fixing them touches `mappingContainerKey`, which is SHARED with the unrelated 6d-6 nested front-matter completion feature — a cross-module change needing its own TDD pass across both consumers (SAFEGUARDS: no cross-module refactor without plan mode). Low priority (anchors/quoted containers are rare in practice; the failure mode is silence, not a false alarm). (Adversarial review, Session 47.)

- [ ] **Embedded-completion secondary-edit filter — defense-in-depth for `command` + divergent primary `textEdit`** (surfaced by the Session 28 6e-1 adversarial review; both refuted 0/3 as out-of-v1-scope). `providers/embedded.ts` `filterOutOfCellEdits` strips out-of-cell `additionalTextEdits` by region membership (the v1 obligation — plan §6 6e-1 DONE #3), but does NOT inspect (a) a post-accept `item.command` (an arbitrary command an embedded server could attach) or (b) a divergent PRIMARY `item.textEdit`/`item.range`. Both are non-issues in v1: `command` is mitigated by `itemResolveCount=0` (the forward omits the 4th arg, and there is no `resolveCompletionItem`), and a divergent primary range is **structurally impossible** to land out-of-cell (VS Code clamps a primary completion range to a single line containing the cursor — vscode.d.ts `CompletionItem.range`). Becomes relevant only if a future 6e slice enables item resolution to surface real auto-imports — then region-check/strip `command` and validate the primary `textEdit` range too. Low priority. (Adversarial review, Session 28.)

- [ ] **Audit `src/providers/embedded.ts`'s existing completion/hover/go-to-def/signature-help forwarding vdocs for spurious global-Problems-panel leakage** (surfaced as a side effect of Session 68's code-cell diagnostics forwarding research, filed Session 69 per that plan's §9 Q4). Posit's own PR history (`quarto-dev/quarto` PR #832, pre-v1.133.0) shows their equivalent pre-existing request-forwarding vdocs were, before an explicit fix, accidentally surfacing spurious entries in VS Code's global Problems panel — a risk inherent to any project using request-forwarding virtual documents, independent of whether diagnostics forwarding is deliberately built (this project chose not to — see item 10, CLOSED Session 69). This project's own equivalent vdocs have never been checked for the same accidental-leakage bug. Small, self-contained, unranked — no known severity yet, since no firsthand check has been done.

- [ ] **`CURATED_EXECUTE_KEYS` hidden-visibility fidelity tuning** (surfaced by the Session 23 6d-6 adversarial review; refuted 0/3, the refuters defended inclusion 3/3). The curated execute children include `enabled`/`daemon`/`daemon-restart`, which carry `hidden:true` in the Quarto 1.7.33 schema — i.e. Quarto's OWN completion suppresses them. Including them is a deliberate, defensible call (`execute:\n  enabled: false` is this project's canonical render-clean idiom, and all three are valid/documented/render-accepted options), but a future session may choose to (a) mirror Quarto's visibility (drop the hidden ones), or (b) strengthen the exact-equality shape-lock test to cross-check the live schema with a `hidden` filter so the curated set can't silently drift from a real schema entry. Pure judgment/test-hardening; no wrong/missing completion today (the names ARE all genuine execute children, verified firsthand). Low priority. (Adversarial review, Session 23.)

- [ ] **Empty value slot abutting a trailing inline comment can glue value to comment** (surfaced by the Session 22 6d-5 adversarial review; refuted 0/3 as out-of-slice-scope, but a real cross-module pattern). When the value slot is EMPTY and a pre-existing inline comment follows (`key:  # c`, cursor before `#`), accepting a value inserts it with no separating space before the comment (`key:  true# c`). Root cause is the shared value-slot grammar: BOTH `core/qmd/model.ts` `slotsOf` (6d-2 cell path) and `core/yaml-context.ts` `topLevelSlots` (6d-5 FM path) compute an empty value span when the slot abuts a comment, and the provider's leading-space-on-`:` heuristic only fires when the slot abuts the COLON, not a comment. Contrived trigger (empty slot + pre-existing comment + cursor exactly before `#`); not a path a normal accept produces. A faithful fix covers BOTH call sites in one pass (suppress value completion when the empty slot abuts a comment, or append a separating space when the next non-space char is `#`) — cross-module, its own TDD pass (SAFEGUARDS: no cross-module refactor without plan mode; FM #18). Low severity. (Adversarial review, Session 22.)

- [ ] **Multi-cursor / multi-selection formatting** (deferred from Phase 7 formatting toggles, Session 14). `features/formatting.ts` toggles only the PRIMARY selection (`editor.selection`); a multi-cursor edit would need to apply N replacements and recompute N resulting selections across shifting offsets. Low priority, its own TDD slice.

- [ ] **Project-wide CLI spawn timeout** (surfaced by the Session 20 6d-3 adversarial review; refuted as a 6d-3-scoped defect, but a real cross-cutting hardening). No `quarto` spawn has a timeout — `quarto/cli.ts` `resolveBinary()` (`--version`, used by render/preview/verify) and the new `quartoSharePath()` (`--paths`, on the completion hot path) both `await execFileAsync(...)` with no `timeout`. A wedged/very-slow binary therefore HANGS rather than degrading: for the schema reader the first `#|` completion stays pending (never reaching the catch→curated fallback) and the cached promise pins it for the session. Low severity (pathological — a healthy `quarto --paths` returns in ~50 ms; VS Code also cancels slow completion providers). A consistent fix adds a `{ timeout }` to ALL CLI spawns in one pass (so the degrade-to-curated/degrade-gracefully guarantee holds under hang, not just under reject) — a deliberate cross-file change, its own TDD/verification pass, NOT a 6d-3 spot-fix (FM #18). (Adversarial review, Session 20.)

- [ ] **Schema-reader cache: retry after a transient first-load failure** (deferred from the Session 20 6d-3 review; refuted as v1-acceptable). `features/yaml-schema-source.ts` caches the load PROMISE once per session (`cached ??= loadSchemaIndex()`); since `loadSchemaIndex` always resolves (curated on failure), a transient first-load failure (e.g. a momentarily-busy CLI) PINS the curated fallback until a window reload, even after Quarto becomes reachable. Low severity (window reload fixes it; first-load failures are rare; documented as an accepted v1 limitation). A faithful fix caches only a SUCCESSFUL parse and retries (debounced) after a fallback, distinguishing "real schema" from "fell back to curated." Its own small TDD pass. (Adversarial review, Session 20.)

- [ ] **`parseSchemaIndex` all-or-nothing degradation across categories (forward-compat)** (surfaced by the Session 21 6d-4 review; refuted 0/3 as v1-acceptable). 6d-4 changed the fallback guard from per-category (`cellFields.length>0 ? … : CURATED`) to joint (`cellFields.length===0 && fmFields.length===0 ? CURATED_SCHEMA_INDEX : indexOf(cellFields, fmFields)`), consistent with replace-semantics (Learning #27). So a future schema that populates one category but EMPTIES the other (e.g. document-* present, all cell-* gone) would yield `[]` for the empty category instead of its curated set. Impossible today (the installed 1.7.33 file has 94 cell + 383 document fields; both always populated) and within the documented-acceptable degradation envelope (`[]` = fewer suggestions, never a false "unknown key"; never throws). A faithful fix would degrade PER-category to the matching curated set — but that mixes real+curated for the empty category, a deliberate divergence from replace-semantics; defer until a real partial-schema Quarto version appears. Trivial. (Adversarial review, Session 21.)

- [ ] **Mid-line HTML comments in `scanRegions`** (deferred from Phase 7 math-preview adversarial review, Session 15; confirmed 3/3 refuters). `core/qmd/model.ts` classifies HTML comments per WHOLE line (`COMMENT_OPEN`/`COMMENT_FULL_LINE`), so a comment that **opens mid-line** (`text <!--`) or **closes mid-line** (`--> $x$`) is mis-handled at the character level. Two symptoms in `core/math-regions.ts`: math inside a mid-line-opened comment is reported (false positive), and real math after a mid-line `-->` is missed (false negative). **This is a shared-model gap — it affects `core/refs.ts` (cross-ref index) identically**, not just math. A faithful fix needs character-level comment tracking in `scanRegions` (interacting with inline code spans + fences) and a full regression across outline/refs/citations — its own TDD pass (SAFEGUARDS: no cross-module refactor without plan mode). Low severity (mid-line comments are uncommon in `.qmd`). Documented in `core/math-regions.ts`'s docstring.


- [ ] **Honest test name for the math-preview "re-render on edit" integration test** (surfaced by the Session 16 diagram-preview review; the identical pattern exists in `test/integration/suite/math-preview.test.ts`). Its only assertion is panel-count, which proves no-stacking, NOT that a re-render occurred (webview content is F5-only residue). Rename the test + comment to what it proves, as was done for the diagram-preview test this session. Trivial, test-only — left untouched in Session 16 to honor FM #8/#18 (another feature's code). (Adversarial review, Session 16.)

- [ ] **Consolidate the `#|`/`//|` cell-option-prefix grammar between `refs.ts` and `model.ts`** (deferred 6d-2 follow-up, surfaced by the Session 18 6d-1 adversarial review). Session 18 aligned `core/qmd/model.ts` `CELL_OPTION_PREFIX` to Quarto's real `^#\s*\| ?` (column-0-anchored, ws allowed between `#` and `|`), but `core/refs.ts` `CELL_LABEL_OPTION` still has a leading `\s*` — so it would index an **indented** `  #| label: fig-x` that Quarto treats as code (a pre-existing latent looseness, now *diverging* from the faithful model regex). The faithful fix routes refs' `label:` extraction through `findCellOptionLines` (which 6d-2 extends with a value slot, making the value/id available) and drops the duplicate regex — a cross-module change, so its own TDD pass (SAFEGUARDS: no cross-module refactor without plan mode). Low severity (indented cell options are non-idiomatic). (Adversarial review, Session 18.)
- [ ] **Consolidate the YAML value-slot grammar between `yaml-context.ts` and `model.ts`** (surfaced Session 24, 6d-6 cont.). Session 24 extracted `valueSlotAfterColon` in `core/yaml-context.ts` so the top-level AND nested front-matter value-slot grammar (skip leading ws, strip an unquoted trailing inline comment, keep quoted scalars) is ONE implementation. But `core/qmd/model.ts` `slotsOf` (the cell-option `#|` value path, 6d-2) still computes the SAME grammar in its own mirror. The faithful fix routes both through one shared helper — a cross-module change (SAFEGUARDS: no cross-module refactor without plan mode; FM #18), so its own TDD pass. Low severity (the two are byte-identical today; drift risk only). (Session 24, 6d-6 cont.)
- [ ] **De-duplicate the integration `EXTENSION_ID` constant** (surfaced Session 11; now 13 copies). `const EXTENSION_ID = "rmsharp.vscode-quarto-ext"` is copy-pasted into all 13 `test/integration/suite/*.test.ts` files (the Session 28 `embedded.test.ts` added the 13th). When the `publisher` changed this session, every copy went stale at once and RED'd 8 "should be discoverable" failures. Extract to one shared module (ideally derived from `package.json`'s `publisher`+`name`) so an identity change can't silently break discoverability again. Low risk, pure test refactor — its own TDD-exempt pass. (Learning #18.)
- [ ] **Indented (4-space) code blocks as a skip-region** (deferred from Phase 6b adversarial review). `core/qmd/model.ts` `scanRegions` skips YAML front matter, HTML comments, and *fenced* code, but not CommonMark §4.4 *indented* code blocks (a line indented ≥4 spaces after a blank line). So `findBodyLines` emits them and `core/refs.ts` indexes a `{#fig-…}` shown inside one as a **phantom** cross-ref label (verified against `quarto render` → `?@fig-…` unresolved). The faithful fix must not false-skip 4-space **list-item continuation** content (the model tracks no list context), so it needs its own list-aware TDD pass. Low severity (uncommon construct). (Adversarial review B/D, Session 8.)
- [ ] **The `🐉1` EPERM discriminator cannot run as root or on Windows, and the whole temp-sweep suite is POSIX-shaped** (found by the Session 103 adversarial review; **the CODE is correct on every platform — this is test reach, not a defect**). `isProcessDead`'s only discriminating test asserts `isProcessDead(1) === false`, which works because `kill(1,0)` throws `EPERM` for a non-root user. Measured S103: **as root `kill(1,0) does NOT throw`, so the shipped and the naive `catch { dead }` implementations become identical and the assertion passes while proving nothing**; on Windows there is no PID 1 at all (NT allocates PIDs in multiples of 4). S103 made the test probe its premise and `skip()` loudly rather than pass vacuously, so it is honest — but that means **🐉1 is UNVERIFIED whenever CI runs as root (containers routinely do) or on Windows**, and the suite would go quietly green. Also unmeasured on those platforms: `process.getuid` is undefined on Windows so G3's uid check is skipped by design; `fs.mkdtemp` applies no POSIX mode there (`%LOCALAPPDATA%\Temp` is per-user, so privacy holds by a different mechanism). **Fix options:** a root-safe discriminator (spawn a child as another uid — needs privileges), or a seam that injects the `kill` probe so the three outcomes (no-throw / EPERM / ESRCH) can be exercised without a real foreign process. **Sizing: LOW** — this project runs its suite on macOS as a normal user, where the discriminator does work; the risk is a future CI change silently disarming the one test that catches the inversion.
- [ ] **Item 17b — Reticulate execution pathway for mixed R/Python Knitr documents** (`quarto.cells.useReticulate`; split from item 17, Session 109). In a Knitr-engine document Posit can route `{python}` cells through R's `reticulate` instead of Jupyter. **Grounded S109:** `reticulate` appears nowhere in `src/`; Run Cell today delegates by language only (`src/core/execution-delegate.ts` `pickDelegate`/`delegateCommandsFor`, Python → Jupyter) with no notion of the document's compute engine. So this needs (1) a config `quarto.cells.useReticulate`, (2) ~~**engine detection (Knitr vs Jupyter) — which does not exist yet and is the load-bearing unknown to scope first**~~ — **SUPERSEDED, Session 165: it exists.** `documentEngineForScoping` (`src/core/document-engine.ts`) now answers knitr/jupyter/markdown/julia for a document from its front matter AND, since S165, from its cell languages the way `markdownExecutionEngine` does. It was built for DIAGNOSTICS scoping, so a router would need to check that its declines (`undefined` on an `.Rmd`, an `{{< include >}}`, an unreadable selector) are acceptable for EXECUTION — where guessing wrong sends a cell to the wrong interpreter rather than merely mis-scoping a squiggle, a different and stricter bar, and (3) a delegate-selection branch that, when engine=Knitr ∧ flag set, sends `{python}` cells down the R pathway. Severity LOW, **narrow audience** (only mixed R/Python Knitr users). ⚠ The "is engine detection worth building for this alone?" gate is MOOT since Session 165 — it is built. What remains to confirm is whether a diagnostics-grade resolver is safe enough to ROUTE execution with.
- [ ] **Notebook-renderer: minor pandoc-fidelity divergences (all LOW, mostly PRE-EXISTING; accepted/documented Session 116).** Surfaced by the S116 adversarial panel, each verified firsthand vs quarto pandoc 3.6.3; none fixed (LOW + pathological or cosmetic). (1) **Apostrophe not entity-escaped** — a `'` in a value renders literally (`title="Don't"`) where pandoc writes `&#39;`; markdown-it's `escapeHtml` deliberately omits `'`. **Cosmetic only — the value sits inside a double-quoted attribute, so the parsed DOM is byte-identical**; no injection. (2) **Empty `::: {}`** falls through unrendered; pandoc emits a bare `<div>` (pinned by an existing test; pointless input). (3) **Fenced div interrupts an open paragraph** (no blank line before `:::`) where pandoc requires the blank line — PRE-EXISTING (`alt:["paragraph",…]` ruler registration since S113/S114, byte-identical old-vs-new), shared with the callout path. (4) **1–3 leading spaces before `:::`** still open a div; pandoc requires column 1 — PRE-EXISTING opening-fence indent handling. (5) **Non-ASCII class/id/key names** (`.класс`) fall through; pandoc accepts Unicode letters — PRE-EXISTING ASCII `\w` grammar. (6) **Escaped separator in an UNQUOTED value** (`key=a\ b`) falls through; pandoc unescapes to `a b` (`unescapeAll` does not handle `\`+space). (7) **A bare `X` and a literal `data-X` in the same div** — the plugin dedups on the post-`data-` name and keeps one; pandoc emits both (a duplicate, invalid-HTML attribute) — the plugin's single-attr output is arguably cleaner. Each is niche; revisit only if a real document hits one. **S118 addendum (custom-title panel, all LOW, all PRE-EXISTING in root cause, each verified firsthand vs quarto render 1.7.33):** (8) **Pandoc heading attributes leak into an extracted title** — `::: {.callout-note}` / `## Heading {#id .cls}` shows title `Heading {#id .cls}` where quarto shows `Heading` (the `{…}` becomes the heading's attributes). Root cause is the PRE-EXISTING markdown-it gap that it does not implement pandoc's header-attribute syntax — the same literal `{…}` also leaks in a body `<h2>` (when `title=` is present so the heading is not extracted); the S118 change only makes it newly visible in the title bar. Fixing = parse pandoc heading attributes (affects all headings, not just titles), out of scope. (9) **`}` inside a quoted callout `title=` flips the callout to a plain generic div** — `::: {.callout-note title="a}b"}` renders `<div class="callout-note" title="a}b">` (no admonition) where quarto renders a titled callout, because `calloutType`'s strict `ATTR_BLOCK` gate `/^\{[^}]*\}$/` rejects any interior `}` while `parseDivAttrs`'s relaxed gate accepts a `}` in a quoted value. PRE-EXISTING (the strict gate since S113/S114); fix = derive the callout type from `parseDivAttrs`'s parsed classes so the two gates agree (a refactor of the byte-identical callout path). (10) **Multi-line setext "heading" extracted as a multi-line title** — `Line one` / `Line two` / `======` inside a callout: markdown-it treats it as a 2-line setext H1 and the extractor lifts it into the title (embedded newline), where pandoc does not treat it as a heading at all (default title, body paragraph). PRE-EXISTING markdown-it-vs-pandoc setext divergence; pathological. (11) **Whitespace-only quoted attribute value renders a callout** — `::: {.callout-note title=" "}` renders a callout with a single-space title where pandoc rejects the whole `:::` line as paragraph text. PRE-EXISTING `tokenizeDivAttrs` leading-space quirk affecting all quoted values (generic divs too). **S119 addendum (collapse panel, both LOW, both PRE-EXISTING in root cause, each verified firsthand vs quarto render 1.7.33):** (12) **A bare `collapse` flag (no `=value`) on a callout renders a plain non-collapsible callout** where quarto rejects the whole fenced-div block as literal `:::` text (invalid pandoc attribute syntax → no callout at all). Root cause is the PRE-EXISTING `calloutType`-vs-`parseDivAttrs` leniency: `calloutType`'s `ATTR_BLOCK` regex matches `.callout-note` even when `tokenizeDivAttrs` rejects the block, so `attrs` is `null`, the collapse (and title) paths are never reached, and the callout renders plain/default. Any invalid bareword inside a callout block (`{.callout-note xyz}`) manifests it identically; byte-identical pre-collapse-change. Same fix as divergence (9): derive the callout type from `parseDivAttrs`'s parsed classes so the two gates agree. Out of scope. (13) **A quoted collapse value with LEADING whitespace** (`collapse=" true"`) renders an expanded-collapsible callout where quarto emits the whole `:::` line as literal text. PRE-EXISTING leading-space handling — the SAME root as divergence (11); a `title=" Hi"` behaves identically (plain callout for us, literal text in quarto), an upstream fence-recognition property unchanged by the collapse work (trailing-only `"true "` and interior `"a b"` both parse in quarto and match us). Out of scope.
  - **S117 addendum (nested manifestation of (2) and (3)):** the S117 adversarial panel flagged that when an empty `::: {}` (divergence 2) or a paragraph-glued `::: {.inner}` (divergence 3) appears NESTED inside another div, the depth counter mis-pairs (empty `{}` is not counted as an opener since the plugin does not render it as a div; a glued opener IS counted since the plugin's `alt:["paragraph"]` registration interrupts paragraphs). Adjudicated FIRSTHAND (pre-fix vs post-fix vs pandoc): **both are consequences of the pre-existing standalone divergences here (2)/(3), NOT of the S117 depth fix** — for same-length fences the empty-`{}` case is byte-identical pre-fix and post-fix, and the glued case is now MORE self-consistent post-fix (the interrupted inner div is contained by the outer, where pre-fix left it orphaned). Because `isDivOpener` is deliberately consistent with what the plugin actually renders as a div, making it match pandoc's broader opener set (empty `{}`, trailing-colon `::: {.x} :::`, degenerate literal-brace fallbacks) would DE-sync depth from the rendered structure — so these stay out of scope until the standalone divergences (2)/(3) are themselves addressed. Still LOW/pathological.
- [ ] **`docs/POSIT-COMPARISON.md` understates notebook-renderer parity (PRE-EXISTING, LOW).** Lines ~651–652 still assert "This project has no equivalent notebook-renderer contribution." — stale since S113 (when `contributes.notebookRenderer` `quarto-mit.markdown-it-callout` first shipped), NOT caused by the S122 box-CSS fix; surfaced by the S122 completeness-lens sweep over 17d. The project now ships a `notebookRenderer` extending `vscode.markdown-it-renderer` that renders Quarto callouts + generic fenced divs in `.ipynb` markdown cells (S113–S122). Fix = update that parity row to note the contribution (narrower scope than Posit's full-markdown renderer, but no longer "no equivalent"). Doc-only, no logic → TDD-exempt, its own small session. Filed rather than fixed in S122 to hold 1-and-done / avoid "while I'm at it" scope creep.
