{
  "quarto": "1.7.33",
  "build": "S177 (b8b8d27+), measured 2026-08-01; the 13 fence-token rows unchanged since S172 (2d25f4a+), measured 2026-07-27; the 8 leading-whitespace rows unchanged since S171 (5d6ef0d+); the 12 .Rmd rows unchanged since S170 (5786d0f+); the 66 pre-.Rmd rows unchanged since S166 (d855cd2+), measured 2026-07-26",
  "note": "What this build is EXPECTED to conclude for each corpus document. `rows` is the comparison input; every row that is not `agree` must carry a reason in `known`, or the oracle fails (test/oracle/classify.ts unexplainedRows). Re-seeding this file to silence a failure is the thing that gate exists to prevent. All seven non-agreeing rows below were confirmed PRE-EXISTING by replaying the pre-S165 build (87b3f38) over this same corpus: that build scores 16 cardinal FP / 9 lost TP / 41 agree, and every row here is wrong there too. S170 ADDED the twelve `.Rmd`/`.rmd`/`.Rmarkdown` rows, all measured `agree` on the shipping build and none carrying a `known` reason — the corpus was 100% `.qmd` until then, so the extension branch of documentEngineForScoping had never been inside the oracle's horizon. Replayed against the pre-S170 build (`git archive af5a4bb src` -> QMD_ORACLE_SRC), those same twelve score 8 LOST TP and 4 agree: the whole corpus reads 63 agree / 11 lost TP / 4 cardinal FP there, against 71 / 3 / 4 here. The cardinal-FP count is IDENTICAL on both sides — that is the measurement that made a widening safe to ship, and it is why the twelve rows exist. S171 ADDED the eight leading-whitespace rows. Quarto's ENGINE partitioner opens with `lines(markdown.trimLeft())`, so no amount of leading whitespace hides the front matter from it, while scanRegions opens front matter only at line 0 — S165 could therefore only DECLINE, and declining means keeping the per-cell language approximation, which answers knitr for an {r} cell. MEASURED, not derived, by replaying the pre-S171 build (`git archive d112f35 src` -> QMD_ORACLE_SRC) over this same 86-document corpus: pre-S171 scores 73 agree / 3 lost TP / 10 CARDINAL FP, this build scores 78 / 3 / 5. Five cardinal false positives closed, zero rows regressed, and the one row still failing is identical on both sides. NOTE the corpus already carried a row named 'REVIEW blank line BEFORE --- + engine: markdown' baselined `agree` — it uses rThenPy, whose flagged cell is {python}, so the per-cell fallback answered jupyter and stayed silent for the wrong reason. A row named after a defect is not a row that can observe it. S172 ADDED the thirteen fence-token rows and CLOSED the `{r.foo} dotted token + echo` row above (its `known` entry is removed in the same commit). Quarto's cell recognizer `breakQuartoMd` captures the language as `([=A-Za-z]+)` with an option tail of `( *[ ,].*)?` — letters and `=` only, no digits, dots, hyphens, underscores or tabs — and a token that fails it is not a cell to quarto AT ALL, so nothing inside is validated. Our old `[^}]*` tail TRUNCATED where quarto REJECTS, and was wrong in BOTH directions: it admitted eight non-cells, and it could not span a `}` inside a quoted chunk option, so the well-formed knitr header `{r, fig.cap=\"}\"}` was not a cell to us while quarto validates it. MEASURED, not derived, by replaying the pre-S172 build (`git archive faaeae9 src` -> QMD_ORACLE_SRC) over this same 99-document corpus: pre-S172 scores 80 agree / 6 lost TP / 13 CARDINAL FP, this build scores 91 / 4 / 4. Nine cardinal false positives closed, two lost true positives recovered, zero rows regressed. ⚠ NOTE the corpus already carried TWO rows named after this defect — `{r9} digit token + cache` and `{r9} then a real {r} cell + cache on the {r}` — both baselined `agree`, and both vacuous: `cache` is knitr-ONLY and `r9` is not counted as `r` by the language scan, so those documents resolve markdown and `cache` is out of scope whatever the scanner decides. They were silent for a reason that has nothing to do with cell recognition. Every S172 row therefore uses the engine-AGNOSTIC `echo`, whose scope is identical under every engine, so a flag can only come from whether we built a cell. This is the FOURTH instance of this corpus's horizon warning and the third where a row LOOKED like coverage. S177 ADDED the eleven column-0 YAML-delimiter rows and CLOSED the 'S171 indented --- + NO engine + {r} + cache (KNOWN residual)' row (its `known` entry is removed in the same commit). `breakQuartoMd`'s `yamlRegEx` is `/^---\\s*$/` — anchored at COLUMN 0 and tested on EVERY line, not only line 0 — and a fence builds a cell only when `inPlainText()` (`!inCodeCell && !inCode && !inYaml`), so a column-0 `---` opens a YAML region that swallows every cell below it and NOTHING inside one is validated. The single exemption is `isYamlDelimiter`'s `skipHRs` arm — a `---` with a blank line BOTH above and below is a thematic break — and it applies only when a region would OPEN, since `skipHRs` is passed `!inYaml`. MEASURED, not derived, by replaying the pre-S177 build (`git archive 4907e2f src` -> QMD_ORACLE_SRC) over this same 110-document corpus: pre-S177 scores 97 agree / 4 lost TP / 9 CARDINAL FP, this build scores 103 / 4 / 3. SIX cardinal false positives closed, zero lost true positives, zero rows regressed. The six CONTROL rows are `agree` on BOTH builds, which is what makes 'no lost TP' a measurement rather than a hope: each renders exit 1, so any of them going silent would be a real loss. ⚠ The fix is scoped to the VALIDATION surface ON PURPOSE. The swallow is a property of `breakQuartoMd`, not of the document: measured on the swallowed shapes, the rendered HTML still carries the cell as a real highlighted code cell (knitr executes it, pandoc emits it, and the `#|` line is consumed as an option rather than printed). So `findAllCells` — and with it the outline, run-cell and the embedded-LSP virtual documents — is deliberately untouched, and only `collectValueSources` drops the option lines. Every S177 row uses the engine-AGNOSTIC `echo` for the S172 reason: the mechanism here is cell PARTITIONING, so a knitr-only `cache` could let a row pass on the ENGINE answer instead of on whether we validated a cell.",
  "known": {
    "include(child {julia}) then {r} + cache": "CARDINAL FP, PRE-EXISTING (wrong in 87b3f38 too). Quarto expands `{{< include >}}` PRE-engine, so it counts the child's {julia} and resolves jupyter; we cannot see the child from the open buffer and answer knitr. S165 measured BOTH options and chose to DECLINE rather than answer \"ambiguous\", because ambiguity would cost the knitr-only fields on every ordinary include-bearing book chapter — a rare conditional FP against a common lost TP is the reverse of this project's settled trade. BACKLOG: An {{< include >}} makes the document's language set unknowable.",
    "nested include(child→grand {julia}) then {r} + cache": "CARDINAL FP, PRE-EXISTING. Same root cause as the row above, proving includes RECURSE (doc -> child -> grandchild). Same filed item.",
    "execute: {engine: markdown} FLOW + {r} + cache": "CARDINAL FP, PRE-EXISTING. Quarto honours the FLOW spelling `execute: {engine: markdown}` and renders exit 0; neither value enumerator emits a flow-mapping MEMBER, so `documentEngineForScoping` sees nothing to read. S165 made any scalar token on `execute:` UNREADABLE so this declines rather than letting the language fallback squiggle every cell — the FP is unchanged, but it no longer widens. BACKLOG: The FLOW spellings of the engine: override.",
    "include(child {r}) then {python} + cache": "LOST TP (safe), PRE-EXISTING. The include decline in the other direction: quarto sees the child's {r}, resolves knitr and rejects (exit 1); we decline and stay silent. The safe half of the same filed trade.",
    "REVIEW engine: &a knitr (TP we give up)": "LOST TP (safe), PRE-EXISTING. We compare a RAW token where quarto resolves the PARSED node, so an anchored `&a knitr` is unreadable to us and we decline. Quarto really does select knitr here (exit 1). Separating this from its exit-0 twins needs real YAML node resolution. BACKLOG: Node-property spellings of the override.",
    "REVIEW engine: | + literal body (TP we give up)": "LOST TP (safe), PRE-EXISTING. A literal block scalar KEEPS its newline so quarto reads `markdown\\n` and falls through to the languages (exit 1), while the `>-` folded twin strips it and matches (exit 0). Telling them apart needs chomping semantics — exactly the trap S163 filed. Same filed item as the row above.",
    "S172 {=html} raw block + echo (KNOWN residual, lost TP)": "LOST TP, PRE-EXISTING, and MEASURED for the first time by S172 (it had been filed by the S160 review as 'NOT re-verified'). Quarto's cell recognizer captures the language as `([=A-Za-z]+)`, so a leading `=` IS a legal cell language: `{=html}` + `#| echo: banana` renders quarto exit 1, and in a KNITR document `#| cache: banana` there renders exit 1 too — i.e. a raw block takes the DOCUMENT ENGINE's schema exactly like an ordinary cell. Our CELL_INFO deliberately keeps its letter-led rule, so we build no cell and stay silent. Adopting the `=`-led branch WIDENS what we squiggle onto a whole new block class (the cardinal-sin direction) and hands `cell.lang === \"=html\"` to the outline, the virtual-document language map, run-cell and the crossref index — its own deliverable, filed in BACKLOG. Guarded by the FP GUARD pins in test/unit/cells.test.ts and test/unit/yaml-value-flags.test.ts, which are the ONLY tests the completing mutant kills."
  },
  "rows": {
    "{r} alone + cache": "agree",
    "{julia} then {r} + cache": "agree",
    "{r} + cache then {julia}": "agree",
    "{julia} then {r} + echo (agnostic)": "agree",
    "{python} alone + cache": "agree",
    "{r} then {python} + cache": "agree",
    "{python} + cache then {r}": "agree",
    "{r} then {sql} + cache": "agree",
    "{sql} alone + cache": "agree",
    "{sql} alone + echo": "agree",
    "{r} then {ojs} + cache": "agree",
    "{ojs} alone + cache": "agree",
    "{ojs} alone + echo": "agree",
    "{R} uppercase + cache": "agree",
    "{r, echo=FALSE} attrs + cache": "agree",
    "{julia} in a ```` example block, then {r} + cache": "agree",
    "{julia} in a blockquote, then {r} + cache": "agree",
    "{julia} 3-space indented, then {r} + cache": "agree",
    "{julia} in 4-space indented code, then {r} + cache": "agree",
    "{julia} in an HTML comment, then {r} + cache": "agree",
    "{julia} tab-indented, then {r} + cache": "agree",
    "````{julia} 4 backticks, then {r} + cache": "agree",
    "``` {julia} space after ticks, then {r} + cache": "agree",
    "{julia} in a front-matter block scalar, then {r} + cache": "agree",
    "```{julia} x trailing text, then {r} + cache": "agree",
    "```{ julia } spaces in braces, then {r} + cache": "agree",
    "include(child {julia}) then {r} + cache": "cardinal-fp",
    "include(child {r}) then {python} + cache": "lost-tp",
    "{r} + cache then include(child {julia})": "agree",
    "nested include(child→grand {julia}) then {r} + cache": "cardinal-fp",
    "engine: banana + {julia} then {r} + cache": "agree",
    "engine: banana + {r} + cache": "agree",
    "engine: MARKDOWN (case typo) + {r} + cache": "agree",
    "execute:/engine: banana + {r} + cache": "agree",
    "execute: {engine: markdown} FLOW + {r} + cache": "cardinal-fp",
    "execute: {echo: false} FLOW + {r} + cache": "agree",
    "no front matter, {r} + cache": "agree",
    "blank first FM line + {r} + cache": "agree",
    "engine: markdown + {r} + cache (S164 override)": "agree",
    "engine: knitr + {python} + cache (S164 override)": "agree",
    "jupyter: python3 + {r} + cache (S164 alias)": "agree",
    "{r} then {dot} + cache (handler in a knitr doc)": "agree",
    "{r} then {dot} + echo (handler in a knitr doc)": "agree",
    "{dot} alone + echo": "agree",
    "{r9} digit token + cache": "agree",
    "{r.foo} dotted token + cache": "agree",
    "{r.foo} dotted token + echo": "agree",
    "{r9} then a real {r} cell + cache on the {r}": "agree",
    "REVIEW engine: &a markdown": "agree",
    "REVIEW engine: *a alias": "agree",
    "REVIEW engine: >- folded body": "agree",
    "REVIEW engine: empty + continuation": "agree",
    "REVIEW nested execute:/engine: &a markdown": "agree",
    "REVIEW markdown: !!bool true": "agree",
    "REVIEW markdown: &a true": "agree",
    "REVIEW markdown: | + body": "agree",
    "REVIEW markdown: + column-0 sequence": "agree",
    "REVIEW execute: *a mapping alias": "agree",
    "REVIEW execute: &a + engine child": "agree",
    "REVIEW blank line BEFORE --- + engine: markdown": "agree",
    "REVIEW engine: &a knitr (TP we give up)": "lost-tp",
    "REVIEW engine: | + literal body (TP we give up)": "lost-tp",
    "REVIEW engine: banana (must still fall through)": "agree",
    "REVIEW jupyter: false (must still fall through)": "agree",
    "validate-yaml: false + {r} + cache": "agree",
    "per-cell #| validate-yaml: false + cache": "agree",
    ".Rmd {python} + cache": "agree",
    ".Rmd {python} + echo (agnostic control)": "agree",
    ".Rmd {python} + cache: true (VALID — must stay silent)": "agree",
    ".rmd lowercase {python} + cache": "agree",
    ".Rmarkdown {python} + cache": "agree",
    ".Rmd {r} + cache (unchanged — the language agreed already)": "agree",
    ".Rmd {sql} + cache": "agree",
    ".Rmd {ojs} + cache": "agree",
    ".Rmd {dot} handler + cache (exempt)": "agree",
    ".Rmd engine: markdown + {python} + cache (the veto)": "agree",
    ".Rmd include(child {julia}) + {python} + cache": "agree",
    ".Rmd + _quarto.yml engines: [jupyter, knitr] + {python} + cache": "agree",
    "S171 blank line + engine: markdown + {r} + cache": "agree",
    "S171 blank line + NO engine + {r} + cache (must still flag)": "agree",
    "S171 blank line + engine: markdown + {r} + echo (agnostic control)": "agree",
    "S171 two blank lines + engine: markdown + {r} + cache": "agree",
    "S171 spaces-only line + engine: markdown + {r} + cache": "agree",
    "S171 leading tab + engine: markdown + {r} + cache": "agree",
    "S171 indented --- + engine: markdown + {r} + cache": "agree",
    "S171 indented --- + NO engine + {r} + cache (KNOWN residual)": "agree",
    "S172 {python3} digit token + echo": "agree",
    "S172 {fortran95} digit token + echo": "agree",
    "S172 {fortran} digit-free control + echo": "agree",
    "S172 {r9} digit token + echo (the agnostic twin of the vacuous cache row)": "agree",
    "S172 {r-foo} hyphen token + echo": "agree",
    "S172 {r_foo} underscore token + echo": "agree",
    "S172 {ré} non-ASCII token + echo": "agree",
    "S172 {r<TAB>echo=FALSE} tab-separated token + echo": "agree",
    "S172 {r=1} '=' then a digit + echo": "agree",
    "S172 {mermaid=x} glued token + echo (quarto validates it)": "agree",
    "S172 {mermaid} bare handler + echo (stays exempt)": "agree",
    "S172 {r, fig.cap=\"}\"} brace in a quoted option + echo": "agree",
    "S172 {=html} raw block + echo (KNOWN residual, lost TP)": "lost-tp",
    "S177 indented --- + {r} + echo (agnostic twin of the S171 cache row)": "agree",
    "S177 setext-H2 --- above the cell + echo": "agree",
    "S177 --- with a blank line ABOVE but text BELOW + echo": "agree",
    "S177 --- inside an HTML COMMENT + echo": "agree",
    "S177 --- inside a ~~~ TILDE fence + echo": "agree",
    "S177 CONTROL blank-surrounded --- (a true HR) + echo": "agree",
    "S177 CONTROL indented --- with NO closing --- + echo": "agree",
    "S177 CONTROL region opens and CLOSES above the cell + echo": "agree",
    "S177 CONTROL --- inside a ``` fence + echo": "agree",
    "S177 CONTROL --- inside an {r} cell body + echo": "agree",
    "S177 CONTROL cell ABOVE a later --- + echo": "agree"
  }
}
