# Create a project

**Quarto: Create Project** scaffolds a `_quarto.yml` plus a starter document for a chosen project type — `default`, `website`, `book`, or `manuscript` — then opens the new project as your workspace.

Projects are how Quarto renders multi-file books and websites as one coherent output.
